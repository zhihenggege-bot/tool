# PSC330 与 PSC316 联调测试指南

本文档用于现场测试 PSC330RK-V10 内机和 PSC316RK-V12 外机。测试顺序按“先通讯、再安全、再单功能、最后联动”安排，避免一上来直接带压缩机跑导致误动作。

## 1. 测试范围

本次需要验证两条主链路：

```text
HMI / Modbus Poll
  <-> PSC330 A0B0 / USART1 / Modbus RTU 从站
  <-> PSC330 Parameter[]
  <-> PSC330 A2B2 / UART5 / Modbus RTU 主站
  <-> PSC316 Parameter[]
```

正式运行原则：

- HMI 只接 PSC330 的 A0B0。
- HMI 不直接写 PSC316。
- PSC316 的设置参数由 PSC330 校验、转发、回读确认。
- PSC316 代码测试时可以直连调试，但正式屏工程不要绕过 PSC330。

## 2. 测试前准备

### 2.1 工具

建议准备：

- Keil5，用于分别编译/烧录 330 和 316。
- USB-RS485 转换器，至少 1 个，推荐 2 个。
- Modbus Poll 或同类工具。
- 万用表，用于测 DO/AO 输出。
- 温湿度/压力/电流可模拟输入源，或现场真实传感器。
- 串口参数统一确认：Modbus RTU，9600bps，8N1，CRC16。

### 2.2 工程位置

| 项目 | 路径 |
|---|---|
| PSC330 内机 | `D:\Companty\WEll thinker\F\code\PSC330RK-V10-内机` |
| PSC316 外机 | `D:\Companty\WEll thinker\F\code\PSC316RK-V12-外机` |

### 2.3 接线

| 链路 | 接线 |
|---|---|
| HMI / Modbus Poll 到 PSC330 | 接 PSC330 A0B0 |
| PSC330 到 PSC316 | PSC330 A2 接 PSC316 A2，PSC330 B2 接 PSC316 B2 |
| 多台 PSC316 | A2B2 总线并联，站号不同 |

## 3. 地址换算规则

代码内部使用 `Parameter[N]`，Modbus Poll 通常填 0 基地址 `N`。

威纶通 4x 地址一般为：

```text
4x 地址 = Parameter 地址 + 1
```

例子：

| 代码地址 | Modbus Poll 0 基地址 | 威纶通 4x |
|---:|---:|---:|
| `Parameter[1830]` | 1830 | 41831 |
| `Parameter[165]` | 165 | 4166 |
| `Parameter[1420]` | 1420 | 41421 |

如果读不到，优先检查屏/工具到底使用 0 基地址还是 1 基地址。

## 4. 330 基础通讯测试

### 4.1 HMI 到 330

目标：确认 HMI 能读写 PSC330。

步骤：

1. USB-RS485 接 PSC330 A0B0。
2. Modbus Poll 设 Slave ID 为 PSC330 A0B0 地址，通常为 1。
3. 读 Holding Register。
4. 先读常规参数区，如 `Parameter[160]`、`Parameter[161]`、`Parameter[162]`。
5. 写一个允许写的设定值，再回读确认。

判定：

- 能稳定读回，说明 A0B0 正常。
- 写入越界值应被拒绝或保持原值。
- 运行状态区、AI 校准区、PSC316 诊断只读区不应被 HMI 写动。

重点代码：

| 功能 | 代码位置 |
|---|---|
| HMI 写保护 | `PSC330RK-V10-内机\User\UserSrc\modbus.c` |
| 参数定义 | `PSC330RK-V10-内机\User\UserHead\User.h` |

### 4.2 330 到 316 网关通讯

目标：确认 PSC330 能通过 A2B2 读写 PSC316。

1# 外机测试地址：

| 内容 | 330 参数地址 | 威纶通 4x | 说明 |
|---|---:|---:|---|
| `OutdoorUnitTypeSet` 镜像 | 1830 | 41831 | 0=15~43C，1=0~43C，2=-15~43C |
| `OutdoorFanTypeSet` 镜像 | 1831 | 41832 | 0~4 |
| 316 在线状态 | 1863 | 41864 | 1=在线 |
| 316 通讯故障 | 1864 | 41865 | 1=故障 |
| 写状态 | 1865 | 41866 | 0=空闲，1=写中，2=成功，3=失败 |
| 最近写参数地址 | 1866 | 41867 | 例如 165 |
| 最近写目标值 | 1867 | 41868 | HMI 写入值 |
| 最近回读值 | 1868 | 41869 | 316 回读值 |
| 错误码 | 1869 | 41870 | 0=无错误 |
| 状态刷新计数 | 1870 | 41871 | 应持续变化 |

测试步骤：

1. 确认 PSC316 A2B2 地址和 330 网关配置一致。
2. 读 `1863`，应为 1。
3. 读 `1864`，应为 0。
4. 写 `1830=0/1/2`，观察 `1865` 从 1 变为 2。
5. 观察 `1866=165`、`1867=目标值`、`1868=回读值`。
6. 断开 A2B2，确认 `1864` 变为 1 或写入失败。
7. 恢复 A2B2，确认在线和刷新计数恢复。

错误码解释：

| 错误码 | 含义 | 处理 |
|---:|---|---|
| 0 | 无错误 | 正常 |
| 1 | 写入值超出 PSC316 参数范围 | 改 HMI 输入范围或参数上下限 |
| 2 | 目标外机通讯故障/离线 | 查 A2B2、站号、终端电阻 |
| 3 | 写队列忙 | 降低 HMI 连续写入频率 |
| 4 | 写后回读不一致 | 查 316 是否保存失败或地址不一致 |
| 5 | 写 316 站号不匹配当前外机号 | 不要把 1# 外机写成 2# 地址 |

## 5. 330 内机单机功能测试

测试 330 单机时，建议先不带压缩机实负载，可以用外机通讯状态或模拟输出确认逻辑。

### 5.1 开关机时序

目标：确认普通制冷、制热、通风都走独立启动分支。

步骤：

1. `UserSwitchSet=0`，确认所有运行输出关闭。
2. 设制冷模式，`UserSwitchSet=1`。
3. 观察风机、压缩机需求、阀门、EEV 是否按顺序出现。
4. 切制热模式，重复。
5. 切通风模式，确认不产生压缩机需求。
6. 停机，确认压缩机/阀门/风机按延时关闭。

通过条件：

- 制冷、制热、通风不是混用同一套粗糙时序。
- 停机后无残留压缩机开机命令。
- 外机禁用或保护存在时，330 不继续下发压缩机有效需求。

重点代码：

| 功能 | 代码位置 |
|---|---|
| 内机主状态机 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |
| 普通三模式启动分支 | `IndoorCoolingStartSequence()`、`IndoorHeatingStartSequence()`、`IndoorVentilationStartSequence()` |
| 模式统一 | `IndoorNormalizeWorkMode()`、`IndoorSetNormalWorkMode()` |

### 5.2 双变量模式统一

目标：确认 `UserWorkModeSetTEMP` 是真实 0~5 模式，`UserWorkModeSet` 只是兼容老逻辑。

测试点：

| 模式 | 期望 |
|---|---|
| 制冷 | `UserWorkModeSetTEMP` 为制冷，老变量映射为制冷 |
| 制热 | `UserWorkModeSetTEMP` 为制热，老变量映射为制热 |
| 通风 | `UserWorkModeSetTEMP` 为通风，老变量映射为通风/停压机兼容 |
| 消毒 | 不误触普通制冷/制热 |
| 排毒 | 不误触普通制冷/制热 |
| 自动 | 自动冷热切换后两个变量保持一致 |

如果 HMI 上看到“模式显示和实际动作不一致”，优先查此处。

### 5.3 压缩机能量 PID

目标：验证温湿度需求到压缩机数量/能量输出。

测试步骤：

1. 设置制冷模式。
2. 将室温模拟到高于设定值，观察制冷需求上升。
3. 调整湿度高于设定，观察除湿/再热相关需求。
4. 设置 `CompControlModeSet=1`，切到环境温度控制。
5. 改变 HLM/HLC/HRM/HRC 相关条件，观察 PID 输出变化。

通过条件：

- 死区内不频繁加减载。
- 温差增大时输出增加。
- 湿度需求存在时，不应被自动冷热切换误判成单纯制热。
- 输出受最小/最大压缩机数量限制。

重点代码：

| 功能 | 代码位置 |
|---|---|
| 压缩机 PID | `PSC330RK-V10-内机\User\UserSrc\NergryAdjust.c` 的 `SysNergryAction_PID()` |
| 环境温度控制 | `IndoorEnvTempEnergyAction()` |
| 需求显示 | `ReheatDemandDisp`、`HumidDemandDisp`、`OutputForbidReasonDisp` |

### 5.4 自动冷热切换

目标：验证 QHB/QHS 边界不抖动。

测试步骤：

1. 开自动模式。
2. 室温高于制冷切换阈值，确认切到制冷。
3. 室温低于制热切换阈值，确认切到制热。
4. 在阈值附近缓慢变化，确认不会频繁来回切。
5. 湿度需求高时，确认不会因为再热比例误切制热。

关注参数：

| 参数 | 作用 |
|---|---|
| `QHB` | 自动转制热边界 |
| `QHS` | 自动转制冷/保持边界 |
| `HumiditySet` | 湿度设定 |
| `RoomTempDiffSet` | 温度死区相关 |

### 5.5 再热、辅热、预热、加湿

目标：确认模拟量和开关量输出符合需求。

测试项目：

| 功能 | 测试方法 | 通过条件 |
|---|---|---|
| 再热 | 降低送风/回风温度或提高除湿需求 | `ReheatAO0` 随需求变化，开关量不过冲 |
| 辅热 | 设置制热需求，模拟低温 | 辅热分级/比例正确 |
| 预热 | 模拟低温新风，启动预热 | `PreheatAO1` 或电加热段数输出正确 |
| 加湿 | 湿度低于设定 | `HumidAO2`、`HumidValveY13` 输出正确 |
| 加湿故障 | 触发 `HumidifierFaultX10` | 加湿输出关闭并报故障 |

预热电加热段数：

| `PreheatSegSet` | 含义 |
|---:|---|
| 1 | 单段开关 |
| 2 | 1:2 两段 |
| 3 | 1:2:4 三段，默认 |

默认 3 段是因为规格书按 1:2:4 分级时调节范围最细，兼容性最好；如果现场只有 1 段或 2 段电加热，必须改成实际段数。

### 5.6 新风阀、混风阀、CO2、热回收

目标：确认新风/混风/CO2/热回收逻辑不会与制冷制热冲突。

测试项目：

| 功能 | 关键参数 | 通过条件 |
|---|---|---|
| 新风阀控制依据 | `FreshValveCtrlBasisSet` | 按 CO2/压差/手动电压对应动作 |
| 房间压差 | `RoomPressureSet` | 压差需求能影响阀门输出 |
| CO2 控制 | `CO2Set` | CO2 高于设定时新风需求增加 |
| 手动新风电压 | `FreshValveManualVoltSet` | 手动模式输出固定电压 |
| 混风阀类型 | `MixAirValveTypeSet` | 0~10V 或相应类型正确 |
| 热回收 | `HeatRecoveryEnableSet` | 满足温差时开启，不满足时关闭 |

重点代码：

| 功能 | 代码位置 |
|---|---|
| 阀门/热回收控制 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |
| 参数宏 | `PSC330RK-V10-内机\User\UserHead\User.h` |

### 5.7 送风机和风量闭环

目标：确认不同模式下风机电压、闭环和保护。

测试步骤：

1. 通风模式，确认风机目标约 8V 或设定通风电压。
2. 制冷/制热模式，确认风机目标约 10V 或设定运行电压。
3. 模拟风压/风量反馈偏低，确认风量需求上升。
4. 模拟压力保护输入，确认按 `PressureProtectEnableSet` 是否启用处理。

关注参数：

| 参数 | 作用 |
|---|---|
| `FanAirSet` | 目标风量 |
| `FanAirDiffSet` | 风量回差/死区 |
| `FanAirKpSet/Ti/Td` | 风量 PID |
| `PressureProtectEnableSet` | 送/回风压差保护使能，默认 0 旁路 |

### 5.8 EEV 控制

目标：确认 8 路 EEV、过热度 PID、化霜开度、关阀速度。

测试步骤：

1. 压缩机运行，观察对应 EEV 进入初始开度。
2. 过热度偏大，EEV 应开大。
3. 过热度偏小，EEV 应关小。
4. 蒸发温度高于 12C，确认特殊修正。
5. 化霜时确认开度切到 `DefrostEEVStepSet` 或规格化霜开度。
6. 压缩机停机后，EEV 先等待约 3s，再按 `EEVStepLimitSet` 逐秒关阀。

关注参数：

| 参数 | 作用 |
|---|---|
| `EEVSpecPidEnableSet` | 规格书 EEV PID 使能 |
| `EEVSuperHeatTargetSet` | 过热度目标 |
| `EEVSuperHeatDeadSet` | 死区 |
| `EEVPidKpSet/Ti/Td` | EEV PID |
| `EEVStepLimitSet` | 单次步数限制/关阀速度，默认 20 |
| `DefrostEEVStepSet` | 化霜开度 |

重点代码：

| 功能 | 代码位置 |
|---|---|
| EEV 控制 | `PSC330RK-V10-内机\User\UserSrc\eevCTRL.c` |

### 5.9 330 保护测试

测试时先不带压缩机，确认保护动作，再带负载复测。

| 保护 | 输入/条件 | 期望 |
|---|---|---|
| 消防 | `FireAlarmX1` | 整机停机或进入消防联锁 |
| 阀门联锁 | `ValveLinkX4` | 禁止相关输出 |
| 漏水 | `LeakProtectX13` | 停机/报警 |
| 加湿器故障 | `HumidifierFaultX10` | 加湿输出关闭 |
| 压差保护 | `PressureProtectEnableSet=1` 后触发压力开关 | 停机/报警 |
| 传感器故障 | 断开温湿度/压力传感器 | 安全降级或禁止对应输出 |

## 6. 316 外机单机功能测试

316 单机测试可以直连 PSC316 A0B0，但这只用于调试。正式联调仍从 330 写入。

### 6.1 基础通讯

1. USB-RS485 接 PSC316 A0B0。
2. Slave ID 用 `HMIAddrSet`，默认 1。
3. 读 `Parameter[165~197]`。
4. 写一个安全参数，例如 `OutdoorUnitTypeSet`，断电重启后确认保持。

### 6.2 外机类型和禁用

| 参数 | 地址 | 测试 |
|---|---:|---|
| `OutdoorUnitTypeSet` | 165 | 0、1、2 三种制冷环温类型 |
| `OutdoorFanTypeSet` | 166 | 0~4 五种风机类型 |
| `OutdoorDisableSet` | 167 | 置 1 后外机所有运行命令清零 |
| `CompDisableSet1` | 168 | 1# 压缩机禁止启动 |
| `CompDisableSet2` | 169 | 2# 压缩机禁止启动 |

通过条件：

- `OutdoorDisableSet=1` 时压缩机、风机、四通阀、频率给定全部关闭。
- 单台禁用只影响对应压缩机，不误停另一台可运行压缩机。

### 6.3 压缩机和四通阀时序

测试步骤：

1. 给 `Comp1OnOFFFalg=1`，观察风机先开。
2. 等 `FanONLaySet` 后压缩机允许开。
3. 制热模式下再等 `FourWayValveDelaySet` / `FourValveLaySet` 后四通阀动作。
4. 停机时，压缩机先关，四通阀和风机按延时关闭。

关注参数：

| 参数 | 作用 |
|---|---|
| `FanONLaySet` | 风机提前开机时间 |
| `MinStopTimeSet` | 压缩机最小停机 |
| `MinRunTimeSet` | 压缩机最小运行 |
| `FourWayValveDelaySet` | 新规格四通阀延时 |

### 6.4 冷凝风机控制

按 `OutdoorFanTypeSet` 测 5 种模式：

| 类型 | 含义 | 主要观察 |
|---:|---|---|
| 0 | 单风机双速 | 低速/高速互锁 |
| 1 | 双定速风机 | 两路风机分级 |
| 2 | 单变频风机 | 频率随冷凝温度增减 |
| 3 | 一定速 + 一双速 | 定速与双速互锁 |
| 4 | 一变频 + 一定速 | 变频优先，定速补充 |

关注参数：

| 参数 | 作用 |
|---|---|
| `CondFanTLSet` | 冷凝目标温度 |
| `CondFanTLDSet` | 冷凝温度回差 |
| `FanMaxFreqSet` | 变频风机最高频率 |
| `FanMinFreqSet` | 变频风机最低频率 |
| `FanStepSpeedSet` | 加减速速度 |
| `FanMinRunTimeSet` | 最小运行时间 |
| `FanMinStopTimeSet` | 最小停止时间 |
| `FanTempHoldTimeSet` | 低速保持/退出判定 |

### 6.5 化霜

测试目标：确认规格书化霜条件和结束条件。

进入化霜条件需要同时满足：

- 制热模式。
- 有压缩机运行。
- 环境温度低于 `DefrostEnvTempSet` 并持续约 60s。
- 压缩机运行超过 `DefrostCompRunMinSet`。
- 翅片温度低于 `DefrostFinTempStartSet` 并持续约 30s，或翅片传感器故障进入定时化霜降级。
- 除霜间隔达到 `DefrostIntervalAbove0Set` 或 `DefrostIntervalBelow0Set`。
- 另一系统没有正在化霜。

退出化霜条件任一满足即可：

- 翅片温度达到 `DefrostFinTempEndSet`。
- 化霜时间达到 `DefrostTimeAbove0Set` 或 `DefrostTimeBelow0Set`。
- 手动停止化霜。
- 高压开关动作作为化霜退出条件。

关注参数：

| 参数 | 作用 |
|---|---|
| `DefrostEnvTempSet` | 定时化霜环境温度 |
| `DefrostEnvIntervalSet` | 传感器故障时定时化霜间隔 |
| `DefrostEnvTimeSet` | 定时化霜时间 |
| `DefrostCompRunMinSet` | 化霜前最小运行 |
| `DefrostFinTempStartSet` | 化霜开始翅片温度 |
| `DefrostEnvFinGapSet` | 环温-翅温温差 |
| `DefrostIntervalAbove0Set` | 环温大于 0C 化霜间隔 |
| `DefrostIntervalBelow0Set` | 环温小于 0C 化霜间隔 |
| `DefrostFinTempEndSet` | 化霜结束翅片温度 |
| `DefrostTimeAbove0Set` | 环温大于 0C 最大化霜时间 |
| `DefrostTimeBelow0Set` | 环温小于 0C 最大化霜时间 |

### 6.6 外机保护

| 保护 | 测试方法 | 期望 |
|---|---|---|
| 高压开关 | 触发高压输入 | 对应压缩机立即停 |
| 高压传感器 | 高压 > 38.0bar 持续 2s | 对应压缩机保护 |
| 低压 | 低压低于告警并持续 5s | 对应压缩机保护 |
| 化霜低压屏蔽 | 化霜中/化霜后 60s | 不误报低压 |
| 排气高温 | 超过 `ExhaustHightTempSet` | 对应压缩机保护 |
| 电流过高 | 超过 `CurrentHighSet` | 对应压缩机保护 |
| 风机故障 | 风机过载/通讯故障 | 整机或相关系统停机 |
| 环温传感器故障 | 断开环境温度 | 整机保护 |

重点代码：

| 功能 | 代码位置 |
|---|---|
| 高低压/过载 | `PSC316RK-V12-外机\WELLTHINKER\User\ErrorCheck.c` |
| 化霜 | `PSC316RK-V12-外机\WELLTHINKER\User\DefAction.c` |
| 外机风机/环温保护 | `PSC316RK-V12-外机\WELLTHINKER\User\UserAction.c` |

## 7. 330 + 316 联动测试

### 7.1 写 316 参数

从 HMI 写 PSC330 的 1830 起始镜像区，不要直接写 316。

例：把 1# 外机风机类型改成单变频。

1. 写 `Parameter[1831]=2`。
2. 看 `Parameter[1865]` 写状态。
3. 成功后应为 2。
4. `Parameter[1866]` 应为 166。
5. `Parameter[1867]` 与 `Parameter[1868]` 应一致。
6. 断电重启 316 后，重新读取仍应为 2。

### 7.2 330 需求下发到 316

测试步骤：

1. 330 制冷开机。
2. 模拟室温高于设定值。
3. 观察 330 压缩机需求输出。
4. 观察 316 `Comp1OnOFFFalg`、`Comp2OnOFFFalg`。
5. 观察 316 压缩机实际输出、频率给定。
6. 降低室温回到死区，观察卸载/停机。

通过条件：

- 330 有需求时，316 收到对应启停和频率。
- 316 自身保护触发时，330 能读回外机故障和运行状态。
- 330 停机后，316 命令清零。

### 7.3 316 状态回读到 330

当前 316 状态 `1420~1495` 已由 330 读到 RAM，并部分发布到旧显示区。

常用检查：

| 内容 | 316 地址 | 330 可见位置 |
|---|---:|---|
| 化霜请求1 | 1420 | 1# 外机诊断 `1871` 可见当前 `1420`；旧区 `1550+70n+0` |
| 环境温度 | 1422 | 旧区 `1550+70n+2` |
| 压缩机1状态 | 1425 | 旧区 `1550+70n+5` |
| 翅片温度1 | 1431 | 旧区 `1550+70n+11` |
| 高压1 | 1443 | 旧区 `1550+70n+23` |
| 低压1 | 1444 | 旧区 `1550+70n+24` |
| 故障字1 | 1471 | 旧区 `1550+70n+51` |

`n` 为外机号从 0 开始。

## 8. 回归测试清单

每次烧录后至少做这 12 项：

| 序号 | 项目 | 通过条件 |
|---:|---|---|
| 1 | 330 A0B0 通讯 | HMI 能读写允许参数 |
| 2 | 330 到 316 A2B2 | 316 在线，刷新计数变化 |
| 3 | 316 参数写入 | 写状态 2，目标值=回读值 |
| 4 | 330 停机 | 所有压缩机命令清零 |
| 5 | 330 制冷 | 有温差时产生制冷需求 |
| 6 | 330 制热 | 有温差时产生制热需求 |
| 7 | 330 通风 | 只开风机，不开压缩机 |
| 8 | EEV | 过热度变化时步数正确变化 |
| 9 | 加湿/再热/预热 | 对应 AO/DO 输出正确 |
| 10 | 316 风机类型 | 0~4 输出互锁正确 |
| 11 | 316 化霜 | 进入/退出条件正确 |
| 12 | 故障保护 | 高压、低压、传感器、急停均安全 |

## 9. 测试记录模板

```text
日期：
测试人员：
330 固件版本/提交：
316 固件版本/提交：
HMI 工程版本：

通讯：
- 330 A0B0：
- 330 A2B2 -> 316：
- 316 站号：

功能：
- 330 开关机：
- 330 制冷：
- 330 制热：
- 330 通风：
- 330 EEV：
- 330 加湿/再热/预热：
- 316 风机：
- 316 化霜：
- 316 保护：

问题：
1.
2.

处理：
1.
2.
```

