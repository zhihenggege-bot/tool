# PSC330 DI 接线与代码修改指南

> 适用于：所有 DI 统一 NC→GND 接线（常闭接地，断开悬空=74HC165上拉=1）  
> 生成日期：2026-06-22

---

## 1. 当前 DI 接线状态速查

| DI | 功能 | 开关类型 | 正常状态 | 代码处理 | 是否需要改码 |
|----|------|---------|---------|---------|:---:|
| X00 | 电源相序 | NC→GND | 闭合=0 | `InPutStatusSet8=1`（正相） | **参数155=1即可** |
| X01 | 消防 | NC→GND | 闭合=0 | `InPutStatusSet9=1`（正相） | **参数156=1即可** |
| X02 | 远程开关 | NC→GND | 闭合=0=停 | `InPutStatusSet10=1`（正相） | **参数157=1即可** |
| X03 | 泵过载 | NC→GND | 闭合=0 | `InPutStatusSet11=1`（正相） | **参数158=1即可** |
| X04 | 风阀连锁 | NC→GND | 闭合=0 | `InPutStatusSet12=1`（正相） | **参数159=1即可** |
| X05 | 滤网压差 | NC→GND | 闭合=0 | `X_05 = X05`（正相） | **不用改** |
| X06 | 电加热过温 | NC→GND | 闭合=0 | `X_06 = X06`（正相） | **不用改** |
| X07 | 预热过温 | NC→GND | 闭合=0 | `X_07 = X07`（正相） | **不用改** |
| X10 | 加湿器故障 | NC→GND | 闭合=0 | `X_10 = X10`（正相） | **不用改** |
| X11 | 排风机过载 | NC→GND | 闭合=0 | `X_11 = X11`（正相） | **不用改** |
| X12 | 回风机过载 | NC→GND | 闭合=0 | `X_12 = X12`（正相） | **不用改** |
| X13 | 漏水 | NC→GND | 闭合=0 | `X_13 = X13`（正相） | **不用改** |
| X14 | 回风压差 | NC→GND | 闭合=0 | `X_14 = !X14`（**反相**） | **已改码** |
| X15 | 送风压差 | NC→GND | 闭合=0 | `X_15 = !X15`（**反相**） | **已改码** |

---

## 2. 核心规则

> **74HC165 输入悬空 = 内部弱上拉 = 高电平(1)**  
> **接 NC 闭合到 GND = 低电平(0)**

所以"换开关类型"的本质问题只有一个：**什么时候闭合**。

| 开关类型 | 正常时 | 故障时 | 165读到 | 正相代码行为 | 反相代码行为 |
|---------|--------|--------|--------|------------|------------|
| **NC→GND** (常闭接地) | 闭合=0 | 断开=1 | 0→1上跳 | `0不报 1报` ✓ | `0报 1不报` ✗ |
| **NO→GND** (常开接地) | 断开=1 | 闭合=0 | 1→0下跳 | `1报 0不报` ✗ | `1不报 0报` ✓ |

结论：
- **NC→GND 需要正相代码**（`X_0n = X0n`）
- **NO→GND 需要反相代码**（`X_0n = !X0n`）
- **X14/X15 特殊**：它们不是故障信号，是"压差正常=1"的使能信号。NC→GND 下闭合=0，需要反相得 1 → `!X14`

---

## 3. 如果以后换开关，怎么改

### 情况A：某个 DI 从 NC 换成 NO（还是一样接 GND）

逻辑就反了——正常时断开=1 误报，闭合=0 不报。需要把代码从正相改为反相。

**改法取决于这个 DI 属于哪一组：**

#### X00~X04：改参数（不用重编译）
```c
// 当前（NC→GND，正相，闭合=0=正常）：
// InPutStatusSet8~12 = 1

// 改成 NO→GND（断开=1=正常，闭合=0=报警）：
// 把对应参数从 1 改成 0（反相）
// Modbus 写：155=0（X00）/ 156=0（X01）/ 157=0（X02）/ 158=0（X03）/ 159=0（X04）
```

原理：`InPutStatusSet=1` → `X_0n = X0n`（正相）；`InPutStatusSet=0` → `X_0n = !X0n`（反相）。

#### X05：改 user.c 一行码（需重编译）
```c
// 文件：User/UserSrc/user.c  DI_Points_Process()
// 当前（NC→GND）：X_05 = (1)?(X05):(!X05);  → X_05 = X05
// 改成 NO→GND：X_05 = (0)?(X05):(!X05);     → X_05 = !X05
// 简单写就是：X_05 = !X05;
```

#### X06~X13：改 user.c 一行码（需重编译）
```c
// 文件：User/UserSrc/user.c  DI_Points_Process()
// 当前（NC→GND）：X_06 = X06;
// 改成 NO→GND：X_06 = !X06;

// 同理 X_07, X_10, X_11, X_12, X_13 每条单独改
```

#### X14/X15：改 user.c（需重编译）
```c
// 当前（NC→GND，反相）：X_14 = !X14;
// 如果换成 NO→GND：X_14 = X14;   （删掉感叹号）
// 如果换成 NO→VCC（闭合=1=正常）：X_14 = X14; （也要正相）
```

### 情况B：某个 DI 开关不接到 GND 了，改接到 VCC

这就完全颠倒了：

| 接线方式 | 闭合时 | 断开时 | 正相代码 | 反相代码 |
|---------|--------|--------|---------|---------|
| **NC→GND** | DI=0 | DI=1 | 0不报1报 | 0报1不报 |
| **NO→VCC** | DI=1 | DI=0 | 1报0不报 ✗ | 1不报0报 |
| **NC→VCC** | DI=1 | DI=0 | 1报0不报 ✗ | 1不报0报 |
| **NO→GND** | DI=0 | DI=1 | 0不报1报 | 0报1不报 ✗ |

规则：NC→GND 和 NO→VCC 的效果相反（虽然都是"正常闭合"，但接到不同电平）。**统一用 NC→GND 最省事，不要再换其他接法。**

---

## 4. 开关换型时的验证步骤

换完开关并改完代码后，逐路验证：

1. **重启，看总报警**：`861=0`、`710=0`
2. **看故障字**：`630=0`、`631=0`
3. **人为模拟故障**（拔线/断开触点），确认报警触发
4. **恢复接线，复位**（写 Modbus 地址 0x0001=1 触发 `SysErrorReset`），确认报警消失

---

## 5. 代码相关行速查

```
文件: User/UserSrc/user.c

行 446-480  DI_Points_Process()   — DI 读取+滤波
行 448-456  极性赋值 (X_00~X_05)
行 463-469  _En 推导 (Cnt>=30 → 3s防抖)
行 472-493  计数累加 (X00~X15)

行 495-500  InputStartCheck()     — Cnd门控放开

文件: User/UserHead/User.h

行 142-158   业务宏映射 (X00_En→PowerPhaseErrX0 等)
行 382-386   InPutStatusSet8~12 参数定义

文件: User/UserSrc/ErrorCheck.c

行 199-270   BoardErrAction()     — 故障判断与动作
行 269       水流门控 (PressureProtectEnableSet==0)
行 288       泵过载判断

参数:
  155~159    InPutStatusSet8~12   — 0=反相 1=正相
  386        PressureProtectEnableSet — 0=旁路 1=启用压差保护
  594~623    故障字 (HREST)
  630~631    SYSErr1/SYSErr2   — 打包故障字
  703        ErrTotal          — 累计故障次数
  861        ErrorOutFlag      — 总报警
  888~889    Supply/ReturnPressureOkDisp
  895        IndoorSpecFaultDisp — 规格故障码
```
