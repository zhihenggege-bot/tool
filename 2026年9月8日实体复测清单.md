# 2026年9月8日 PSC330/PSC316 实体复测清单

**编制日期：** 2026年9月7日  
**计划测试日期：** 2026年9月8日  
**适用固件：** 2026年9月7日最新源码生成的 PSC330、PSC316 固件  
**测试接口：** COM13 -> PSC330 A0/B0；PSC330 网关 -> PSC316 A2/B2  
**调试接口：** ST-Link 接 PSC330 或 PSC316，按当天实际连接板卡选择  
**测试原则：** 只有实体板卡动作、实际寄存器读写、真实输出边沿、ST-Link 内部变量或真正断电重启才算实体测试。只读源码、编译成功、脚本执行成功不能标记实体通过。

## 1. 状态符号

- `[ ]` 尚未执行。
- `[x]` 已执行且满足严格通过标准。
- `[~]` 已执行，但边沿、范围、采样精度或附加路径仍不足。
- `[!]` 已执行并发现异常，或测试工况无效。
- `[-]` 按用户要求暂缓，不做物理断线类测试。
- `[S]` 仅源码核对，不计入实体通过。

### 1.1 明日必须执行的最新修改回归

以下八项直接受2026年9月7日最新PSC330修改影响，必须使用最新固件逐项实体执行：

- `[ ]` AO送风机：启动时同时确认`Y03 + DA7`，停机时均按顺序归零；
- `[ ]` AO回风机：启动时同时确认`Y06 + 回风机AO`，停机时均按顺序归零；
- `[ ]` DO送/回风机：不得因为DO运行而错误产生对应AO输出；
- `[ ]` 风机AO与风阀AO通道冲突：必须禁止不合法输出，不得误启动；
- `[ ]` 通风关机：送风机先关，约30秒后回风机关闭，排风机按实际启动顺序反向关闭；
- `[ ]` 制冷关机：先完成制冷负载退出，再按最新风机关机顺序退出；
- `[ ]` 制热关机：制热负载退出并等待60秒后，送风机先关、回风机后关；
- `[ ]` 制热启动早期取消：取消后未到达的风机、制热、压缩机和加湿阶段不得继续投入。

历史已严格通过、明日不作机械重复：

- `[x]` EEV特殊蒸发温区规则；
- `[x]` PSC330和PSC316普通关闭额外`+20`步；
- `[x]` PSC330和PSC316紧急关闭额外`+10`步；
- `[x]` PSC330和PSC316关闭速度`20步/秒`；
- `[x]` 参数328的`+30、-30、0、+90、-90`五种启动矩阵。

上述历史通过项只有在EEV源码再次修改、固件版本无法确认、现场出现不一致动作时才重测。参数328明日只需回归它与最新反向关机状态机的交互，不必重跑全部五种启动矩阵。

## 2. 测试前统一准备

### 2.1 固件和参数基线

1. 记录 PSC330、PSC316 HEX 文件路径、生成时间、文件 SHA256。
2. 用 ST-Link 读取 PSC330 当前 Flash，与目标 HEX 做比较；如能操作 PSC316，也记录 PSC316 Flash/版本信息。
3. 在测试开始前通过 COM13 读取并保存：
   - `Parameter[709]` 开关机步骤；
   - `Parameter[398]` 模式/测试状态；
   - `Parameter[236]`、`453`、`454`、`493`、`495`、`540`；
   - PSC330 AI/NTC 校准区；
   - PSC316 网关镜像区 `1830` 起始区域；
   - 1#、2# PSC316 的 `165~206` 参数和运行/故障状态区；
   - 当前运行模式、运行命令、故障锁存、报警、压缩机命令/反馈、EEV 当前步数和目标步数。
4. 测试开始前确认：
   - `Parameter[709]=0`；
   - 330、316 无故障；
   - 运行命令为 0；
   - 所有压缩机、风机、风阀、加热器、加湿器和 EEV 处于可记录的初始状态；
   - `run_psc330_monitor` 如已打开，不要擅自关闭；测试脚本必须正确释放 COM13。

### 2.2 统一采样内容

每个涉及启停的案例至少记录以下时间戳：

| 类别 | 必须记录 |
|---|---|
| 330 模式 | 运行命令、实际模式、目标模式、`SysStep`、自动切换方向和计时 |
| 风阀/风机 | Y04 送风阀、Y06 回风机、送风机 DO/AO、回风机 DO/AO、Y10 排风机、排风 AO |
| 保护 | X00、X02、X04、X05、X06、X07、X10、X11、X12、X13 当前位、锁存位、Y05 |
| 压缩机 | 330 下发命令、316 压缩机命令、继电器/反馈边沿、故障位、累计运行时间 |
| EEV | 当前步数、目标步数、脉冲/相位、方向、PID 步数、蒸发温度、吸气压力、饱和温度、吸气温度、过热度 |
| 输出 | DA0、DA1、DA7、DA9、Y03、Y04、Y06、Y10、Y11、Y13 及实际电压/继电器状态 |

每个案例结束后必须恢复：

1. 运行命令为 0，`SysStep=0`；
2. 所有临时参数恢复到测试前备份值；
3. 故障输入、故障锁存和 Y05 按初始状态恢复；
4. 压缩机、风机、阀门、加热、加湿和 EEV 输出归零；
5. COM13 正常释放，监控软件仍能独立运行。

### 2.3 历史通过项目的处理

历史上已经有完整实体证据、且不受2026年9月7日源码修改影响的项目，明日不必机械重复。以下情况必须重测：

1. 修改代码直接影响的输出、状态机或共用函数；
2. 历史案例只有旧固件证据；
3. 历史结论为`[~]`、`[!]`或测试工况无效；
4. 固件下载、参数迁移或地址映射可能改变结果；
5. 现场出现与历史记录不一致的新动作。

明日优先验证最新修改，不把时间花在已经严格通过且未受影响的通信、辅助加热矩阵等项目上。

## 3. 第一优先级：2026年9月7日修改回归

### 3.1 AO 送风机的启动 DO 与输出互锁

**修改目的：** 送风机使用模拟量控制变频器时，除 DA7 输出目标电压外，还必须给对应变频器启动 DO；送风机为 DO 模式时不得错误产生 AO；送风机和送风阀存在 AO 通道冲突时必须安全禁止。

**前置条件：**

- 分别准备送风机 DO、送风机手动 AO、送风机自动 AO 三种配置；
- 另准备送风阀 AO 冲突配置；
- 回风机配置分别使用 DO 和 AO；
- 记录 Y03/送风机启动 DO、DA7、Y04。

**测试步骤：**

1. 停机状态设置送风机为 DO，启动通风，记录 Y03、DA7、Y04。
2. 停机后设置送风机为手动 AO，给定 0V、5V、10V，启动通风，记录 Y03 与 DA7。
3. 设置送风机为自动 AO，给出有效风速或风压需求，记录 Y03 与 DA7。
4. 设置送风机 AO 与送风阀 AO 发生通道冲突，尝试启动通风。
5. 每种配置都执行正常停机并记录归零时间。

**预期动作：**

- DO 模式：Y03 按送风机启动阶段动作，DA7 不应被错误驱动。
- AO 模式：Y03 作为变频器启动信号动作，DA7 按需求输出 0~1000。
- AO 冲突：对应冲突通道禁止错误启动，Y03 与 DA7 不得同时形成不合法输出。
- 停机：Y03、DA7、Y04 按状态机归零。

**严格通过标准：**

- AO 送风机启动时，Y03 和 DA7 均有正确状态；
- DO 送风机模式不产生错误 DA7；
- 冲突配置不能造成设备误启动；
- 同一配置连续启动/停机两轮结果一致。

### 3.2 AO 回风机的启动 DO 与输出互锁

**前置条件：** 回风机分别使用 DO、手动 AO、自动 AO；记录 Y06、DA4/回风 AO、回风压差输入。

**测试步骤：**

1. 回风机 DO 模式启动通风，记录 Y06 和回风 AO。
2. 回风机手动 AO 模式给出 0V、5V、10V，记录 Y06 和回风 AO。
3. 回风机自动 AO 模式建立风速/风压需求，记录 Y06 和回风 AO。
4. 回风机 AO 与回风阀 AO 冲突时启动。
5. 停机并记录 Y06、回风 AO 和回风阀归零。

**严格通过标准：**

- 回风机 AO 模式必须同时有 Y06 启动信号和模拟量输出；
- 回风机 DO 模式不得错误产生 AO；
- 冲突配置不得导致 Y06 或 AO 误输出；
- 正常关机时回风机必须按送风机之后的规定时序关闭。

### 3.3 正常关机顺序：通风、制冷、制热

**规格基准：**

1. 送风机先关闭；
2. 约 30 秒后回风机关闭；
3. 排风机依据参数328和本次实际启动顺序反向关闭，满足“先开的后关”；
4. 风机关闭后再等待约 30 秒关闭风阀；
5. 关闭顺序应依据本次实际启动时间线反向执行。

**测试步骤：**

1. 通风模式启动，记录 Y04、送风 AO/DO、Y06、回风 AO/DO、Y10、排风 AO 的启动边沿。
2. 通风稳定运行后关机，记录上述输出的关闭边沿。
3. 制冷模式重复一次；若无有效制冷负载，至少确认风机/风阀主状态机。
4. 制热模式重复一次；记录制热、预热、加湿和压缩机先行关闭状态。
5. 参数 328 分别设置 `+30`、`0`、`-30`，至少选择其中一项在运行中修改，确认当前停机仍按启动时快照执行。

**预期动作：**

`送风机关闭 -> 约30s -> 回风机关闭；排风机按328和实际启动顺序反向关闭 -> 全部风机关后约30s -> 风阀关闭`

**严格通过标准：**

- 送风机关闭边沿早于回风机；
- 送风机与回风机关闭边沿差值为 30 秒，允许使用规格书允许的调节范围；
- 排风机不得早于其规定的前置风机；
- 风阀不得早于对应风机错误关闭；
- 通风、制冷、制热三种路径均通过，不能只测一个共用状态机路径。

**重点风险：** 2026年9月7日历史记录中曾出现 Y06 先于送风机关闭。该项必须用高采样率重新确认，发现一次真实反序即判该路径不通过。

### 3.4 排毒和消排毒风机顺序

**规格及当前策略：**

- 排毒阶段只能排风机先开；
- 排毒阶段风机间隔最大 60 秒；
- 消排毒进入排毒阶段后同样使用排风机先开和不超过 60 秒的限制；
- 消毒阶段的送风机、消毒装置及切换到排毒阶段的间隔单独记录。

**测试矩阵：**

| 模式 | 参数328 | 预期第一台 | 最大间隔 |
|---|---:|---|---:|
| 排毒 | +30 | 排风机 | 60s |
| 排毒 | -30 | 排风机 | 60s |
| 排毒 | 0 | 排风机 | 60s，不能按同时启动替代 |
| 排毒 | +90 | 排风机 | 约60s，不得达到90s |
| 排毒 | -90 | 排风机 | 约60s，不得送风机先开 |
| 消排毒排毒阶段 | +90/-90 | 排风机 | 约60s |

**记录：**

- 新风阀、送风阀、回风阀、排风阀；
- 送风机、排风机、消毒装置；
- 消毒结束时间、送风机关闭时间、回风阀关闭时间；
- 排毒阶段排风机和送风机边沿。

**严格通过标准：**

- 所有排毒相关案例排风机先开；
- 送风机与排风机间隔不超过 60 秒；
- 参数负值不能使送风机先开；
- 消排毒在从消毒阶段转排毒阶段时不能跳过排风优先规则；
- 结束后停机或恢复原模式均不得误启动。

### 3.5 制热启动顺序及启动中途取消

**规格顺序：**

`送回风阀 -> 150s/无风阀10s -> 排风机/送风机/回风机 -> 送回风压差确认 -> 预热允许 -> 主制热/热泵/辅热允许 -> 加湿允许`

**测试步骤：**

1. 制热启动前清除所有需求，记录初始输出。
2. 建立预热需求、热泵制热需求、主制热需求、辅热需求和加湿需求。
3. 启动制热，逐项记录：
   - Y04；
   - 送风机 DO/AO；
   - Y06、回风 AO；
   - 送风压差确认达到 10 秒；
   - 回风压差确认达到 10 秒；
   - 预热需求/输出；
   - 主制热 AO/分段输出；
   - 316 压缩机命令和反馈；
   - 辅热需求/输出；
   - 加湿需求、Y13、加湿 AO。
4. 在以下四个阶段分别取消运行：
   - 风阀预开阶段；
   - 送风机启动前；
   - 送风压差确认阶段；
   - 回风压差确认后、加湿投入前。
5. 每次取消后观察至少完整停机周期。

**严格通过标准：**

- 压差确认未完成前，预热、主制热、热泵、辅热、加湿不得投入；
- 预热允许先于或不晚于主制热/热泵/辅热；
- 加湿不得早于制热主链路；
- 取消后未到达的后级设备不得因为旧计时器继续投入；
- 在风机尚未启动阶段取消时，送风 AO、Y06、回风 AO 不得在取消后被状态机再次启动；
- 故障、消防、急停或模式切换取消时不得自动重新启动。

**历史风险：** 2026年9月7日已发现启动早期取消时送风 AO 和 Y06 仍短暂启动，约 151 秒后才完成停机。修改后必须重点回归。

### 3.6 今日修改回归的执行顺序

建议按以下顺序快速完成，发现疑点时先记录，集中到最后复测：

1. AO送风机：Y03+DA7；
2. AO回风机：Y06+DA4；
3. DO风机不误出AO；
4. AO通道冲突安全禁止；
5. 通风关机顺序；
6. 制冷关机顺序；
7. 制热关机顺序；
8. 制热启动早期取消；
9. 正常制热启动严格顺序；
10. 参数328排风机反向关机边沿。

## 4. EEV历史通过基线与条件性回归

本章保留完整方法，作为版本变更、现场异常或最终验收时的条件性回归规程。2026年9月7日已经使用ST-Link完成PSC330和PSC316关闭超行程、关闭速度及特殊蒸发温区实体测试；若明日下载的EEV相关代码未再变化，不列入八项强制回归。

### 4.1 EEV 测试参数基线

| 参数 | 默认值 | 规格范围 | 测试要求 |
|---|---:|---:|---|
| 过热度设定 Y | 5℃ | 2~10℃ | 读回并测试偏差 |
| 过热度幅差 YD | 2℃ | 2~5℃ | 死区边界 |
| KP | 5 | 1~100 | 当前固件 PID 参数 |
| TP | 5s | 1~10s | 记录每周期边沿 |
| TI | 100s | 1~1000s | 保持当前公式 |
| TD | 0s | 规格文字与固件范围有冲突 | 按当前固件实现 |
| 起始开度 | 200步 | 0~500 | 启动初始化 |
| 每周期最大步数 | 20步 | 3~50 | 正、负方向都测 |
| 最大开度 | 500步 | 200~500 | 上限钳位 |
| 化霜开度 | 350步 | 100~500 | 化霜动作 |

> PID 公式以当前固件为准。规格书中的公式存在文字/括号问题，本轮只验证当前代码已经采用的公式及其方向、限幅和历史值处理，不按错误文字公式反推结果。

### 4.2 运行模式归属

1. 制冷：PSC316 室外机 EEV 应关闭；PSC330 室内机 EEV 按吸气过热度控制。
2. 热泵制热：PSC330 室内机 EEV 应关闭；PSC316 室外机 EEV 按吸气过热度控制。
3. 化霜：室内机 EEV 为化霜设定步数，室外机 EEV 关闭。
4. 停机/普通故障：对应 EEV 以 20 步/秒关闭，普通关闭目标为当前位置加 20 步后再归零。
5. 紧急停机：对应 EEV 以 20 步/秒关闭，急停目标为当前位置加 10 步后再归零。

### 4.3 特殊蒸发温区矩阵

**测试前提：**

- 必须有有效压缩机运行；
- EEV 当前开度必须大于 0；
- 记录蒸发温度、过热度、PID 原始步数和最终目标变化；
- 必须能区分 5 秒 PID 周期和网关聚合采样。

**矩阵：**

| 蒸发温度 | PID 计算步数 | 预期 |
|---|---:|---|
| >12℃ | >=0 | 每个 TP 周期关闭 5 步 |
| 10~12℃ | >=0 | EEV 保持，不动作 |
| >12℃ | <0 | 按正常过热度 PID，不强制关闭5步 |
| 10~12℃ | <0 | 按正常过热度 PID |
| <10℃ | 任意 | 按正常过热度 PID |
| 任意 | 死区内 | 过热度在 `Y ± YD/2` 内保持 |

**操作步骤：**

1. 将 EEV 运行到约 200 步以上。
2. 先施加 10~12℃、PID 非负工况，至少观察 3 个 TP 周期。
3. 将蒸发温度升至 12℃以上，记录至少 5 个 TP 周期的目标步数。
4. 通过吸气温度或压力校准建立 PID 负值工况，确认特殊规则不错误覆盖负值。
5. 设置过热度进入死区，确认 PID 不动作。
6. 测试结束后恢复传感器校准和参数。

**严格通过标准：**

- 10~12℃且 PID 非负时，连续 3 个周期目标不变；
- >12℃且 PID 非负时，目标每 5 秒关闭 5 步，允许最后归零钳位；
- 特殊规则不能被 PID 历史不足、死区判断或旧状态提前返回跳过；
- PID 为负时不强制执行特殊关闭；
- 采样记录不能把网关跳过中间目标误判为 10 步/周期。

### 4.4 普通关闭 +20 步超行程和 20 步/秒

**操作步骤：**

1. 建立有效压缩机和 EEV 运行，令当前步数稳定在约 100 步。
2. 触发普通停机或普通故障。
3. 用 ST-Link 连续读取关闭模式、关闭目标、当前步数、脉冲相位和步进方向。
4. 记录目标初始值以及每秒变化。
5. 等待当前步数和目标均归零。

**预期：**

若当前步数为 100，普通关闭初始目标应为 120；目标随后按：

`120 -> 100 -> 80 -> 60 -> 40 -> 20 -> 0`

**严格通过标准：**

- 初始目标等于触发时当前位置加 20；
- 中段关闭速度为 20 步/秒；
- 末段只允许因归零钳位少于 20 步；
- 普通故障期间手动开度/PID 不能重新打开；
- 故障恢复前不能继承旧 PID 目标继续开阀。

### 4.5 紧急关闭 +10 步超行程和 20 步/秒

**操作步骤：**

1. 建立 EEV 当前开度约 100 步。
2. 触发急停。
3. ST-Link 读取急停模式、关闭目标、当前步数和脉冲。
4. 记录每秒关闭边沿和最终归零。

**预期：**

若当前步数为 100，急停初始目标应为 110；目标随后按：

`110 -> 90 -> 70 -> 50 -> 30 -> 10 -> 0`

**严格通过标准：**

- 初始目标等于急停触发时当前位置加 10；
- 中段为 20 步/秒；
- 急停过程中普通关闭的 +20 不得再叠加成 +30；
- 除规格明确的报警输出例外外，所有负载输出应关闭；
- EEV 最终目标和当前步数均为 0。

### 4.6 普通关闭转急停

1. EEV 处于约 100 步；
2. 先触发普通关闭，确认目标为约 120；
3. 尚未归零时触发急停；
4. 观察目标是否重新按当前步数加 10 计算；
5. 检查是否出现 +30 步、方向反转或重新开阀。

**通过标准：**

- 急停接管后不叠加普通关闭的 +20；
- 关闭过程连续，不因模式切换重新初始化为更大目标；
- 手动/PID 不能覆盖急停关闭。

### 4.7 真实运行 PID、行程和化霜

**需要实体完成：**

- 制冷内机 EEV 真实吸气过热度上升、下降、死区、正负方向；
- 热泵外机 EEV 真实吸气过热度上升、下降、死区、正负方向；
- 0~500 步全行程；
- 单周期正向和负向不超过 20 步；
- 化霜时内机 EEV=350 步，外机 EEV=0；
- 压缩机故障、传感器故障、普通停机和急停时对应 EEV 安全关闭；
- 双独立 EEV 故障时只关故障系统；
- 共用 EEV 时不得出现健康压缩机运行但共用 EEV 被关闭。

## 5. 第三优先级：压缩机低载和队列

### 5.1 低载两台同外机优先

**前置条件：** 两台 PSC316、四台压缩机在线且健康，环境温度控制方式有效，低载目标为 2。

**步骤：**

1. 清除运行时间差导致的旧选择状态；
2. 建立低载需求；
3. 记录四台压缩机健康状态、累计运行时间、目标状态和实际反馈；
4. 重复启动两轮。

**预期：**

`外机1压缩机1 + 外机1压缩机2`

或在主外机选择已锁定时，使用当前锁定外机的两台健康压缩机。不能因为每次刷新运行时间就五分钟左右重新换外机。

### 5.2 主外机一台故障时跨外机补位

1. 低载目标为 2；
2. 外机1两台压缩机运行；
3. 对外机1压缩机2建立有效高压/过载故障；
4. 记录故障成立、故障压缩机关断、补位命令、补位反馈；
5. 检查剩余运行数和目标数。

**预期：**

`外机1压缩机1 + 外机2压缩机1`

**通过标准：**

- 健康压缩机保留；
- 故障压缩机被排除；
- 从下一可用外机补足一台；
- 补位仍遵守全局 30 秒命令/反馈间隔、最小停机 120 秒；
- 不因故障恢复立刻发生来回振荡。

### 5.3 主外机整机不可用

1. 低载目标为 2；
2. 使外机1两台压缩机均故障或整机禁用；
3. 观察外机2两台压缩机是否接替；
4. 检查外机1恢复后是否无条件抢回。

**预期：**

`外机2压缩机1 + 外机2压缩机2`

### 5.4 全局启动队列

**必须覆盖：**

- 1、2、3、4 台外机可用配置；
- 目标压缩机数 1、2、3、4；
- 两台外机四压缩机时目标 1、2、3、4；
- 四台外机八压缩机时目标 1、2、3、4、5、6、7、8；
- 已运行外机优先；
- 未运行外机在运行时间差小于 2 小时时按外机序号；
- 运行时间差真正超过 2 小时且发生重新选择时按累计运行时间较短者；
- 同一外机双压缩机按累计运行时间较短者；
- 故障、禁用压缩机和禁用外机跳过。

**每次必须记录：**

- 330 目标数量和需求 `N/Z`；
- 330 下发命令时间；
- 316 压缩机反馈时间；
- 相邻命令间隔和相邻实际反馈间隔；
- 反馈晚于 30 秒、晚于 60 秒、无反馈时的队列状态。

**严格通过标准：**

- 全局启动命令/实际反馈间隔不得小于 30 秒；
- 后一台不应在前一台反馈未成立时被错误推进；
- 无反馈超过 60 秒时命令撤销或临时排除，后续健康压缩机可继续加载；
- 不能把命令采样延迟误判为实际反馈间隔；
- 健康运行外机不因累计时间每个控制周期刷新而主动轮换。

### 5.5 最小运行、最小停机和减载

- 最小运行时间：压缩机从实际运行反馈成立起至少 180 秒；
- 最小停机时间：压缩机从实际停止反馈起至少 120 秒；
- 关机/开机动作间隔：至少 30 秒；
- 减载优先关闭运行时间较长者；
- 再加载时不得选择仍处于最小停机锁定的压缩机；
- 化霜外机不参与正常加减载。

## 6. 第四优先级：制热、加湿、再热和风阀

### 6.1 主制热/再热/辅热输出

每种对象均测试：

1. 0~10V 连续输出；
2. 1:2 分级；
3. 1:2:4 分级；
4. 1:1:1 可控硅；
5. 需求上升；
6. 需求保持；
7. 需求自然下降到 0；
8. 停机归零；
9. 非法共用/独立组合互斥。

**通过标准：**

- 模拟量与需求方向一致；
- 分级输出按规格逐级投入、逐级退出；
- 自然下降与停机强制归零都通过；
- 主制热、再热、辅热不能争用同一物理输出；
- 电加热故障时电加热归零且加湿器关闭，不误停其他允许运行设备。

### 6.2 加湿

测试：

- 加湿 AO PID 上升、保持、完整下降；
- 电极式、电热式、蒸汽式配置；
- Y13 开关型自动输出；
- 制热故障、预热故障、电加热故障时强制关闭；
- 故障恢复后加湿重新允许；
- 停机和模式切换归零。

严格要求：故障成立后 Y13、加湿 AO 和加湿需求均归零；不能只观察到报警而不检查实际输出。

### 6.3 风阀、风速、风压、CO2、焓差

覆盖：

- 房间压差低/中/高；
- CO2 低/中/高；
- 焓差有利/不利；
- 手动新风 0/2.5/5/10V；
- 混风反向；
- 排风跟随；
- 自动模式全部组合；
- 边界限幅；
- AI3 风速/风压上升、保持、下降、故障降级和无跳变恢复。

每个点同时记录 Y04、Y06、送风 AO、回风 AO、排风 AO，避免将阀门和风机点位混淆。

## 7. 第五优先级：保护和恢复

### 7.1 PSC330 保护

通过寄存器反转或模拟输入进行实体测试，优先不做物理断线：

| 保护 | 确认时间 | 预期动作 |
|---|---:|---|
| X00 相序/电源 | 3s | 报警、停所有室外机 |
| X02 外部连锁 | 3s | 关机停所有设备，恢复后自动恢复 |
| X04 风阀连锁 | 3s | 报警、整机停机、锁存/复位 |
| X05 滤网压差 | 15s | 报警，按规格恢复 |
| X06 主加热故障 | 3s | 电加热停止、加湿全关，其他设备按规格继续 |
| X07 预热故障 | 3s | 预热停止、相关加湿联锁 |
| X10 加湿故障 | 3s | 加湿停止、报警 |
| X11 排风机故障 | 3s | 排风机停机，确认是否整机停机按规格 |
| X12 回风机故障 | 3s | 室内外机停止、报警 |
| X13 漏水 | 3s | 室内外机停止、报警、锁存 |
| X14/X15 压差 | 风机启动60s后再计10s | 停机、报警 |

传感器故障要分通风和非通风模式：

- 非通风：室外机和室内机负载停止，风机风阀按规格例外；
- 通风：内风机可继续启停和运行；
- 恢复/复位后不得自动恢复到故障前危险负载，除规格明确自动恢复者外。

### 7.2 PSC316 保护

覆盖：

- 高压；
- 低压；
- 排气温度；
- 吸气温度；
- 翅片温度；
- 环境温度；
- 压缩机过载；
- 冷凝风机故障；
- 通信/网关异常；
- 运行中普通故障 EEV 安全关闭。

每项至少检查对应压缩机、对应 EEV、对应外机、Y05/网关故障状态和恢复后的可用容量。

### 7.3 紧急停机

必须同时观察：

- 压缩机命令和实际反馈；
- 送风机 DO/AO、回风机 DO/AO、排风机；
- 所有风阀；
- 主制热、预热、辅热、加湿；
- EEV +10 步超行程和 20 步/秒；
- Y05 综合报警。

当前历史疑点：急停时 Y05 曾保持吸合。需先按现场定义确认 Y05 是否允许作为报警例外；若规格坚持“除 EEV 外所有用电设备失电”，则 Y05 也必须失电。

## 8. 第六优先级：自动化霜和可靠性

实体覆盖：

- 化霜开始条件；
- 低压检测屏蔽；
- 室外风机关；
- 四通阀失电；
- 压缩机按 30 秒间隔投入；
- 内机 EEV 化霜开度；
- 化霜结束温度；
- 最大化霜时间；
- 压缩机关闭后 60 秒待机；
- 化霜结束后辅热和制热恢复；
- 两台外机协调；
- 连续多轮化霜。

可靠性覆盖：

- 长时间制冷；
- 长时间制热；
- 多轮启停；
- 传感器边界抖动；
- 长时间 COM13 轮询；
- 参数保存和真正断电重启；
- 316 运行时间保存、CRC 破坏检测；
- 四块 316 独立校准保存。

## 9. 第七优先级：来电自启、定时、维护和DA8

### 9.1 来电自启

- `[ ]` 允许状态下，通风运行断电后恢复状态、模式和设定值；
- `[ ]` 允许状态下，制冷运行断电后恢复；
- `[ ]` 允许状态下，制热运行断电后恢复；
- `[ ]` 屏蔽状态下，断电后保持模式和设定值但上电必须关机；
- `[ ]` 上电20秒安全延时和EEV归零期间不得提前启动负载；
- `[ ]` 消排毒过程中断电，不得无条件从中间阶段继续；
- `[ ]` 外部连锁或故障存在时不得被来电自启绕过。

以上RC16/EEPROM保持必须使用真正断电，ST-Link复位只能作为补充。

### 9.2 定时开关机

- `[ ]` 完全停机时定时开机；
- `[ ]` 正常运行时定时关机并完成原模式停机顺序；
- `[ ]` 状态机进行中不重复触发；
- `[ ]` 故障、消防、急停期间禁止定时启动；
- `[ ]` 手动/远程/PLC/定时控制方式切换无误启动；
- `[ ]` 定时启用参数断电保存。

### 9.3 维护提醒

- `[ ]` 关闭维护提醒时不报警；
- `[ ]` 仅在`SysStep=2`时累计运行小时；
- `[ ]` 达到维护间隔时提醒成立；
- `[ ]` 停机时间不累计；
- `[ ]` 维护复位后累计和提醒清零，复位命令自动清除；
- `[ ]` 真正断电后累计小时、间隔和启用状态保持。

### 9.4 冷水阀DA8

- `[ ]` 停机和非制冷模式DA8为0；
- `[ ]` 制冷自动需求上升、保持、下降；
- `[ ]` 手动0、250、500、1000与实际0~10V对应；
- `[ ]` 传感器部分停机、普通故障和急停输出安全；
- `[ ]` 防冻路径按当前规格定义到安全开度；
- `[ ]` DA8与其他AO所有者不冲突。

## 10. 不能通过实体操作完成的项目

按用户要求暂缓：

- 物理断开 PSC316 通信；
- RS485 物理断线和恢复；
- 必须拆线才能形成且不能通过寄存器反转模拟的故障；
- 没有板卡、没有实际负载时的真实冷媒压力准确性。

这些项目必须标记 `[-]` 或“待现场条件满足”，不能写成通过。

## 11. 明日记录模板

```text
日期：
测试人员：
PSC330 HEX/Flash：
PSC316 HEX/Flash：
COM13：
ST-Link连接：

案例编号：
模式：
前置参数：
前置状态：
操作过程：
实际边沿：
关键寄存器：
输出测量：
故障/恢复：
参数恢复：
结论：[x]/[~]/[!]/[-]
问题记录：
原始日志：
```

## 12. 明日结束时必须输出的总结

1. 今日实际完成的实体案例；
2. 每个案例的实际时间和边沿；
3. 通过、部分通过、异常、工况无效、未测试、暂缓；
4. 发现的新问题；
5. 修改后仍需回归的项目；
6. 所有参数、故障和输出是否恢复；
7. 明确注明“没有把源码检查或编译成功当作实体通过”。
