# 交接说明 2026-09-14

> 本文档由 Claude Code 在 2026-09-14 会话中断后重建。
> 前一段会话（`1d8e5114`，26MB）在 09-14 06:08 因上下文超限（1,018,515 + 32,000 > 1,048,576）中断，对话内容丢失，本文从会话记录 + 仓库现状反推而成。

---

## 1. 项目是什么

**定频直膨机（恒温恒湿/新风处理机组）空调控制板固件**，双板主从：

| 侧 | 板卡 | MCU | 职责 |
|---|---|---|---|
| 内机 | PSC330RK-V10（主控，Modbus 主站） | STM32F407 | 房间温湿度 PID、风阀、加湿/再热/预热、送/回/排风机、4 路室内 EEV、消排毒、定时 |
| 外机 | PSC316RK-V12（从站） | STM32F103VC | 压缩机 0-8 台加减载、冷凝风机、化霜、四通阀、外机 EEV、压力/温度采集 |

- **规格书基线：20260428 版**。`采用电子膨胀阀定频直膨机控制规格书260124.doc` 已作废，不得作为判据。
- 裸机超循环无 RTOS，SysTick 标志位驱动；Modbus RTU 9600 8N1。
- 链路：`HMI/监控 ↔ A0B0 ↔ 330 Parameter[] ↔ A2B2 ↔ 316 Parameter[]`。**HMI 读外机必须经 330 镜像区，不要直连 316。**
- 工具链：Keil MDK（`D:\Keil_v5`），工程 `PSC330RK-V10-内机/Project/PSC330RK-V10.uvprojx`。
- 监控：`psc330_monitor_tool`（Python + tkinter，COM 口）、MCGS 组态屏。

**仓库结构**：根目录是多仓库并置。`PSC330RK-V10-内机/`、`PSC316RK-V12-外机/`、`psc330_monitor_tool/` 各自是独立 git 仓库；根仓库只收文档/工具/HMI。

---

## 2. 最近几天做了什么

| 日期 | 内容 |
|---|---|
| 9/8 | 实体复测回归（9/7 那批 AO/DO 风机与关机顺序改动）。**8 项"必须执行"的回归至今全是 `[ ]`** |
| 9/9 | 330 `bbe81a9` 风机输出与关机顺序协调；《压缩机 0-8 台加减载完整流程图》 |
| 9/10 | 外机 `6bcf8b6` **环境保护解耦**；20:23 `bbd8975` 删死函数；写《环境保护解耦测试步骤》 |
| 9/11 | 外机 `0caac2e` 规格书参数与逻辑偏差修正；产出《固件审计清单.html》（33000 行只读审计，78 条待处理） |
| 9/13 | 点位表大更新（`PSC330接HMI读写地址逐点表.md` 209KB 等） |
| 9/14 | 三端落地 **AI 通道配置**（330 `f4e0f3b` / 316 `e20972a` / 监控 `2756f66`）；随后查冬季阀门 → 出错（见 §3）→ 会话中断 |

**两个术语**

- **环境保护解耦**：外机 `AmbientProtectAction()` 的计时缺陷修复。原逻辑"进入/恢复计时"依赖 `AnyCompActuallyRunning()`，一台压缩机故障停机时被判为 0 → 重新累计 → 备用压缩机补位被拖到约 326s。修复改为由 `AmbientLockout` 当前状态分流。阈值、回差、传感器故障立即锁定、风机预转 15s、压缩机启动间隔 30s **均未改动**。只动外机一个函数。
- **AI 通道配置**：每路 AI 可配 12 个字段（使能/信号类型 0-10V|4-20mA/信号上下限/工程量上下限/副下限副上限/校准偏移/断线判据/单位代码/小数位），取代旧的电压电流量程参数。落点：内机 `Parameter[1229..1288]`、外机本地 `302~349` + 版本标记 `350`（写 12641=0x3161）、330 网关镜像 `2680~2875`、316 发布区 `1528~1576`。

---

## 3. 本次会话解决的问题

### 3.1 【已修】345/346 参数改错位（off-by-one）

**背景**：09-14 05:47 有人（上个会话）想把 346 冬季阀门最小开度默认值从 0 改成 100（=10%），让规格书 3.11.10 冬季防冻生效。

**实际发生的**：改动落在了 **345**，不是 346。

| 参数 | 索引 | 说明 | 改前 | 改后（错误） | 现在 |
|---|---|---|---|---|---|
| `AirPressureSet` 风压设定 | `INDOOR_SPEC_BASE+30` = **345** | min=0, **max=1** | 0 | **100（越界）** | **0** ✅ |
| `MinValveOpenSet` 冬季阀门最小开度 | `INDOOR_SPEC_BASE+31` = **346** | min=-1000, max=32767 | 0 | 0（没改到） | **100（10.0%）** ✅ |

`INDOOR_SPEC_BASE = 315`（[User.h:632](PSC330RK-V10-内机/User/UserHead/User.h#L632)）。

**根因**：上个会话里校验脚本已报警"def[346] 还是 0，补丁可能没落到那一行"，但随后用肉眼数了 `//340--349` 那一行，得出"第 6 位就是索引 346"——把**位置序号和数组索引混了一位**，并以肉眼结论推翻了脚本。

**修复方式**：`tools/byte_patch.mjs tools/byte_patch_fix345_346.mjs`（latin1 读写，只换 ASCII，其他字节不动）。`user.c` 是 GBK，**不能**用普通文本编辑器改。

### 3.2 【已完成】冷水阀改名后未编译验证

`ColdWaterValve*` → `SteamHotWaterValve*`（DA8 实际接的是蒸汽阀/热水阀），改了 3 个文件（`User.h` / `ErrorCheck.c` / `UserAction.c`，32 增 32 删）。改名本身完整一致（`ColdWaterValve` 残留 0 处），但上个会话在"编译验证"这一步中断了。

**本次已补验**：Keil 批处理构建 `0 Error(s), 0 Warning(s)`。

> ⚠️ 但注意：《固件审计清单.html》记载**本工程构建永远报 0 Error 0 Warning**，因为部分源码缺失、靠旧 `.o` 链接。**绿色构建不等于改动进了固件。**

### 3.3 【已解决】330 内机 EEV 开度全为 0 → **烧录的固件是陈旧的**

**结论**：重新用本树编译出的 hex 烧录后，EEV 行为立刻与源码一致——`EEV1 目标开度 = 200`、`当前开度 = 200`（200 = `EEVStartStepSet` 起始开度）。

**这说明 09-14 15:00 之前烧录的固件不是这棵树编出来的**，其中的 EEV 开阀条件与源码不符。与审计清单记载的"内机 7 个模块源码丢失、靠旧 `.o` 链接、构建永远不报错"完全吻合。

**教训：这个工程的绿色构建 + 烧录，都不能证明改动进了固件。改完必须重新编译再烧。**

详见 §4。

---

## 4. 未解决问题：内机 EEV 开度为什么是 0

### 现场实测（COM6，9600 8N1，从站 1，FC3 只读）

| 寄存器 | 名称 | 值 |
|---|---|---|
| 236 | 工作模式 | 0 = **制冷** |
| 709 | SysStep | 2 = **运行** |
| 710 | SysErrStatus | **0**（无故障） |
| 1406 | ModUrgencyStop | **0**（无急停） |
| 881 / 2678 | 目标压缩机台数 / CN | 4 / 4 |
| 950 / 951 | ModeCompControl1/2（内机对 1# 外机的压缩机命令位） | **1, 1**（命令开机） |
| 1555/1556、1625/1626 | 1#、2# 外机压缩机 1/2 状态 | **2, 2 / 2, 2**（全部运行） |
| 1212–1219 | EEVNewAddValue_ON | **[1,1,1,1,0,0,0,0]**（任务在跑，通道 1-4 已配置） |
| 1145–1152 | EEV1~8 **目标开度** | **全 0** |
| 1153–1160 | EEV1~8 **当前开度** | **全 0** |
| 1144 | EEV 当前调度号 | 99 |
| 365 | EEVSpecPidEnableSet | 1 |
| 366/367/368/369/370/371/411/372 | Y / YD / KP / TI / TD / 每周期最大调整 / 起始开度 / 化霜开度 | 50 / 20 / 5 / 100 / 0 / 20 / 200 / 350（**全部等于规格书默认值**） |
| 612–627、1217 | NTC 蒸发温度故障位、故障字3 | 全 0 |

`ModeCompControl1/2 = UnitWriteStatus[ModeNumDisp].CompControl[]`（[DispAction.c:232](PSC330RK-V10-内机/User/UserSrc/DispAction.c#L232)）。`EEVNewAddValue_ON` **只在 `eevCTRL.c` 内被写**，故非零值可证明 EEV 任务在运行。

### 源码逻辑

`EEV_Action` → `EEVSpecAutoAction`（[eevCTRL.c:297](PSC330RK-V10-内机/User/UserSrc/eevCTRL.c#L297)）：

```c
coolingCommand = configured && (SysStep == 2) &&
                 (UserWorkModeSetTEMP == MODE_COOL) &&
                 (UnitWriteStatus[module].CompControl[comp] == 1);
if(coolingCommand){
    if((*initialFlag == 1) || (*init == 1)){ Parameter[NewAddValue+i] = startStep; ... return; }
    ...
}
/* 否则：关阀 */
Parameter[NewAddValue+i] = EEVCloseStepTarget(i, current, maxStep, EEV_CLOSE_NORMAL);
```

`startStep = EEVStartStepSet (411) = 200`，`maxStep = EEVSpecMaxStep()` = `limit(EEVOpenMaxSizeSet=500, 200, limit(MotorTotalPulseSet=500,200,500))` = **500**。

**四个条件现场都满足**（configured：`ModuleNumSet=3` 时模块 1、2 通过、模块 3 被 `module >= ModuleNumSet` 挡掉，正好对应 ON 标志 `[1,1,1,1,0,0,0,0]`）。

**所以按源码，目标开度应该是 200，实际却是 0。源码与实际行为不一致。**

### 结论：烧录的固件是陈旧的 ✅

把本次新编出的 `Project/Obj/PSC330RK-V10.hex`（2026-09-14 15:00）重新烧录后，板子重启（SysStep 走 1 → 2），进入运行态时读到：

```
目标 1145-1152 = [200, 0, 0, 0, 0, 0, 0, 0]
当前 1153-1160 = [200, 0, 0, 0, 0, 0, 0, 0]
ON   1212-1219 = [1, 0, 0, 0, 0, 0, 0, 0]
```

**200 正是 `startStep = EEVStartStepSet`，与源码预测完全一致。** 此前那个固件里的 EEV 开阀条件与当前源码不同，属于审计清单记载的"陈旧 `.o` 链入"问题。

**推论（重要）**：这个工程里，"构建 0 Error 0 Warning" + "已烧录" **都不能证明改动进了固件**。任何改动都必须经过「重新编译 → 重新烧录 → 实机复读」三步才算验证。

### 本轮改动后的构建产物

- `PSC330RK-V10-内机/Project/Obj/PSC330RK-V10.axf` / `.hex` —— 2026-09-14 15:00，`0 Error(s), 0 Warning(s)`
- **尚未烧录**

---

### 3.4 EEV 十分钟实录（2026-09-14 15:10:25 ~ 15:20:24）

原始数据：`EEV动作记录_20260914_变化点.txt`、`EEV动作记录_20260914_分析.txt`（297 个变化点）

| 时刻 | 事件 |
|---|---|
| 15:10:25 | 记录开始。SysStep=2 制冷，EEV1=195、EEV2=200 |
| 15:10:45 | 1#外机压机 1/2 运行 |
| 15:11:43 | EEV3 接入，开度 **0 → 200**（起始开度） |
| 15:12:19 | 2#外机压机 1 运行 |
| 15:13:23 | EEV4 接入，开度 **0 → 200** |
| 15:13:29 | 2#外机压机 2 运行（4 台全开） |
| **15:18:48** | **EEV1 一路关到 0** |
| **15:19:12** | **EEV2 一路关到 0** |
| 15:20:24 | 结束：目标 = 当前 = [0, 0, 40, 58, 0, 0, 0, 0] |

**结论**

1. EEV1~4 全程在调，**目标与当前 282/297 帧完全一致**——阀门跟踪无滞后，驱动正常。
2. 四路**全程单向关小**。步长分布 `{-2: 多数, -1: 少数, -5: 仅 1~2 次}`，速率约 **2 步 / 12s**，远低于规格 3.9 的上限（每周期 20 步 / 关闭 20 步每秒）。
3. **EEV1、EEV2 在 15:18:48 / 15:19:12 完全关闭到 0，而对应压缩机仍在运行。** 制冷工况下室内 EEV 全关 = 室内换热器无冷媒流过，**这点需要判断是否为预期**。
4. **规则 A 几乎没触发**：蒸发温度全程 27~32℃（>12℃），若 PID 步数 ≥0 则应强制 −5 步/周期；实测 −5 只出现 1~2 次，说明 PID 输出绝大多数时候为负。
5. EEV3/EEV4 在接入瞬间跳到 200，与 `startStep` 逻辑吻合。EEV5~8 恒 0（未配置）。
6. 调度号在 `99 / 0 / 2` 之间跳。

**遗留疑点**：PID 输出为负意味着 `E(K) = 过热度 − Y < 0`，即实测过热度低于设定 5.0℃。但本次**没有记录实测过热度**。下次记录应补上外机镜像的吸气温度（1#外机 `1570/1571`、2#外机 `1640/1641`），用 `吸气温度 − 蒸发温度` 直接算过热度。

> ⚠️ **本机 DLP 会拦截 `.csv`**：`EEV动作记录_20260914.csv` 被替换成 UTF-16LE 的「该文件为我司机密文件」包裹头，数据不可读。`.txt` 未被拦截。**以后记录一律用 `.txt`/`.log`，不要用 `.csv`。**

---

## 5. 未提交状态

`PSC330RK-V10-内机` 工作区有 4 个文件已改**未提交**：

```
M User/UserHead/User.h        (冷水阀改名)
M User/UserSrc/ErrorCheck.c   (冷水阀改名)
M User/UserSrc/UserAction.c   (冷水阀改名)
M User/UserSrc/user.c         (345/346 修复)
```

最新提交：`f4e0f3b feat(PSC330): AI 通道配置 + 316 网关扩展 + 压缩机分布防抖`（09-14）

---

## 6. 阻塞级问题（来自《固件审计清单.html》）

1. **内机 7 个模块源码丢失**，固件靠旧 `.o` 链入，含 `sysrun.c`（提供 `SysRunInit` / `Time_check` / `IWDG_Configuration` / `WriteParamaterEEprom` 与全部时标变量）
2. **外机 `eev.c` 彻底丢失**，`MT1Output/MT2Output` 只存在于 427KB 的 `eev.o`
3. 内机 `eev.c`、`Adciic.c` 在磁盘上却不参与编译
4. 外机 `stm32f4xx_it.c` 源比被链接的 `.o` 晚 11 个月
5. **两个固件目录的源码完全不在版本控制内**（匹配 `PSC330RK|PSC316RK` 的跟踪文件 = 0）
6. 构建永远 `0 Error 0 Warning`，改了不生效也不报警
7. `.codex-tmp/`、`_utf8tmp/` 里的陈旧副本会造成 grep 假命中

**第 1、2 条意味着：现在改的某些代码可能根本进不了固件。**

---

## 7. 待办清单

### P0 — 构建完整性
- [ ] 15 个缺失源文件重建（外机 8、内机 7）
- [ ] 两个固件目录纳入版本控制

### P1 — 本次遗留
- [ ] 烧录 09-14 15:00 的 hex，验证 §4 的 EEV 问题
- [ ] 提交 `PSC330RK-V10-内机` 的 4 个改动文件
- [ ] 实机验证 346=100（10%）后冬季防冻功能是否真正生效

### P2 — 现场复测（9/8 清单里 8 项"必须执行"仍全是 `[ ]`）
- [ ] 环保解耦用例 A/B（P0）+ C/D/E/F 回归；A 的通过基线是 `t3−t0` 由 **326.1s 降到 ≤~40s**
- [ ] 压差确认未完成前，预热/主制热/热泵/辅热/加湿不得投入

### P3 — 点位表 / 工具
- [ ] MCGS 点位表 256 行待手工补录（`.xlsx` 被 DLP 加密）
- [ ] 监控"整通道/整组写入"按钮下发会被 330 拒绝（网关只有一个异步写槽，FC16 多寄存器写被整体丢弃）→ 改逐字段循环或移除
- [ ] 316 AI 配置改造必须**同时重设三张参数表**（`Parameter_min/max` 上电比对会把越界值静默回退）
- [ ] 监控须标注"未实现/不可用"点位：316 压缩机电流 `529/530`、AI4/AI5、AI13 后，330 X16/X17

### P4 — 已核实的功能缺陷
- `UnitWriteStatus[]/UnitReadStatus[]` 索引无边界检查，可被 `Parameter[197]` 触发越界
- 内机 `Parameter[16]` 被 `EEVAdjustTimeSet1` 与 `EEVPidScanTimeSet` 两个宏占用 → PID 扫描周期被钳成 10s（规格 5s）
- 外机 `DefrostEnvIntervalSet`（索引 183）def=60 但 max=50 —— **默认值超出自身上限**，且 ≠ 规格 30
- 其余见《固件审计清单.html》

---

## 8. 环境备忘

- 本机 Claude Code 走 DeepSeek 1M 端点，上下文上限 1,048,576。已设 `autoCompactWindow: 800000` 防止顶穿。
- 串口：COM6 = CH340（330 调试口），COM13 = CP210x。
- 只读探测脚本模板见本次会话：FC3 读保持寄存器，9600 8N1，从站 1。

---

## 9. 行为约定（延续）

- 20260428 规格书为唯一基线
- 改代码用 `tools/byte_patch.mjs`（GBK 文件必须字节级改，保住 CRLF）
- 不要误伤其他功能代码；不要删除 30s 保持逻辑
- 外机变频风机用 0-10V，**无 485**；串口通讯代码是有意废弃的，**不要当缺陷报**
- 用户要求暂缓的用例须标 `[-]`，不得写成通过
