=== 330 §3.3.5 紧急停机 —— 失电瞬间抓取（ST-Link 直读 RAM）===
内核状态 = State.RUNNING

--- 基址自检：ST-Link 读的 Parameter[] vs Modbus ---
  [236]模式        ST-Link=1  Modbus=1  一致
  [709]SysStep   ST-Link=0  Modbus=0  一致
  [872]CtrlMode  ST-Link=0  Modbus=0  一致

--- 触发前 ---
  [236]模式=制热  [709]SysStep=0  [605]EnviTempErr=0  [1406]ModUrgencyStop=0
  output: (全0)
  DA    : (全0)
  EEV New(目标): [0, 0, 0, 0, 0, 0, 0, 0]
  EEV Old(剩余): [0, 0, 0, 0, 0, 0, 0, 0]

⚠ output[] 和 DA[] **全是 0** —— 现在没有负载可失电，触发它证明不了「全失电」。
  --force：照样触发。

--- 基线采样 5s（确认稳定）---
  1621/1621 拍  (全0) | (全0)

--- 触发：写 Parameter[605] EnviTempErr = 1（--direct 对照）---
  ⚠ 这条**绕开** ValveLink 闩锁 ⇒ ErrTotal 不增 ⇒ 预期 Y05 **不亮**。
  写 [605]=1 回读 1  OK
  t=0 = 写完那一刻，紧接着进高速采样。

--- 高速采样（整块读 output+DA，看过渡拍）---
  采了 970 拍，平均间隔 3.1 ms，覆盖 t=+2 ~ +3005 ms
  派生：[605]EnviTempErr=1  [1406]ModUrgencyStop=1  链路通

  口径：'用电设备' = output[] 除 output[5](Y05) 外的 15 路 + DA[10] 全部。
        （Y05 是综合报警，急停下本来就还亮着，不能算进'失电'判据。）
  （触发后就没采到带电状态？）
  第一拍【设备全断】     t=+5ms
      output: (全0)
      DA    : (全0)
  ⚠ 另采到 970 拍 output[] **含 Y05 在内全为 0** 的瞬态（t=+2 ~ +3005 ms）
     = ErrorCheck.c:631 清掉 output[5] 之后、DispAction.c:475 把它拉回之前的
       那个窗口。同一扫描内，微秒级，能被 3.5ms 的采样撞上说明它确实存在。

--- 触发后稳态 ---
  [709]SysStep=0  [605]EnviTempErr=1  [1406]ModUrgencyStop=1
  output: (全0)
  DA    : (全0)

=== 判据①「除电子膨胀阀外所有用电设备均失电」===
  ✅ output[16] 与 DA[10] 全为 0，**且 Y05 也没亮** —— 符合 --direct 的预期
     （[605] 单独置位不进 ErrTotal，所以 Y05 不亮）。

=== 判据②「output[] 与 DA[] 是否同一拍清干净」===
  没采到任何中间态 —— output[] 与 DA[] 一起归 0。
  （采样间隔 3.1 ms；ErrorCheck.c:631/632 是同扫描内两个相邻 for，间隔微秒级，采不到是正常的。）

=== 判据③「关闭步数＝当前步数 + 10」（内机 8 路）===
  触发前 Old(剩余): [0, 0, 0, 0, 0, 0, 0, 0]
  触发后 Old(剩余): [0, 0, 0, 0, 0, 0, 0, 0]
  触发前 New(目标): [0, 0, 0, 0, 0, 0, 0, 0]
  触发后 New(目标): [0, 0, 0, 0, 0, 0, 0, 0]
  8 路 EEV 全为 0 —— 本次没抓到（阀本来就在 0 位，Δ 无意义）。
  要复现 §21.11 的 +10，需先让 EEV 停在非 0 步（制冷/制热运行中）再触发。
  注：`Old` 是**剩余计数**，写入即 current+10，随后每 `uc1s` 一拍 -20 ⇒ 20 步/秒。
      本次触发后已过 1s+，看到的值低于 +10 属正常。要抓写入瞬间需看上面的过渡采样。

--- 复原：清 [962] [960] [964] 闩锁 + [605] EnviTempErr ---
  [605]=0  [1406]=0  [962]=0  OK 已解除
  output: (全0)    DA: (全0)
  ⚠ 机器现在是**停机**状态（SysStep 已被打到 0）。恢复运行：
      python.exe tools/rw330.py 398=1
  内核状态 = State.RUNNING
