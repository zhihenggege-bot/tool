# 20260911 环境保护解耦修复 — 实机测试步骤

> 对应改动:`PSC316RK-V12-外机/WELLTHINKER/User/UserAction.c` 的 `AmbientProtectAction()`（提交 `6bcf8b6`）。
> 目标:验证"制冷一台压缩机故障停机后备用机补位不再等约5分半"，且不牺牲既有环境/传感器保护。
> 只改了外机 PSC316 一个函数；PSC330（内机）、监控软件均未改动。

---

## 0. 准备

### 0.1 编译与烧录
1. Keil 打开 PSC316 工程，编译 `WELLTHINKER/User/UserAction.c`。
   - **预期一条无害警告**：`AnyCompActuallyRunning` 定义于 `UserAction.c:1684`，本次解耦后不再被调用 → `#177-D declared but never referenced`。**能正常编过**，不影响运行。（如需清警告，可另行单独删除该 static 函数，行为 100% 安全。）
2. 烧录到**外机板**。PSC330 内机板、监控软件**不需要**重新烧录/更新。
3. 记录固件版本/提交号 `6bcf8b6`，便于对照。

### 0.2 参数确认（HMI 或 Modbus 读取，测前拍照存档）
| 参数 | 期望值 | 说明 |
|---|---|---|
| `MinStopTimeSet` | 180s | 有效最小停机 = max(值,120)，当前 180 |
| `FanONLaySet`（冷凝风机延时，Param[TIME+5]） | 15s | 风机预转，本次未改 |
| 需求量 demand | 3 台 | 与上次复现一致 |
| Cn（可用压缩机） | 4 台 | 需求 3 + 备用 1 |
| `UserWorkModeSet` | 0=制冷 | 用例 A 用 |
| 环境温度 `EnviTempAI0Disp` | 处于允许启动区（非越界） | 保证 5.5min 若复现只能来自本缺陷 |

### 0.3 监控设置
- 启动 Python 监控工具，轮询周期 ≈2s（与上次一致，边沿误差 ±2.1s 记入）。
- 重点采集位/量：
  - 外机故障字 `OutdoorFaultStatusDisp` 的 **`0x0020` = AmbientLockout**（本修复主观测量）。
  - 冷凝风机物理输出位（上次记为 XY 增加 `0x0400` / 风机 `Y02`）。
  - 压缩机接触器输出 `Y00`/`Y01`（及对应 `Comp1Y05/Comp2Y06` 反馈）。
  - 总报警、各压缩机低压故障标志。
- 每个用例连续采集，记录关键**边沿时间戳**。

> ⚠️ 安全前置：涉及低压/传感器人为触发，须在可控工装上进行；每个用例结束后复位故障、待最小停机计时清零再进入下一例。

---

## 1. 用例 A（P0 主验证）— 制冷压缩机故障补位不再等 300s

**这是本次修复的核心，必须通过。**

**前置**：制冷稳定运行，demand=3、Cn=4，三台压缩机实际运行；环温正常；**备用压缩机已停机 ≥180s**（关键：否则补位会被 min-stop 正常拖住，属预期，不是缺陷）。

**步骤**：
1. 对**外机2的压缩机1**人为制造低压故障（断开低压开关 `Comp1LowX01` 或降压），使其停机。
2. 若外机2另一台此刻未运行，则该外机 `AnyCompActuallyRunning()` 变 0——正是上次复现条件。
3. 记录：`t0` = 低压故障成立（`Y00 1→0`）；`t1` = 外机2 `AmbientLockout`(0x0020) 状态；`t2` = 冷凝风机物理输出接通；`t3` = 备用压缩机 `Y01` 接通并反馈。

**通过判据**：
- ✅ `AmbientLockout` **不再被置 1 达 300s**。修复后本机压缩机全停不再重累计恢复计数，故障期间若环温正常，`0x0020` 应保持 0（或短暂后立即为 0）。
- ✅ `t3 − t0`（故障→补机）**约 30~40s 量级**（受正常调度 + 15s 风机预转 + 压缩机 30s 启动间隔约束），**不再是约 326s（5分26s）**。
- ✅ 末态总运行台数 = 3（故障机停、备用机补上）。

**对比基线**：上次实测 `t3−t0 ≈ 326.1s`。本次应 **≤ ~40s**（备用机 min-stop 已满足前提下）。

---

## 2. 用例 B（P0 次要场景）— 制热刚切模式不再等 300s

**前置**：环温处于制热允许启动区（恢复区，约 −10.0℃ < 环温 < RWS）。

**步骤**：
1. 从制冷切到制热（或上电即制热），记录切换时刻 `t0`。
2. 观察 `AmbientLockout`(0x0020) 与首台压缩机允许启动时刻 `t1`。

**通过判据**：
- ✅ 切换后**不再无条件锁 ~300s**。因环温在允许区，`JustSwitched` 播种应判定 `AmbientLockout=0`，机组在正常调度（含 15s 风机预转）后即可启动，`t1−t0` 为秒级而非 ~300s。
- ✅ 切换瞬间仍有一次停机再排序（`ClearOutdoorRunCommand`），四通阀换向正常。

---

## 3. 用例 C（回归）— 环温传感器故障仍立即锁定

**步骤**：制冷或制热运行中，断开/短路环温传感器使 `SensorStatus[6]=1`。

**通过判据**：
- ✅ `AmbientLockout` **立即置 1**（不等待），机组停（`ClearOutdoorRunCommand`）。
- ✅ 恢复传感器后，按各模式恢复计时重新解锁。

---

## 4. 用例 D（回归）— 真实环温越界仍按规定计时保护

**D1 制冷低温**：环温降至低于停止点（`LowStopTemp`，缺省型 = LWS−20）。
- ✅ 连续满 **300s** 才锁 `AmbientLockout=1`；恢复需环温高于启动点（`LowStartTemp`=LWS）连续 **300s**。

**D2 制冷高温**：环温 ≥ 43.0℃（`EnviTempAI0Disp≥430`）。
- ✅ 连续 **60s** 锁；<43℃ 连续 **60s** 恢复（且需同时满足低温侧恢复条件才整体解锁）。

**D3 制热高温/低温**：环温 > RWS+2.0℃ 或 < −12.0℃。
- ✅ 连续 **300s** 锁；进入恢复区（约 −10.0℃ < 环温 < RWS）连续 **300s** 解锁。

> 关键：这些是修复要**保留**的安全行为，阈值/时长未改，只是不再依赖"压缩机是否在跑"。

---

## 5. 用例 E（回归）— 切模式时环温越界，仍应保持锁定（防误放行）

**步骤**：把环温置于越界（如制热时环温 < −12℃），再切模式/机型。

**通过判据**：
- ✅ `JustSwitched` 播种判定为越界 → `AmbientLockout=1` 保持锁定，**不得**因"清了计数"就放行；随后按恢复计时解锁。
- ✅ 处于滞环夹层（如制热 −12℃~−10℃ 之间）时保守锁定，由恢复计时解锁。

---

## 6. 用例 F（回归）— 风机预转与压缩机启动间隔不变

**通过判据**：
- ✅ 冷凝风机预转 = **15s**（`FanONLaySet`），风机物理输出连续运行满 15s 才允许压缩机。
- ✅ 压缩机与压缩机之间启动间隔 = **30s**（内机 `COMP_START_INTERVAL_100MS`）。
- ✅ 风机→压缩机间隔约 15s+抖动（**< 30s 属正常**，30s 不是加在风机上的）。

---

## 7. 记录与结论模板

| 用例 | 关键时间戳/观测 | 期望 | 实测 | 结论 |
|---|---|---|---|---|
| A 制冷补位 | t3−t0 | ≤~40s | | |
| A AmbientLockout | 0x0020 是否 300s | 不再 300s | | |
| B 制热切模式 | t1−t0 | 秒级 | | |
| C 传感器故障 | 锁定时延 | 立即 | | |
| D1 制冷低温 | 锁/恢复时长 | 300s/300s | | |
| D2 制冷高温 | 锁/恢复时长 | 60s/60s | | |
| D3 制热越界 | 锁/恢复时长 | 300s/300s | | |
| E 越界切模式 | 是否保持锁定 | 保持 | | |
| F 风机/间隔 | 15s/30s | 不变 | | |

**总体通过条件**：A、B 通过（缺陷消除）且 C、D、E、F 全部回归通过（保护未削弱）。任一回归项失败 → 暂停，回看 `AmbientProtectAction()` 对应分支。

---

## 附:改动要点速查（供排障对照）
- 进入/恢复计时**不再看** `AnyCompActuallyRunning()`；改由 `AmbientLockout` 当前状态分流：
  - 未锁定 → 仅按真实越界累计"进入"计时，达阈值才锁。
  - 已锁定 → 仅按进入恢复区累计"恢复"计时，达阈值才解锁。
- 模式/机型切换：清历史计数 + 一次停机再排序，**按当前环温电平播种** `AmbientLockout`（`JustSwitched`），不再无条件 300s。
- 传感器故障、阈值、回差、风机预转、压缩机启动间隔:**均未改**。
