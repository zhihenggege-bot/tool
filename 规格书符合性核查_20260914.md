# 规格书符合性核查报告（20260428 版）

> 核查日期：2026-09-14
> 基线：`采用电子膨胀阀风冷定频直膨机控制规格书` **版本号 20260428**
> 代码版本：内机 `PSC330RK-V10-内机` / 外机 `PSC316RK-V12-外机`，HEAD

## 核查方法（两遍）

**第一遍**：按章节拆成 4 组并行检索代码，逐条给结论。
**第二遍**：由我逐条复核第一遍的关键结论，核对代码位置与数值，并纠正判断过重/过期的条目。

标注：
- 🔍 = 第二遍亲自复核过（引用代码原文）
- 📋 = 第一遍小组结论（未逐条复核）

**时基基准（🔍 已确认）**：`uc100ms` 是"一次调用脉冲"标志，数字输入统一去抖
`X0x_En=(X0x_Cnt>=30)?1:0`（`user.c:512-518`，计数在 `user.c:522-542` 被 `uc100ms` 门控）
→ **30 计数 = 3.0 s**。`uc1s` 同理每 1000 ms 加 1。

---

## 一、总体结论

**不是"整机符合"，也不是"遍地不符合"。** 准确的说法是：

- **控制算法层面基本对齐**：3.3 时序、3.4 参数、3.6/3.7/3.8 的 PID 与判据、3.9 EEV 全部规则与系数、
  分段加热映射（四张图）、4.1 机型约束、4.2、4.3 冷凝风机三种类型、4.4 化霜、4.5 四通阀、第 5 节紧急停机——**均符合**。
- **保护层面大部分对齐**：3.11 去抖时间除 3.11.8 外全部精确命中；触发动作基本正确。
- **但有 4 个实打实的缺口**（见第三节），其中 1 个会在化霜时**误报故障并中断化霜**。
- **环境与工程层面的风险大于算法层面**：`dac.c` / `sysrun.c` 等源文件缺失、
  AI 通道配置错误、参数与固件不同步。

---

## 二、逐章结果

### 3.10 室内机变频送回风机控制  📋🔍

| 条目 | 结论 | 关键证据 |
|---|---|---|
| 五参数默认/范围 | ✅ | `Ventilation=80`/`CoolHeat=100`/`AirVolume=1000`/`DuctArea=100`，均与规格一致 |
| `AirPressureSet` 上限 | ⚠️ | 代码 `max=9999`，规格 **0-1500** 🔍 |
| 通风 8V / 冷热 10V | ✅ | `FanControl.c:438-441` 按模式选 `VentilationFanVoltSet`/`CoolHeatFanVoltSet`，×10 后输出 |
| 风速/风压双路径 | ✅ | `WindSet=SupplyFanSensorTypeSet`；两条闭环都在（`FanControl.c:502-527`） |
| 风速→风量换算 | ✅ 🔍 | `target = AirVolumeSet*100/(36*DuctAreaSet)`，独立代数验算一致 |
| Min~Max 限幅 | ✅ | 闭环内 + 输出前双重限幅（`FanControl.c:427-432`、`UserAction.c:915`）+ HMI 写入校验（`modbus.c:320-325`） |
| `SupplyFanModeSet` 出厂默认 | ⚠️ | 默认 **1(DO)**，0-10V 控制出厂不激活，须 HMI 改 2/3 🔍 |
| 回风机是否跟随 8V/10V | ❓ 倾向符合 | 代码只有手动 0-10V（`ReturnFanManualVoltSet`）；但规格书 **3.1 参数设置表**里回风机就只有「无/开关/0-10V手动」，**正文与参数体系自相矛盾**。按 3.1 理解则符合 |
| AO 满量程标定 | ⚠️ 无法验证 | **`dac.c` 缺失**（只有 `dac.o`），0-10V 标定无法从源码确认 🔍 |

### 3.11 室内机保护（全 10 项）

| 条款 | 结论 | 规格去抖 | 代码去抖 | 触发动作 |
|---|---|---|---|---|
| 3.11.1 电源/相序 | ✅ | 3s | **3.0s** | 停所有室外机（不清室内机）✅；报警✅；复位清✅ |
| 3.11.2 外部连锁 | ✅ | 3s | **3.0s** | 走正常停机时序 SysStep 1/2→3→4→5，**非立即断电**（最坏 30~90s 全停）；重接自动恢复✅ |
| 3.11.3 内风机故障 | ✅ / ⚠️ / ❌ | 3s | **3.0s** | 主通道 X03 送风机过载 → `ModUrgencyStop` 内外机全停✅；**X11 排风机过载只停自身**⚠️；**`FanVVFErr` 变频器故障位全工程无置位点** ❌🔍 |
| 3.11.4 滤网压差 | ✅ | 15s | **15.0s** | 仅报警不停机 ✅ |
| 3.11.5 风阀连锁 | ✅ | 3s | **3.0s** | `EnviTempErr`→`ModUrgencyStop` 立即全停 ✅ |
| 3.11.6 消防警报 | ✅ | 3s | **3.0s** | 同 3.11.5 ✅ |
| 3.11.7 送回风机压差 | ✅ | 60s 后检测+10s | **60.0s + 10.0s** | `SysErrStatus`→SysStep=4 紧急停机，内外机全停 ✅ |
| 3.11.8 传感器故障 | ⚠️ 偏差 | 未规定 | **(GErrCheckSet+3)s＝默认 5.0s** | 非通风停内外机且**风机风阀确实保留**✅；但见下方偏差 |
| 3.11.9 电加热故障 | ✅ | 3s | **3.0s** | 只停主加热/预热 + **加湿器全关**，**不影响其它设备**（已核实不在停机名单内）✅ |
| 3.11.10 冬季防冻 | ✅（代码） | 无 | 每周期 | `MinValveOpenSet` 强制 `ReheatAO0`/`PreheatAO1` ≥ 最小开度✅ |

**3.11.8 的偏差（📋）**：
1. 传感器故障检测**按"当前配置/模式是否需要该传感器"门控**——不需要时只报警、不执行停机
2. 风速/风压变送器**仅在 `SupplyFanModeSet == AUTO_AO` 时检测**，非该配置下既不报警也不停机
3. **消毒/排毒模式绕过停机分支**（`UserAction.c:3349-3356` 提前 return），这些模式下传感器故障只报警不停机

**3.11.10 的三处边界（📋，我确认其中一条）**：
- 新风温度探头故障或联锁/急停时**放弃**最小开度保持 → 阀门可能被清零（盘管冻裂风险）
- **DA8（蒸汽/热水阀）未纳入最小开度保护** 🔍 —— 规格书 3.11.10 写的是"蒸汽阀门/热水阀门"，
  而实现只覆盖 DA0/DA1；DA8 的代码是**制冷冷水阀逻辑**（只在 `MODE_COOL` 工作，制热模式恒 0）
- 仅在执行器介质参数明确为蒸汽/热水时生效

### 3.12 室内机其它功能  📋🔍

| 条目 | 结论 | 说明 |
|---|---|---|
| 维护提醒 | ✅（有条件） | 计时（`uc1h` 累加，`Switch.c:33-53`）、HMI 可改（362/363/364）、到点置位 `MaintenanceRemindFlag`（寄存器 894）均实现 🔍 |
| └ 默认间隔 | ⚠️ | `Parameter_def[363]=0` → **开箱永不提醒**，须 HMI 先设小时数 🔍 |
| └ 提醒方式 | ⚠️ | **固件不主动弹窗**，只置标志位由触摸屏轮询（对比试用到期有 `UseTimeFullWindow=29` 弹窗机制） |
| 定时开关机 | ✅ | `TimeStatus`(=1388, HMI 写 0/1) 真实驱动开停机并置 TIMERUN/TIMESTOP；**固件无本地时刻表**，日程在触摸屏侧 |

### 4.1 室外机类型  📋

| 条目 | 结论 |
|---|---|
| 三种机型（15-43 / 0-43 / -15-43）可选 | ✅ |
| 机型 × 风机组合约束（双速/两定速、变频/定+双速、变频/定+变频） | ✅ `UserAction.c:1525-1534` 逐个对应 |
| LWS 15(15-25)、RWS 18(5-20) | ✅ 默认值一致 |
| **全新风时 LWS 默认改 22 / RWS 改 10** | ❌ **内外机两侧均无实现** |
| 制冷/制热环温上下限按机型区分 | ✅（类型①用 LWS / ②=0 / ③=-14.0℃） |

### 4.2 压缩机加减载  📋
✅ **符合。** 外机不做自主决策，完全跟随室内机指令（`Parameter[1400]`），只做使能过滤、
最小停机、风机预转约束。**"外机按压力控制"的老实现在 `BoardFanControl()` 里，已是死代码** 🔍

### 4.3 室外冷凝风机  📋🔍

**11 个公共参数默认值全部与规格一致**（TL=300/TLD=150/Z=70/ZD=50/最高频=50/最低频=5/
加减载速度=10/最低连续运行=30/最低停机=30/冷凝盘管保持=60）🔍

**冷凝温度取值（🔍 已核实）**：
```c
if(OutdoorUnitTypeSet == 0) { *T1 = FinTemp1AI5Disp; *T2 = FinTemp2AI7Disp; }  // 类型① 翅片
else { *T1 = OutdoorPTTempFromPressure(HighPressDisp1); ... }                  // 类型②③ 高压
```
与规格「类型①由翅片提供 / ②③由高压传感器提供」**完全对应** ✅

| 机型 | 结论 |
|---|---|
| ①15-43℃ 单风机双速 | ✅（环温>25℃ 或 冷凝>TL+TLD 保持 5s→高速；冷凝<TL 保持 60s→低速；切换 15s；中间态保持） |
| ①15-43℃ 双风机两定速 | ✅ **含"两风机等时长轮换"**（`OutdoorFanCommandRunSeconds` 取小者） |
| ②0-43℃ 单风机变频 | ✅（34-36℃ 保持 / <34 减载 / >36 加载；未开且 >45℃ 满频；30s 保持） |
| ②0-43℃ 双风机（一定一双速） | ✅ 4 段分界、每段 15s 延时、段1备注两个条件、段4强制进入（环温>20℃保持30s使能、<18℃退出、冷凝≥50℃保持10s）全部对应 |
| ③ 单风机变频 | ✅ 同② |
| ③ 双风机（一定一变） | ❓ **按框图实现，但本次无图无法核对** |
| 4.3.2 热泵制热 | ✅（定速控制/变频满频；双风机按 Z/ZD 分档 30s 保持；等时长轮换） |
| 时间常数 | ✅ 15s 切换间隔、30s 未标保持时间、30s 最小运行/停机 全部实现 |
| 无故障停机延时 15s | ✅（正常停机路径）；⚠️ **故障硬停机是立即断电**，不走 15s |

**三处待确认（📋）**：
1. ⚠️ TypeII 双速 **低↔高换速只用 3s 死区**，不走"最低停机时间 30s"；代码注释自称依据 20260428
2. ⚠️ "变频风机满载 2HZ/S" 只在 `FanTypeSet==4 且定速风机已运行` 时生效，单变频机型无此档；"满载"定义需澄清
3. ⚠️ 类型①"环温>25度"用的保持时间是 **5s**，而通用条款说未标注环境温度应按 30s

### 4.4 化霜  📋🔍

**11 个参数默认值逐项一致** 🔍（含此前误报的 `DefrostEnvIntervalSet`，现为 30/20/50，**commit `0caac2e` 已修**）

| 条目 | 结论 |
|---|---|
| 开始条件 7 条 | ✅ **确认为严格 AND**（`DefAction.c:266-285` 单一 `if` 全部 `&&`）🔍 |
| 结束条件 3 条 | ✅ 翅片 40℃ / 高压开关跳开（**不报警**）/ 到时间；故障压缩机除外 |
| 化霜开始动作 | ✅ 发指令、风机关、**四通阀失电**、压缩机全开（未开的隔 30s）；❌ **屏蔽低压开关未实现** |
| 化霜结束动作 | ✅ 全关 → **延时 60s** 进入热泵待机 → 发结束指令 |
| 4.4.0 定时化霜时长档位 | ⚠️ 代码按 **>4.0℃** 减 1 分钟，规格写"高于 **6℃**" |
| 运行提示 | ⚠️ `DefrostPermit`(Parameter[1409]) 默认 0，**无 PSC330 主控写入时化霜永不启动** |

### 4.5 四通阀  📋

| 条目 | 结论 |
|---|---|
| 制热模式压缩机启动延时 30S 得电 | ✅ `moduleAction.c:278-287`，参数 `FourWayValveDelaySet` def=30/20/120 ✅ |
| 除霜及制冷时不得电 | ✅ 制冷 `FourOutSet=0`；除霜 `DefAction.c:967-968` + `moduleAction.c:294-298` 强制 `Four=0` |

### 4.6 室外机保护  📋

| 条款 | 结论 | 说明 |
|---|---|---|
| 4.6.1 压缩机保护 | ✅ | 逐机故障→停对应系统+报警；全部故障→外机关+外机故障 |
| 4.6.2 传感器故障 | ✅ | AI0/AI1 高压按 `PressSensor` 决定是否检测；对应系统停机；全部故障→外机关 |
| 4.6.3 环温传感器故障 | ✅ | → 对应外机停 + 外机故障报警 |
| 4.6.4 高压故障 | ✅ | 机械开关立即；模拟 >38.0Bar 维持 2s（`3800`/`20×100ms`）；**化霜时不检测（连机械开关也不报警）** |
| 4.6.5 低压故障 | ✅ 判定正确 | 开关断开维持 5s（`50×100ms`）；❌ **无化霜屏蔽**（见缺陷 #1） |
| 4.6.6 轴流风机故障 | ✅ | 按机型组合判定（`OutdoorConfiguredFanFault`）→ 外机关+报警 |

---

## 三、缺陷清单（按严重度）

### 🔴 P0 — 会导致误动作或功能失效

**1. 化霜时未屏蔽低压开关** ❌🔍
- 规格书 4.4.3 明文要求「化霜开始：向室内机发出化霜开始指令，**屏蔽低压开关**，室外机风机关，四通阀失电，所有压缩机开」
- 代码：`EnableCheckLow[]` 只在 `DefAction.c:546/552/1227` 被**写入**，`ErrorCheck.c` 里**一次都没读** → 死变量
- **commit `5f86e9a`（26.0911-A）删掉了门控** 🔍：
  ```
  -			&& EnableCheckLow[0]),
  -			&& EnableCheckLow[1]),
  ```
- **后果**：化霜时压缩机全开、四通阀失电（制冷回路）、风机停 → 低压侧压力下降 →
  低压开关跳开 5 秒即置 `CompLowPress1Err` → **误报低压故障并停系统 → 化霜中断**
- 旧版 `_utf8tmp/ErrorCheck.c` 里有该门控，可作参考恢复

**2. `FanVVFErr`（送风机变频器故障）无任何置位点** ❌🔍
- 只有宏定义（`User.h:917`）、报警计数（`DispAction.c:422`）、停机名单（`ErrorCheck.c:602`）
- **全工程没有任何 `FanVVFErr = ...` 赋值** → 该故障永远不会被检出
- 若现场靠变频器故障继电器接 X03，则等价可用；否则该故障无检测

**3. 316 侧 AI 通道配置错误（压力链路）** ❌
- 实测 1# 外机的压力通道配置：AI0/AI2 被设成 **4-20mA**（其余是 0-10V）、
  低压通道带偏移 **43/44/45/46**、信号区间仍是 `400~2000`（对应 0.5~4.5V，装不下 0-10V 全量程）
- **后果**：压力换算错 → 饱和温度错 → 过热度为负 → **内机 EEV 一路关死**
- 已实测：`停机态下高低压倒挂`（高压 7.21 bar < 低压 11.21 bar），静态就错

### 🟡 P1 — 与规格有出入

**4. 全新风时 LWS=22 / RWS=10 未实现** ❌ —— 内外机两侧代码均无

**5. 3.11.8 传感器故障的三处门控偏差** ⚠️ —— 按需门控、AI3 仅 AUTO_AO、消毒/排毒模式绕过停机

**6. 3.11.2 外部连锁走正常停机时序** ⚠️ —— 最坏 30~90s 才全停，非"立即"

**7. `AirPressureSet` 上限 9999（规格 0-1500）** ⚠️

**8. 维护提醒默认间隔 0 → 开箱永不提醒；固件不主动弹窗** ⚠️

**9. `SupplyFanModeSet` 出厂默认 DO → 3.10 的 0-10V 控制出厂不激活** ⚠️

**10. 定时化霜时长档位边界：>4.0℃ vs 规格"高于 6℃"** ⚠️

**11. TypeII 双速低↔高换速 3s 死区，不走 30s 最低停机** ⚠️（代码注释自称依据 20260428）

**12. "变频满载 2HZ/S" 只在 FanTypeSet==4 时生效** ⚠️

### 🟢 P2 — 工程/环境风险

**13. 内机缺失源文件** ❌🔍 —— `Project/Obj` 下有 **20 个 `.o` 没有对应 `.c`**，
经 `.map` 核对**其中有 4 个真参与链接**：
```
sysrun.o       参与链接   ← 提供时标变量、看门狗、参数读写 EEPROM
io.o           参与链接
i2c_driver.o   参与链接
startup_stm32f40xx.o  参与链接（启动文件，正常）
```
其余（dac/valve/inver/normalfunction/usart/pt100/ds1302/can/adc/ff/diskio/socket/modbus_tcp/csv/cc936/bsp_sdio_sd）
在**当前** map 里未链接，但 `dac.o` 仍是 3.10 模拟量输出与分段加热的驱动 →
**这些模块的行为改不了，只能靠改调用方**

**14. 内机两个固件目录的源码不在版本控制内** ❌（沿用审计结论）

**15. 老压力控制死代码与其参数仍留在 HMI 点表** ⚠️（`FanClosePressure`/`FanOpenPressure`/`FanValue1/2` 等
仍在参数表、HMI 上可写但无效，容易误导调试者）

**16. AO 满量程标定无法从源码确认** ⚠️（`dac.c` 缺失）

---

## 四、需要你决策的四点

1. **类型③双风机（一定一变）**按框图实现 —— **需要那张框图才能核**
2. **TypeII 双速换速 3s 死区** vs 规格 30s —— 代码注释自称依据 20260428，**以哪版为准？**
3. **"变频风机满载 2HZ/S" 的"满载"定义** —— 是"变频已到顶"还是"双风机第二级投入"？
4. **回风机是否要跟随 8V/10V** —— 规格书 3.1 与 3.10 正文自相矛盾，**以哪个为准？**

---

## 五、我在核查中纠正的两条旧结论

1. ❌ **「外机还停留在压力控制老实现」—— 错。**
   老实现在 `BoardFanControl()` 里，首句 `if(OutdoorBoardFanControl()) return;` 且所有分支 `return 1`，
   注释写明 `Old BoardFanControl dead code ... skipped at runtime`。**现役是按冷凝温度分段控制，三种机型都实现了。**

2. ❌ **「`DefrostEnvIntervalSet` def=60 越界」—— 错（过期信息）。**
   现为 `def=30 / min=20 / max=50`，与规格一致；commit `0caac2e`(9/11) 已修。
   我引用的是审计清单里的旧记录，没回代码验证。

---

## 六、已确认符合的部分（可打勾）

| 章节 | 结论 |
|---|---|
| 3.3 开关机时序 | ✅ 实测 180s = 150s 风阀 + 30s 风机间隔，与规格吻合 |
| 3.4 参数（21 项） | ✅ 默认值逐项等于规格值（两遍独立核对） |
| 3.6 / 3.7 / 3.8 | ✅ PID 公式代数验算吻合、BX 表边界一致、死区与全部边界条件正确 |
| 3.9 EEV | ✅ 全部系数与规则 A/B/C 逐条对上 |
| 分段加热映射 | ✅ 对着四张图逐点核对（0-10V / 1:2 / 1:1:1  40%-70% / 1:2:4  15% 档位） |
| 第 5 节 紧急停机 | ✅ 除 EEV 外全部失电；关闭步数 +10（与 3.9 的 +20 正确区分） |
| 4.2 压缩机加减载 | ✅ 完全跟随室内机指令 |
| 4.3 冷凝风机 | ✅ 三种机型 + 冷凝温度取值来源 + 全部时间常数 |
| 4.4 化霜 | ✅ 7 条 AND 开始条件、3 条结束条件、参数默认值；❌ 仅"屏蔽低压开关"缺失 |
| 4.5 四通阀 | ✅ 制热延时 30s 得电、除霜/制冷不得电 |
| 4.6 保护 | ✅ 六小节全部符合（4.6.5 的化霜屏蔽缺失计入缺陷 #1） |
