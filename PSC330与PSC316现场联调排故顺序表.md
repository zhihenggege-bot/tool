# PSC330与PSC316现场联调排故顺序表

## 1. 适用范围

本表用于现场联调以下场景：

- `PSC330RK-V10-内机` 作为主控
- `PSC316RK-V12-外机` 作为外机从站
- 330 通过 RS485 读取 316 状态并联动报警、停机、启停
- HMI / Modbus Poll 直接读写 330 或 316 寄存器

本文档重点解决：

- 330 为什么报警
- 330 为什么停机
- 330 的 `Y05` 为什么亮
- 316 到底哪里故障
- 故障解除后该怎么复位、怎么确认恢复正常

## 2. 先记住的结论

### 2.1 330 的 `Y05` 是什么

330 当前代码里，`Y05` 不是风机、不是加热、不是开机输出。

`Y05` 是 **综合报警输出**。

只要 330 认为系统存在故障、联锁停机、外机异常联动等情况，`Y05` 就可能亮。

### 2.2 330 报警不一定是 330 自己坏了

330 会读取 316 外机状态。

所以很多现场现象其实是：

1. 316 先报码
2. 330 读到 316 异常
3. 330 进入停机/禁止运行/报警输出
4. `Y05` 亮

### 2.3 这次现场已验证过的真实链路

本次现场故障链路是：

1. 316 温度探头接线异常
2. 316 `1422/1431/1432` 出现 `9999`
3. 316 `612/613/614` 置位
4. 330 转发区 `1602` 读到 `9999`
5. 330 `737=1`
6. 330 报警，`Y05` 亮
7. 修好 316 探头后，330 报警自动解除

这个链路后续可作为标准排故模板。

## 3. 现场先看哪几个寄存器

## 3.1 先看 330

| 地址 | 名称 | 正常期望 | 用途 |
|---:|---|---|---|
| 703 | `ErrTotal` | 尽量为 0 | 330 当前累计/统计故障数 |
| 710 | `SysErrStatus` | 0 或 1 | 330 是否进入系统故障态 |
| 737 | `EnviOFFfalg` | 0 | 外机环境条件导致的禁止运行标志 |
| 861 | 报警相关状态 | 0 | 常用于辅助判断当前是否还在报警链路内 |
| 524 | `SysErrorReset` | 写命令用 | 330 故障复位命令 |
| 1601~1608 | 330 转发的 316 外机状态 | 根据现场变化 | 330 看外机最关键的一组镜像区 |

### 3.2 再看 316

| 地址 | 名称 | 正常期望 | 用途 |
|---:|---|---|---|
| 1402 | `SysErrorReset` | 写命令用 | 316 故障复位 |
| 1422 | `EnviTempAI0Disp` | 不为 `9999` | 316 环温 |
| 1431 | `FinTemp1AI5Disp` | 不为 `9999` | 316 1#盘管温度 |
| 1432 | `FinTemp2AI7Disp` | 不为 `9999` | 316 2#盘管温度 |
| 1443 | `HighPressDisp1` | 有合理数值 | 316 1#高压 |
| 1444 | `LowPressDisp1` | 有合理数值 | 316 1#低压 |
| 1445 | `HighPressDisp2` | 有合理数值 | 316 2#高压 |
| 1446 | `LowPressDisp2` | 有合理数值 | 316 2#低压 |
| 1429 | `ErrTotal` | 0 | 316 当前故障总数 |
| 1471~1478 | `ERR1Disp~ERR8Disp` | 全 0 | 316 当前故障打包字 |
| 600~627 | 详细故障位 | 全 0 | 316 当前详细故障定义区 |

## 4. 最实用的排故顺序

建议现场固定按下面顺序查，不要上来就改参数。

### 第一步：先看 330 是否真在报警

读 330：

- `703`
- `710`
- `737`
- `861`

判断规则：

- `Y05` 亮，通常说明 330 仍然认为系统异常
- `737=1`，优先怀疑 **外机环境/探头/联锁链路**
- `710=0` 但 `Y05` 亮，不代表没问题，很多时候只是还没进入“硬故障停机位”，但报警链路已成立

### 第二步：看 330 转发来的外机镜像区

读 330：

- `1601~1608`

重点看：

- `1602`

如果 `1602=9999`，高度怀疑 330 读到的 **316 环温异常**。

这时不要先怀疑 330 通讯解析，先直接去看 316 本机。

### 第三步：直接看 316 本机状态

读 316：

- `1422`
- `1431`
- `1432`
- `1443~1446`
- `1429`
- `1471~1478`
- `600~627`

判断原则：

- 温度显示 `9999` -> 对应探头链路故障
- 压力显示 `9999` -> 对应压力传感器链路故障
- 压力显示一直 `0` -> 常见是没接、没供电、没信号

### 第四步：先修硬件，再复位

先把探头、压力、接线、供电修好，再复位。

不要在硬件故障还存在时反复点复位，否则报码会立刻回来。

## 5. 常见故障现象与处理

## 5.1 现象：330 `Y05` 亮

优先检查：

1. 330 `737`
2. 330 `1602`
3. 316 `1422`
4. 316 `1429`
5. 316 `1471~1478`

经验判断：

- 如果 330 `1602=9999`
- 同时 316 `1422=9999`

那么基本就是 **316 环温链路异常引起的 330 联动报警**。

## 5.2 现象：316 温度显示 `9999`

重点检查：

- 探头有没有插紧
- 探头有没有插错口
- 探头线有没有断
- NTC 阻值是否正常
- NTC 小板 / ADS1115 / I2C 排线是否异常
- 公共供电 / 地线是否正常

本次现场验证过：

- 环温、盘管探头没接好时，316 会直接显示 `9999`
- 330 会继续联动报警

## 5.3 现象：316 高压显示 `0`

重点检查：

- 高压传感器是否已安装
- 传感器是否有供电
- 信号线是否接到正确 AI 口
- 传感器输出是否真的有电压
- 量程/类型是否匹配板卡要求

高压长期为 `0` 一般不是正常运行值，更像是 **高压传感器没有进来**。

## 5.4 现象：316 低压显示 `9999`

重点检查：

- 低压传感器供电
- 低压传感器信号线
- AI 输入链路
- 传感器是否损坏

## 5.5 现象：330 停机但 330 本机看不出明确硬故障

这时不要只盯 330。

按顺序查：

1. 330 `1601~1608`
2. 316 `1422~1446`
3. 316 `1429`
4. 316 `1471~1478`
5. 316 `600~627`

很多时候停机是 316 先报，330 只是跟停。

## 6. 316 故障位快速理解

本次联调里最常用的 316 故障位如下：

| 地址 | 名称 | 含义 |
|---:|---|---|
| 608 | `AI0TempErr` | 1#高压模拟量故障 |
| 609 | `AI1TempErr` | 2#高压模拟量故障 |
| 610 | `AI2TempErr` | 1#低压模拟量故障 |
| 611 | `AI3TempErr` | 2#低压模拟量故障 |
| 612 | `EnviTempErr` | 环温探头故障 |
| 613 | `Fin1TempErr` | 1#盘管探头故障 |
| 614 | `Fin2TempErr` | 2#盘管探头故障 |

## 7. 316 常用显示点位速查

| 地址 | 含义 |
|---:|---|
| 1422 | 环温 |
| 1431 | 1#盘管温度 |
| 1432 | 2#盘管温度 |
| 1433 | 1#排气温度 |
| 1434 | 2#排气温度 |
| 1435 | 1#吸气温度 |
| 1436 | 2#吸气温度 |
| 1443 | 1#高压 |
| 1444 | 1#低压 |
| 1445 | 2#高压 |
| 1446 | 2#低压 |

## 8. 330 与 316 复位方法

## 8.1 复位 316

向 316 写：

- `1402 = 1`

代码会处理后自动清零或由主站再写回 `0`。

复位后立刻看：

- `1429`
- `1471~1478`
- `600~627`

如果故障条件仍存在，报码会重新出现。

## 8.2 复位 330

向 330 写：

1. `524 = 1`
2. 再写 `524 = 0`

复位后看：

- `703`
- `737`
- `861`
- `1602`

## 9. 一套推荐的现场排故流程

### 场景 A：330 报警，`Y05` 亮

1. 读 330 `703/710/737/861`
2. 读 330 `1601~1608`
3. 如果 `1602=9999`，立刻去看 316 `1422`
4. 再看 316 `1431/1432`
5. 再看 316 `1429/1471~1478/600~627`
6. 修好探头或压力链
7. 复位 316
8. 再复位 330
9. 确认 `Y05` 熄灭

### 场景 B：316 本机报码，但 330 还没明显报码

1. 先在 316 把 `1422~1446` 看全
2. 再看 `1429/1471~1478/600~627`
3. 先修 316 硬件故障
4. 故障消除后再确认 330 是否同步恢复

### 场景 C：探头修好了，担心还有残留

修好后确认：

- 316 `1422/1431/1432` 不再是 `9999`
- 316 `1429=0`
- 316 `1471~1478=0`
- 330 `1602` 不再是 `9999`
- 330 `737=0`
- 330 `Y05` 熄灭

## 10. 现场最终确认清单

全部完成后，建议按下面清单走一遍：

- 316 `1422` 正常
- 316 `1431` 正常
- 316 `1432` 正常
- 316 `1443~1446` 正常
- 316 `1429=0`
- 316 `1471~1478=0`
- 316 `600~627` 无活动故障
- 330 `1602` 正常
- 330 `737=0`
- 330 `861=0`
- 330 `Y05` 熄灭

## 11. 这次现场经验总结

这次已经验证：

- 330 报警不一定是 330 自己故障
- 316 探头异常会通过 485 状态区传给 330
- 330 会据此报警、停机或禁止运行
- 先查外机真实状态，比先改 330 参数更有效

后续再遇到“330 报警但看不懂”的情况，优先套用本表。
