# PSC330 与 PSC316 问题排查与参数代码修改指南

本文档按现场现象组织：先改参数，再查通讯，再看代码。原则是能通过参数解决的不要改代码；确认是逻辑缺陷或规格变更时再改代码。

## 1. 总原则

### 1.1 先判断问题属于哪一层

| 层级 | 典型现象 | 优先检查 |
|---|---|---|
| HMI/地址层 | 读不到、写错地址、数值差 10 倍 | 0 基/1 基地址、从站号、数据格式 |
| 330 内机逻辑 | 需求不对、模式不对、输出被禁止 | 330 参数、`OutputForbidReasonDisp`、内机状态机 |
| 330->316 网关 | 写 316 失败、外机离线 | 1830~1997 镜像和诊断 |
| 316 外机逻辑 | 外机收到命令但不启动 | 316 禁用、保护、时序、风机类型 |
| 硬件/接线 | 输出有命令但继电器/电压不动 | DO/AO 接线、电源、模块、传感器 |

### 1.2 修改顺序

推荐顺序：

1. 查 HMI 地址和单位。
2. 查参数范围、默认值、现场配置。
3. 查通讯诊断。
4. 查保护和禁止原因。
5. 查代码调用链。
6. 最后才改控制算法。

## 2. 通讯问题

### 2.1 HMI 读不到 330

现象：

- HMI 全部显示空白。
- Modbus Poll 超时。
- 写入 330 没反应。

先查参数/接线：

| 项目 | 正确值 |
|---|---|
| 接口 | PSC330 A0B0 |
| 协议 | Modbus RTU |
| 波特率 | 9600 |
| 格式 | 8N1 |
| 地址 | PSC330 A0B0 从站地址，通常 1 |
| 地址格式 | Modbus Poll 用 0 基地址，威纶通一般用 4x=N+1 |

代码位置：

| 功能 | 文件 |
|---|---|
| 330 Modbus 从站 | `PSC330RK-V10-内机\User\UserSrc\modbus.c` |
| 参数宏 | `PSC330RK-V10-内机\User\UserHead\User.h` |

常见处理：

- 如果所有地址都差 1，改 HMI 地址基准。
- 如果只有某段不能写，先确认是否只读保护，不要直接删保护。
- 如果能读不能写，查 HMI 功能码是否用 06/16 写保持寄存器。

### 2.2 330 能读，写参数不生效

可能原因：

| 原因 | 说明 | 处理 |
|---|---|---|
| 写了只读区 | 运行状态、AI 校准、诊断区被保护 | 改 HMI 到正确 RW 地址 |
| 值超上下限 | `Parameter_min/max` 拦截 | 改输入范围或确认规格后改上下限 |
| 工程权限未打开 | 部分工程参数需要权限 | 查工程权限参数/超时逻辑 |
| 写到 PSC316 诊断区 | 316 诊断 `base+33~41` 只读 | 只能读，不能写 |

代码位置：

| 功能 | 文件 |
|---|---|
| HMI 写保护 | `PSC330RK-V10-内机\User\UserSrc\modbus.c` |
| 316 网关写保护 | `PSC330RK-V10-内机\User\UserSrc\modulecontrol.c` |

### 2.3 330 到 316 离线

现象：

- `base+33` 在线状态为 0。
- `base+34` 通讯故障为 1。
- `base+35` 写状态为 3。
- `base+39` 错误码为 2。

1# 外机诊断地址：

| 诊断 | 330 地址 | 4x |
|---|---:|---:|
| 在线状态 | 1863 | 41864 |
| 通讯故障 | 1864 | 41865 |
| 写状态 | 1865 | 41866 |
| 错误码 | 1869 | 41870 |
| 刷新计数 | 1870 | 41871 |

处理顺序：

1. 查 PSC330 A2/B2 到 PSC316 A2/B2 接线。
2. 查 A/B 是否接反。
3. 查 316 的 A2B2/内机通讯站号。
4. 多台 316 时确认站号不重复。
5. 查终端电阻和屏蔽接地。
6. 只接 1 台 316 单独测试。
7. 用 Modbus Poll 直连 316 A0B0 验证 316 本机是否活着。

代码位置：

| 功能 | 文件 |
|---|---|
| 330 外机网关 | `PSC330RK-V10-内机\User\UserSrc\modulecontrol.c` |
| 330 网关宏 | `PSC330RK-V10-内机\User\UserHead\User.h` |
| 316 地址参数 | `PSC316RK-V12-外机\WELLTHINKER\User\User.h` |
| 316 Modbus | `PSC316RK-V12-外机\WELLTHINKER\modbus\modbus.c` |

### 2.4 写 316 参数失败

看 330 诊断：

| 诊断 | 说明 |
|---|---|
| `base+35=1` | 正在写 |
| `base+35=2` | 写成功 |
| `base+35=3` | 写失败 |
| `base+36` | 最近写入的 316 参数地址 |
| `base+37` | 目标值 |
| `base+38` | 回读值 |
| `base+39` | 错误码 |

处理：

| 错误码 | 处理 |
|---:|---|
| 1 | HMI 写入越界，改 HMI 输入范围或 316 `Parameter_min/max` |
| 2 | 316 离线，查 A2B2 和站号 |
| 3 | HMI 连续写太快，加写入节流，等状态回 0/2 后再写下一笔 |
| 4 | 316 写后回读不一致，查 316 保存、地址映射、是否被本机逻辑覆盖 |
| 5 | 不允许把当前外机写成其他站号，按外机号写对应地址 |

不建议：

- 不要让 HMI 直接写 316 来绕过 330。
- 不要取消回读确认。
- 不要把诊断区改成可写。

## 3. 330 模式和开关机问题

### 3.1 模式显示对，但动作不对

可能原因：

| 原因 | 检查 |
|---|---|
| `UserWorkModeSet` 和 `UserWorkModeSetTEMP` 没同步 | 看内机模式统一函数 |
| 自动冷热切换改了模式 | 查 QHB/QHS 和湿度需求 |
| 保护禁止输出 | 看 `OutputForbidReasonDisp` |
| HMI 写了老地址但显示新模式 | 统一 HMI 读写真实模式变量 |

参数：

| 参数 | 用途 |
|---|---|
| `UserWorkModeSet` | 老兼容模式，给旧逻辑用 |
| `UserWorkModeSetTEMP` | 当前真实 0~5 模式 |
| `UserAutoModeSet` | 自动模式开关 |
| `QHB/QHS` | 自动冷热切换边界 |

代码位置：

| 功能 | 文件/函数 |
|---|---|
| 模式统一 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` 的 `IndoorNormalizeWorkMode()` |
| 统一写模式 | `IndoorSetNormalWorkMode()` |
| 自动冷热切换 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |

如果要改：

- 只改 `IndoorSetNormalWorkMode()` 的映射规则，不要在各处散改两个变量。
- 自动模式切换边界只改 QHB/QHS 或对应切换函数，避免破坏手动模式。

### 3.2 开机后不启动

先查：

| 检查项 | 说明 |
|---|---|
| `UserSwitchSet` | 是否开机 |
| 模式 | 制冷/制热/通风是否正确 |
| 温湿度需求 | 是否已经在死区内 |
| 消防/漏水/阀门联锁 | 有保护时禁止输出 |
| 316 在线 | 外机离线时压缩机不能正常联动 |
| 外机禁用 | `OutdoorDisableSet` 或 `CompDisableSet1/2` |

330 代码位置：

| 功能 | 文件 |
|---|---|
| 开关机状态机 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |
| 保护检查 | `PSC330RK-V10-内机\User\UserSrc\ErrorCheck.c` |
| 压缩机需求 | `PSC330RK-V10-内机\User\UserSrc\NergryAdjust.c` |

316 代码位置：

| 功能 | 文件 |
|---|---|
| 外机禁用/环温保护 | `PSC316RK-V12-外机\WELLTHINKER\User\UserAction.c` |
| 压缩机时序 | `PSC316RK-V12-外机\WELLTHINKER\User\moduleAction.c` |
| 外机保护 | `PSC316RK-V12-外机\WELLTHINKER\User\ErrorCheck.c` |

### 3.3 停机后还有输出

先查：

- 330 停机命令是否已经写下去。
- 316 是否处于最小运行时间或停机延时。
- 四通阀和风机是否在正常延时关闭。
- 手动调试输出是否没关。

相关参数：

| 参数 | 项目 |
|---|---|
| `FanONLaySet` | 风机提前/延后时间 |
| `FourValveLaySet` | 四通阀延时 |
| `FourWayValveDelaySet` | 316 新规格四通阀延时 |
| `MinRunTimeSet` | 压缩机最小运行 |
| `MinStopTimeSet` | 压缩机最小停机 |

如果要改：

- 330 普通时序改 `UserAction.c` 的普通三模式状态机。
- 316 压缩机/风机/四通阀实际输出时序改 `moduleAction.c`。
- 禁用/保护下最终限幅改 `OutdoorProtectOutputLimit()`，不要只改前面的命令变量。

## 4. 330 温湿度、PID、能量问题

### 4.1 温度到了但压缩机不加/减载

先查参数：

| 参数 | 作用 |
|---|---|
| `UserColdTempSet` | 制冷温度设定 |
| `UserHeatTempSet` | 制热温度设定 |
| `RoomTempDiffSet` | 死区/温差 |
| `CompControlModeSet` | 压缩机控制模式 |
| `CompNumSet` | PID 设定压缩机数量 |
| `CompVVFMaxFreSet` | 最大频率 |
| `CompVVFMinFreSet` | 最小频率 |

代码位置：

| 功能 | 文件/函数 |
|---|---|
| 压缩机 PID | `PSC330RK-V10-内机\User\UserSrc\NergryAdjust.c` 的 `SysNergryAction_PID()` |
| 环境控制 | `IndoorEnvTempEnergyAction()` |
| 常规能量计算 | `NergryAdjust.c` |

调试方法：

1. 把温度设定拉开 3~5C，避免死区内看不出动作。
2. 先用固定湿度，排除湿度需求干扰。
3. 看压缩机需求显示。
4. 看 330 是否下发到 316。
5. 看 316 是否被保护拦截。

### 4.2 自动冷热切换频繁

优先改参数：

| 参数 | 建议 |
|---|---|
| `QHB` | 加大制热进入边界回差 |
| `QHS` | 加大制冷进入边界回差 |
| `RoomTempDiffSet` | 适当增大死区 |

代码位置：

| 功能 | 文件 |
|---|---|
| 自动冷热切换 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |

改代码原则：

- 保持“湿度需求抑制再热比例导致的冷转热误判”。
- QHS 要保持合理上下限，避免现场写入极端值造成不切换。

### 4.3 除湿/再热冲突

现象：

- 湿度高时压缩机不除湿。
- 再热过大导致温度上升。
- 自动模式误切制热。

先查参数：

| 参数 | 作用 |
|---|---|
| `HumiditySet` | 湿度设定 |
| `ReheatKpSet/Ti/Td` | 再热 PID |
| `ReheatFullOnDevSet` | 再热全开偏差 |
| `ReheatFullOffDevSet` | 再热全关偏差 |
| `HumidKpSet/Ti/Td` | 加湿 PID |
| `HumidFullOnDevSet` | 加湿全开偏差 |
| `HumidFullOffDevSet` | 加湿全关偏差 |

代码位置：

| 功能 | 文件 |
|---|---|
| 再热/加湿/预热输出 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |
| 能量需求 | `PSC330RK-V10-内机\User\UserSrc\NergryAdjust.c` |

## 5. 330 EEV 问题

### 5.1 EEV 不动作

先查：

| 检查 | 说明 |
|---|---|
| 压缩机是否运行 | 停机时 EEV 会关闭 |
| EEV 手动控制 | 手动使能会覆盖自动 |
| `EEVSpecPidEnableSet` | 是否启用规格 PID |
| 初始开度参数 | 是否为 0 或过小 |
| 电机驱动/接线 | 输出脉冲是否存在 |

参数：

| 参数 | 作用 |
|---|---|
| `MotorTotalPulseSet` | 总脉冲 |
| `EEVCloseMinSizeSet` | 最小关阀步数 |
| `EEVOpenMaxSizeSet` | 最大开度 |
| `EEVInitialSizeSet1/4/6` | 初始开度表 |
| `EEVStepLimitSet` | 单次步数限制 |
| `DefrostEEVStepSet` | 化霜开度 |

代码位置：

| 功能 | 文件 |
|---|---|
| 330 EEV | `PSC330RK-V10-内机\User\UserSrc\eevCTRL.c` |

### 5.2 EEV 震荡

优先调参数：

| 参数 | 调整方向 |
|---|---|
| `EEVSuperHeatDeadSet` | 适当增大死区 |
| `EEVPidKpSet` | 过冲大则降低 |
| `EEVPidTiSet` | 积分太猛则增大 |
| `EEVPidTdSet` | 噪声大则降低或关闭 |
| `EEVStepLimitSet` | 限制每次最大变化 |

代码不要轻易改 PID 公式，先用参数把现场稳定性调出来。

### 5.3 停机后 EEV 关得太慢/太快

优先改：

| 参数 | 说明 |
|---|---|
| `EEVStepLimitSet` | 默认 20 步/s，越大关得越快 |

代码位置：

| 功能 | 文件 |
|---|---|
| EEV 停机关阀 | `PSC330RK-V10-内机\User\UserSrc\eevCTRL.c` |

## 6. 330 预热、再热、加湿、新风问题

### 6.1 预热电加热段数不对

参数：

| 参数 | 取值 |
|---|---|
| `PreheatSegSet` | 1=开关，2=1:2，3=1:2:4 |

现场怎么设：

| 现场硬件 | 参数 |
|---|---:|
| 只有 1 段电加热 | 1 |
| 两段 1:2 | 2 |
| 三段 1:2:4 | 3 |

默认 3 的原因：规格书按 1:2:4 分级时控制最细，能覆盖 1~7 档输出；但现场硬件不是三段时必须改成实际段数。

代码位置：

| 功能 | 文件 |
|---|---|
| 预热输出 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |
| 参数定义 | `PSC330RK-V10-内机\User\UserHead\User.h` |

### 6.2 新风阀/混风阀不按 CO2 动作

先查参数：

| 参数 | 作用 |
|---|---|
| `FreshValveCtrlBasisSet` | 新风阀控制依据 |
| `RoomPressureSet` | 房间压差设定 |
| `CO2Set` | CO2 设定 |
| `FreshValveManualVoltSet` | 手动电压 |
| `MixAirValveTypeSet` | 混风阀类型 |

处理：

- 如果要按 CO2 控制，确认 `FreshValveCtrlBasisSet` 选到 CO2。
- 如果现场没有 CO2 传感器，不要启用 CO2 闭环。
- 如果阀门方向反了，优先查 AO 接线和阀门类型，再考虑代码反向。

代码位置：

| 功能 | 文件 |
|---|---|
| 阀门控制 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |

### 6.3 热回收阀不开

先查参数：

| 参数 | 作用 |
|---|---|
| `HeatRecoveryEnableSet` | 热回收使能 |
| `HeatRecoveryOnDevSet` | 开启温差 |
| `HeatRecoveryOffDevSet` | 关闭温差 |

处理：

- 使能必须为 1。
- 开启温差要大于关闭温差，避免抖动。
- 传感器异常时应保持关闭。

## 7. 316 外机启动问题

### 7.1 316 收到开机但压缩机不开

先查参数：

| 参数 | 作用 |
|---|---|
| `OutdoorDisableSet` | 外机整体禁用 |
| `CompDisableSet1/2` | 单台压缩机禁用 |
| `MinStopTimeSet` | 最小停机时间未到不能开 |
| `FanONLaySet` | 风机提前时间 |
| `FourWayValveDelaySet` | 四通阀延时 |
| `Copheatfunction` | 制热时是否允许压缩机制热 |

再查保护：

| 保护 | 影响 |
|---|---|
| 高压 | 停对应压缩机 |
| 低压 | 停对应压缩机 |
| 排气温度 | 停对应压缩机 |
| 电流过高 | 停对应压缩机 |
| 风机故障 | 可停整机 |
| 环温传感器故障 | 停整机 |

代码位置：

| 功能 | 文件 |
|---|---|
| 外机禁用和环温保护 | `PSC316RK-V12-外机\WELLTHINKER\User\UserAction.c` |
| 压缩机时序 | `PSC316RK-V12-外机\WELLTHINKER\User\moduleAction.c` |
| 保护处理 | `PSC316RK-V12-外机\WELLTHINKER\User\ErrorCheck.c` |

### 7.2 316 一开机马上停

常见原因：

| 原因 | 检查 |
|---|---|
| 高压输入常闭/常开配置反 | 看输入状态和接线 |
| 低压延时太短 | `CoolLowCheckSet`、`HeatLowCheckSet` |
| 电流设定太低 | `CurrentHighSet` |
| 排气温度异常 | `ExhaustHightTempSet`、传感器 |
| 环温保护 | `OutdoorUnitTypeSet`、`LWS`、`RWS` |
| 风机未启动导致压力升高 | `OutdoorFanTypeSet`、风机输出 |

处理：

- 先看 316 故障字 `ERR1Disp~ERR8Disp`。
- 再看高压、低压、排气温度、电流实时值。
- 如果是传感器断线，不要通过放宽保护掩盖。

## 8. 316 风机问题

### 8.1 风机类型不对

参数：

| 参数 | 取值 |
|---|---|
| `OutdoorFanTypeSet=0` | 单风机双速 |
| `OutdoorFanTypeSet=1` | 双定速 |
| `OutdoorFanTypeSet=2` | 单变频 |
| `OutdoorFanTypeSet=3` | 一定速 + 一双速 |
| `OutdoorFanTypeSet=4` | 一变频 + 一定速 |

相关输出互锁代码：

| 功能 | 文件 |
|---|---|
| 风机类型控制 | `PSC316RK-V12-外机\WELLTHINKER\User\UserAction.c` |
| 输出映射/限幅 | `PSC316RK-V12-外机\WELLTHINKER\User\ErrorCheck.c` |

如果现场风机接线与代码定义不一致：

1. 优先改参数 `OutdoorFanTypeSet`。
2. 仍不匹配，再核对 `User.h` 里的 DO 宏。
3. 最后修改 `IoOutAction()` 的风机类型输出映射。

### 8.2 变频风机频率不变

先查：

| 参数 | 说明 |
|---|---|
| `FanMaxFreqSet` | 最大频率 |
| `FanMinFreqSet` | 最小频率 |
| `FanStepSpeedSet` | 每秒加减速度 |
| `CondFanTLSet` | 目标冷凝温度 |
| `CondFanTLDSet` | 回差 |
| `FanMinRunTimeSet` | 最小运行 |
| `FanMinStopTimeSet` | 最小停止 |

再查：

- `OutdoorFanTypeSet` 是否为 2 或 4。
- 冷凝温度是否真的超过加载条件。
- AO 输出是否有变化。
- 变频器通讯是否正常。

## 9. 316 化霜问题

### 9.1 不进入化霜

按下面顺序查：

| 条件 | 参数/信号 |
|---|---|
| 是否制热 | `UserWorkModeSet=1` |
| 是否有压缩机运行 | `Comp[0/1]`、运行状态 |
| 环温是否低 | `EnviTempAI0Disp < DefrostEnvTempSet` |
| 是否持续 60s | 需要等待 |
| 压缩机运行是否够 | `DefrostCompRunMinSet` |
| 翅片温度是否低 | `DefrostFinTempStartSet` |
| 翅片低温是否持续 30s | 需要等待 |
| 温差是否够 | `DefrostEnvFinGapSet` |
| 间隔是否到 | `DefrostIntervalAbove0Set/Below0Set` |
| 另一系统是否正在化霜 | 同时化霜互锁 |

代码位置：

| 功能 | 文件/函数 |
|---|---|
| 化霜判断 | `PSC316RK-V12-外机\WELLTHINKER\User\DefAction.c` 的 `DefEnable()` |

### 9.2 化霜退不出来

查退出条件：

| 条件 | 参数/信号 |
|---|---|
| 翅片温度达到结束 | `DefrostFinTempEndSet` |
| 最大化霜时间到 | `DefrostTimeAbove0Set/Below0Set` |
| 手动停止 | `Comp1DefrostFlag/Comp2DefrostFlag=2` |
| 高压退出 | 高压输入 |

如果现场化霜时间太短/太长：

- 环温大于 0C，调 `DefrostTimeAbove0Set`。
- 环温小于 0C，调 `DefrostTimeBelow0Set`。
- 翅片结束温度偏高导致不退出，调 `DefrostFinTempEndSet`，但不能低到化霜不干净。

### 9.3 化霜时报低压

当前逻辑：

- 化霜期间屏蔽低压。
- 化霜结束后约 60s 继续屏蔽低压。

若仍报低压：

1. 确认 `SysHaveDef[]` 或 `DefrostSignal[]` 是否真的置位。
2. 确认低压是在化霜前还是化霜后 60s 之后发生。
3. 查 `OutdoorLowPressureMaskedByDefrost()`。

代码位置：

| 功能 | 文件 |
|---|---|
| 低压化霜屏蔽 | `PSC316RK-V12-外机\WELLTHINKER\User\ErrorCheck.c` |

## 10. 316 环境温度保护问题

### 10.1 低温制冷不开

参数：

| 参数 | 说明 |
|---|---|
| `OutdoorUnitTypeSet=0` | 15~43C |
| `OutdoorUnitTypeSet=1` | 0~43C |
| `OutdoorUnitTypeSet=2` | -15~43C |
| `LWS` | 制冷低环温启动点 |

处理：

- 如果现场要求低温制冷，确认 `OutdoorUnitTypeSet` 选 1 或 2。
- 如果选 0，低于 15C 附近不开是正常保护。

### 10.2 高温制热不开

参数：

| 参数 | 说明 |
|---|---|
| `RWS` | 热泵制热高环温启动/恢复边界 |

处理：

- 环温高于 `RWS` 附近时制热保护是正常。
- 若现场允许更高环温制热，先调整 `RWS`，不要直接去掉保护。

代码位置：

| 功能 | 文件/函数 |
|---|---|
| 外机环温保护 | `PSC316RK-V12-外机\WELLTHINKER\User\UserAction.c` 的 `AmbientProtectAction()` |

## 11. 参数应该在哪里改

### 11.1 330 常改参数

| 需求 | 参数 | 文件宏 |
|---|---|---|
| 制冷温度 | `UserColdTempSet` | 330 `User.h` |
| 制热温度 | `UserHeatTempSet` | 330 `User.h` |
| 湿度 | `HumiditySet` | 330 `User.h` |
| 温度死区 | `RoomTempDiffSet` | 330 `User.h` |
| 自动冷热切换 | `QHB/QHS` | 330 `User.h` |
| 压缩机 PID | `PidAdjustTimeSet*`、`ProportionCoeSet*`、`IntegralCoeSet*`、`DerivativeCoeSet*` | 330 `User.h` |
| 再热 PID | `ReheatKpSet/Ti/Td` | 330 `User.h` |
| 预热 PID | `PreheatKpSet/Ti/Td` | 330 `User.h` |
| 加湿 PID | `HumidKpSet/Ti/Td` | 330 `User.h` |
| EEV PID | `EEVPidKpSet/Ti/Td` | 330 `User.h` |
| EEV 关阀速度 | `EEVStepLimitSet` | 330 `User.h` |
| 压差保护 | `PressureProtectEnableSet` | 330 `User.h` |
| 预热段数 | `PreheatSegSet` | 330 `User.h` |

### 11.2 316 常改参数

通过 330 镜像区写 316：

```text
330 地址 = 1830 + (外机号 - 1) * 42 + 偏移
316 地址 = 165 + 偏移
```

| 需求 | 316 参数 | 1# 330 地址 |
|---|---:|---:|
| 外机类型 | 165 | 1830 |
| 风机类型 | 166 | 1831 |
| 外机禁用 | 167 | 1832 |
| 1#压缩机禁用 | 168 | 1833 |
| 2#压缩机禁用 | 169 | 1834 |
| 制冷低环温 | 170 | 1835 |
| 制热高环温 | 171 | 1836 |
| 冷凝目标温度 | 172 | 1837 |
| 冷凝回差 | 173 | 1838 |
| 变频风机最大频率 | 176 | 1841 |
| 变频风机最小频率 | 177 | 1842 |
| 风机加减速 | 178 | 1843 |
| 化霜环温 | 182 | 1847 |
| 化霜最小运行 | 185 | 1850 |
| 化霜开始翅温 | 186 | 1851 |
| 化霜结束翅温 | 190 | 1855 |
| 四通阀延时 | 193 | 1858 |
| 316 HMI 地址 | 194 | 1859 |
| 316 A2B2 地址 | 197 | 1862 |

注意：写 197 站号时，330 会检查外机号，避免把 1# 写成 2#。

## 12. 代码应该在哪里改

### 12.1 330 代码修改入口

| 问题 | 优先文件 |
|---|---|
| HMI 读写权限、写保护 | `PSC330RK-V10-内机\User\UserSrc\modbus.c` |
| 330 参数定义、地址 | `PSC330RK-V10-内机\User\UserHead\User.h` |
| 默认值、上下限 | `PSC330RK-V10-内机\User\UserSrc\user.c` |
| 开关机时序、模式、阀门、新风、热回收 | `PSC330RK-V10-内机\User\UserSrc\UserAction.c` |
| 压缩机能量、PID、自动冷热 | `PSC330RK-V10-内机\User\UserSrc\NergryAdjust.c` |
| EEV | `PSC330RK-V10-内机\User\UserSrc\eevCTRL.c` |
| 风机/风量 | `PSC330RK-V10-内机\User\UserSrc\FanControl.c` |
| 保护 | `PSC330RK-V10-内机\User\UserSrc\ErrorCheck.c` |
| 330 转发 316 | `PSC330RK-V10-内机\User\UserSrc\modulecontrol.c` |

### 12.2 316 代码修改入口

| 问题 | 优先文件 |
|---|---|
| 316 参数定义、地址 | `PSC316RK-V12-外机\WELLTHINKER\User\User.h` |
| 默认值、上下限 | `PSC316RK-V12-外机\WELLTHINKER\User\user.c` |
| 外机类型、环温保护、冷凝风机 | `PSC316RK-V12-外机\WELLTHINKER\User\UserAction.c` |
| 压缩机/风机/四通阀时序 | `PSC316RK-V12-外机\WELLTHINKER\User\moduleAction.c` |
| 化霜 | `PSC316RK-V12-外机\WELLTHINKER\User\DefAction.c` |
| 高低压/过载/故障分层 | `PSC316RK-V12-外机\WELLTHINKER\User\ErrorCheck.c` |
| EEV | `PSC316RK-V12-外机\WELLTHINKER\User\eevCTRL.c` |
| 316 Modbus | `PSC316RK-V12-外机\WELLTHINKER\modbus\modbus.c` |

## 13. 常见现场现象速查

| 现象 | 先改参数 | 再查代码 |
|---|---|---|
| HMI 写 316 没反应 | 看 1865~1869 诊断 | 330 `modulecontrol.c` |
| 316 离线 | 站号、A2B2 接线 | 330 `modulecontrol.c`，316 `modbus.c` |
| 制冷不开 | 温度设定、死区、外机禁用、环温 | 330 `NergryAdjust.c`，316 `UserAction.c` |
| 制热不开 | `RWS`、`Copheatfunction`、四通阀延时 | 316 `UserAction.c`、`moduleAction.c` |
| 通风开了压缩机 | 模式变量/HMI 地址 | 330 `UserAction.c` |
| 自动冷热抖动 | QHB/QHS、死区 | 330 `UserAction.c` |
| 除湿时误切制热 | 湿度设定、再热参数 | 330 `UserAction.c` |
| EEV 震荡 | EEV PID、死区、步数限制 | 330/316 `eevCTRL.c` |
| 风机类型输出错 | `OutdoorFanTypeSet` | 316 `UserAction.c`、`ErrorCheck.c` |
| 化霜不进 | 化霜环温、翅温、间隔、运行时间 | 316 `DefAction.c` |
| 化霜低压报警 | 化霜状态是否置位 | 316 `ErrorCheck.c` |
| 高压频繁 | 风机参数、冷凝目标、制冷剂、传感器 | 316 `UserAction.c`、`ErrorCheck.c` |
| 加湿不出 | 湿度设定、加湿故障输入 | 330 `UserAction.c`、`ErrorCheck.c` |
| 预热段数不对 | `PreheatSegSet` | 330 `UserAction.c` |
| 压差保护误报 | `PressureProtectEnableSet=0/1`、压力开关 | 330 `ErrorCheck.c` |

## 14. 修改后必须验证

每次改代码后至少验证：

1. Keil 全量编译无错误。
2. 330 A0B0 能读写。
3. 330 A2B2 能读 316。
4. 316 设置参数写入后回读一致。
5. 停机时压缩机命令清零。
6. 保护触发时输出被最终限幅。
7. 断电重启后关键参数不丢。
8. HMI 地址没有 0/1 基偏移。
9. 多台外机站号不冲突。
10. 手动调试退出后没有残留输出。

