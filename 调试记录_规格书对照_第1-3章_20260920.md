# PSC330 内机 —— 规格书 §1~§3 逐句对照固件代码（2026-09-20）

生成方式：11 节并行审计 → 每条**非「符合」**的发现再上 3 个对抗视角（代码事实 / 规格书字面 / 运行时后果，**默认立场是「推翻」**）→ 3 个里 ≥2 个没能推翻才算通过。

判定记号：`符合` / `不符合` / `未实现` / `部分符合` / `无法核对`

## §1.1 控制器简介 + §1.2 机组简要说明

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 1.1 机组内外机均采用葳尔思克单片机控制 | 符合 | PSC330RK-V10-内机/Project/PSC330RK-V10.uvprojx：`<Device>STM32F407ZETx</Device>` `<OutputName>PSC330RK-V10</OutputName>`；工程直接链接厂商预编译对象（uvprojx 内引用 adc.o/io.o/sysrun.o/usart.o/dac.o/eev.o/i2c_driver.o/normalfunction.o/w3.o，对应源码不在仓库，只剩 User/wellthinker/inc/ 下的头文件）。316 侧：PSC316RK-V12-外机/USER/PSC316RK-V12.uvprojx `<Device>STM32F103VC</Device>`，源码目录名即 WELLTHINKER/ | 规格书未指定 MCU 型号，MCU 选型属代码自行决定（330=F407、316=F103），不是缺陷 |
| 1.1 内机采用 PAC330RK-V10 控制板 | **[被推翻]** 部分符合 | PSC330RK-V10-内机/User/UserHead/User.h:1690-1692 `#define PSC330_VERSION_MODEL 330` / `PSC330_VERSION_MAJOR 10` / `PSC330_VERSION_MINOR 12`；modulecontrol.c:612-617 把这 6 个字发布到 HMI 2622..2627 | 型号数字 330 + 大版本 V10 对得上，但代码/工程/目录一律写 **PSC330RK**，规格书写 **PAC330RK**，P/C 一字之差。代码里根本没有 'PAC330RK-V10' 这个字符串常量，只上报数字 330/10/12。同文档 316 写的是 PSC316RK，故判为规格书笔误可能性大，但按字面不能算全符 ／复核 2/3 推翻 |
| 1.1 外机采用 PSC316RK-V12 控制板 | 符合 | PSC316RK-V12-外机/WELLTHINKER/User/User.h:140-142 `#define PSC316_VERSION_MODEL 316` / `MAJOR 12` / `MINOR 14`；USER/PSC316RK-V12.uvprojx `<OutputName>PSC316RK-V12-20250327</OutputName>`；330 侧 modulecontrol.c:1540-1552 校验回读的 316 版本：`Version[0]==316 && Version[1]<=999 && ... Version[3]>=2000 && Version[3]<=2099 && Version[4]>=1 && Version[4]<=12 && Version[5]>=1 && Version[5]<=31` | 规格书只写 V12，代码当前是 V12.14；小版本号规格书未约束 |
| 1.1 (每个外机一个) | 符合 | 330 按 316 从站地址 1..4 逐台单写：modulecontrol.c:1178-1184 `ucUart5Buff[0]=ModuleNum; ucUart5Buff[1]=16; /*写功能码*/ Addr=PLCctrlPara; ... ucUart5Buff[Num*2+8]=j;/*CRC*/`；单元槽数 User.h:1697 `#define PSC316_HMI_UNIT_NUM 4`；每模块独立状态 modulecontrol.c:12-13 `UnitWriteInfo UnitWriteStatus[18]; UnitReadInfo UnitReadStatus[18];` | 是主从 1:多，不是自动发现；外机台数由 Parameter[388] 指定。注意：memory 已记『330→316 无广播通路，下发只能按外机单写、仅 HMI 触发』只对**设置类**参数成立，压机/除霜/使能等 14 字控制块 (PLCctrlPara=1400) 是 330 每扫描自动逐台下发的（modulecontrol.c:1180-1190） |
| 1.2 整个机组分为室内机和室外机两部分 | 符合 | PSC330RK-V10-内机/User/UserSrc/UserAction.c:2078 `ModuleNumSet = OutdoorUnitNumSet + 1;   // ModuleNumSet includes indoor board 0 plus outdoor units 1~4.`；模块 0=本板（内机），1..4=外机；对外机的通讯口是 UART5(9600)（modulecontrol.c:892 `UART5Init(9600);`） | 代码里用『模块号 0..4』表达，未用『室内机/室外机』字样 |
| 1.2 室内机 1 台 | 符合 | PSC330RK-V10-内机/User/UserSrc/user.c:9 `uint8_t BoardAddressSet=0;` —— 全局变量，全工程无任何赋值处 → 本固件恒为系统板；modulecontrol.c:881 `if(BoardAddressSet>0)return;   //只有系统板才能发送数据`；ErrorCheck.c:308 `if(BoardAddressSet!=0)return;` | BoardAddressSet 的初值即最终值，源码级不可把它配成外机板；真正的拨码读取在预编译 io.o 里（源码不在仓库） |
| 1.2 室外机最多 4 台 | 符合 | PSC330RK-V10-内机/User/UserSrc/UserAction.c:2076-2077 `if(OutdoorUnitNumSet < 1)OutdoorUnitNumSet = 1; if(OutdoorUnitNumSet > 4)OutdoorUnitNumSet = 4;`；User.h:727 `#define OutdoorUnitNumSet Parameter[388]  // Outdoor unit count: 1=A, 2=A/B, 3=A/B/C, 4=A/B/C/D`；user.c:333 `Parameter_max[388]=4; Parameter_min[388]=1; Parameter_def[388]=1;`；HMI 写模式号再夹一层 modbus.c:295 `if(addr == MODEPARA)return (value >= 1) && (value <= OutdoorUnitNumSet) && (value <= 4);`；UserAction.c:2138-2139 `if(ModuleNumSet>5)ModuleNumSet=5; if(ModuleNumSet <2)ModuleNumSet=2;` | 三处独立钳位（参数上下限表 / 扫描钳位 / HMI 写校验）互相冗余，结论一致，无互相打架 |
| 1.2 每台室外机有 1~2 个系统 | **[被推翻]** 部分符合 | 『2 系统』实现完整：modulecontrol.c:1578-1579 回读 `CompStatus[0]`/`CompStatus[1]`（压缩机1/2状态）；UserAction.c:2456 `MaxUseCompNum=(ModuleNumSet-1)*2;`；下发 modulecontrol.c:1136-1150 `CompControl[0]`/`CompControl[1]` → ModComp1/2OnOFFFalg；EEV 8 通道按 `*module = channel/2 + 1; *comp = channel % 2;`（eevCTRL.c:56-58）映射到 4 外机×2 系统 | 『1 个系统』在 330 侧没有入口：UserAction.c:1969 无条件 `FourValveSet =1;//双系统`（User.h:311 `#define FourValveSet Parameter[MAKER+0]`，MAKER=78），即 330 恒按双系统算；单系统只能靠 316 自己的 `FourValveSet`（316 Parameter[78]，见 PSC316RK-V12-外机/PSC316RK-V12_Modbus寄存器地址映射表.md:108 『四通阀系统数 0=单 1=双』）配置。规格书没规定谁配，故记部分符合。另注 User.h:1747 `#define CompNum 4` 是数组容量，实际每台只用 [0..1]，而 ErrorCheck.c:446 `for(j=0;j<CompNum;j++)` 会多扫 2 个恒为 0 的槽 ／复核 3/3 推翻 |
| 1.2 每台室外机 1~2 个轴流风机 | 符合 | 316 侧定义在 PSC316RK-V12-外机/WELLTHINKER/User/User.h:438 `#define OutdoorFanTypeSet Parameter[OUTDOOR_SPEC+1]   // 0=1fan-2spd,1=2fan-1spd,2=1fan-VFD,3=1fix+1-2spd,4=1fix+1VFD` —— 1 风机/2 风机、定频/双速/变频都有；330 侧只做类型透传：modbus.c:566-578 `SpecBase = PSC316_HMI_BASE + i * PSC316_HMI_UNIT_STEP; ... OutdoorUnitTypeValue = ...; OutdoorFanTypeValue = ...; PSC316Gateway_HMIWriteSpecPair(Addr, OutdoorUnitTypeValue, OutdoorFanTypeValue)` | 330 侧无风机档位/转速命令，只有类型转发 |
| 1.2 室外机为共用风系统，即风机为所有压缩机共用 | 无法核对 | 330 侧检索无任何『按系统/按压机分配风机』的代码：对外机唯一自动下发是 14 字控制块 modulecontrol.c:1180-1190（Addr=PLCctrlPara=1400，User.h:1467），内容为压机开关机/频率/除霜/使能，不含风机 | 该行为整机在 316 内实现（316 有 `Fan1Y03`/`Fan3Y07`、`Fan1OverX12`/`Fan2OverX17`，见 PSC316RK-V12-外机/WELLTHINKER/User/User.h:242-252，且 OutdoorFanXXX 由整机压机状态驱动）。330 侧没有对应可核对象，按『不臆测』记无法核对；不因此判缺陷 ／复核 1/3 推翻 |
| 1.2 多个室外机按压缩机型号相同配置 | **[被推翻]** 部分符合 | PSC330RK-V10-内机/User/UserSrc/ErrorCheck.c:481-487 `for(i=2;i<ModuleNumSet;i++)//配置不一致报警 { if(UnitReadStatus[i].DIPboard != UnitReadStatus[1].DIPboard) { //DIPError=1; } }` —— 比较循环在，但唯一动作被注释掉；DIPboard 来自 316 的 Parameter[1008]（modulecontrol.c:1559 `UnitReadStatus[ucUart5Buff[0]].DIPboard = (ucUart5Buff[19]<<8)+ucUart5Buff[20];//Parameter[1008]`），即该外机的拨码/配置字 | 『规格书写了、代码没做』：一致性判据存在但形同虚设（既不上报也不停机）。规格书只写了『按压缩机型号相同配置』这句约束，没写违反时的动作，所以只记部分符合，不替规格书补锅 ／复核 3/3 推翻 |
| 1.2 内机与外机通讯方式为 Modbus RTU 通讯（RS485 接口） | 符合 | 协议层：modbus.c:190-205 `CreatCRC()` 标准 Modbus CRC16 查表（auchCRCHi/auchCRCLo 表在 modbus.c:146-188）；330↔316 链路用 FC3/FC6/FC16 手搭帧并**校验接收 CRC**：modulecontrol.c:1487-1489 `CRC_Result = CreatCRC(ucUart5Buff,(ucUart5ReceLen-2)); CRC_Recedat = (ucUart5Buff[ucUart5ReceLen-2]<<8)+ucUart5Buff[ucUart5ReceLen-1]; if(CRC_Recedat != CRC_Result){ucUart5ReceLen = 0;return;}`；HMI 从站层 modbus.c:651-674 `if(0 == CreatCRC(ucUart1Buff,ucUart1ReceLen)) ... if(ucUart1Buff[0]==1) ... switch(ucUart1Buff[1]){case 1/3/5/6/16}`。RS485 半双工方向控制：发送前置 TE `modbus.c:1409 RS485_5E_TE();`（UART5=A2B2 外机，同 pattern 见 :212/:687/:1141/:1581），发送完成中断置 RE `PSC330RK-V10-内机/User/src/stm32f4xx_it.c:289 RS485_5E_RE();`；方向宏定义 User/wellthinker/inc/usart.h:14-28 `#define RS485_5E_RE() GPIO_ResetBits(GPIOD,GPIO_Pin_0)` / `RS485_5E_TE() GPIO_SetBits(...)` | 两个坑必须记：(1) 模块链路波特率是 **9600**（sys.c:36 `UART5Init(9600);//A2B2`、modulecontrol.c:892），不是 modbusport.c:144-147 的 115200；(2) `modbusport.c` 那套 `MB_RTU_Init()`/`modbus_handle()`/`RTU_Send()` **既未编进工程也无人调用**（uvprojx 里没有 modbusport.c，main.c:24-28 只跑 SystemUserInit/SystemAction/User），是废弃路径，别拿它当通讯依据。8N1 组帧参数无法从源码核对（USART1Init/UART5Init 实现在预编译 usart.o，.c 不在仓库），故本条按协议层判『符合』 |
| 1.2 WIFI 接口在内机板上 | **[被推翻]** 未实现 | 全仓（330 与 316 两个工程、含 uvprojx 的预编译对象清单）grep `wifi\|esp\|socket\|AT+` 无任何命中；330 六个串口去向全部有归属，无一是 WIFI：A0B0=HMI（modbusport.c:85）、A1B1=送风机变频器（FanControl.c:52『变频器使用的串口是A1B1的Uart4』）、A2B2=外机（modulecontrol.c:892）、A3B3=内部/模块（internal/internal.h:13 `#define internalUARTEN USART2_485EN`）、A4B4 见 stm32f4xx_it.c:340 `RS485_6E_RE()` | 就本板固件而言：零实现、零预留。不能排除 WIFI 由触摸屏/HMI 或外挂模块承担（仓库内无证据，也不在本次审计范围），但规格书这句话写的是『在内机板上』，故按未实现记 ／复核 3/3 推翻 |
| 1.2 可远程开关机及监控（WIFI 那句的后半） | **[被推翻]** 不符合 | 现有『远程』通路只有三条，都不是 WIFI：Switch.c:133-151 HMI(A0B0) `if(UserSwitchSet==0) //本地` / `HMIOnOffCommandSet`；Switch.c:153-167 干接点远程 `if(UserSwitchSet==1) { //远程开关 if(FarSwitch ==0)... }`，FarSwitch=User.h:158 `FarSwitchX02 X02_En //X2 远程开关/外部连锁`（Switch.c:85 `FarSwitch = FarSwitchX02;`）；Switch.c:168-182 PLC `TMOnOffFlag`（User.h:1011 `Parameter[SYSPARA+35] //PLC开关`） | 『可远程开关机及监控』这个能力本身在代码里是成立的（监控=HMI 读 Parameter[] 全表），缺的只是 WIFI 这个载体 ／复核 3/3 推翻 |
| 1.2 室内机包含：新风阀 | 符合 | PSC330RK-V10-内机/User/UserHead/User.h:185 `#define FreshValveY14 Y14 //Y14 新风阀`；User.h:195 `#define FreshValveAO3 DA[3] //DA3 新风阀`；User.h:653 `#define FreshValveModeSet Parameter[INDOOR_SPEC_BASE+20] //新风阀控制方式`；User.h:715 `FreshValveCtrlBasisSet`（0-10V 调节依据）、User.h:718 `FreshValveManualVoltSet` | 开关+0-10V+混风联动都齐，与 §3 参数表一致 |
| 1.2 室内机包含：回风阀 | 符合 | User.h:180 `#define ReturnValveY07 Y07 //Y7 回风阀`；User.h:655 `#define ReturnValveModeSet Parameter[INDOOR_SPEC_BASE+22] //回风阀控制方式`；User.h:656 `#define ReturnValveManualVoltSet Parameter[449]  //0-10V manual return valve command, 0.1V` | User.h:196 `ReturnFanAO4 DA[4] //DA4 回风阀/回风机选择` —— 回风阀与回风机共用一路 AO，是代码自定的复用，规格书未规定 |
| 1.2 室内机包含：送风阀 | 符合 | User.h:177 `#define SupplyValveY04 Y04 //Y4 送风阀`；User.h:654 `#define SupplyValveModeSet Parameter[INDOOR_SPEC_BASE+21] //送风阀控制方式`；User.h:612 `#define SupplyValveManualVoltSet Parameter[130]  /* 0~100, 0.1V */` | 规格书里送风阀只有『无/开关/0-10V手动』三种，代码一致（无自动分支） |
| 1.2 室内机包含：预热装置 | 符合 | User.h:193 `#define PreheatAO1 DA[1] //DA1 新风预热`；User.h:186-188 `PreHeat1Y15 Y15 //Y15 新风预热1` / `PreHeat2Y16` / `PreHeat3Y17`；User.h:651 `#define PreheatModeSet Parameter[INDOOR_SPEC_BASE+18] //新风预热方式`；User.h:741 `#define PreheatActuatorMediumSet Parameter[452]  // 0=electric, 1=steam valve, 2=hot-water valve` | 1 路 AO + 3 路开关量与规格书预热方式的『0-10VDC / 1:2 / 1:2:4 / 1:1:1（可控硅）』一一对应 |
| 1.2 室内机包含：蒸发器 | 符合 | User.h:137-139 `AI[5]~AI[16] are NTC0~NTC11: ... NTC4~NTC11 A-1/A-2/B-1/B-2/C-1/C-2/D-1/D-2 evaporating temperatures.`；User.h:150-151 `#define CompEva1_4 AI[9] //A-1系统蒸发器温度NTC4` / `CompEva1_5 AI[10]`；UserAction.c:2016-2023 `CompEva14Disp = AI[9]; CompEva15Disp = AI[10]; ... EvapTempD2Disp = AI[16];` | 8 路蒸发温度 = 4 外机 × 2 系统，与『最多4台外机、每台1~2系统』自洽。蒸发器本体是机械件，代码里只有其温度测点 |
| 1.2 室内机包含：送风机 | 符合 | User.h:623 `#define SupplyFanType Parameter[307] //送风机类型 0定频 1 0-10V 2变频 3 WT600`；User.h:199 `#define SupplyFanAO7 DA[7] //DA7 送风机/送风机变频给定`；User.h:647 `#define SupplyFanModeSet Parameter[INDOOR_SPEC_BASE+14] //送风机控制方式`；User.h:614-616 `SupplyFanManualVoltSet/SupplyFanSensorTypeSet`；变频器走 A1B1：FanControl.c:46-52 注释『蓝海华腾变频器 波特率 9600 站点:1 协议:1-8-1-N』 | 送风机既有 DO/0-10V 也有 Modbus 变频器通路（UART4），规格书只写开关/手动0-10V/自动0-10V，变频器挂网属代码自定的额外实现 |
| 1.2 室内机包含：加热装置 | 符合 | User.h:173-175 `#define Heat1Y00 Y00 //Y0 电加热1` / `Heat2Y01` / `Heat3Y02`；User.h:192 `#define ReheatAO0 DA[0] //DA0 再热/电加热1`；User.h:201 `#define HeatAO9 DA[9] //DA9 电加热3/可控硅3` | 3 路开关量 + 2 路 AO（DA0/DA9），对应规格书『1:1:1（可控硅）』的 1 路模拟量 + 2 路开关量组合 |
| 1.2 室内机包含：加湿器 | 符合 | User.h:184 `#define HumidValveY13 Y13 //Y13 加湿阀`；User.h:194 `#define HumidAO2 DA[2] //DA2 加湿器`；User.h:652 `#define HumidifyModeSet Parameter[INDOOR_SPEC_BASE+19] //加湿控制方式`；User.h:164 `#define HumidifierFaultX10 X10_En //X10 加湿器故障` | 『无/0-10VDC/开关』三选对应 AO2 + Y13 两条通路 |
| 1.2 室内机包含：制冷电子膨胀阀 | 符合 | User.h:220-235 `#define EEVPARAMETER 10` 下挂 15 个 EEV 参数（MotorTotalPulseSet/EEVCloseMinSizeSet/EEVOpenMaxSizeSet/EEVTempDev1..3Set/EEVAdjustTimeSet1..3/EEVInitialSizeDlySet/GetTempTimeSet/DefrostEXVOpenSizeSet 等）；每扫描执行 sys.c:44-52 `EEV_Priority(); MT1Output(); ... MT8Output();`（8 路阀电机）；PID 实现在 eevCTRL.c（`EEVSpecChannelMap()` 把通道映射到 module/comp）；驱动源在 User/EEV/THB6128.c、User/EEV/eev.c，工程以预编译 Obj/eev.o 链接 | 8 路 EEV = 4 外机 × 2 系统，本板直接驱动阀电机，不经 316 |
| 1.2 室内机包含：压差开关 | 符合 | User.h:168-169 `#define ReturnPressureX14 X14_En //X14 回风压差开关` / `#define SupplyPressureX15 X15_En //X15 送风压差开关`；User.h:742-743 `#define SupplyPressureSwitchSet Parameter[453]  // 0=not installed, 1=installed` / `ReturnPressureSwitchSet Parameter[454]`；另有过滤网压差 User.h:161 `#define FilterBlocking X05_En //X5 过滤网压差开关`，判定见 ErrorCheck.c:431-441（15s、只报警不停机） | 装了才判：SupplyPressureSwitchSet/ReturnPressureSwitchSet 是『未装/装』开关，属规格书未细化、代码自定的保护 |
| 1.2 室内机包含：温湿度传感器 | 符合 | UserAction.c:1992 `RoomPressureDisp = PressSensorAI0Disp;`；UserAction.c:1994 `HumiditySensorAI1Disp = IndoorScaleAiChannel(1, AI[1]);`（房间湿度）；UserAction.c:2011 `RoomTempAI11Disp = AI[5];`（NTC0 房间/回风温度）；User.h:135-139 固定映射注释 `AI0 room pressure, AI1 room humidity, AI2 CO2, AI3 supply pressure/speed, AI4 fresh-air humidity. AI[5]~AI[16] are NTC0~NTC11` | 温+湿两路都有；每路可经 `IndoorScaleAiChannel()` 按各自的 AI 配置块换算（AI 配置块基址见 User.h INDOOR_AI_CFG_BASE 注释），换算法属代码自定 |
| 1.2 室内机包含：排风阀 | 符合 | User.h:182 `#define ExhaustValveY11 Y11 //Y11 排风阀`；User.h:198 `#define ExhaustValveAO6 DA[6] //DA6 排风阀`；User.h:657 `#define ExhaustValveModeSet Parameter[INDOOR_SPEC_BASE+23] //排风阀控制方式`；User.h:617 `#define ExhaustValveDelaySet Parameter[135]  /* 0~90s */`（对应规格书『需有相对排风机开关对应延时，无源接点』） | 开关（带延时）+ 0-10V 两条通路都在 |
| 1.2 室内机包含：排风机 | 符合 | User.h:181 `#define ExhaustFanY10 Y10 //Y10 排风机`；User.h:649 `#define ExhaustFanModeSet Parameter[INDOOR_SPEC_BASE+16] //排风机控制方式`；User.h:646 `#define ExhaustFanDelaySet Parameter[INDOOR_SPEC_BASE+13] //排风机延时s`；故障输入 User.h:165 `ExhaustFanOverX11 X11_En //X11 排风机过载` | 规格书里排风机只有『无/开关（需相对送风机延时，无源接点）』，代码一致，无 AO |
| 1.2 室内机包含：消毒装置 | 符合 | User.h:183 `#define DisinfectY12 Y12 //Y12 消毒/排毒杀菌装置`；User.h:641 `#define DisinfectRunTimeSet Parameter[INDOOR_SPEC_BASE+8] //消毒运行时间 min`；User.h:1116-1117 `DisinfectStepDisp` / `DisinfectRunCountDisp` 诊断量；时序在 UserAction.c 的 `IndoorDisinfectExhaustAction()`（§3.4/§3.2 已实测） | 无 |
| 1.2 室外机包含：压缩机 | 符合 | 回读：modulecontrol.c:1578-1579 `CompStatus[0]`/`CompStatus[1]`（压缩机1/2状态），语义 User.h 侧注释与 module.h:62 `int16_t CompStatus[4]; //压缩机状态 停止 故障 制冷 制热 除霜 禁用`；下发：modulecontrol.c:1136-1150 `CompControl[0]/[1]` → ModComp1/2OnOFFFalg，随 14 字块 FC16 写 316 地址 PLCctrlPara=1400；台数管理 UserAction.c:2446-2461 `LoadOutput` 统计运行台数、`MaxUseCompNum=(ModuleNumSet-1)*2`；轮换在 CompAction.c | 使能位语义做了一次取反映射：modulecontrol.c:1170-1172 `/* PSC330 CompUse: 1 available, 0 disabled. PSC316 CompEnable: 0 enabled, 1 disabled. */ Comp1Enable = Parameter[CompUse+(ModuleNum-1)*2+0] ? 0 : 1;`，属代码对两侧语义差的补偿，规格书未规定 |
| 1.2 室外机包含：四通阀 | **[被推翻]** 部分符合 | 330 侧只有 User.h:311 `#define FourValveSet Parameter[MAKER+0]`（MAKER=78，User.h:310），且 UserAction.c:1969 无条件 `FourValveSet =1;//双系统`——330 不驱动四通阀，也无可写参数入口；316 侧四通阀相关量是它自己的 Parameter：PSC316RK-V12-外机/PSC316RK-V12_Modbus寄存器地址映射表.md:185-189 `\| 193 \| 4x 194 \| FourWayValveDelaySet \| 四通阀切换延时 \| RW \| 30s \| 20~120 \|`，而地址 165..206 正是 330 的 316 设置镜像窗口（User.h:1596-1597 `#define PSC316_SET_BASE 165` / `#define PSC316_SET_NUM 42`），HMI 可经 modbus.c:578 `PSC316Gateway_HMIWriteSpecPair(...)` / :608 `PSC316Gateway_HMIWrite(Addr+i, ModDat)` 改写并写回 316 | 『室外机包含四通阀』成立，但 330 只做透传：四通阀的动作逻辑在 316 的化霜/模式状态机里，330 侧没有可核的控制代码。这条不算缺陷，但和『压缩机』那种 330 主动下发的程度差一档，故记部分符合 ／复核 2/3 推翻 |
| 1.2 室外机包含：轴流风机 | 符合 | 330 侧无风机命令，只有类型转发：modbus.c:566-578 `OutdoorUnitTypeValue` / `OutdoorFanTypeValue` → `PSC316Gateway_HMIWriteSpecPair(Addr, OutdoorUnitTypeValue, OutdoorFanTypeValue)`；回读侧 316 的风机档位/转速量在 330 的 UnitReadStatus 中没有对应字段（modulecontrol.c:1578-1599 解析的 40 字块里没有风机项，VVF[] 在 :1602-1618 被全部填 0） | 对应规格书『1~2 个轴流风机』的计数与型式在 316 侧定义（PSC316RK-V12-外机/WELLTHINKER/User/User.h:438 `0=1fan-2spd,1=2fan-1spd,2=1fan-VFD,3=1fix+1-2spd,4=1fix+1VFD`）。且与本项目既有结论一致：外机变频风机走 0-10V，无 485（见 memory『外机变频风机用0-10V,无485』） |
| 1.2 室外机包含：制热电子膨胀阀 | 符合 | 330 侧定义独立的手动/同步窗口：modulecontrol.c:9-10 `#define PSC316_EEV_MANUAL_BASE 155` / `#define PSC316_EEV_MANUAL_WORDS 4`；写回镜像 modulecontrol.c:760-771 `else if((PSC316WriteJob.addr >= PSC316_EEV_MANUAL_BASE) && ...) { offset = ...; UnitWriteStatus[unitNo].ELEMODSET1[offset] = value[0]; ... }`；回读阀位 modulecontrol.c:1592-1595 `BII[5] ... //电机1目前行动的步数` / `BII[6]`/`BII[7]`/`BII[8]`（电机1~4）；相关量 `ConHeatValveEEV1..4`（User.h:1424-1427）、过热度 `BII[0]/BII[1]`（modulecontrol.c:1591-1592 上方两行） | 制热 EEV 在 316 本体上，330 提供手动设定 + 步数回读 + 过热度回读。与室内 EEV（330 直接驱动 THB6128）是两套独立机构，符合规格书『室内=制冷EEV / 室外=制热EEV』的划分 |
| 1.2 室外机包含：压力传感器 | 符合 | modulecontrol.c:1596-1599 `AII[0] = ...//1#高压` / `AII[1] ...//1#低压` / `AII[2] ...//2#高压` / `AII[3] ...//2#低压`（每台外机 2 个系统的双压力）；压力标定窗口 User.h:1624-1626 `#define PSC316_PRESSURE_CAL_HMI_BASE 2654` / `PSC316_PRESSURE_CAL_WORDS 4` / `..._END 2670`；本板侧压力保护阈值 User.h:248-249 `PressureLow Parameter[PROTECT+8]` / `PressureHigh Parameter[PROTECT+9]` | 压力传感器本体在 316，330 做镜像 + HMI 标定透传 |
| 1.2 室外机包含：排气温度传感器 | 符合 | modulecontrol.c:1586-1587 `AII[7] = ...//排气温度1` / `AII[8] = ...//排气温度2`（每台外机 2 个系统各一路）；配套的排气过温保护阈值 User.h:243 `#define ExhaustHightTempSet Parameter[PROTECT+3] /*-*///排气温度上限 110（100～125）`；同为镜像的还有 AII[5]/[6] 翅片温度、AII[9]/[10] 吸气温度、AII[11]/[12] 阀后温度（modulecontrol.c:1583-1588） | 『等』字覆盖的其余已镜像量（蒸发温度 `BII[2]/BII[3]`、主路过热度 `BII[0]/BII[1]`、除霜条件 `CanDefrost[0]/[1]`、压机累计运行秒数 `CompTotalRunSeconds`）在 modulecontrol.c:1575-1601 一并解析，此处不单列条目 |

## §2 输入输出点

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 2.输入输出点 —— 见"风冷定频直膨机内外机点位表" | 无法核对 | 仓库根 d:\Companty\WEll thinker\F\code 下不存在名为「风冷定频直膨机内外机点位表」的原件；只有派生件：PSC330_PSC316_IO点位表_20260907.csv/.xlsx、PSC330_PSC316_IO点位导出_20260907.zip、PSC330_PSC316_IO点位说明_20260907.md、PSC330_PSC316内外机完整IO与通讯点位表.md（20260917） | 这是本节点最大的核对障碍：规格书正文没有任何IO条目，全部指向一份不在仓库的原件。以下所有条目实际核对的是「仓库内派生点表 vs 代码」，不是「规格书 vs 代码」。派生点表本身也可能是错的（本次已发现3处：316消抖时间、DA0/DA1/DA9可控硅复用说明、[507]兼容显示） ／复核 0/3 推翻 |
| 点表文件本身必须可读才能核对 | **[被推翻]** 部分符合 | head -c 16 "PSC330_PSC316_IO点位表_20260907.csv" = \0\0[\0\345\213\207...（OLE 复合文档头，DLP 加密）；同名 .xlsx 同样加密。未加密副本在 PSC330_PSC316_IO点位导出_20260907.zip 内（PK 头），解出后为 UTF-8 BOM 的 89 行 CSV | 任务提示里给的 csv/xlsx 路径直读会拿到加密流。本次核对用的是 zip 内的 csv（89 行数据）+ 已解密的 .md。这属于工具链已知坑（.csv 会被 DLP 加密），不是代码问题 ／复核 3/3 推翻 |
| PSC330：14 DI、16 DO、5 AI、12 NTC、10 AO，共57个物理点 | 符合 | PSC330RK-V10-内机\User\wellthinker\inc\io.h:6 `#define TOL_OUTPUT_CHANEL 16`；io.h:7 `#define TOL_INPUT_CHANEL 14`；\User\wellthinker\inc\dac.h:5,8 `#define DA_CHANEL 10` / `DA[DA_CHANEL]`；\User\wellthinker\inc\adc.h:24 `extern volatile uint16_t AI[18]`（0~4=AI，5~16=12路NTC，17未用） | 14+16+5+12+10=57，与点表自述一致 |
| 总表：89行，其中87个物理点、2个逻辑预留点 | 符合 | zip 内 csv 数据行 89 行 = 16(330 DI，含2个逻辑) + 16(330 DO) + 5(AI) + 12(NTC) + 10(AO) + 30(316) ；物理点 89-2=87 | 算术自洽 |
| 点表说明："当前工程没有IO底层驱动实现源码，无法可靠导出MCU GPIO管脚；本表以板上X/Y/AI/AO/NTC端子为物理点位" | 符合 | d:\Companty\WEll thinker\F\code\PSC330RK-V10-内机\User\wellthinker\inc\io.h:76-79 只有声明 `void I0Init(void); void WriteOutput(void); void ReadInput(void); void SW_Read(void);`；全仓 find -iname io.c 无结果；仅 Project/Obj/CentralizedControlV10-20230721.lnp 里有 ".\obj\io.o" | → **GPIO 管脚级映射无法核对**（源缺失，io.o 是旧2023构建的二进制）。代码侧只能核到「逻辑通道号 ↔ 语义」；点表自己也只承诺到端子级，二者口径一致，所以这条不算缺陷，但审计边界必须写清 |
| PSC330 DO 通道数 = 16 | 符合 | io.h:6 `TOL_OUTPUT_CHANEL 16`；io.h:24-40 `#define Y00 output[0]` … `#define Y17 output[15]`，无 Y20/Y21 | 点位表点名列是 Y00~Y07 + Y10~Y17，与代码 output[0..15] 的 8+8 分组完全一致 |
| PSC330 DI 通道数 = 14 | 符合 | io.h:7 `TOL_INPUT_CHANEL 14`；io.h:42-56 `#define X00 input[0]` … `#define X15 input[13]`，无 X16/X17 的 input[] 项 | X16/X17 只有 *_En 变量和显示位，没有 input[] 项 —— 与点表"逻辑预留"定性一致 |
| PSC330 AO 通道数 = 10 | 符合 | dac.h:5 `#define DA_CHANEL 10`；User.h:192-201 `ReheatAO0 DA[0]` … `HeatAO9 DA[9]` | EEV 的 THB6128 步进/PWM（User\EEV\THB6128.c:4 `DA0[2]`）是另一个同名数组，非现场0-10V AO，不冲突 |
| PSC330 AI 通道数 = 5（AI0~AI4） | 符合 | User.h:565-569 `#define INDOOR_AI_CFG_BASE 1229` / `INDOOR_AI_CFG_CHANNELS 5`；UserAction.c:1990-2003 `IndoorScaleAiChannel(0..4, AI[0..4])` 共5路 | adc.h 的 AI[18] 是采样数组，前5项才是现场AI通道 |
| PSC330 NTC 通道数 = 12（NTC0~NTC11） | 符合 | UserAction.c:2005-2016 把 AI[5]~AI[16] 共12项搬到显示位；User.h:115,126 `UserA5ValueSet Parameter[495]` … `UserA16ValueSet Parameter[484]` | NTCn = AI[n+5]，n=0..11；AI[17] 未使用 |
| PSC330 通道数之外还有 SW1~SW8 | 符合 | io.h:8-10 `SW_CHANEL 8` / `sw[8]` / `#define SW1 sw[0]`；User\UserSrc\user.c:1986-1987 `/* DIP SW1~SW4 real-time */ { unsigned char dip=(SW1?0:1); ... Parameter[502]=dip; }` | **规格书/点表没规定、代码自行决定**：SW1~SW4 是板载地址拨码（不是现场IO），SW5~SW8 定义了但全程未使用。点表不列举它们是合理的，不算漏项 |
| PSC330 DI X00 = 电源/相序保护，状态1350，极性155 | 符合 | User.h:156 `#define PowerPhaseErrX0 X00_En //X0 电源/相序`；User.h:1163 `X00InputDisp Parameter[1350]`；User.h:394 `InPutStatusSet8 Parameter[155]`；UserAction.c:2050 `X00InputDisp = X00_En;` |  |
| PSC330 DI X01 = 消防报警，状态1351，极性156 | 符合 | User.h:157 `#define FireAlarmX1 X01_En //X1 消防`；User.h:1164 `X01InputDisp Parameter[1351]`；User.h:395 `InPutStatusSet9 Parameter[156]` |  |
| PSC330 DI X02 = 远程启停/外部连锁输入，状态1352，极性157 | 符合 | User.h:158 `#define FarSwitchX02 X02_En //X2 远程启停/外部连锁`；User.h:1165 `X02InputDisp Parameter[1352]`；User.h:396 `InPutStatusSet10 Parameter[157]`；UserAction.c:2049 `FarSwitchX02Dis = FarSwitchX02;` |  |
| PSC330 DI X03 = 送风机/泵过载，状态1353，极性158 | 符合 | User.h:159 `#define PumpOverX00 X03_En //X3 送风机过载`；User.h:1166 `X03InputDisp Parameter[1353]`；User.h:397 `InPutStatusSet11 Parameter[158]`；UserAction.c:2043 `PumpOverX00Dis = PumpOverX00;` | 代码宏名尾号是 X00 但实指 X03（历史命名残留），点表"程序变量"列照抄成 `X03 / PumpOverX00` —— 语义对得上，但读表人容易误读成 X00 |
| PSC330 DI X04 = 风阀联锁，状态1354，极性159 | 符合 | User.h:160 `#define ValveLinkX4 X04_En`；User.h:1167 `X04InputDisp Parameter[1354]`；User.h:398 `InPutStatusSet12 Parameter[159]`；User.h:1203 `#define X4ValveLinkDisp X04InputDisp` |  |
| PSC330 DI X05 = 过滤器堵塞/过滤器压差，状态1355，极性389 | 符合 | User.h:161 `#define FilterBlocking X05_En //X5 过滤器压差开关`；User.h:1168 `X05InputDisp Parameter[1355]`；User.h:728 `InPutStatusSet13 Parameter[389] // X05 polarity: 0=invert, 1=normal`；UserAction.c:2045 `FilterBlockingDis = FilterBlocking;` | 极性参数从 155~159 跳到 389，点表也照实写了 389，一致 |
| PSC330 DI X06 = 加热、再热或主加热故障，状态1356，极性390 | 符合 | User.h:162 `#define HeatFaultX6 X06_En`；User.h:1169 `X06InputDisp Parameter[1356]`；User.h:729 `InPutStatusSet14 Parameter[390] // X06 polarity`；UserAction.c:2044 `HeaterOverX03Dis = HeatFaultX6;` |  |
| PSC330 DI X07 = 预热故障，状态1357，极性391 | 符合 | User.h:163 `#define PreHeatFaultX7 X07_En`；User.h:1170 `X07InputDisp Parameter[1357]`；User.h:730 `InPutStatusSet15 Parameter[391] // X07 polarity`；User.h:1205 `#define X7PreHeatFaultDisp X07InputDisp` |  |
| PSC330 DI X10 = 加湿器故障，状态1358，极性392 | 符合 | User.h:164 `#define HumidifierFaultX10 X10_En`；User.h:1171 `X10InputDisp Parameter[1358]`；User.h:731 `InPutStatusSet16 Parameter[392] // X10 polarity`；UserAction.c:2047 `HumidifierX03Dis = HumidifierFaultX10;` | 同样是宏名尾号失真（HumidifierX03Dis 实指 X10） |
| PSC330 DI X11 = 排风机过载，状态1359，极性393 | 符合 | User.h:165 `#define ExhaustFanOverX11 X11_En`；User.h:1172 `X11InputDisp Parameter[1359]`；User.h:732 `InPutStatusSet17 Parameter[393] // X11 polarity`；User.h:1206 `#define X11ExhaustFanOverDisp X11InputDisp` |  |
| PSC330 DI X12 = 回风机过载，状态1360，极性394 | 符合 | User.h:166 `#define ReturnFanOverX12 X12_En`；User.h:1173 `X12InputDisp Parameter[1360]`；User.h:733 `InPutStatusSet18 Parameter[394] // X12 polarity`；User.h:1207 `#define X12ReturnFanOverDisp X12InputDisp` |  |
| PSC330 DI X13 = 漏水保护，状态1361，极性395 | 符合 | User.h:167 `#define LeakProtectX13 X13_En`；User.h:1174 `X13InputDisp Parameter[1361]`；User.h:734 `InPutStatusSet19 Parameter[395] // X13 polarity`；User.h:1208 `#define X13LeakProtectDisp X13InputDisp` |  |
| PSC330 DI X14 = 回风压差开关，状态1362，极性396 | 符合 | User.h:168 `#define ReturnPressureX14 X14_En //X14 回风压差开关`；User.h:1175 `X14InputDisp Parameter[1362]`；User.h:735 `InPutStatusSet20 Parameter[396] // X14 polarity` |  |
| PSC330 DI X15 = 送风压差开关，状态1363，极性397 | 符合 | User.h:169 `#define SupplyPressureX15 X15_En //X15 送风压差开关`；User.h:1176 `X15InputDisp Parameter[1363]`；User.h:736 `InPutStatusSet21 Parameter[397] // X15 polarity`；UserAction.c:2048 `ErrorOutY00Dis = SupplyPressureX15;` |  |
| PSC330 DI X16/X17 = 逻辑预留显示位，无物理采样点，地址1364/1365 | 符合 | User.h:1177-1178 `X16InputDisp Parameter[X_DIAG_BASE+14]` / `X17InputDisp` (X_DIAG_BASE=1350, User.h:1162) 即 1364/1365；UserAction.c:2064-2065 `X16InputDisp = 0; X17InputDisp = 0;`；User\UserSrc\user.c:532 `X_16 = 0; X_17 = 0;`；io.h:7 `TOL_INPUT_CHANEL 14` | 两处（显示层/采样层）都把它钉成 0，与点表"逻辑预留"判定一致 |
| PSC330 DI 极性语义："0=反相/常闭断开报警，1=正相/常开闭合报警" | 符合 | user.c:500-505 `X_00 = (InPutStatusSet8)?(X00):(!X00);` … `X_05 = (InPutStatusSet13)?(X05):(!X05);`；user.c:528-531 `X_06 = (InPutStatusSet14)?(X06):(!X06);` … `X_15 = (InPutStatusSet21)?(X15):(!X15);` | 极性参数=1 用原值、=0 取反，与点表描述字面一致 |
| PSC330 DI 消抖时间（点表未写，代码为 3s） | 符合 | user.c:513-515 `if(uc100ms == 0) return; if(X_00&&Cnd_X00){ X00_Cnt++; ... }`；user.c:499-511 `X00_En =(X00_Cnt>=30)?1:0;` … X15_En | **规格书/点表没规定、代码自行决定**：100ms 拍 × 30 = 3s 确认。330 点表没写消抖时间所以不冲突；但 316 点表写了"约300ms"，那是错的，见 316 条目 |
| PSC330 DO Y00 = 再热/电加热1份，状态814 | 符合 | User.h:173 `#define Heat1Y00 Y00 //再热1`；User.h:1067 `#define UserPumpY00Dis Parameter[814]`；UserAction.c:2034 `UserPumpY00Dis=Y00;`；UserAction.c:2812/538 `Heat1Y00 = (lv & 1)?1:0;` / `Heat1Y00 = 0;` | 地址 814 = 基址+0 对应 Y00，命名前缀（UserPump）是历史残留，宏名尾号才是真索引 |
| PSC330 DO Y01 = 再热/电加热2份，状态815 | 符合 | User.h:174 `#define Heat2Y01 Y01 //再热2`；User.h:1068 `FanValveY01Dis Parameter[815]`；UserAction.c:2035 `FanValveY01Dis=Y01;`；UserAction.c:2813 `Heat2Y01 = (lv & 2)?1:0;` |  |
| PSC330 DO Y02 = 再热/电加热4份，状态816 | 符合 | User.h:175 `#define Heat3Y02 Y02 //再热3`；User.h:1069 `HeaterY02Dis Parameter[816]`；UserAction.c:2036 `HeaterY02Dis=Y02;`；UserAction.c:2821 `Heat3Y02 = (lv & 4)?1:0;` |  |
| PSC330 DO Y03 = 送风机/送风输出，状态817 | 符合 | User.h:176 `#define UserPumpY00 Y03 //送风机`；User.h:1070 `HeaterY03Dis Parameter[817]`；UserAction.c:2037 `HeaterY03Dis=Y03;`；UserAction.c:970 `UserPumpY00 = (supplyOn && supplyFanConfigured)?1:0;` |  |
| PSC330 DO Y04 = 送风阀，状态818 | 符合 | User.h:177 `#define SupplyValveY04 Y04 //送风阀`；User.h:1071 `HeaterY04Dis Parameter[818]`；UserAction.c:967 `SupplyValveY04 = (supplyValveOpen && (SupplyValveModeSet == VALVE_OUTPUT_DO))?1:0;` |  |
| PSC330 DO Y05 = 综合报警输出，状态819 | 符合 | User.h:178 `#define ErrorOutY00 Y05 //报警输出`；User.h:1072 `HeaterY05Dis Parameter[819]`；User\UserSrc\DispAction.c:475-476 `if(ErrorOutFlag==1) ErrorOutY00=1; else ErrorOutY00=0;` |  |
| PSC330 DO Y06 = 回风机，状态820 | 符合 | User.h:179 `#define ReturnFanY06 Y06 //回风机`；User.h:1073 `HeaterY06Dis Parameter[820]`；UserAction.c:971 `ReturnFanY06 = (returnOn && returnFanConfigured)?1:0;` |  |
| PSC330 DO Y07 = 回风阀，状态821 | 符合 | User.h:180 `#define ReturnValveY07 Y07 //回风阀`；User.h:1074 `HeaterY07Dis Parameter[821]`；UserAction.c:968 `ReturnValveY07 = (returnValveOpen && (ReturnValveModeSet==VALVE_OUTPUT_DO))?1:0;` |  |
| PSC330 DO Y10 = 排风机，状态1366 | 符合 | User.h:181 `#define ExhaustFanY10 Y10`；User.h:1179-1180 `#define Y_DIAG_BASE 1366` / `Y10OutputDisp Parameter[1366]`；UserAction.c:2066 `Y10OutputDisp = Y10;`；UserAction.c:972 `ExhaustFanY10 = (exhaustOn && (ExhaustFanModeSet==EXHAUST_FAN_DO))?1:0;` |  |
| PSC330 DO Y11 = 排风阀/热回收阀，状态1367 | 符合 | User.h:182 `#define ExhaustValveY11 Y11`；User.h:1181 `Y11OutputDisp Parameter[1367]`；UserAction.c:969 `ExhaustValveY11 = (exhaustValveOpen && (ExhaustValveModeSet==VALVE_OUTPUT_DO))?1:0;` | 点表的"功能随系统配置复用"在代码里体现为 ModeSet 开关（VALVE_OUTPUT_DO），核得上 |
| PSC330 DO Y12 = 消毒/杀菌装置，状态1368 | 符合 | User.h:183 `#define DisinfectY12 Y12 //消毒/杀菌`；User.h:1182 `Y12OutputDisp Parameter[1368]`；UserAction.c:1567 `DisinfectY12 = disinfect ? 1 : 0;` |  |
| PSC330 DO Y13 = 加湿阀/开关型加湿输出，状态1369 | 符合 | User.h:184 `#define HumidValveY13 Y13 //加湿阀`；User.h:1183 `Y13OutputDisp Parameter[1369]`；UserAction.c:532 `HumidValveY13 = 0;`（配 HumidifyModeSet 的 DO 分支） |  |
| PSC330 DO Y14 = 新风阀，状态1370 | 符合 | User.h:185 `#define FreshValveY14 Y14 //新风阀`；User.h:1184 `Y14OutputDisp Parameter[1370]`；UserAction.c:703 `IndoorOutputByMode(FreshValveModeSet, &FreshValveY14, &FreshValveAO3, freshDemand);` |  |
| PSC330 DO Y15 = 新风预热1份，状态1371 | 符合 | User.h:186 `#define PreHeat1Y15 Y15 //新风预热1`；User.h:1185 `Y15OutputDisp Parameter[1371]`；UserAction.c:490 `IndoorSetScrOutputs(preheatDemand, IndoorPreheatScrRegion, &PreheatAO1, &PreHeat1Y15, &PreHeat2Y16);` |  |
| PSC330 DO Y16 = 新风预热2份，状态1372 | 符合 | User.h:187 `#define PreHeat2Y16 Y16 //新风预热2`；User.h:1186 `Y16OutputDisp Parameter[1372]`；UserAction.c:1212 `PreHeat2Y16 = 0;` |  |
| PSC330 DO Y17 = 新风预热4份，状态1373 | 符合 | User.h:188 `#define PreHeat3Y17 Y17 //新风预热3`；User.h:1187 `Y17OutputDisp Parameter[1373]`；UserAction.c:3175 附近 `PreheatModeSet == HEAT_OUTPUT_STAGE_1_2_4` 分支驱动 PreHeat3Y17 |  |
| 点表 DO 列地址分段：Y00~Y07→814~821，Y10~Y17→1366~1373 | 符合 | User.h:1067-1074 `Parameter[814]`~`Parameter[821]`（宏名前缀与Y号错位，但地址=814+index 齐）；User.h:1179-1187 `Y_DIAG_BASE 1366` +0~+7 | 两段各自连续，代码写显示位的地方（UserAction.c:2034-2041、2066-2073）与点表逐行一致 |
| PSC330 DO 备注："分段1:2:4三路组合可形成约0/15/30/45/60/75/90/100%输出" | **[被推翻]** 部分符合 | UserAction.c:2808-2822：STAGE_1_2 走 `Heat1Y00=(lv&1)`、`Heat2Y01=(lv&2)`；STAGE_1_2_4 再加 `Heat3Y02=(lv&4)`；UserAction.c:2582-2587 `IndoorSegLevel()` `maxLevel=(1<<segCount)-1` | 1:2:4 是 0~7 共8级的二进制组合，实际是 n/7 = 0/14.3/28.6/42.9/57.1/71.4/85.7/100%，点表的"0/15/30/45/60/75/90/100"是四舍五入到5%的约数。数值口径略有偏差，量级和级数（8级）一致 ／复核 3/3 推翻 |
| 同一句 1:2:4 备注被复制到了全部16行 DO（含 Y03~Y14 等非分段口） | 符合 | csv 第18~33行（Y00~Y17）的"备注"列文本完全相同；典型如 Y12(消毒装置,第28行)、Y05(综合报警,第23行) 也挂着"分段1:2:4三路组合…" | **规格书没规定、点表自行决定**：这是表格生成时的复制粘贴瑕疵，代码侧无对应问题（Y12/Y05 不参与 1:2:4 解码）。不影响任何IO判定，只是读表人容易被误导 |
| PSC330 AI0 = 房间压差，折算500、控制/显示902、原始值904、配置459、校准499 | 符合 | User.h:852 `PressSensorAI0Disp Parameter[ParameterSaveSize+0]`(500)；User.h:1133 `RoomPressureDisp Parameter[INDOOR_SPEC_DIAG_BASE+30]`(872+30=902)；User.h:1142 `AI0RawDisp …+32`(904)；User.h:101 `A0TypeSet Parameter[ParameterSaveSize-41]`(459)；User.h:110 `UserA0ValueSet Parameter[499]`；UserAction.c:1990 `RoomPressureDisp = PressSensorAI0Disp;` | 点表把 459(AI类型选择) 与派生 .md 把 1229(新AI配置块基址) 都标成"配置地址"，两者其实都对——459 是旧类型选择、1229 是2026-09-13新增的每通道独立配置块（User.h:565-569）。点表口径不唯一，但不是代码问题 |
| PSC330 AI1 = 房间/回风湿度，折算501、原始值905、配置460、受432同步选择 | 符合 | User.h:853 `HumiditySensorAI1Disp Parameter[ParameterSaveSize+1]`(501)；User.h:1143 `AI1RawDisp`(905)；User.h:102 `A1TypeSet`(460)；User.h:821 `TempHumiSelectSet Parameter[432]`；UserAction.c:1992 `HumiditySensorAI1Disp = IndoorScaleAiChannel(1, AI[1]);` |  |
| PSC330 AI2 = CO2浓度，903、原始值906、配置461、默认量程到参数466(5000) | 符合 | User.h:1135 `CO2Disp Parameter[INDOOR_SPEC_DIAG_BASE+31]`(903)；User.h:1144 `AI2RawDisp`(906)；User.h:103 `A2TypeSet`(460+1=461)；User.h:97 `A466ValueSet Parameter[ParameterSaveSize-34]`(466, 默认5000)；UserAction.c:1995 `CO2Disp = IndoorScaleAiChannel(2, AI[2]);` |  |
| PSC330 AI3 = 送风压力/风量风速复用，1125(压力)、1126(风量)、原始值907、配置462 | 符合 | User.h:1367 `MainFanPaDisp Parameter[1125]`、User.h:1368 `MainFanm3hDisp Parameter[1126]`；User.h:1145 `AI3RawDisp`(907)；User.h:104 `A3TypeSet`(462)；UserAction.c:1998-1999 `MainFanPaDisp = IndoorScaleAiChannel(3, AI[3]); MainFanm3hDisp = IndoorScaleAiChannel2(3, AI[3]);` | 同一物理 AI 同时算两个工程量，与点表"复用/联锁说明"一致 |
| PSC330 AI4 = 新风湿度，519、原始值908、配置463、校准457 | 符合 | User.h:867 `#define FreshAirHumidityDisp SysOutTempHumidDisp` → User.h:874 `SysOutTempHumidDisp Parameter[ParameterSaveSize+19]`(519)；User.h:1146 `AI4RawDisp`(908)；User.h:105 `A4TypeSet`(463)；User.h:114 `UserA4ValueSet Parameter[457]` |  |
| PSC330 NTC0/NTC1/NTC2/NTC3 = 房间(511)/送风(510)/新风(512)/预热后(513)，校准495/494/493/492，原始码1374~1377 | 符合 | User.h:857 `SysOutTempAI10Disp …+10`(510)、User.h:858 `RoomTempAI11Disp …+11`(511)、User.h:859/865 `Innercoil1Disp …+12`=FreshAirTempDisp(512)、User.h:860/866 `Innercoil2Disp …+13`=PreheatAfterTempDisp(513)；User.h:115-118 `UserA5ValueSet 495…UserA8ValueSet 492`；User.h:1188-1191 `NTC_RAW_DIAG_BASE 1374` +0~+3；UserAction.c:2005-2008 赋值 | 点表按 NTC 物理序号排（NTC0..NTC3），地址列是 511/510/512/513 —— 非单调但代码逐条对得上，说明点表地址列是真读代码得来的 |
| PSC330 NTC4/NTC5/NTC6/NTC7 = A-1(515)/A-2(516)/B-1(514)/B-2(517)，校准491/490/489/488，原始码1378~1381 | 符合 | User.h:862-863 `CompEva14Disp …+15`(515)、`CompEva15Disp …+16`(516)、User.h:861/868 `Innercoil3Disp …+14`=EvapTempB1Disp(514)、User.h:864/869 `Innercoil6Disp …+17`=EvapTempB2Disp(517)；User.h:119-122 `UserA9ValueSet 491…UserA12ValueSet 488`；UserAction.c:2009-2012 |  |
| PSC330 NTC8/NTC9/NTC10/NTC11 = C-1(520)/C-2(521)/D-1(522)/D-2(523)，校准487/486/485/484，原始码1382~1385 | 符合 | User.h:870-873 `EvapTempC1Disp …+20`(520)、`EvapTempC2Disp …+21`(521)、`EvapTempD1Disp …+22`(522)、`EvapTempD2Disp …+23`(523)；User.h:123-126 `UserA13ValueSet 487…UserA16ValueSet 484`；User.h:1198-1200 `NTC10RawDisp/NTC11RawDisp`(1384/1385)；UserAction.c:2013-2016 | 校准一律 495-n（n 为 NTC 号），点表逐行给出的 495..484 完全吻合 |
| PSC330 AO DA0 = 再热/辅热/主制热或SCR第1组，914，兼容显示506 | 部分符合 | User.h:192 `#define ReheatAO0 DA[0] //DA0 再热/SCR组1`；User.h:1237 `FinalDA0Disp Parameter[914]`；UserAction.c:3198 `FinalDA0Disp = DA[0];`；User.h:854 `DA0ValueDisp Parameter[ParameterSaveSize+6]`(506)；UserAction.c:3015 `DA0ValueDisp = ReheatAO0;` | 主址914与兼容显示506都成立。但点表"复用/联锁说明"写的"1:1:1 SCR的0～40%区段"是旧方案口径——见后面 DA1/DA9 条目 ／复核 1/3 推翻 |
| PSC330 AO DA1 = 预热/共享加热或SCR第2组，915，兼容显示507 | 不符合 | User.h:193 `#define PreheatAO1 DA[1] //DA1 预热`；User.h:1238 `FinalDA1Disp Parameter[915]` —— 但 User.h:855 `DA1ValueDisp Parameter[ParameterSaveSize+7]`(507) 全工程只在 ErrorCheck.c:660 被赋 0（`DA1ValueDisp=0;`），从未等于 DA[1] | 两处不符：(1) 兼容显示507是死地址，永远读不到 DA1 的值（DA0 的 506 则有 UserAction.c:3015 持续刷新）；(2) "SCR第2组"是旧3路AO方案的说法，现方案 DA1 只是预热专用AO（UserAction.c:492 `IndoorSetAO(&PreheatAO1, preheatDemand);`），40~70% 区段实际由开关量 Y01/Y16 承担 ／复核 1/3 推翻 |
| PSC330 AO DA2 = 加湿器模拟量输出，916，配置334 | 符合 | User.h:194 `#define HumidAO2 DA[2] //DA2 加湿器`；User.h:1239 `FinalDA2Disp Parameter[916]`；User.h:652 `HumidifyModeSet Parameter[INDOOR_SPEC_BASE+19]`(315+19=334)；UserAction.c:533 `IndoorSetAO(&HumidAO2, 0);` |  |
| PSC330 AO DA3 = 新风阀模拟量输出，917，配置335 | 符合 | User.h:195 `#define FreshValveAO3 DA[3] //DA3 新风阀`；User.h:1240 `FinalDA3Disp Parameter[917]`；User.h:653 `FreshValveModeSet Parameter[INDOOR_SPEC_BASE+20]`(335)；UserAction.c:703 `IndoorOutputByMode(FreshValveModeSet, &FreshValveY14, &FreshValveAO3, freshDemand);` |  |
| PSC330 AO DA4 = 回风机或回风阀模拟量输出，918，配置330/337/449 | 符合 | User.h:196 `#define ReturnFanAO4 DA[4] //DA4 回风阀/回风机`；User.h:1241 `FinalDA4Disp Parameter[918]`；User.h:648 `ReturnFanModeSet …+15`(330)、User.h:655 `ReturnValveModeSet …+22`(337)、User.h:656 `ReturnValveManualVoltSet Parameter[449]`；User.h:1266 `#define INDOOR_AO_CONFLICT_RETURN_DA4 0x02` | 点表"回风机与回风阀共用DA4，配置必须互斥"在代码里有专门的冲突标志位实现，一致 |
| PSC330 AO DA5 = 混风阀模拟量输出，919，配置"由风阀控制配置决定" | 符合 | User.h:197 `#define MixValveAO5 DA[5] //DA5 混风`；User.h:1242 `FinalDA5Disp Parameter[919]`；User.h:1129 `MixValveDemandDisp Parameter[INDOOR_SPEC_DIAG_BASE+26]` | 混风阀开度=1-新风阀开度（规格书 §3.1）在混风需求处算，没有独占配置寄存器，点表写"由风阀控制配置决定"是准确的 |
| PSC330 AO DA6 = 排风阀模拟量输出，920，配置338 | 符合 | User.h:198 `#define ExhaustValveAO6 DA[6] //DA6 排风阀`；User.h:1243 `FinalDA6Disp Parameter[920]`；User.h:657 `ExhaustValveModeSet Parameter[INDOOR_SPEC_BASE+23]`(338)；User.h:1270 `#define INDOOR_CONFIG_CONFLICT_EXHAUST_FOLLOW 0x20` |  |
| PSC330 AO DA7 = 送风机或送风阀模拟量输出，921；送风机控制值1124 | 符合 | User.h:199 `#define SupplyFanAO7 DA[7] //DA7 送风/送风机`；User.h:1244 `FinalDA7Disp Parameter[921]`；User.h:1366 `#define MainFanSpeedCtrl Parameter[1124]`；User.h:623 `SupplyFanType Parameter[307]`、User.h:647 `SupplyFanModeSet …+14`(329)、User.h:654 `SupplyValveModeSet …+21`(336)、User.h:660-661 `SupplyFanMinVoltSet …+26`(341)/`MaxVoltSet …+27`(342)；User.h:1267 `INDOOR_AO_CONFLICT_SUPPLY_DA7 0x04` |  |
| PSC330 AO DA8 = 冷水阀模拟量输出，922（目标）；925（实际） | 符合 | User.h:200 `#define SteamHotWaterValveAO8 DA[8] //DA8 冷水阀/蒸汽热水阀`；User.h:1245 `FinalDA8Disp Parameter[922]`；User.h:1146 `#define SteamHotWaterValveAO8Disp Parameter[925] //DA8 actual cold-water valve output, 0..1000` | 点表"现场判断优先读取925实际输出"与代码注释口径一致 |
| PSC330 AO DA9 = 加热/SCR第3组，923，配置332/333/450~452 | **[被推翻]** 部分符合 | User.h:201 `#define HeatAO9 DA[9] //DA9 加热3/SCR组3`；User.h:1246 `FinalDA9Disp Parameter[923]`；UserAction.c:451-454 `if((HeatReheatShareSet == 0) && (...)) heatAO = &HeatAO9;`；User.h:739-741 `MainHeatActuatorMediumSet Parameter[450]`/`ReheatActuatorMediumSet 451`/`PreheatActuatorMediumSet 452` | 地址和"共用=否时再热/辅热专用口"都成立；但点表"1:1:1 SCR的70～100%区段；与DA0/DA1成组"是旧3路AO方案的口径，现代码（UserAction.c:399-433）是「1路AO(余数通道)+2路开关量」，DA9 与 DA0 是**二选一**而非成组 ／复核 2/3 推翻 |
| 点表 AO 列"配置/极性地址"整体（DA0→332、450、451；DA1→333、452；DA4→330、337、449；DA6→338；DA7→307、329、336、339~342） | 符合 | User.h:650 `ReheatModeSet …+17`(332)、User.h:651 `PreheatModeSet …+18`(333)；User\UserSrc\user.c:277-280 `Parameter_max[332]=4; … Parameter_max[335]=2;`（0~4 = 无/AO/1:2/1:2:4/SCR）；User.h:770-774 `HEAT_OUTPUT_NONE 0 / AO 1 / STAGE_1_2 2 / STAGE_1_2_4 3 / SCR 4`；UserAction.c:457-460 `IndoorHeatAnalogMode == HEAT_OUTPUT_AO/SCR` | 332/333 就是〖再热方式〗〖预热方式〗，值域 0~4 与 HEAT_OUTPUT_* 枚举一一对应；450~452 是三种执行机构介质。地址全部核得上 |
| 点表 330 AO 复用说明："DA0 = 1:1:1 SCR的0～40%，DA1 = 40～70%，DA9 = 70～100%" | **[被推翻]** 不符合 | UserAction.c:399-433 注释与实现：`一套加热 = 1 路可控硅(模拟量,占40%) + 2 组电加热(开关量,各占30%)`；`region 0：demand 0~400 → 组1=0 组2=0，AO 0~100%(占总0~40%)`；`region 1：400~700 → 组1=1，总输出40%~70%`；`region 2：700~1000 → 组1=组2=1，总输出70%~100%`；UserAction.c:481 `IndoorSetScrOutputs(heatDemand, IndoorHeatScrRegion, heatAO, &Heat1Y00, &Heat2Y01);` | 三段分界的 40%/70% 与代码的 400/700 阈值吻合，这一点对；但"三个区段各由一路AO（DA0/DA1/DA9）承担"不成立——代码是**一路AO(余数通道, DA0 或 DA9) + 两路开关量(Y00/Y01 或 Y15/Y16)**。点表生成于 2026-09-07，代码的 1:1:1 改成 1AO+2DO 是 2026-09-20（UserAction.c:444 注释日期），点表没跟上。这属于「点表 vs 代码」失配，不是规格书问题 ／复核 3/3 推翻 |
| PSC330 AO 单位：0～1000 对应 0～10V（点表"0.01V（显示值/100）"） | 符合 | UserAction.c:373-376 `static void IndoorSetAO(...) { *ao = (uint16_t)IndoorLimitValue(value, 0, 1000); }`；User.h:1146 `//DA8 actual cold-water valve output, 0..1000` | 代码侧量纲上限 1000，与点表一致 |
| 点表 330 所有状态位标"RO（状态监视）" | 符合 | User\modbus\modbus.c:324 `if((addr >= ParameterSaveSize) && (addr < 1300))return 0;`（500~1299 只读，覆盖 814~821）；modbus.c:326 `if((addr >= PLCMonitor) && (addr < PSC316_HMI_BASE))return 0;`（1550~1829 只读）；modbus.c:344 `if(addr < ParameterSaveSize)` … 之后 `return 0`（1300+ 一律只读，覆盖 1350~1385） | 814~821、1350~1385 都在只读区，点表"RO（状态监视）"成立。AI 校准地址（457/496~499）落在 modbus.c:339 `if((addr >= 450) && (addr < ParameterSaveSize))return 0;` —— 普通HMI写不进，点表写"校准RW"只对工程工具成立 |
| 点表 330 AI 类型地址 459~463 标"RW" | 符合 | modbus.c:329 `if((addr >= (ParameterSaveSize - 41)) && (addr <= (ParameterSaveSize - 37)))return 1;`（=459~463，显式放行） | 代码专门为 A0~A4 类型选择开了 HMI 写口，与点表 RW 一致 |
| PSC316：8 DI、8 DO、4压力AI、8 NTC、2 AO，共30个物理点 | 符合 | d:\Companty\WEll thinker\F\code\PSC316RK-V12-外机\SYSTEM\inc\io.h:7-8 `TOL_OUTPUT_CHANEL 8` / `TOL_INPUT_CHANEL 8`；SYSTEM\inc\dac.h:5,8 `#define DA_CHANEL 2`；SYSTEM\inc\adc.h:11 `extern volatile uint16_t AI[14]`（AI[0..3]=4压力, AI[4..5]=硬件兼容空槽, AI[6..13]=8 NTC） | 8+8+4+8+2=30 ✓。AI4/AI5 是ADC数组内不参与业务的槽位，点表备注已说明 |
| PSC316 DI X00~X07 语义（1#高压/1#低压/2#高压/2#低压/1#冷凝风机过载/2#冷凝风机过载/1#压缩机过载/2#压缩机过载） | 符合 | PSC316RK-V12-外机\WELLTHINKER\User\_utf8tmp\User.h:127-134 `Comp1HightOverX06 X00_En` / `Comp1LowX01 X01_En` / `Comp2HightOverX10 X02_En` / `Comp2LowX03 X03_En` / `Fan1OverX12 X04_En` / `Fan2OverX17 X05_En` / `Comp1OverX06 X06_En` / `Comp2OverX07 X07_En`（GBK 原文同处，_utf8tmp 为转码副本） |  |
| PSC316 DO Y00~Y07 语义（1#压缩机/2#压缩机/风机1/风机2/1#四通阀/2#四通阀/曲轴箱加热带/双速风机高速） | 符合 | PSC316RK-V12-外机\WELLTHINKER\User\_utf8tmp\User.h:738-747 `Comp1Y05 Y00` / `Comp2Y06 Y01` / `Fan1Y03 Y02` / `Fan3Y07 Y03` / `Four1Y02 Y04` / `Four2Y03 Y05` / `Under1Y13 Y06` / `FanHighY07 Y07` / `Fan2HighY04=FanHighY07` / `WaHeaterY07=FanHighY07` | 点表备注"Y02/Y03/Y07的具体风机作用随外机环境型号和冷凝风机型号配置变化"，代码里确实有别名叠加（Fan2HighY04/WaHeaterY07 都指向 Y07），一致 |
| PSC316 1470 XY状态字：bit0~7 = X00~X07，bit8~15 = Y00~Y07 | 符合 | WELLTHINKER\User\User.h:1012 `#define XYstatus Parameter[PLCREADPara+50]`，User.h:952 `#define PLCREADPara 1420` → 1470；WELLTHINKER\User\UserAction.c:191-192 `XYstatus = (X00<<0)+(X01<<1)+…+(X07<<7)+(Y00<<8)+(Y01<<9)+…+(Y07<<15);` | 点表"状态字位"列的 bit0~bit15 逐位对上 |
| PSC316 AI/NTC/AO 地址：1443~1446 / 1422,1423,1431~1436 / 1468,1469 | 符合 | WELLTHINKER\User\User.h:978-999 `HighPressDisp1 PLCREADPara+23`(1443)/`LowPressDisp1 +24`(1444)/`HighPressDisp2 +25`(1445)/`LowPressDisp2 +26`(1446)；User.h:955 `EnviTempAI0Disp +2`(1422)、User.h:956 `SlaveTempAI20Disp +3`(1423)、User.h:964-969 `FinTemp1AI5Disp +11`(1431)/`FinTemp2AI7Disp +12`(1432)/`CompExhaustAI12Disp +13`(1433)/`CompExhaustAI13Disp +14`(1434)/`MainTempAI16Disp +15`(1435)/`MainTempAI17Disp +16`(1436)；User.h:1008-1009 `DA0ValueDisp +48`(1468)/`DA1ValueDisp +49`(1469)；UserAction.c:108-186 赋值链 | NTC 的物理序号与 AI[] 下标错位是代码本来的设计（NTCn=AI[n+6]），点表"程序变量"列照实写出 AI[6]~AI[13]，没有强行对齐，说明是读代码生成的 |
| PSC316 外机状态经330网关镜像：1~4号基址 1550/1620/1690/1760，步长70，状态字在 base+50 | 符合 | PSC330RK-V10-内机\User\UserHead\User.h:1578-1584 `#define PLCMonitor 1550` + 注释 `1550-1619: outdoor unit 1 mirror of 1420-1478 / 1620-1689: unit 2 / 1690-1759: unit 3 / 1760-1829: unit 4`；User.h:1599 `#define PSC316_STATUS_BASE 1420`；点表给的 1600/1670/1740/1810 = 1550+50 / 1620+50 / 1690+50 / 1760+50 | 点表与 .md 两处基址写一致（md 第14行也写 1550/1620/1690/1760） |
| PSC316 DI 极性参数 X00~X07 = 2~9，"极性为1时使用原值，为0时逻辑取反" | 符合 | PSC316RK-V12-外机\WELLTHINKER\User\User.h（GBK）`#define InPutStatusSet0 Parameter[SYS+0]`，User.h `#define SYS 2` → 2..9；WELLTHINKER\User\user.c:449-456 `X_00 = (InPutStatusSet0)?(X00):(!X00);` … `X_07 = (InPutStatusSet7)?(X07):(!X07);` |  |
| 点表 316 DI 备注："固件约300ms确认后形成Xxx_En故障输入" | **[被推翻]** 不符合 | PSC316RK-V12-外机\WELLTHINKER\User\user.c:470 `if(uc100ms == 0) return;` → user.c:471-478 `if(X_00&&Cnd_X00){ X00_Cnt++; ... }`（100ms 一拍）；user.c:461-465 `X00_En =(X00_Cnt>=30)?1:0;` → 30×100ms = **3s**；user.c:458 注释 `//延迟3S 防抖动` | 点表写的是 300ms，代码实为 3s，差 10 倍。330 的消抖是同一套（user.c:513 `if(uc100ms == 0) return;` + user.c:499 `X00_En =(X00_Cnt>=30)?1:0;`）。这是点表自身的错，不是规格书问题（规格书 §2 未提时间） ／复核 3/3 推翻 |
| PSC316 DA0/DA1 单位 0~1000 对应 0~10V，且"EEV驱动中的PWM/步进脉冲不属于现场0～10V AO" | 符合 | SYSTEM\inc\dac.h:5-6 `#define DA_CHANEL 2` / `#define PWM_CHANEL 2`（PWM 与 DA 分属两个数组）；WELLTHINKER\User\User.h:122-123 `#define DA0value DA[0]` / `#define DA1value DA[1]` | 代码把 EEV 的 PWM/步进脉冲放在 PWM[] 与 THB6128 驱动里，与 DA[] 分离，点表的排除口径成立 |
| PSC330 显示/镜像层逐通道赋值完整（点表地址不是死地址） | 符合 | UserAction.c:2034-2041（Y00~Y07→814~821）、UserAction.c:2050-2065（X00~X17→1350~1365，X16/X17 恒0）、UserAction.c:2066-2073（Y10~Y17→1366~1373）、UserAction.c:3198-3207（DA0~DA9→914~923）、UserAction.c:1990-2016（AI/NTC→500/501/510-517/519-523）、UserAction.c:3015（DA0→506） | 唯一的死地址是 [507]（DA1 兼容显示），见 DA1 条目 |
| 点表"程序变量"列引用的宏名与代码一致但从字面看会误导 | 符合 | User.h:159 `PumpOverX00 X03_En`、User.h:176 `UserPumpY00 Y03`、User.h:178 `ErrorOutY00 Y05`、User.h:164 `HumidifierFaultX10 X10_En`；点表对应行写 `X03 / PumpOverX00`、`Y03`(功能=送风机)、`Y05`(功能=综合报警)、`X10 / HumidifierFaultX10` | **规格书没规定、代码自行决定**：这些宏名尾部的 X00/Y00 是历史残留（真实索引在右边），点表的功能列填对了、变量列照抄宏名。不算缺陷，但审计/现场排障时按宏名尾号去数点位会数错（例如把 UserPumpY00 当 Y00） |

## §3.1 工作模式（4 种模式的开关定义）

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 机组工作模式：【通风】、【制冷】、【制热】、【消排毒】 | 符合 | UserHead/User.h:486-491 `#define MODE_COOL 0 / MODE_HEAT 1 / MODE_VENT 2 / MODE_DISINFECT 3 / MODE_EXHAUST 4 / MODE_AUTO_DISINFECT 5`；user.c:235 `Parameter_max[236] = 5; // UserWorkModeSetTEMP: 0 cool,1 heat,2 vent,3 disinfect,4 exhaust,5 auto`；UserAction.c:606-613 IndoorNormalizeWorkMode() | 规格书写 4 种模式，代码 6 个枚举：消排毒拆成 3 个值(3/4/5)，与 4) 的「三种类型」对应。归一化把非 0/1 一律映射成 MODE_VENT 给 UserWorkModeSet[1]（UserAction.c:609-611），属代码自行决定（规格书未规定）。 |
| 1)【通风】：室外机…不开启 | 符合 | UserAction.c:3435-3443 `if(UserWorkModeSetTEMP == MODE_VENT){ IndoorClearOutdoorRunCommands(); ... return; }` → UserAction.c:111-123 `UnitWriteStatus[i].CompControl[j]=0; UnitWriteStatus[i].CompVVFfre[j]=0;` + IndoorStopCompressors() | 该分支不检查 SysStep，SysStep==0 也照跑；SysStep 1/3/4/5 在 3411-3421 提前 return，属开关机序列本身。 |
| 1)【通风】：…加湿装置…不开启 | 符合 | UserAction.c:3438 `IndoorStopIndoorLoads();` → 581-585 → 529-534 `IndoorStopHumidifier(){ HumidDemandDisp=0; HumidValveY13=0; IndoorSetAO(&HumidAO2,0); }`；另 2516-2518 HumidtCtrl 的 `humidityModeAllowed` 要求 MODE_COOL/HEAT，通风下必清 | 蒸汽/热水加湿阀 AO8 也一并清（UserAction.c:577 `SteamHotWaterValveAO8 = 0;`）。 |
| 1)【通风】：…加热装置不开启 | 符合 | UserAction.c:559-579 IndoorStopHeatPreheatLoads() 清 Heat1/2/3Y00/01/02、PreHeat1/2/3Y15/16/17、IndoorHeatAnalogDemand、IndoorPreheatAnalogDemand；HeatControl_UP 另有 MODE_VENT 守卫（2701-2706）；PreHeatControl 要求 MODE_HEAT（3118） | 例外：冬季防冻 IndoorEnforceWinterValveMinimum（UserAction.c:1085-1128，由 user.c:2003/2007 每轮无条件调用）在环温<0°C 且 MinValveOpenSet>0 时把再热/预热 AO 抬到最小开度，会开蒸汽/热水阀。属 §3.11.10 保护，与本节字面冲突但不构成缺陷。 |
| 2)【制冷】：分为2种类型 〖恒温制冷〗/〖恒温恒湿制冷〗 | 符合 | UserHead/User.h:607 `#define CoolControlTypeSet Parameter[125] /* 0 constant temperature, 1 constant temperature/humidity */`；user.c:236 `Parameter_max[125]=1; Parameter_min[125]=0; Parameter_def[125]=0;` | 「制冷类型」没有并入工作模式[236]，而是独立参数[125]。规格书未规定该参数名与映射，属代码自行决定。 |
| 〖恒温制冷〗：根据室内温度设定值与当前室内温度检测值控制室外机的开启度 | 符合 | NergryAdjust.c:376-378 `if(mode == MODE_COOL){ err = RealTempDisp - ControlTempDisp; ...}`；474-503 `CompNumSet = target; ... SystemNengry = 1/2`；UserAction.c:3446-3452 MODE_COOL 时调 SysNergryAction_PID()+CompAction() | 定频机「开启度」落成压缩机台数：CompAction.c:361 `UnitWriteStatus[module].CompControl[comp] = 1;`，不是 0-10V 模拟量。规格书用「开启度」一词，未说清台数还是模拟量，此为实现口径。 |
| 〖恒温制冷〗：…控制…再热量；再热是否启用及再热方式可选择 | 符合 | UserAction.c:2663-2669 `if(UserWorkModeSetTEMP == MODE_COOL){ if(ReheatEnableSet){ outputMode = ReheatModeSet; ...} }`；参数 User.h:609 `ReheatEnableSet Parameter[127]`、User.h:650 `ReheatModeSet Parameter[INDOOR_SPEC_BASE+17]`(=Parameter[332]，user.c:277 max=4/min=0)；2748 `error = ControlTempDisp - RealTempDisp` 驱动 PID | «再热是否启用»=否 时 outputMode 停在 HEAT_OUTPUT_NONE，2701 的守卫整段清空并 return，即再热全关，与规格书「选否时再热/辅热方式无效」一致。 |
| 〖恒温制冷〗（全新风时可根据环温控制室外机的开启度） | **[被推翻]** 部分符合 | NergryAdjust.c:276 `if((CompControlModeSet == 1) && IndoorIsFreshAirOnlySystem())` → 292 `IndoorEnvTempEnergyAction();`；192-216 MODE_COOL 分支按 FreshAirTempDisp 与 EnvCoolFullLoadTempSet/EnvCoolUnloadTempSet 给台数 | ①必须先满足 [315] CompControlModeSet==1（环境温度控制），只写「全新风」不切换；②「全新风」判据是代码自定：NergryAdjust.c:31-36 `(ReturnFanModeSet==NONE) && (ReturnValveModeSet==NONE) && (MixAirValveTypeSet==0)`，规格书未给判据。故判部分符合。 ／复核 3/3 推翻 |
| 〖恒温恒湿制冷〗：根据室内温湿度设定值与当前室内温湿度检测值控制室外机的开启度 | 部分符合 | 湿度进入外机 PID 的唯一路径是 NergryAdjust.c:375 `if((HumUseSet == 1) && ... && (HumiditySensorAI1Disp > (HumiditySet + HumidityDiffSet/2))) ys = 20;`，随后进 397-400 分子 `(eCool[0] + ys - eCool[1])*ti*t + ...` | 该行只门控 HumUseSet[248]，完全不看 CoolControlTypeSet[125]。后果：①[125]=1 但 [248]=0 时湿度对压缩机台数毫无影响；②[125]=0（恒温）但 [248]=1 时湿度反而参与，「恒温」语义被破坏。规格书 §3.4.2 原文（scratch_spec.txt:228）写 YS 的触发是「机组有恒湿要求」，用 [248] 近似是代码自行决定。 ／复核 1/3 推翻 |
| 〖恒温恒湿制冷〗：…控制…室内机的再热量和加湿量 | 符合 | 再热与恒温制冷共用同一条通路（UserAction.c:2663-2669 取 outputMode，2833 IndoorApplyHeatAnalogOutputs()）；加湿量 UserAction.c:2507-2575 HumidtCtrl()，2517-2518 要求 `(MODE_COOL && CoolControlTypeSet == 1)`；输出 2560-2574 按 HumidifyModeSet 分 AO/DO | 两类型在再热上无差别，规格书也没要求差别。 |
| 〖恒温恒湿制冷〗：再热及加湿方式可选择 | 符合 | ReheatModeSet=Parameter[332]，user.c:277 `Parameter_max[332]=4; Parameter_min[332]=0;`（无/0-10V/1:2/1:2:4/可控硅）；HumidifyModeSet=Parameter[334]，user.c:279 `Parameter_max[334]=2; Parameter_min[334]=0;`（无/0-10V/开关）；编码 HEAT_OUTPUT_* User.h:770-774，HUMID_OUTPUT_AO/DO/NONE 见 UserAction.c:2560-2574 | 可控硅档（HEAT_OUTPUT_SCR=4）对应规格书「1：1：1（可控硅）」，实现见 UserAction.c:416-433 IndoorSetScrOutputs()，1 路 AO + 2 路开关量。 |
| 3)【制热】：分为2种类型 〖恒温制热〗/〖恒温恒湿制热〗 | 符合 | UserHead/User.h:608 `#define HeatControlTypeSet Parameter[126]`；user.c:237 `Parameter_max[126]=1; Parameter_min[126]=0; Parameter_def[126]=0;` | 制热类型与制冷类型分开各一个参数（[125]/[126]），规格书未规定，属代码自行决定。 |
| 〖恒温制热〗：根据室内温度设定值与当前室内温度检测值控制制热量 | 符合 | 热泵制热：NergryAdjust.c:426 `err = ControlTempDisp - RealTempDisp`，474-503 → CompNumSet；非热泵制热：UserAction.c:2748 `error = ControlTempDisp - RealTempDisp` → 2798 demand → 2800-2834 输出 | 「制热量」在热泵下= 压缩机台数，在非热泵下= 电加热档位（outputMode = HeatModeSet-1）。规格书未规定【室内温度设定值】对应哪个寄存器，代码用 ControlTempDisp=Parameter[SYSPARA+39]（User.h:1015）。 |
| 〖恒温制热〗（全新风时可根据环温控制室外机的开启度） | **[被推翻]** 部分符合 | NergryAdjust.c:217-241 `else if(UserWorkModeSet == MODE_HEAT){ ... IndoorEnvLinearCompTarget(maxComp, unloadTemp-temp, unloadTemp-fullTemp); }`，temp = FreshAirTempDisp（188），入口同 276 | 与第 8 条同一入口，需 CompControlModeSet==1 + 代码自定「全新风」判据；「可」为可选，但代码把它绑死在 [315]=1 上，规格书未写这个前置条件。 ／复核 3/3 推翻 |
| 〖恒温制热〗：制热方式可选择 | 符合 | UserHead/User.h:610 `#define HeatModeSet Parameter[128] /* 0 none, 1 heat pump, 2 AO, 3 1:2, 4 1:2:4, 5 SCR */`；user.c:239 `Parameter_max[128]=5; Parameter_min[128]=0; Parameter_def[128]=1;` | 6 档与规格书「无，热泵制热，0～10VDC，1：2，1：2：4，1：1：1（可控硅）」一一对应；默认值 1（热泵制热）是代码自定。 |
| 当〖制热方式〗为热泵制热时，向室外机输出室外机的开启度 | 符合 | UserAction.c:3446-3452 `if((UserWorkModeSetTEMP == MODE_COOL) \|\| ((UserWorkModeSetTEMP == MODE_HEAT) && (HeatModeSet == 1) && (IndoorHeatingStartStage >= 1))){ SysNergryAction_PID(); CompAction(); } else { IndoorClearOutdoorRunCommands(); }` | 门控多了一层 IndoorHeatingStartStage>=1（3447-3448 判定、3475-3478 自增）。规格书未写这条延时；非热泵制热(HeatModeSet>=2)走 else 清外机指令，符合「只有在热泵制热时才输出外机开启度」。 |
| 〖恒温恒湿制热〗：根据室内温湿度设定值与当前室内温湿度检测值控制制热量和加湿量 | **[被推翻]** 部分符合 | 加湿量已按湿度闭环：UserAction.c:2543-2557 HumidtCtrl 的 PID（error = HumiditySet - HumiditySensorAI1Disp），2518 要求 HeatControlTypeSet==1 才允许；但制热量两条通路 NergryAdjust.c:426-471 与 UserAction.c:2748-2796 都只含温度误差，热侧分子（NergryAdjust.c:445-448）无任何 YS/湿度项 | 规格书要求「按室内温湿度控制制热量」，代码只按温度 —— 这部分是「规格书写了但代码没有」。加湿量部分实现。 ／复核 2/3 推翻 |
| 〖恒温恒湿制热〗：制热方式及加湿方式可选择 | 符合 | 同 HeatModeSet（User.h:610）+ HumidifyModeSet（User.h:652，Parameter[334]）；UserAction.c:2517-2518 `(((UserWorkModeSetTEMP == MODE_COOL) && (CoolControlTypeSet == 1)) \|\| ((UserWorkModeSetTEMP == MODE_HEAT) && (HeatControlTypeSet == 1))) ? 1 : 0` | 湿控使能同时看模式与类型参数，与规格书「恒温恒湿」的语义对齐。 |
| 〖恒温恒湿制热〗…当〖制热方式〗为热泵制热时，向室外机输出室外机的开启度 | 符合 | UserAction.c:3446-3452（与第 16 条同一处；该 if 不含 HeatControlTypeSet 条件，故恒温恒湿制热同样输出外机指令） | 代码里恒温制热与恒温恒湿制热的外机控制路径完全相同（都只看温度），加湿与否不改外机指令；规格书此处未要求湿度参与外机开启度，故不算不符合，但两类型的「外机侧」实际无差别。 |
| 4)【消排毒】：分为三种类型：〖消毒〗〖排毒〗〖自动消排毒〗 | 符合 | UserHead/User.h:489-491 MODE_DISINFECT 3 / MODE_EXHAUST 4 / MODE_AUTO_DISINFECT 5；UserAction.c:1570-1850 IndoorDisinfectExhaustAction()，1638-1644 选定 `phase = 2`（排毒）或 `phase = 1`（消毒）；UserAction.c:3398-3405 三种模式统一提前 return，不进常规制冷制热链 | 规格书这三项后面内容为空（提取丢表/图），只能核到「三种类型存在且可分别选中」。 |
| 〖消毒〗：（规格书原文为空） | **[被推翻]** 无法核对 | scratch_spec.txt:93 `〖消毒〗：` 后无内容（原文档该处为表/图，提取丢失） | 卡在规格书本身，无法判断代码是否一致。代码行为（供参考）：phase 1 → UserAction.c:1746 `IndoorDisinfectOutputs(1, 1, 0, 0, 0);`，1764 disinfect 置 1 → 1567 `DisinfectY12 = disinfect ? 1 : 0;`，送风阀开/回风阀开/排风阀关（1550-1556）。 ／复核 3/3 推翻 |
| 〖排毒〗：（规格书原文为空） | **[被推翻]** 无法核对 | scratch_spec.txt:94 `〖排毒〗：` 后无内容（提取丢图） | 无法核对。代码行为（供参考）：phase 2 → UserAction.c:1752/1783/1809 IndoorDisinfectOutputs(2, ...)，新风阀开/排风阀开（1550-1556），带风机时序 IndoorSpecialFanStartTimeline（1734-1739）。 ／复核 3/3 推翻 |
| 〖自动消排毒〗：（规格书原文为空） | **[被推翻]** 无法核对 | scratch_spec.txt:95 `〖自动消排毒〗：` 后无内容（提取丢图） | 卡在规格书。代码把「自动」实现为「先消毒再排毒」：UserAction.c:1772-1778 `if((UserWorkModeSetTEMP == MODE_AUTO_DISINFECT) && (ExhaustRunTimeSet != 0)){ phase = 2; step = 5; }`；且 1638-1641 若消毒时间为 0 则直接 `phase = 2`。这条「自动=消毒+排毒」是代码自行决定，规格书无据。 ／复核 3/3 推翻 |
| 【消毒模式运行时间】 60（0~999）min，若为0，则没有消毒功能 | 符合 | user.c:268 `Parameter_max[323]=999; Parameter_min[323]=0; Parameter_def[323]=60;`；UserAction.c:1767 `if((DisinfectRunTimeSet == 0) \|\| (runCount >= (unsigned int)DisinfectRunTimeSet * 60))` 到时结束；1608-1614 时间为 0 时回落到 LastNormalModeSet 并清输出 | 「若为0则没有消毒功能」实现为「不执行、退回上次正常模式」；规格书未写退回目标，代码自定为 LastNormalModeSet（1630-1636，越界则降为 MODE_COOL）。 |
| 【排毒模式运行时间】 60（0~999）min，若为0，则没有排毒功能 | 符合 | user.c:269 `Parameter_max[324]=999; Parameter_min[324]=0; Parameter_def[324]=60;`；UserAction.c:1786 `if((ExhaustRunTimeSet == 0) \|\| (runCount >= (unsigned int)ExhaustRunTimeSet * 60))`；1609-1611 为 0 时同样回落 |  |
| 【结束后运行模式选择】 0：停机（默认为0）；1：正常运行 | 符合 | UserAction.c:1833-1847 `if(AfterModeSelectSet){ IndoorSetNormalWorkMode(LastNormalModeSet); HMIOnOffCommandSet=1; SysStatus=NORRUN; } else { HMIOnOffCommandSet=0; SysStatus=NORSTOP; }`；user.c:270 `Parameter_max[325]=1; Parameter_min[325]=0; Parameter_def[325]=0;` | 默认 0（停机）与规格书一致。 |
| （制冷/制热模式可设定是否自动切换） | 符合 | UserHead/User.h:638 `#define AutoCoolHeatEnableSet Parameter[INDOOR_SPEC_BASE+5]`(=Parameter[320])；user.c:265 `Parameter_max[320]=1; Parameter_min[320]=0; Parameter_def[320]=0;`；UserAction.c:1431-1440 IndoorAutoSwitchCanContinue()、1483-1541 切换状态机（QHS 保持后 1531-1541 切模式） | 细则属 §3.5（scratch_spec.txt:291-301），本轮只核「可设定」。另 UserAction.c:1485 `if(CompControlModeSet == 1){...return;}` 不切换，对应 §3.5 标题「环境温度控制加减载时无此模式」。 |
| 备注1：再热：制冷时恒温用或恒湿时再热用 | 符合 | UserAction.c:2663-2669 仅 MODE_COOL 下由 ReheatEnableSet 打开再热；温控与湿控共用同一条再热 PID（2748-2834），故「恒温用」与「恒湿用」都能拿到再热 | 代码没有按 [125] 区分「恒温用」/「恒湿用」，两者行为完全相同。规格书没要求区分，不算缺陷，但也没有实现出「恒湿时再热」（除湿再热）的独立逻辑。 |
| 备注2：辅热：热泵制热时补充加热量 | 符合 | UserAction.c:2671-2693 `else if(UserWorkModeSetTEMP == MODE_HEAT){ if(HeatModeSet == 1){ ... if(ReheatEnableSet && (ReheatModeSet != HEAT_OUTPUT_NONE)){ outputMode = ReheatModeSet; heatAllow = separateStageConflict ? 0 : IndoorHeatPumpFullLoad(); } } }`；2722-2728 化霜时 `demand = 1000` 辅热全开 | IndoorHeatPumpFullLoad()（733+）要求可用压缩机全会开才允许辅热，符合「补充加热量」语义；规格书未写这条门槛，属代码自行决定。 |
| 备注3A：〖制热方式〗非「热泵制热」时，〖再热/辅热方式〗中选项均不指辅热，此时〖再热是否启用〗选「是」时指再热，否则〖再热/辅热方式〗无效 | **[被推翻]** 部分符合 | UserAction.c:2695-2699 `else if((HeatModeSet >= 2) && (HeatModeSet <= 5)){ outputMode = HeatModeSet - 1; heatAllow = 1; }` —— 非热泵制热只认 [128]，ReheatModeSet 确实被忽略（与「不指辅热」一致） | 但 2698 `heatAllow = 1` 未再叠加 ReheatEnableSet 门槛，即「再热是否启用=否」时非热泵制热仍照常加热。规格书这句「否则〖再热/辅热方式〗无效」是讲 ReheatModeSet 无效（代码已做到），并没说要禁掉 [128] 的制热，所以判部分符合而非不符合 —— 语义含糊处不做臆测。 ／复核 2/3 推翻 |
| 备注3B：〖制热方式〗为「热泵制热」时，〖再热/辅热方式〗中选项均指辅热，此时〖再热是否启用〗选「是」时再热/辅热共用且〖再热/辅热方式〗不能为「无」 | 部分符合 | UserAction.c:2688-2693 `if(ReheatEnableSet && (ReheatModeSet != HEAT_OUTPUT_NONE)){ outputMode = ReheatModeSet; ...}` —— 辅热必须 [127]=是，且 [332]≠无 才投（半条满足） | 「不能为无」没有做成参数校验或联动：user.c:277 `Parameter_min[332]=0;`（0=无 仍可写），[127]=1 时允许 [332]=0，代码只在行为上退化为不投辅热。另 UserAction.c:887-891 额外把「共用=否 + 热泵制热 + 再热使能」报成 INDOOR_CONFIG_CONFLICT_HEATPUMP_AUX(0x08)（User.h:1268），备注 3B 里没有这一条 —— 该位只进显示寄存器 IndoorAOConflictDisp[940]（UserAction.c:901），不影响输出，但属代码自加约束。 ／复核 1/3 推翻 |
| 备注4：〖制热与再热/辅热是否共用〗选否时，制热与再热/辅热不能同时选分段电加热器 | 符合 | 配置期报冲突：UserAction.c:892-895 `if((HeatReheatShareSet == 0) && heatUsesStage && reheatUsesStage){ conflict \|= INDOOR_CONFIG_CONFLICT_SEPARATE_STAGE; }`（heatUsesStage/reheatUsesStage 定义在 870-875，把 SCR 也算作分段口）；运行期禁用：2654-2661 separateStageConflict → 2668 / 2692 `heatAllow = separateStageConflict ? 0 : ...` | 规格书只说「不能同时选」，代码做成了「冲突位提示 + 运行期禁再热」。另「共用」还决定再热/辅热走哪一路 AO：UserAction.c:451-454，共用=否 时改走 HeatAO9（DA9），共用=是 走 DA0（ReheatAO0）。规格书未规定硬件口分配，属代码自行决定。 |
| 〖制热与再热/辅热是否共用〗是，否 | 符合 | UserHead/User.h:611 `#define HeatReheatShareSet Parameter[129] /* 0 separate, 1 shared */`；user.c:240 `Parameter_max[129]=1; Parameter_min[129]=0; Parameter_def[129]=1;` | 默认 1（共用）是代码自定，规格书未给默认值。 |
| 1)【通风】（通风模式同样有开机序列，且用独立步号） | 符合 | UserAction.c:1358-1360 `if(mode == MODE_COOL)stepBase = 110; else if(mode == MODE_HEAT)stepBase = 120; else if(mode == MODE_VENT)stepBase = 130;`；分流调用 2301-2312 `if(MODE_COOL)... else if(MODE_HEAT)... else IndoorVentilationStartSequence(...)` | 启动序列的时长/风阀时序属 §3.3，本轮只记录通风有独立步号基址 130（DisinfectStepDisp）。 |
| 1)【通风】（通风模式下送风机初始风量，规格书本节未规定） | 符合 | FanControl.c:438-441 `if((UserWorkModeSetTEMP == MODE_COOL) \|\| (UserWorkModeSetTEMP == MODE_HEAT)) initialDemand = CoolHeatFanVoltSet * 10; else initialDemand = VentilationFanVoltSet * 10;` | 规格书 §3.1 未规定通风风量，代码用独立参数 VentilationFanVoltSet（User.h:648 附近 INDOOR_SPEC_BASE+24）—— 属代码自行决定，不是缺陷。 |

## §3.1 参数设置（15 项 + 2 个时间 + 结束后模式）

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 【路径约定】本节所有 evidence 路径均相对于 d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/ | 符合 | User/UserHead/User.h 与 User/UserSrc/{user.c,UserAction.c,ErrorCheck.c,FanControl.c,NergryAdjust.c} | 读的是工作区当前版本（User.h 1762行 / UserAction.c 含 2026-09-20 改动）。.bak 文件未纳入核对。 |
| 〖再热是否启用〗是，否 | 符合 | User/UserHead/User.h:609 `#define ReheatEnableSet Parameter[127]`；User/UserSrc/user.c:238 `Parameter_max[127]=1; Parameter_min[127]=0; Parameter_def[127]=0;`；消费点 User/UserSrc/UserAction.c:2665 `if(ReheatEnableSet)`（决定 outputMode=ReheatModeSet）、UserAction.c:1504/1507（冷热切换判据）、UserAction.c:872-875、UserAction.c:1106-1107、User/UserSrc/NergryAdjust.c:478 | 两档与规格书一致。默认值规格书未规定，代码取 0（禁用）；下标127落在HMI寄存器窗口125~135内（User.h:606注释）。 |
| 〖再热/辅热方式〗无，0～10VDC，1：2，1：2：4，1：1：1（可控硅） | 符合 | User/UserHead/User.h:650 `#define ReheatModeSet Parameter[INDOOR_SPEC_BASE+17]`（=332）；User/UserSrc/user.c:277 `Parameter_max[332]=4; Parameter_min[332]=0; Parameter_def[332]=0;` → 恰好5档；枚举 User.h:770-774 `HEAT_OUTPUT_NONE=0/AO=1/STAGE_1_2=2/STAGE_1_2_4=3/SCR=4`；消费 UserAction.c:2667/2688-2691、UserAction.c:2811(1:2)、2815-2821(1:2:4)、2823(SCR) | 档位顺序与规格书逐字一致（1=0~10VDC、2=1:2、3=1:2:4、4=可控硅）。规格书未规定默认值，代码取“无”。 |
| 〖再热/辅热方式〗中「1：2」= 两段按 1:2 加权 | 符合 | User/UserSrc/UserAction.c:2582-2601 `IndoorSegLevel(demand,2,&lv)`：lv1 门限 170（=1/3 的中点167）、lv2 门限 500（=2/3 的中点）、lv3 门限 830；两路 DO 为 UserAction.c:2812-2813 `Heat1Y00=(lv&1); Heat2Y01=(lv&2)` | 门限带迟滞（>170升/>0降、>500升/<330降、>830升/<660降），迟滞值是代码自行决定。 |
| 〖再热/辅热方式〗中「1：2：4」= 三路二进制加权 | 符合 | User/UserSrc/UserAction.c:2603-2605 注释 `1:2:4 contactors form binary-weighted levels 0..7`；门限 75/225/375…（每权重单位≈1000/7）；UserAction.c:2818-2821 `Heat1Y00/Heat2Y01/Heat3Y02 = lv&1/&2/&4` | 三路触点分别代表 1/7、2/7、4/7 能力，与“1：2：4”命名吻合。 |
| 〖再热/辅热方式〗中「1：1：1（可控硅）」 | 部分符合 | User/UserSrc/UserAction.c:400-444 注释+实现 `IndoorSetScrOutputs()`：`一套加热 = 1 路可控硅（模拟量，占总能力 40%）+ 2 组电加热（开关量，各占 30%）`；`remainder = demand - 300*group1 - 300*group2; IndoorSetAO(scrAO,(remainder*1000+200)/400);`；region 由 UserAction.c:378-396 `IndoorScrRegion()` 用 400/300、700/600 迟滞给出 | 规格书只写了“1：1：1（可控硅）”四字，未定义通道数/权重/门限（纯文本提取也无图）。代码自行解释为“1路AO+2路DO、40/30/30”，不是三路等分的 1:1:1；门限 400/700 也是代码自定。用户此前已表态这套输出方案要改成“各1路AO+2路开关量(阈值333/666)”但说暂不改码 —— 属“规格书没规定、代码自行决定”，不是硬缺陷，但需与硬件点表对齐后再定。 ／复核 0/3 推翻 |
| 备注3A：〖制热方式〗非“热泵制热”时，〖再热/辅热方式〗中选项均不指辅热 | 符合 | User/UserSrc/UserAction.c:2695-2697 `else if((HeatModeSet >= 2) && (HeatModeSet <= 5)){ outputMode = HeatModeSet - 1; heatAllow = 1; }` —— MODE_HEAT 下 HeatModeSet∈2..5 时完全用 HeatModeSet 派生输出方式，**完全忽略 ReheatModeSet**，即不产生辅热 | 反证也成立：MODE_COOL 分支 UserAction.c:2663-2667 才用 `outputMode = ReheatModeSet`，即非热泵时该参数只作再热。 |
| 备注3A：〖再热是否启用〗选“否”时，〖再热/辅热方式〗无效 | 符合 | User/UserSrc/UserAction.c:2665 `if(ReheatEnableSet){ outputMode = ReheatModeSet; ... }`；UserAction.c:2688 `if(ReheatEnableSet && (ReheatModeSet != HEAT_OUTPUT_NONE))`；UserAction.c:1106 `reheatAOConfigured = (ReheatEnableSet && (ReheatModeSet == HEAT_OUTPUT_AO))` | 所有消费点都以 ReheatEnableSet 为总闸。 |
| 备注3B：〖制热方式〗为“热泵制热”时，〖再热/辅热方式〗中选项均指辅热 | 符合 | User/UserSrc/UserAction.c:2673-2694 `if(HeatModeSet == 1){ heatPumpAux = 1; ... if(ReheatEnableSet && (ReheatModeSet != HEAT_OUTPUT_NONE)){ outputMode = ReheatModeSet; heatAllow = separateStageConflict ? 0 : IndoorHeatPumpFullLoad(); } }` | UserAction.c:2677-2687 的注释记录了 2026-09-20 删掉 `(HeatReheatShareSet != 0)` 的改动，理由与备注3B逐字对应（辅热条件只有三条，不含“共用”）。 |
| 备注3B：再热/辅热共用且〖再热/辅热方式〗不能为“无” | 部分符合 | “不能为无”只在 UserAction.c:887-890 被转成状态位：`if((HeatModeSet == 1) && ReheatEnableSet && ((HeatReheatShareSet == 0) \|\| (ReheatModeSet == HEAT_OUTPUT_NONE))) conflict \|= INDOOR_CONFIG_CONFLICT_HEATPUMP_AUX;`；`IndoorAOConflictDisp = Parameter[940]`（User.h:1254），全工程仅 User.h:1268 定义、UserAction.c:901/ErrorCheck.c:658 写入，**无任何消费点**（grep 无第三处引用） | 即：非法组合只是被“上报”到 Parameter[940]，不阻止用户设置，也不自动改值。运行期靠 UserAction.c:2688 的 `ReheatModeSet != HEAT_OUTPUT_NONE` 守卫自然失效（不加热），但这不是参数校验。 ／复核 0/3 推翻 |
| 备注4：〖制热与再热/辅热是否共用〗选否时，制热与再热/辅热不能同时选分段电加热器 | **[被推翻]** 部分符合 | 判据 User/UserSrc/UserAction.c:2654-2661 `separateStageConflict = (ReheatEnableSet && (HeatReheatShareSet==0) && (HeatModeSet in {3,4,5}) && (ReheatModeSet in {STAGE_1_2, STAGE_1_2_4, SCR}))`；同式在 UserAction.c:892-894 也置 `INDOOR_CONFIG_CONFLICT_SEPARATE_STAGE`；效果 UserAction.c:2668/2692 `heatAllow = separateStageConflict ? 0 : ...` | 实际效果是“热泵旁路下不给辅热/再热能力”，属于运行期豁免；`INDOOR_CONFIG_CONFLICT_SEPARATE_STAGE`（User.h:1269）本身同样零消费点，配置不被拒绝。 ／复核 2/3 推翻 |
| 〖加湿方式〗无，0～10VDC，开关 | 符合 | User/UserHead/User.h:652 `#define HumidifyModeSet Parameter[INDOOR_SPEC_BASE+19]`（=334）；User/UserSrc/user.c:279 `Parameter_max[334]=2; Parameter_min[334]=0; Parameter_def[334]=0;` → 3档；枚举 User.h:775-777 `HUMID_OUTPUT_NONE=0/AO=1/DO=2`；消费 UserAction.c:2560-2568（AO→`HumidAO2`、DO→`HumidValveY13`）、UserAction.c:553、UserAction.c:2520、ErrorCheck.c:323/330 | 档位顺序与规格书一致（无/0-10VDC/开关）。默认值代码取 0（无）。 |
| 〖制热方式〗无，热泵制热，0～10VDC，1：2，1：2：4，1：1：1（可控硅） | 符合 | User/UserHead/User.h:610 `#define HeatModeSet Parameter[128]`；User/UserSrc/user.c:239 `Parameter_max[128]=5; Parameter_min[128]=0; Parameter_def[128]=1;` → 6档；消费 UserAction.c:2673（==1 热泵走辅热）、UserAction.c:2695-2697（2..5 → `outputMode = HeatModeSet - 1`）、UserAction.c:451-453、UserAction.c:870-871 | 档位与规格书逐字一致（0无/1热泵/2=0~10VDC/3=1:2/4=1:2:4/5=可控硅）。默认值规格书未规定，代码取 1（热泵制热）。 |
| 〖制热方式〗=1：1：1（可控硅）的通道占用 | **[被推翻]** 部分符合 | User/UserSrc/UserAction.c:479-481 `IndoorSetScrOutputs(heatDemand, IndoorHeatScrRegion, heatAO, &Heat1Y00, &Heat2Y01)`；预热侧 UserAction.c:486-490 `IndoorSetScrOutputs(preheatDemand, IndoorPreheatScrRegion, &PreheatAO1, &PreHeat1Y15, &PreHeat2Y16)` | 与“〖再热/辅热方式〗1:1:1”同一实现、同一 40/30/30 取舍，规格书未规定通道映射，故非硬缺陷，与上面那条一并定夺。制热占 DA0+Y0+Y1、预热占 DA1+Y15+Y16，互不抢口（UserAction.c:463-472 注释）。 ／复核 2/3 推翻 |
| 〖制热与再热/辅热是否共用〗是，否 | 符合 | User/UserHead/User.h:611 `#define HeatReheatShareSet Parameter[129]`；User/UserSrc/user.c:240 `Parameter_max[129]=1; Parameter_min[129]=0; Parameter_def[129]=1;`；消费 UserAction.c:451-455 `if((HeatReheatShareSet == 0) && (MODE_COOL \|\| (MODE_HEAT && HeatModeSet==1))) heatAO = &HeatAO9;`（共用=否 → 再热/辅热独占 DA9，制热仍走 DA0）、UserAction.c:888、UserAction.c:892、UserAction.c:2656 | 两档一致。默认值规格书未规定，代码取“是”(1)。 |
| 〖新风阀〗无、开关，0～10VDC | 符合 | User/UserHead/User.h:653 `#define FreshValveModeSet Parameter[INDOOR_SPEC_BASE+20]`（=335）；User/UserSrc/user.c:280 `Parameter_max[335]=2; Parameter_min[335]=0; Parameter_def[335]=0;`；枚举 User.h:778-780 `VALVE_OUTPUT_NONE=0/DO=1/AO=2`；消费 UserAction.c:703 `IndoorOutputByMode(FreshValveModeSet,&FreshValveY14,&FreshValveAO3,freshDemand)`、UserAction.c:1046、UserAction.c:1189-1191 | 档位顺序一致。默认值代码取 0（无）。 |
| 〖0-10V新风阀调节依据〗房间压差（需有房间压差设定值(Pa)输入框） | 符合 | User/UserHead/User.h:715 `#define FreshValveCtrlBasisSet Parameter[INDOOR_SPEC_EXT_BASE+3]`（=377）；user.c:322 `Parameter_max[377]=2; Parameter_min[377]=0; Parameter_def[377]=0;`；压差分支 UserAction.c:688-692 `else { if(RoomPressureSet==0)demand=500; else demand=500+(RoomPressureSet-RoomPressureDisp)*10; }`；设定值 User.h:716 `RoomPressureSet=Parameter[378]`，user.c:323 `Parameter_max[378]=500; Parameter_min[378]=-500; Parameter_def[378]=0;` | basis=0 时同样被 ErrorCheck.c:317 认定为 roomPressureNeeded=1（需房间压差探头）。范围 -500~500 Pa 是代码自行决定（规格书只写“Pa”）。 |
| 〖0-10V新风阀调节依据〗CO2浓度及新回风焓差（需有CO2浓度设定值(PPM)输入框） | 符合 | UserAction.c:676-687 `else if(FreshValveCtrlBasisSet == 1){ if(CO2Set<=0)demand=0; else demand=500+(CO2Disp-CO2Set)/2; roomEnthalpy=IndoorAirEnthalpy10(RoomTempAI11Disp,HumiditySensorAI1Disp,&roomValid); freshEnthalpy=IndoorAirEnthalpy10(FreshAirTempDisp,FreshAirHumidityDisp,&freshValid); if(...)demand=500; }`；焓计算 UserAction.c:639-661；设定值 User.h:717 `CO2Set=Parameter[379]`，user.c:324 `max=5000 min=0 def=800` | CO2 与焓差在同一档里叠加（不是二选一），符合规格书把两者写在一起的字面。ErrorCheck.c:315 以 basis==1 认定需要房间湿度/新风温湿度探头。 |
| 〖0-10V新风阀调节依据〗手动（需有电压输入框） | 符合 | UserAction.c:671-675 `if(FreshValveCtrlBasisSet == 2){ manual = FreshValveManualVoltSet*10; demand = IndoorLimitValue(manual,0,1000); }`；User.h:718 `FreshValveManualVoltSet=Parameter[380]`，user.c:325 `Parameter_max[380]=100; Parameter_min[380]=0; Parameter_def[380]=80;` 单位 0.1V | 三档 basis 的编号(0压差/1 CO2焓差/2手动)与规格书列举顺序严格一致。 |
| 〖回风阀〗无，开关，0～10VDC手动 | 符合 | User/UserHead/User.h:655 `#define ReturnValveModeSet Parameter[INDOOR_SPEC_BASE+22]`（=337）；user.c:282 `Parameter_max[337]=2; Parameter_min[337]=0; Parameter_def[337]=0;`；消费 UserAction.c:968 `ReturnValveY07 = (returnValveOpen && (ReturnValveModeSet==VALVE_OUTPUT_DO))`、UserAction.c:1007-1010 `else if(ReturnValveModeSet==VALVE_OUTPUT_AO) IndoorSetAO(&ReturnFanAO4, returnValveOpen ? ReturnValveManualVoltSet*10 : 0)`、UserAction.c:1050/1176 | 档位与规格书一致。 |
| 〖回风阀〗0～10VDC手动（需有电压输入框） | 符合 | User/UserHead/User.h:656 `#define ReturnValveManualVoltSet Parameter[449]`；User/UserSrc/user.c:353 `Parameter_max[449]=100; Parameter_min[449]=0; Parameter_def[449]=0; //Return valve manual voltage, 0.1V`；消费 UserAction.c:1010 | 下标特意放在 449（不在 315~388 连续块里）是代码自行安排；单位 0.1V，范围 0~10.0V。 |
| 〖回风阀〗(此项选择要求无回风机或回风机不要求0-10V) | 符合 | UserAction.c:876-879 `if((ReturnFanModeSet==RETURN_FAN_MANUAL_AO) && (ReturnValveModeSet==VALVE_OUTPUT_AO)) conflict \|= INDOOR_AO_CONFLICT_RETURN_DA4;`；约束行为 UserAction.c:960-966 `if(IndoorAOConflictDisp & INDOOR_AO_CONFLICT_RETURN_DA4){ returnOn=0; ReturnFanY06=0; }`（DA4 让给回风阀，回风机停） | 这条是本工程里**唯一**被真正执行的两条约束之一（另一条是送风阀/送风机 DA7）。规格书这里是“要求”，代码实现了硬互锁。 |
| 〖混风阀〗无，0～10VDC | 符合 | User/UserHead/User.h:719 `#define MixAirValveTypeSet Parameter[INDOOR_SPEC_EXT_BASE+7]`（=381）；User/UserSrc/user.c:326 `Parameter_max[381]=1; Parameter_min[381]=0; Parameter_def[381]=0;` → 恰好两档（无/AO）；消费 UserAction.c:705-713、UserAction.c:226/592/1054/1157、UserAction.c:313、NergryAdjust.c:35 | 两档一致。 |
| 〖混风阀〗开度与新风阀开度之和为1 | 符合 | User/UserSrc/UserAction.c:705-711 `if(MixAirValveTypeSet){ IndoorSetAO(&MixValveAO5, valveOpen ? (1000 - freshDemand) : 0); MixValveDemandDisp = MixValveAO5; }`，其中 `freshDemand` 即新风阀开度（同函数 UserAction.c:703 刚写入 FreshValveAO3/FreshValveY14） | 严格互补。规格书只说“之和为1”，未规定关闭态如何处理，代码选“混风阀关到0、不保持互补”。 |
| 〖混风阀〗(此项选择要求新风阀根据CO2浓度及新回风焓差控制) | 未实现 | 唯一的关联校验是 UserAction.c:1157-1158 `mixValveValid = ((MixAirValveTypeSet == 1) && (FreshValveModeSet == VALVE_OUTPUT_AO)) ? 1 : 0;` —— 只要求新风阀是 AO，**未要求 FreshValveCtrlBasisSet == 1**；全工程 grep `MixAirValveTypeSet` 无第二处做 basis 校验（UserAction.c:313/226/592/705/1054） | 即该前置条件既不被拒绝也不被上报（Parameter[940] 的 conflict 位里没有对应项，User.h:1264-1271）。 ／复核 1/3 推翻 |
| 〖排风阀〗无，开关，0～10VDC | 符合 | User/UserHead/User.h:657 `#define ExhaustValveModeSet Parameter[INDOOR_SPEC_BASE+23]`（=338）；user.c:283 `Parameter_max[338]=2; Parameter_min[338]=0; Parameter_def[338]=0;`；消费 UserAction.c:969（DO→ExhaustValveY11）、UserAction.c:1016-1019（AO→ExhaustValveAO6）、UserAction.c:1183/1186 | 档位一致。 |
| 〖排风阀〗开关（需有相对排风机开关对应延时，无源接点） | 未实现 | 参数 User/UserHead/User.h:617 `#define ExhaustValveDelaySet Parameter[135]`；默认/范围 User/UserSrc/user.c:246 `Parameter_max[135]=90; Parameter_min[135]=0; Parameter_def[135]=30;` —— 全工程 grep（*.c/*.h）**除 User.h:617 外零命中**，是死参数。实际时序只有固定值：启动时排风阀 DO 在 step1 随所有风阀一起开，UserAction.c:1371-1373 `if(*startSeqCount >= valveDelay){*startSeqStep=2;...}` + UserAction.c:596-600 `IndoorValveOpenDelay()` 固定返回 150 或 10；排风机本身在 step2 才起（UserAction.c:1378-1385） | 有相对先后（阀比风机早 150s 开），但没有任何“可调延时”通道；HMI 上有这个框(135/30s)却不起作用。“无源接点”是硬件属性，代码看不到 —— 那半句无法核对。 ／复核 1/3 推翻 |
| 〖排风阀〗0～10VDC(开度与新风阀开度相等) | 符合 | User/UserSrc/UserAction.c:1016-1019 `if((ExhaustValveModeSet==VALVE_OUTPUT_AO) && (FreshValveModeSet==VALVE_OUTPUT_AO)){ IndoorSetAO(&ExhaustValveAO6, exhaustValveOpen ? freshDemand : 0); }` —— 用的就是同一变量 `freshDemand`，字面相等 | 额外加了“新风阀也必须是AO”的前置条件（UserAction.c:896-900 会置 INDOOR_CONFIG_CONFLICT_EXHAUST_FOLLOW），规格书写在该项括号里正是这个要求，属实现到位。另 UserAction.c:3219-3229 热回收(ReHeatY11Control)会直接抢写 ExhaustValveAO6=1000，与“等于新风阀开度”冲突；但那条受 HeatRecoveryEnableSet 控制且只在 MODE_COOL，属规格书未覆盖的扩展功能。 |
| 〖排风阀〗(此项选择要求新风阀根据CO2浓度及新回风焓差控制) | **[被推翻]** 未实现 | 同混风阀那条：UserAction.c:1159-1160 `exhaustValveAOValid = ((ExhaustValveModeSet==VALVE_OUTPUT_AO) && (FreshValveModeSet==VALVE_OUTPUT_AO)) ? 1 : 0;`，无 FreshValveCtrlBasisSet 校验；ErrorCheck.c:311-313 只把 `ExhaustValveModeSet==VALVE_OUTPUT_AO` 计入 analogAirValveNeeded | 与混风阀同一处缺口，两条可以一起补：把 `(MixAirValveTypeSet\|\|ExhaustValveModeSet==AO) ⇒ FreshValveCtrlBasisSet==1` 加进 IndoorUpdateAOConflict()。 ／复核 3/3 推翻 |
| 〖送风阀〗无，开关，0～10VDC手动（需有电压输入框） | 符合 | User/UserHead/User.h:654 `#define SupplyValveModeSet Parameter[INDOOR_SPEC_BASE+21]`（=336）；user.c:281 `Parameter_max[336]=2; Parameter_min[336]=0; Parameter_def[336]=0;`；消费 UserAction.c:967（DO→SupplyValveY04）、UserAction.c:986-989 `else if(SupplyValveModeSet==VALVE_OUTPUT_AO) IndoorSetAO(&SupplyFanAO7, supplyValveOpen ? SupplyValveManualVoltSet*10 : 0)`；电压框 User.h:612 `SupplyValveManualVoltSet=Parameter[130]`，user.c:241 `max=100 min=0 def=0` | 档位与电压输入框都齐。DA7 与送风机共用，见下一条。 |
| 〖送风阀〗(此项选择要求送风机不要求0-10V) | 符合 | UserAction.c:881-885 `if(((SupplyFanModeSet==SUPPLY_FAN_MANUAL_AO)\|\|(SupplyFanModeSet==SUPPLY_FAN_AUTO_AO)) && (SupplyValveModeSet==VALVE_OUTPUT_AO)) conflict \|= INDOOR_AO_CONFLICT_SUPPLY_DA7;`；约束行为 UserAction.c:953-959 `if(IndoorAOConflictDisp & INDOOR_AO_CONFLICT_SUPPLY_DA7){ supplyOn=0; UserPump=0; UserPumpY00=0; SupplyFanDemandDisp=0; }` | 硬互锁，DA7 让给送风阀、送风机停（ErrorCheck.c:731-736 的 UserPumpY00 输出也做了同样排除）。 |
| 〖预热〗无，0～10VDC，1：2，1：2：4，1：1：1（可控硅） | 符合 | User/UserHead/User.h:651 `#define PreheatModeSet Parameter[INDOOR_SPEC_BASE+18]`（=333）；User/UserSrc/user.c:278 `Parameter_max[333]=4; Parameter_min[333]=0; Parameter_def[333]=0;` → 5档；消费 UserAction.c:3166-3183（1:2 → Y15/Y16；1:2:4 → Y15/Y16/Y17；SCR → 置 region 交 IndoorApplyHeatAnalogOutputs）、UserAction.c:1207-1220、UserAction.c:3115-3116、ErrorCheck.c:334/337 | 档位顺序与再热/辅热方式完全相同，一致。 |
| 〖预热〗另有独立参数 PreheatSegSet（潜在重复/死参数） | **[被推翻]** 未实现 | User/UserHead/User.h:724 `#define PreheatSegSet Parameter[385]  // 预热电加热段数 1=开关 2=1:2 3=1:2:4`；User/UserSrc/user.c:330 `Parameter_max[385]=3; Parameter_min[385]=1; Parameter_def[385]=3;` —— 全工程除这两处外**零引用**（grep 无第三处）；真正生效的是 PreheatModeSet[333] | 与 PreheatModeSet 语义重叠且默认值不同（385 默认 3=1:2:4，333 默认 0=无），若上位机两个寄存器都写会出现“看起来设了却按另一个跑”。属死参数，建议删除或合并。 ／复核 2/3 推翻 |
| 〖送风机〗开关，0～10VDC手动，0～10VDC自动（三项，无“无”） | 符合 | User/UserHead/User.h:647 `#define SupplyFanModeSet Parameter[INDOOR_SPEC_BASE+14]`（=329）；User/UserSrc/user.c:274 `Parameter_max[329]=3; Parameter_min[329]=1; Parameter_def[329]=1;` —— **min=1 恰好排除了“无”**，与规格书只列三项吻合；枚举 User.h:761-764 `SUPPLY_FAN_NONE=0/DO=1/MANUAL_AO=2/AUTO_AO=3`；消费 UserAction.c:907-911、UserAction.c:975-986、UserAction.c:1237-1243、ErrorCheck.c:253/319/731-736、FanControl.c:448 | 默认值 1（开关）是代码自行决定。注意枚举里仍保留了 SUPPLY_FAN_NONE=0 但被 min 挡住，属防御性冗余。 |
| 〖送风机〗0～10VDC手动（需有电压输入框） | 符合 | User.h:616 `#define SupplyFanManualVoltSet Parameter[134]`；user.c:245 `Parameter_max[134]=100; Parameter_min[134]=0; Parameter_def[134]=100;`；消费 UserAction.c:907-910 `if(SupplyFanModeSet == SUPPLY_FAN_MANUAL_AO){ return IndoorLimitValue(SupplyFanManualVoltSet*10,0,1000); }` | 默认 100 = 10.0V，即手动默认满频；规格书未规定默认值。 |
| 〖送风机〗0～10VDC自动（需有风速变送器或风压变送器选择框） | 符合 | User.h:615 `#define SupplyFanSensorTypeSet Parameter[133]`（0 风速/1 风压）；user.c:244 `Parameter_max[133]=1; Parameter_min[133]=0; Parameter_def[133]=0;`；消费 UserAction.c:3395 `WindSet = SupplyFanSensorTypeSet;` → FanControl.c:32-37 `if(WindSet == 1)target = FanSpecAirPressureTarget(); else target = FanSpecAirSpeedTarget();` | 存在两个同义槽位：WindSet=Parameter[308]（User.h:624，注释“风量/风压选择 0风量 1风压”）每扫描被 133 覆盖写（UserAction.c:3395），308 形同虚设。属代码自行决定，建议只留一个。 |
| 〖送风机〗自动-选风速变送器时需有风量设定值（CMH）及风管面积(m2)输入框 | 符合 | User.h:662 `AirVolumeSet=Parameter[INDOOR_SPEC_BASE+28] //10 CMH`、User.h:663 `DuctAreaSet=Parameter[INDOOR_SPEC_BASE+29] //0.01m2`；范围 user.c:288-289 `[343] max=10000 min=0 def=1000`、`[344] max=3000 min=10 def=100`；消费 FanControl.c:17-24 `if((AirVolumeSet>0)&&(DuctAreaSet>0)){ target=(long)AirVolumeSet*100/(36*DuctAreaSet); }` 换算成 0.1m/s | 两个输入框都在且有换算。范围 0~1000.0CMH / 0.1~30.00m2 是代码自行决定（规格书未给量程）。 |
| 〖送风机〗自动-选风压变送器时需有风压设定值(Pa)输入框 | 符合 | User.h:664 `AirPressureSet=Parameter[INDOOR_SPEC_BASE+30] //风压设定 Pa`；user.c:290 `Parameter_max[345]=9999; Parameter_min[345]=0; Parameter_def[345]=5;`；消费 FanControl.c:27-30 `if(AirPressureSet > 0)return FanSpecLimitValue(AirPressureSet,0,9999);` | 默认 5 Pa 偏低但在范围内，规格书未规定默认值。 |
| 〖回风机〗无，开关，0～10VDC手动（需有电压输入框） | 符合 | User.h:648 `#define ReturnFanModeSet Parameter[INDOOR_SPEC_BASE+15]`（=330）；user.c:275 `Parameter_max[330]=2; Parameter_min[330]=0; Parameter_def[330]=0;`；消费 UserAction.c:1079、UserAction.c:949-950、UserAction.c:1002-1010、UserAction.c:1252-1258；电压框 User.h:613 `ReturnFanManualVoltSet=Parameter[131]`，user.c:242 `max=100 min=0 def=0` | 档位与电压框齐。枚举 RETURN_FAN_NONE=0/DO=1/MANUAL_AO=2（User.h:765-767）。 |
| 〖回风机〗开关（需有相对送风机开关对应延时，无源接点） | 符合 | 参数 User.h:614 `#define ReturnFanDelaySet Parameter[132]`，user.c:243 `Parameter_max[132]=90; Parameter_min[132]=0; Parameter_def[132]=30;`；启动消费 UserAction.c:158 `returnDelay = (ReturnFanDelaySet<0)?0:(unsigned int)ReturnFanDelaySet; if(returnDelay>90)returnDelay=90;` 与 UserAction.c:170-172 `*returnStart = *supplyStart; if(ReturnFanModeSet != OUTPUT_NONE)*returnStart += returnDelay;`；停机逆序 UserAction.c:284-288、UserAction.c:315-324 | 启动是“回风机比送风机晚 N 秒”的正延时（规格书未规定方向，代码自行决定）。停机时反向推导 returnDelay=ReturnStart-SupplyStart，所以同一个参数同时定义了停机的提前量。“无源接点”是硬件属性，代码层无法核对。 |
| 〖回风机〗(此项选择要求送风阀不要求0-10V) | **[被推翻]** 未实现 | 代码里只有“回风机 vs 回风阀”的 DA4 互锁（UserAction.c:876-879、UserAction.c:960-966），**没有任何 `ReturnFanModeSet==MANUAL_AO && SupplyValveModeSet==VALVE_OUTPUT_AO` 的校验**（grep SupplyValveModeSet 全部命中：UserAction.c:221/588/883/948/967/977/986/1048/1153/1169/1243/1557、ErrorCheck.c:312/736） | 这条括号要求疑似规格书笔误/复制粘贴（回风机与送风阀在点表上不共用通道，User.h:196 DA4=回风阀/回风机、User.h:199 DA7=送风/送风阀）。按字面要求=未实现；按工程意图=无需实现。建议回确认而非直接补码。 ／复核 2/3 推翻 |
| 〖排风机〗无，开关（需有相对送风机开关对应延时，无源接点） | 符合 | User.h:649 `#define ExhaustFanModeSet Parameter[INDOOR_SPEC_BASE+16]`（=331）；user.c:276 `Parameter_max[331]=1; Parameter_min[331]=0; Parameter_def[331]=0;` → 恰好两档（无/开关）；延时参数 User.h:646 `ExhaustFanDelaySet=Parameter[INDOOR_SPEC_BASE+13]`（=328），user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90; Parameter_def[328]=30;`；消费 UserAction.c:141-148 `IndoorExhaustFanSignedDelay()` 并 clamp ±90，再进 UserAction.c:167-169 `*supplyStart=(fanInterval>0)?fanDelay:0; *exhaustStart=(fanInterval<0)?fanDelay:0;`；另 UserAction.c:173-179、UserAction.c:292-313、UserAction.c:1182 | 带符号延时是代码自行决定：正数=排风机先启（送风机后启），负数=送风机先启。“无源接点”是硬件属性，代码层无法核对。 |
| 【消毒模式运行时间】60（0~999）min，若为0，则没有消毒功能 | 符合 | User.h:641 `#define DisinfectRunTimeSet Parameter[INDOOR_SPEC_BASE+8]`（=323）；user.c:268 `Parameter_max[323]=999; Parameter_min[323]=0; Parameter_def[323]=60;` —— 范围与默认值逐字吻合；为0则无功能 UserAction.c:1607-1612 `if(((UserWorkModeSetTEMP==MODE_DISINFECT)&&(DisinfectRunTimeSet==0)) \|\| ... ){ ... IndoorSetNormalWorkMode(lastMode); ... return; }`、UserAction.c:1638-1640、UserAction.c:1764；计时 UserAction.c:1767 `if((DisinfectRunTimeSet==0) \|\| (runCount >= (unsigned int)DisinfectRunTimeSet*60))` | 单位换算正确（min×60 → 秒）。 |
| 【排毒模式运行时间】60（0~999）min，若为0，则没有排毒功能 | 符合 | User.h:642 `#define ExhaustRunTimeSet Parameter[INDOOR_SPEC_BASE+9]`（=324）；user.c:269 `Parameter_max[324]=999; Parameter_min[324]=0; Parameter_def[324]=60;`；为0则无功能 UserAction.c:1609-1611 `((UserWorkModeSetTEMP==MODE_EXHAUST)&&(ExhaustRunTimeSet==0))`、UserAction.c:1611（自动消排毒下两者皆0才退出）、UserAction.c:1771-1773；计时 UserAction.c:1786 `if((ExhaustRunTimeSet==0) \|\| (runCount >= (unsigned int)ExhaustRunTimeSet*60))` | 范围与默认值与规格书逐字吻合。 |
| 【结束后运行模式选择】0：停机（默认为0）；1：正常运行 | 符合 | User.h:643 `#define AfterModeSelectSet Parameter[INDOOR_SPEC_BASE+10]`（=325）；user.c:270 `Parameter_max[325]=1; Parameter_min[325]=0; Parameter_def[325]=0;` —— 默认0=停机，与规格书“默认为0”一致；消费 UserAction.c:1831-1849 `if(AfterModeSelectSet){ IndoorSetNormalWorkMode((unsigned char)LastNormalModeSet); HMIOnOffCommandSet=1; TouchScreenONOFF=1; SysStatusLoader=1; SysStatus=NORRUN; } else { HMIOnOffCommandSet=0; TouchScreenONOFF=0; SysStatusLoader=0; SysStatus=NORSTOP; }` | 1 时恢复到 LastNormalModeSet（=Parameter[326]，User.h:644，user.c:271 max=5）而不是固定制冷/制热，规格书没说恢复到哪个模式，属代码自行决定。 |
| 【全节】上下限越界体检：默认值是否都落在 min..max 内 | 符合 | 逐项核对 user.c:238-246（127/128/129/130/131/132/133/134/135）、user.c:268-283（323~338）、user.c:288-290（343/344/345）、user.c:322-326（377~381）、user.c:330（385）、user.c:353（449），全部满足 min ≤ def ≤ max，未发现越界 | 本节 15 项参数 + 附属输入框共 20 个 Parameter[] 下标，全部有独立宏定义（User.h 单一定义）、独立 min/max/def 行（user.c）。唯一带符号的是 ExhaustFanDelaySet[-90,90] 与 RoomPressureSet[-500,500]，都合法。 |

## §3.1 备注 1~4

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 备注1：再热：制冷时恒温用 | 符合 | PSC330RK-V10-内机/User/UserSrc/UserAction.c:2663-2669 `if(UserWorkModeSetTEMP == MODE_COOL){ if(ReheatEnableSet){ outputMode = ReheatModeSet; heatAllow = separateStageConflict ? 0 : 1; } }`；输出落地在同文件 2798-2834（IndoorHeatAnalogMode=outputMode, IndoorApplyHeatAnalogOutputs()） | 恒温制冷／恒温恒湿制冷的分界参数是 CoolControlTypeSet=[125]（User.h:607）。再热分支完全不看 [125]，两种制冷都共用同一条再热路径 ⇒ 恒温那半句成立（恒湿见下一条）。 |
| 备注1：……或恒湿时再热用 | 符合 | NergryAdjust.c:375 `if((HumUseSet == 1) && (HumidValveY13 == 0) && (HumidAO2 == 0) && (ReheatDemandDisp < 1000) && (IndoorAbs(RealTempDisp - ControlTempDisp) * 2 <= RoomTempDiffSet) && (HumiditySensorAI1Disp > (HumiditySet + HumidityDiffSet / 2)))ys = 20;` 与同文件 398-400 把 ys 计入制冷 PID；NergryAdjust.c:478 `keepOne = (mode == MODE_COOL) && ReheatEnableSet && (ReheatModeSet != OUTPUT_NONE);` | 实现路径＝规格书 §3.4.2 的 YS：恒湿成立时制冷 PID 多加 YS(=2.0°C) 使压缩机过冷除湿，再由 HeatControl_UP 的再热回路把温度拉回；同时 478 行使压缩机至少留 1 台（对应 §3.4.2「制冷且有再热时 MAX(1,…)」）。备注1 只有这一句，没有更细的约束。 |
| 备注2：辅热：热泵制热时补充加热量（生效范围＝仅热泵制热） | 符合 | UserAction.c:2671-2693 `else if(UserWorkModeSetTEMP == MODE_HEAT){ if(HeatModeSet == 1){ heatPumpAux = 1; ... if(ReheatEnableSet && (ReheatModeSet != HEAT_OUTPUT_NONE)){ outputMode = ReheatModeSet; heatAllow = separateStageConflict ? 0 : IndoorHeatPumpFullLoad(); } } }` | heatPumpAux 只在 HeatModeSet==1 置位（2675）；非热泵制热分支（2695-2699）与制冷分支都不置位。heatPumpAux 另外只用于 ReheatPidStateDisp 区分 WAIT_FULL_LOAD（2760）与化霜门槛（2722）。 |
| 备注2：……补充加热量（能顶住时不投辅热） | 符合 | UserAction.c:2692 `heatAllow = separateStageConflict ? 0 : IndoorHeatPumpFullLoad();`；定义 UserAction.c:733-762（748-758：可用压机 `UnitReadStatus[i].CompStatus[j] != 3` 即 `return 0`，全部为 3=制热 才 `return available > 0`） | 与 §3.6「室外机热泵非满载时辅热全关；满载时根据 PID 调整辅热量」一致。补充量的多少由同一组 PID 参数（ReheatKp/Ti/Td=[347/348/349]）给，见 2777-2796。 |
| 备注3-A 前提：〖制热方式〗非“热泵制热”时 | 符合 | UserAction.c:2663 / 2671 / 2695 三个互斥分支：MODE_COOL；MODE_HEAT && HeatModeSet==1；MODE_HEAT && HeatModeSet>=2 && <=5（2695-2699） | HeatModeSet 枚举 0=无/1=热泵/2=AO/3=1:2/4=1:2:4/5=SCR，见 User.h:610；上限 user.c:239 `Parameter_max[128]=5`。 |
| 备注3-A：〖再热/辅热方式〗中选项均不指辅热 | 符合 | UserAction.c:2695-2699 `else if((HeatModeSet >= 2) && (HeatModeSet <= 5)){ outputMode = HeatModeSet - 1; heatAllow = 1; }` —— 该分支只读 [128]，从不读 [332](ReheatModeSet)；辅热标志 heatPumpAux 仅在 2675 置位 | 非热泵制热时电加热器作为「制热」主输出，用 [128]-1 决定驱动方式，与备注3-A 的措辞一致（同一物理加热器，两种角色用两个参数描述）。 |
| 备注3-A：此时〖再热是否启用〗选“是”时指再热 | **[被推翻]** 部分符合 | UserAction.c:2665-2669（制冷分支：只有 [127]=是 才 outputMode=ReheatModeSet，即 [332] 当作再热用） | 「指再热」只在制冷工况有落点。MODE_HEAT 且 [128]∈[2,5] 时（2695-2699）代码完全不读 [127]/[332]，[332] 在制热工况下既不是辅热也不被用作再热。这与备注1「再热只在制冷用」自洽，但 3A 的前提是「制热方式非热泵」，按字面覆盖的整台配置里含制热工况；规格书没说清这种工况下 [332] 该做什么，故记部分符合而非不符合。 ／复核 2/3 推翻 |
| 备注3-A：否则〖再热/辅热方式〗无效 | 符合 | UserAction.c:2665 `if(ReheatEnableSet)` 才赋值 outputMode；[127]=0 时 outputMode 保持 HEAT_OUTPUT_NONE（2645 初始化），随即被 2701-2721 的守卫清空（Heat1Y00/Heat2Y01/Heat3Y02=0、IndoorHeatAnalogDemand=0、ReheatDemandDisp=0） | [127]/[128]/[129] 取值 0~1，[332] 0~4（user.c:238-240、user.c:277）。固件在写入路径上不做组合校验，只在 [940] 报标志位（见备注4 条）。 |
| 备注3-B：〖制热方式〗为“热泵制热”时〖再热/辅热方式〗中选项均指辅热 | 符合 | UserAction.c:2688-2693 `if(ReheatEnableSet && (ReheatModeSet != HEAT_OUTPUT_NONE)){ outputMode = ReheatModeSet; heatAllow = separateStageConflict ? 0 : IndoorHeatPumpFullLoad(); }`（位于 2673 `if(HeatModeSet == 1)` 之内） | 同一 [332] 在制冷分支（2667）当再热、在热泵制热分支（2691）当辅热，正是 3A/3B 的分工。 |
| 备注3-B：此时〖再热是否启用〗选“是”时再热/辅热共用 | **[被推翻]** 部分符合 | 操作路径 UserAction.c:2688-2693（2677-2686 注释明写删掉 `(HeatReheatShareSet != 0)`），[129] 已不参与辅热使能；但配置校验路径 UserAction.c:887-891 `if((HeatModeSet == 1) && ReheatEnableSet && ((HeatReheatShareSet == 0) \|\| (ReheatModeSet == HEAT_OUTPUT_NONE))){ conflict \|= INDOOR_CONFIG_CONFLICT_HEATPUMP_AUX; }` | 读法判定：站(ii)「语义描述」，不是对 [129] 的硬约束。文本上三条理由——(1) 〖〗里的名字是〖制热与再热/辅热是否共用〗（制热 vs 再热/辅热），而 3B 写的是「再热/辅热共用」（再热 vs 辅热），同词不同指称（全篇「共用」只出现 4 次：共用风系统、[129]×2、3B 这一处）；(2) 平行结构——备注3 标题即「〖再热/辅热方式〗中再热与辅热的确定」，A/B 同模板：'选项均不指辅热'↔'选项均指辅热'／'选是时指再热'↔'选是时再热/辅热共用'／'否则无效'↔'不能为无'；A 只提 [127]，B 若多出 [129] 就破坏平行；(3) 与 §3.1 制冷段「再热同时是冬季加热的辅热」互文，同一加热器两角色本来就是事实描述。代码现状＝操作路径已按(ii)、配置校验路径仍按(i)，两者自相矛盾，故记部分符合。 ／复核 2/3 推翻 |
| 备注3-B：……且〖再热/辅热方式〗不能为“无” | 符合 | UserAction.c:2689 `(ReheatModeSet != HEAT_OUTPUT_NONE)` 作为辅热投运的必要条件；另 UserAction.c:888 把 `ReheatModeSet == HEAT_OUTPUT_NONE` 列入配置冲突（[940] bit3） | 该半句在两条路径上都落地了，且是 0x08 标志位里与备注3-B 唯一真正对得上的一半。 |
| 备注4：〖制热与再热/辅热是否共用〗选否时，制热……不能同时选分段电加热器（制热侧取值集合） | 符合 | UserAction.c:2656-2658 `(HeatReheatShareSet == 0) && ((HeatModeSet == 3) \|\| (HeatModeSet == 4) \|\| (HeatModeSet == 5))`；枚举 User.h:770-774（2=1:2 / 3=1:2:4 / 4=SCR），[128] 上限 user.c:239 | 制热侧允许取值＝{1:2, 1:2:4, 1:1:1(可控硅)}，排除 无(0)/热泵(1)/0-10VDC(2)。「可控硅」是否算「分段电加热器」规格书字面没说，但代码把它计入是对的：共用=否 时再热 SCR 的开关量硬写 Y0/Y1（UserAction.c:480-481 `IndoorSetScrOutputs(heatDemand, IndoorHeatScrRegion, heatAO, &Heat1Y00, &Heat2Y01)`，DO 指针与 heatAO 无关），与制热 SCR 真抢口。 |
| 备注4：……再热/辅热不能同时选分段电加热器（再热侧取值集合） | 符合 | UserAction.c:2659-2661 `((ReheatModeSet == HEAT_OUTPUT_STAGE_1_2) \|\| (ReheatModeSet == HEAT_OUTPUT_STAGE_1_2_4) \|\| (ReheatModeSet == HEAT_OUTPUT_SCR))`；[332] 上限 user.c:277 `Parameter_max[332]=4` | 再热侧允许取值＝{1:2, 1:2:4, 1:1:1(可控硅)}，排除 无(0) 与 0-10VDC(1)。0-10VDC 不计入是正确的：它走模拟量（共用=否 时 DA9，UserAction.c:451-454），不占分段口。 |
| 备注4：实现函数（separateStageConflict / INDOOR_CONFIG_CONFLICT_SEPARATE_STAGE） | 符合 | 两处同规则、条件集合全等：UserAction.c:2654-2661 `separateStageConflict = ((ReheatEnableSet != 0) && (HeatReheatShareSet == 0) && ((HeatModeSet==3)\|\|(HeatModeSet==4)\|\|(HeatModeSet==5)) && ((ReheatModeSet==HEAT_OUTPUT_STAGE_1_2)\|\|(ReheatModeSet==HEAT_OUTPUT_STAGE_1_2_4)\|\|(ReheatModeSet==HEAT_OUTPUT_SCR))) ? 1 : 0;`；UserAction.c:870-875 + 892-895 `heatUsesStage=([128]∈{3,4,5}); reheatUsesStage=([127]&&[332]∈{2,3,4}); if(([129]==0) && heatUsesStage && reheatUsesStage) conflict \|= INDOOR_CONFIG_CONFLICT_SEPARATE_STAGE;`（标志位 User.h:1269 = 0x10，落到 Parameter[940]，User.h:1254） | 唯一写法差异：separateStageConflict 把 [127] 内联进总表达式（2655），IndoorUpdateAOConflict 把 [127] 并进 reheatUsesStage（872），语义等价。外部一致性：监控工具 app.py 的同一校验原写成 `[128] in (3,4)` × `reheat_mode in (2,3)`，两头都漏了可控硅（tools/fix_monitor_scr_validation.py:18-22 已记录并补成 (3,4,5)×(2,3,4)）—— 即固件侧本来是对的，工具侧曾是错的。 |
| 备注4：“不能同时选”——冲突成立后的动作 | 符合 | UserAction.c:2668 `heatAllow = separateStageConflict ? 0 : 1;` / 2692 同理 → 2750-2761 `if(!heatAllow){ demand = 0; ...; ReheatPidStateDisp = heatPumpAux ? REHEAT_PID_STATE_WAIT_FULL_LOAD : REHEAT_PID_STATE_OFF; }` → 2798-2833 以 demand=0 生成输出（各段 level=0，scr 不 active） | 规格书只说「不能同时选」，没说违反时怎么处理。代码选择：不拒绝写入、静默把再热 demand 清 0、并在 [940] bit4(0x10) 上报配置冲突。这属于规格书没规定、代码自行决定（不是缺陷，但需知道现象是「再热悄悄不动作 + 一个标志位」）。 |
| 备注4 仅约束“选否” | 符合 | UserAction.c:2656 与 892 均以 `(HeatReheatShareSet == 0)` 为前提；共用=1 时 separateStageConflict 恒 0，2668/2692 的 heatAllow 不受限 | 规格书确实只写了「选否时」。共用=是 时 [128] 与 [332] 都选分段，代码不拦截 —— 运行上也不冲突，因为制热分支（2673/2695）与再热分支（2663）由模式互斥，二者不会同时驱动 Y0/Y1。 |
| （规格书未规定项）“共用=否”时再热/辅热的模拟量走哪一路 AO | 无法核对 | UserAction.c:451-454 `if((HeatReheatShareSet == 0) && ((UserWorkModeSetTEMP == MODE_COOL) \|\| ((UserWorkModeSetTEMP == MODE_HEAT) && (HeatModeSet == 1)))) heatAO = &HeatAO9;`（444-454 注释：共用=是 ⇒ DA0，共用=否 ⇒ 再热/辅热独占 DA9，制热仍走 DA0） | 规格书只规定〖再热/辅热方式〗含「0～10VDC」以及「共用」的语义，没规定通道分配。这是规格书没规定、代码自行决定 —— 不是缺陷，但要标出来：DA9 只会在「共用=否」的再热/辅热工况出现，若采用了读法(i)（把 [129] 硬锁为 1）就永远用不到 DA9，这也从另一面印证读法(ii)。 ／复核 1/3 推翻 |
| 备注3/4 所依赖的参数取值范围（佐证选项集合与代码枚举一致） | 符合 | user.c:238-240 `Parameter_max[127]=1;Parameter_min[127]=0;...Parameter_max[128]=5;...Parameter_max[129]=1;Parameter_min[129]=0;`；user.c:277 `Parameter_max[332]=4; Parameter_min[332]=0; Parameter_def[332]=0;`；User.h:609-611、650；User.h:770-774 | [332] 上限 4＝HEAT_OUTPUT_SCR(4)；[128] 上限 5＝SCR(5)。同一枚举在制热侧整体偏移 1（2697 `outputMode = HeatModeSet - 1`）：[128]=2→[332]=1(0-10V)、3→2、4→3、5→4。这是代码自己定的映射，规格书两个参数的选项列表顺序与之吻合（无/0～10VDC/1:2/1:2:4/可控硅 与 无/热泵/0～10VDC/1:2/1:2:4/可控硅）。 |

## §3.2 来电自启

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 「来电自启动功能允许」——必须有一个使能参数可设 | 符合 | User/UserHead/User.h:403 `#define UserAutoModeSet Parameter[164]  //0 （0~1）`；User/UserSrc/user.c 的 max 表(160--169 行) `320,320,1000,2,1,1,1,29999,1,1` ⇒ Parameter_max[164]=1；min 表同段 `150,150,0,-300,0,0,0,0,0,0` ⇒ Parameter_min[164]=0；User/UserSrc/Switch.c:99 `if((UserAutoModeSet==1)&&(UserSwitchSet==0)&&(SysStatusLoader))` | PSC330接HMI读写地址逐点表.md:276 记「164 \| UserAutoModeSet \| RW \| 0..1 \| 0 \| 来电自启动：0=屏蔽…1=允许」，与代码一致。另有别名 User.h:645 `#define PowerFailRecoverModeSet UserAutoModeSet`（同一下标，无第二个使能位）。 |
| 「系统会记忆断电前的状态」——记忆项存到哪（EEPROM 映射） | 符合 | 记忆载体就是 Parameter[] 整块，EEPROM 字节地址 = 16 + 2*i。Project/Obj/sysrun_disasm.txt 的 Readeeprom：`0x3ea MOVS r2,#0x10`(地址16) `0x3ec MOV r1,#0x3e8`(长度1000) `0x3f2 BL CI2C_ReadByte`；循环 `0x4d6 CMP r4,#0x1f4`(500) 逐字 `LDRB [sp+2i] / [sp+2i+1] → SXTH`。所以 160→336、161→338、164→344、166→348、236→488。 | ⚠ sysrun.c 源码不在仓库（User/wellthinker/src/ 只有 sys.c；Project/Obj/sysrun.d 指向 ..\User\wellthinker\src\sysrun.c，只有已编译的 sysrun.o）。本条证据来自 .o 的反汇编，不是源码。同理 WriteParamaterEEprom/sysRunfile。 |
| 「记忆项有：系统开/关机状态」 | 符合 | User/UserHead/User.h:405 `#define SysStatusLoader Parameter[166]//系统状态…`；置 1：User/UserSrc/UserAction.c:2338（SysBoardAction case 2 运行中每扫描）、User/UserSrc/Switch.c:148（按开机且 SysStep==0）；清 0：User/UserSrc/Switch.c:144（按关机）。Parameter_max[166]=1、min=0（user.c 同两张表）。 | 该位名字叫 SysStatusLoader，实际语义是「掉电前是否在开机」，不是 HMI 开关机位本身——它与 HMIOnOffCommandSet/TouchScreenONOFF 是两个东西。规格书说的「系统开/关机状态」由它承担。 |
| 「记忆项有：…模式为制冷/制热/通风/消排毒状态」 | 符合 | User/UserHead/User.h:492 `#define UserWorkModeSetTEMP Parameter[236]`；范围 User/UserSrc/user.c:230 `Parameter_max[236] = 5;// 0 cool,1 heat,2 vent,3 disinfect,4 exhaust,5 auto`；枚举 User.h:486-491 `MODE_COOL 0 / MODE_HEAT 1 / MODE_VENT 2 / MODE_DISINFECT 3 / MODE_EXHAUST 4 / MODE_AUTO_DISINFECT 5`；[236] < 500 ⇒ 在保存区内。 |  |
| 「记忆项有：…设定温度」 | **[被推翻]** 部分符合 | User/UserHead/User.h:399-400 `#define UserColdTempSet Parameter[160]`、`#define UserHeatTempSet Parameter[161]`；使用处 User/UserSrc/UserAction.c:2087-2096 `if(UserWorkModeSetTEMP == MODE_HEAT){UserSideControlTemp = UserHeatTempSet;...} else {...UserColdTempSet...}`。两个下标都 < 500 ⇒ 同块持久化。 | 规格书只写一个「设定温度」，代码存的是制冷/制热两路设定值（160/161）。属于「规格书没规定、代码自行决定」，不是缺陷，但要在点位表里说清。 ／复核 3/3 推翻 |
| 「记忆项有：…消排毒状态」——是否真的写进 EEPROM | 符合 | [236] 取值 0..5（user.c:230），3/4/5 即消毒/排毒/自动消排毒；[236] 在 Parameter[0..499] 内，任何改动都会被 WriteParamaterEEprom 落盘。配套：进入消毒时 User/UserSrc/UserAction.c:1630 `if(lastMode <= MODE_VENT)LastNormalModeSet = lastMode;` 把「掉电前正常模式」写进 User.h:644 `LastNormalModeSet Parameter[INDOOR_SPEC_BASE+11]`=Parameter[326]，同样落盘。 | 所以「记忆」这一半是真的做了，包括消排毒的三种类型。 |
| 「记忆项有：…消排毒状态」——上电是否恢复为消排毒 | 部分符合 | User/UserSrc/Switch.c:101-105 `if(UserWorkModeSetTEMP >= MODE_DISINFECT){ if(LastNormalModeSet <= MODE_VENT)UserWorkModeSetTEMP = LastNormalModeSet; else UserWorkModeSetTEMP = MODE_COOL; }` —— 主动把 3/4/5 降级成上一次的正常模式，不恢复消毒/排毒。 | 规格书只说要「记忆」，没写恢复后如何处理消排毒，所以字面上不构成明确冲突（LastNormalModeSet 的注释就叫「掉电前模式」，设计意图明显是「消毒是定时过程，不复跑」）。但存在两处不对称，建议规格书确认：①允许（164=1）时 [236] 被改成 0/1/2，屏蔽（164=0）时这段降级在 if 里不执行，[236] 原样停在 3/4/5 —— 同一个记忆值在两种情形下上电后不一样；②制冷/制热/通风在两种情形下都一样。 ／复核 1/3 推翻 |
| 「来电自启动功能屏蔽时，系统会记忆断电前的状态、模式与设定温度」（记忆写点与使能位是否解耦） | 符合 | WriteParamaterEEprom（sysrun_disasm.txt 0x4e2 起）的核心是 `LDRSH r0,[Old_Parameter+i*2] / LDRSH r1,[Parameter+i*2] / CMP / BEQ` —— 只比较 Parameter 与 Old_Parameter，全函数不读 Parameter[164]；全仓库 UserAutoModeSet 只出现在 User.h:403、User.h:645、Switch.c:99 三处（grep 全 User/ 目录）。Switch.c:113 的 else 分支只写 SysStatus，不碰 Parameter[236]/[160]/[161]。 | ⇒ 无论 164 是 0 还是 1，模式、温度、开关机状态都照常落 EEPROM。规格书这句是真的。 |
| 「但每次上电后进入关机状态」 | 符合 | User/UserSrc/Switch.c:113 `else  SysStatus=NORSTOP;`，紧接 Switch.c:119-122 `if(SysStatus==NORSTOP){HMIOnOffCommandSet=0;OldHMIOnOff=0;TouchScreenONOFF=0;}`。 |  |
| 「但每次上电后进入关机状态」——是否存在旁路把机组又开起来 | 部分符合 | ①Switch.c:113 的 else 分支不清 SysStatusLoader（对比 Switch.c:144 按关机时才清），而 166 刚从 EEPROM 载入可能是 1。②User/UserSrc/ErrorCheck.c:463-469 `if(!RemoteInterlockStopFlag){ if((SysStep>0)\|\|(SysStatus==NORRUN)\|\|(SysStatus==TIMERUN)\|\|HMIOnOffCommandSet\|\|TouchScreenONOFF\|\|SysStatusLoader){RemoteInterlockResumeFlag=1;} }`（X_02 断开 ≥3s，X_02 定义 User/UserHead/User.h:59）。③User/UserSrc/Switch.c:212-222 `if(RemoteInterlockResumeFlag && !RemoteInterlockStopFlag && (SysStep==0) && (SysErrStatus==0)){Old_SysStatus=1;HMIOnOffCommandSet=1;OldHMIOnOff=1;TouchScreenONOFF=1;SysStatusLoader=1;RemoteInterlockResumeFlag=0;}` ⇒ 开机。 | 触发条件：掉电前在开机（166=1）且 164=0（屏蔽）且 161/…模式为 0/1/2（见下）、开关来源为本地（163=0），上电后 X_02 断开≥3s 再闭合。为什么模式 3/4/5 反而没事：UserAction.c:1621-1623 在屏蔽+消排毒模式下会走 IndoorEmergencyStopLoads()（其 348 行 `SysStatusLoader = 0;`）把 166 清掉；模式 0/1/2 在 UserAction.c:1601-1605 直接 `lastMode = UserWorkModeSetTEMP; return;`，不清。残留的 1 要等 UserAction.c:2281 `if((UserWorkTickCount >=3000) && (!RemoteInterlockResumeFlag))SysStatusLoader=0;` 才清，而 SysBoardAction 本身还有 UserAction.c:2248 `if(Systick_1ms<30000){return;}`，3000×100ms=5 分钟 ⇒ 上电后约 5 分钟内该路径有效。这与规格书「屏蔽时每次上电后进入关机状态」矛盾，是本次审计新发现。 ／复核 1/3 推翻 |
| 上电恢复判定写在哪个函数、什么时候执行 | 符合 | User/UserSrc/Switch.c:93-123 的 `if(InitSwitchProcess==0){...return;}` 一次性块；门槛在同文件 Switch.c:86 `if(Systick_1ms<20000){SysStatus =DLAY; return;}`（注释：上电20秒后才启动逻辑控制 等待电子膨胀阀归零）；唯一调用点 User/UserSrc/UserAction.c:3392 `SwitchStatusProcess();`。 | 即：上电 20 秒后的第一次调用执行恢复判定，SysStatusLoader 在那一刻才被读。 |
| 「允许」与「屏蔽」两种情形怎么区分 | 符合 | User/UserSrc/Switch.c:99 判据 `(UserAutoModeSet==1)&&(UserSwitchSet==0)&&(SysStatusLoader)` 成立 ⇒ 走 100-112 分支；否则 Switch.c:113 `else SysStatus=NORSTOP;`。 |  |
| 「允许」时的恢复动作是否完整（开机 + 模式 + 设定温度） | 符合 | 开机：Switch.c:111 `else SysStatus=NORRUN;` + 116-117 `Old_SysStatus=1;HMIOnOffCommandSet=1;OldHMIOnOff=1;TouchScreenONOFF=1;`。模式：不清 [236]，由 EEPROM 载入值直接生效（仅 3/4/5 被降级，见前条），随后 UserAction.c:3393 `IndoorNormalizeWorkMode()` 只在 >5 时才改成 VENT（UserAction.c:608），不会覆盖 0..5。设定温度：[160]/[161] 由 Readeeprom 还原，UserAction.c:2087-2096 直接采用。 | 「设定温度」没有任何显式的恢复代码，它靠 Parameter[] 整块持久化顺带完成——正是规格书想要的效果，但读代码时要意识到没有一条「恢复温度」的语句。 |
| 恢复判据额外要求 UserSwitchSet==0（本地开关） | 无法核对 | User/UserSrc/Switch.c:99 `(UserSwitchSet==0)`；User/UserHead/User.h:402 `#define UserSwitchSet Parameter[163]//开关选择（本地/远程/PLC）0~2`。 | 口径：规格书 3.2 只区分「允许/屏蔽」，没提开关来源。代码自行加了「远程/PLC 开关方式下不执行来电自启」这个条件——属于「规格书没规定、代码自行决定」，不是缺陷（远程方式下开关状态本来就由外部接点决定，恢复会打架），但要让客户知道。 ／复核 0/3 推翻 |
| 恢复时若外部联锁正处于断开状态 | **[被推翻]** 无法核对 | User/UserSrc/Switch.c:106-110 `if(RemoteInterlockStopFlag){RemoteInterlockResumeFlag=1;SysStatus=NORSTOP;}`。 | 规格书 3.2 未规定。代码选择「记住这次本应开机，等联锁恢复后自动补开」（与 Switch.c:212-222 配套）。属于代码自行决定。 ／复核 3/3 推翻 |
| 上电到恢复判定之间，SwitchStatusProcess 自己会不会改记忆量 | 符合 | User/UserSrc/Switch.c:86-91 → `Systick_1ms<20000` 时只执行 `SysStatus =DLAY;` 然后 `return;`，不碰 Parameter[166]/[236]/[160]/[161]。 | 所有 Switch.c 内的 SysStatusLoader=0（144/160/175/194/208/240）都在 20 秒门槛之后的主体内，够不到判定之前。 |
| 上电 0~5 秒 UserAction 整体不跑（因此更早的调用顺序不构成风险） | 符合 | User/UserSrc/UserAction.c:3390-3391 `InitCount +=uc1s; if(InitCount>=10)InitCount=20; if(InitCount<5)return;`。 | 即 SwitchStatusProcess 在 5 秒前根本没被调用；0~20 秒窗口真正会跑的只有 5~20 秒那段。 |
| ★重点核对：IndoorDisinfectExhaustAction 在恢复判定前会不会清掉掉电前开关机状态 | 符合 | 已修（V10.12）。User/UserSrc/UserAction.c:1600 `if(Systick_1ms < 20000)return;`（与 Switch.c:86 同一表达式）；被守住的危险分支 UserAction.c:1621-1626 `if((SysStatus != NORRUN) && (SysStatus != TIMERUN)){ IndoorEmergencyStopLoads(); UserPump=0; SysStep=0; ... return; }` → UserAction.c:348 `SysStatusLoader = 0;`。当前固件确实含此守卫：Project/Obj/PSC330RK-V10.axf（2026-09-20 19:05 链接）反汇编 `0x08009eda: MOV r3,#0x4e20 / 0x08009ede SUBS r2,r2,r3 / SBCS / 0x08009ee4 BCS / 0x08009ee6 POP {r3-r7,pc}`，其后才 `LDRH r0,[r0,#0x1d8]`（=Parameter[236]，0x1d8=472=2×236）与 3/4/5 比较。 | 修复前的机制（UserAction.c:1571-1598 注释自述）：SysStatus 在 0~20 秒恒为 DLAY ⇒ `SysStatus!=NORRUN&&!=TIMERUN` 恒真 ⇒ 20 秒前必调 IndoorEmergencyStopLoads() ⇒ Parameter[166] 被清 0 并落盘，来电自启必然不成立，且 Switch.c:101-105 的降级分支都走不到。只影响掉电前模式 ∈{3,4,5}；0/1/2 在 UserAction.c:1601-1605 就 return 了。 |
| 两个函数的调用顺序（SwitchStatusProcess 必须先于消排毒动作） | 符合 | User/UserSrc/UserAction.c:3392 `SwitchStatusProcess();` 在 3397 `IndoorDisinfectExhaustAction();` 之前，同一轮内先做恢复判定；两个函数用完全相同的门槛表达式（Switch.c:86 与 UserAction.c:1600），所以 Systick_1ms 跨过 20000 的那一拍一定是先判定后跑消排毒。 | 这是 V10.12 修复正确的关键——单看 UserAction.c:1600 那一行不足以说明安全，必须同时看 3392/3397 的先后。 |
| 其它会写 SysStatusLoader 的地方是否也落在 20 秒判定之后 | 符合 | User/UserSrc/UserAction.c:2248 `if(Systick_1ms<30000){ return; }`（SysBoardAction 开头）；其内的 UserAction.c:2281 `if((UserWorkTickCount >=3000) && (!RemoteInterlockResumeFlag))SysStatusLoader=0;`、2345/2389/2417 三处 `if(!RemoteInterlockResumeFlag)SysStatusLoader=0;`，以及第二个 IndoorEmergencyStopLoads 调用点 UserAction.c:2382（case 4），全部在 30 秒之后。 | ⇒ 都不影响 20 秒的判定。但注意 2281 那处「5 分钟后才清」正是前条旁路的成因。 |
| ErrorCheck() 会不会改记忆量 | 符合 | User/UserSrc/ErrorCheck.c:466 只读 `... \|\| SysStatusLoader)`；同一函数内无对 SysStatusLoader/[236]/[160]/[161] 的写。user.c:2001-2002 `UserAction();` 与 `ErrorCheck();` 同轮，ErrorCheck 在 UserAction 之后。 | 它读 SysStatusLoader 只是为了置 RemoteInterlockResumeFlag（见旁路那条）。 |
| ParameterInit() 会不会改记忆量 | 符合 | User/UserSrc/user.c:220-470 的 ParameterInit() 只写 Parameter_max/min/def；对 Parameter[] 的写入只有 user.c:453-470 的 ParameterSaveSize-35..-20 区间与 user.c:475-480 的 Parameter[1000..1005]（RTC），不含 160/161/164/166/236。调用点 user.c:1992（`if(Systick_1ms<5000){ParameterInit(); ...; return;}`）。 | 顺带：Parameter_max[236]=5 是在这里（上电 5 秒内）才设的，而 Readeeprom 在 300ms 就按静态表的 999 校验 [236]，所以 [236] 存 0..5 一定通过；上限 5 只约束 HMI 写入。 |
| IndoorMigrateSpecConfiguration() 会不会覆盖记忆的设定温度 | **[被推翻]** 无法核对 | User/UserSrc/user.c:1946-1961：`if(IndoorWDSDMigrationMarker != INDOOR_WD_SD_MIGRATION_VALUE){ if(UserHeatTempSet == 450)UserHeatTempSet = 250; else { if(UserHeatTempSet < 150)UserHeatTempSet = 150; if(UserHeatTempSet > 320)UserHeatTempSet = 320; } ... IndoorWDSDMigrationMarker = ...; }`；调用点 user.c:1997，在 Readeeprom 之后、20 秒判定之前。 | 规格书没规定。这是上位机版本迁移的一次性动作；正常板子标记已置位就不再触发。但若标记未落盘，上电恢复来的 161 会被改写。属于「规格书没规定、代码自行决定」。 ／复核 3/3 推翻 |
| 记忆量从 EEPROM 读回的时机 | 符合 | Project/Obj/sysrun_disasm.txt 的 sysRunfile：`0x67a LDR r0,[Systick_1ms] / 0x680 MOV r2,#0x12c`(300) `0x684 SUBS r2,r3,r2 / SBCS / BCS` ⇒ 上电 300ms 才往下走；`eepromtt==0 → eepromtt=1; BL Readeeprom`；`eepromtt==3 → BL WriteParamaterEEprom`（每轮）。 | 300ms 远早于 20 秒判定 ⇒ 判定时读到的一定是本周期 EEPROM 里的值。证据来自 .o 反汇编（源码缺失）。 |
| Readeeprom 对存进去的值做越界兜底 | 无法核对 | sysrun_disasm.txt Readeeprom 循环体：`0x490 LDR r0,[Parameter_max] / LDRSH r0,[r0,r4,LSL #1] / CMP r0,r5 / BLT 0x4b2`；越界分支 `0x4b2: LDR r0,[Parameter_def] / LDRH r0,[r0,r4,LSL #1] / STRH → Parameter[i]`，`+1` 后写 Old_Parameter[i]（迫使回写默认值），并 `ReadEEpromError = i`。 | 规格书没规定。对 166（静态 0..1）与 236（静态 0..999）而言只要存的是合法值就不会被替换。属于代码自行决定的健壮性设计。 ／复核 1/3 推翻 |
| 记忆量落盘的粒度与掉电窗口 | 无法核对 | WriteParamaterEEprom（sysrun_disasm.txt 0x4e2 起）：EEpflag=0 找首个 `Parameter[i]!=Old_Parameter[i]` 并记 t；=1 `CI2C_WriteByte(高字节, 0x10+2t)`、记 Systick_eeprom、EEpflag=2；=2 等 8ms 后 `CI2C_WriteByte(低字节, 0x11+2t)`、EEpflag=3；=3 等 8ms 后 `CI2C_ReadByte(sp,2,0x10+2t)` 回读比对，不等则 `CPURomWriteError=t` 且回滚 `Parameter[t]=Old_Parameter[t]`。 | 规格书没规定保存窗口。含义：一次只追一个参数、每个参数约 16ms 两段写+回读；若掉电前刚按了温度键，最后那次改动可能没落盘。这是「记忆」能力的固有边界，报给客户时值得写一句。 ／复核 1/3 推翻 |
| 3.2 全节：判定依据、恢复延时、联锁/远程开关下的行为 | **[被推翻]** 无法核对 | spec 原文（d:\Companty\WEll think\F\code\scratch_spec.txt）3.2 只有两句：「来电自启动功能允许时，系统会记忆断电前的状态。记忆项有：系统开/关机状态、模式为制冷/制热/通风/消排毒状态、设定温度。」「来电自启动功能屏蔽时，系统会记忆断电前的状态、模式与设定温度，但每次上电后进入关机状态。」 | 卡在规格书本身没写：①「状态」的判据是什么（代码用 Parameter[166]，且是「是否在开机」而非「HMI 开关机位」）；②恢复是否有延时（代码 20 秒，Switch.c:86，理由注释写「等待电子膨胀阀归零」）；③屏蔽时是否要清掉 RAM 里的记忆位（代码不清，见旁路那条）；④消排毒恢复后该怎样（代码降级为 LastNormalModeSet）；⑤远程/PLC 开关方式下是否也来电自启（代码不）。这五条都不是「规格书写了代码没做」，而是规格书留白由代码决定。 ／复核 3/3 推翻 |

## §3.3.1 通风开关机

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 「通风模式开机指令→」——通风开机被触发 | 符合 | UserAction.c:2283-2287 `if((SysStatus==TIMERUN)\|\|(SysStatus==NORRUN)){ WaterFlowCount +=uc1s; if(WaterFlowCount>1){IndoorStopSnapshotValid=0;SysStep=1;...} }`；UserAction.c:2301-2311 `else { if(!IndoorVentilationStartSequence(&startSeqStep,&startSeqCount,valveDelay,fanInterval))return; }` | 触发判据是 SysStatus∈{NORRUN,TIMERUN}（与 §3.2 开关机/来电自启同一路）。规格书没规定『通风』对应哪个 SysStatus 具体值/哪个 HMI 寄存器，属于代码自行决定。另：IndoorDisinfectExhaustAction（UserAction.c:3397）在 SysStep 上先抢跑，但 UserAction.c:1601 对 MODE_VENT 直接 return，不干扰通风路径。 |
| 通风模式开机：送风阀开启(若有) | **[被推翻]** 部分符合 | UserAction.c:1369 `IndoorApplyFanValveState(1, 0, 0, 0);` → UserAction.c:1060-1065 `IndoorApplyFanValveState(valveOpen,...) { IndoorApplyFanValveStateDetailed(valveOpen,valveOpen,valveOpen,valveOpen,...) }` → UserAction.c:967 `SupplyValveY04 = (supplyValveOpen && (SupplyValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`（AO 型见 986-990 `IndoorSetAO(&SupplyFanAO7, supplyValveOpen ? SupplyValveManualVoltSet*10 : 0);`） | 阀确实开了、『若有』判据正确；但送风阀/新风阀/回风阀/排风阀/混风阀是在**同一扫描内同时**输出的，规格书用『→』表示的开启级差完全没有实现。开关型风阀物理上无副作用，故判部分符合而非不符合；若现场需要错开（冲击电流/执行器同时动作）则不满足。 ／复核 2/3 推翻 |
| 通风模式开机：新风阀开启（若有） | 符合 | UserAction.c:965 `freshDemand = IndoorApplyFreshAndMixValve(freshValveOpen, forcedFreshDemand);`（此处 freshValveOpen=1）→ UserAction.c:697-704 `if(!valveOpen)freshDemand = 0; else freshDemand = IndoorFreshValveDemand(1); IndoorOutputByMode(FreshValveModeSet, &FreshValveY14, &FreshValveAO3, freshDemand);` | FreshValveModeSet==OUTPUT_NONE 时 IndoorOutputByMode（499-506）自动把 Y14/AO3 清 0，『若有』成立。开启同样与其余风阀同拍。 |
| 通风模式开机：回风阀开启（若有） | **[被推翻]** 部分符合 | UserAction.c:968 `ReturnValveY07 = (returnValveOpen && (ReturnValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`；AO 型 UserAction.c:1007-1011 `else if(ReturnValveModeSet == VALVE_OUTPUT_AO) IndoorSetAO(&ReturnFanAO4, returnValveOpen ? ReturnValveManualVoltSet*10 : 0);` | 功能在；但回风阀 AO 与回风机 AO 共用 DA4，UserAction.c:997-1001 规定两者都配 AO 时该路输出 0（即回风阀打不开）。规格书对回风阀的限制写的是『此项选择要求送风阀不要求0-10V』，而代码实际限制的是『回风机不要求0-10V』——两者表述不一致，属规格书与代码参数约束口径的差异，不是通风开机的次序问题。 ／复核 3/3 推翻 |
| 通风模式开机：排风阀开启（若有） | 符合 | UserAction.c:969 `ExhaustValveY11 = (exhaustValveOpen && (ExhaustValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`；AO 型 UserAction.c:1016-1024 `if((ExhaustValveModeSet==VALVE_OUTPUT_AO)&&(FreshValveModeSet==VALVE_OUTPUT_AO)) IndoorSetAO(&ExhaustValveAO6, exhaustValveOpen ? freshDemand : 0);` | 0-10V 型排风阀取的就是 freshDemand，与规格书『开度与新风阀开度相等』一致；且限定新风阀也是 AO（规格书『此项选择要求新风阀根据CO2浓度及新回风焓差控制』）。 |
| 通风模式开机：混风阀开启（若有） | 符合 | UserAction.c:705-709 `if(MixAirValveTypeSet){ IndoorSetAO(&MixValveAO5, valveOpen ? (1000 - freshDemand) : 0); MixValveDemandDisp = MixValveAO5; }` | 开度=1−新风阀开度，与规格书『开度与新风阀开度之和为1』一致。额外的运行期约束在 UserAction.c:1157-1159/1195-1199：MixAirValveTypeSet 为 1 但新风阀不是 AO 型时，IndoorEnforceConfiguredOutputDisable 会在每扫描末把 MixValveAO5 清 0（对应规格书『此项选择要求新风阀根据CO2浓度及新回风焓差控制』）。 |
| 开启次序本身：送风阀→新风阀→回风阀→排风阀→混风阀（五个『→』） | **[被推翻]** 不符合 | UserAction.c:1369 `IndoorApplyFanValveState(1, 0, 0, 0);` 单次调用即把 4 个 DO 阀（967/968/969 行）与新风阀（703 行）、混风阀（707 行）**在同一扫描**全部写出；全工程搜索无任何风阀分步/级差/斜坡代码（grep 'Ramp\|ValveStep\|OpenDelay' 仅命中 IndoorValveOpenDelay 一处） | 代码是『全部一起开』，规格书是 5 级次序。判定为不符合，但因为都是同一拍置位、开关型风阀无物理差异，实际风险低——需要现场确认是否要求错开。同类问题在关机侧也存在（见风阀关闭次序条）。 ／复核 3/3 推翻 |
| 通风模式开机：150s（风阀已配时） | 符合 | UserAction.c:2299 `valveDelay = (unsigned int)IndoorValveOpenDelay();`；UserAction.c:1373 `if(*startSeqCount >= valveDelay){*startSeqStep = 2; *startSeqCount = 0;}`；UserAction.c:596-600 `static int IndoorValveOpenDelay(void){ if(!IndoorAnyValveConfigured())return 10; return 150; }` | 150s 为硬编码常量，规格书未规定可调，一致。 |
| 通风模式开机：无风阀时取 10S，及其『有无风阀』判据 | 符合 | UserAction.c:586-594 `IndoorAnyValveConfigured()` = SupplyValveModeSet!=OUTPUT_NONE \|\| ReturnValveModeSet!=OUTPUT_NONE \|\| ExhaustValveModeSet!=OUTPUT_NONE \|\| FreshValveModeSet!=OUTPUT_NONE \|\| MixAirValveTypeSet；UserAction.c:598 `if(!IndoorAnyValveConfigured())return 10;` | 判据覆盖了规格书点名的 5 个风阀（送风/新风/回风/排风/混风）的全部。注意判据是『某阀配了输出类型』而不是『该阀在本模式下被用到』——例如只配了排风阀，开机仍会等 150s，与规格书『有风阀』的字面一致。 |
| 通风模式开机：{[排风机启动→30s（±60s可调）]（若有）→送风机启动 | **[被推翻]** 部分符合 | UserAction.c:147-179 `IndoorFanStartTimeline`：`else { *supplyStart = (fanInterval > 0) ? fanDelay : 0; *exhaustStart = (fanInterval < 0) ? fanDelay : 0; }`；UserAction.c:1378 `supplyFan = (*startSeqCount >= IndoorFanSequenceSupplyStart) ? 1 : 0;`；UserAction.c:1379-1380 `exhaustFan = (IndoorFanSequenceExhaustPresent && (*startSeqCount >= IndoorFanSequenceExhaustStart)) ? 1 : 0;`；UserAction.c:139-145 `IndoorExhaustFanSignedDelay(){ signed int delay = ExhaustFanDelaySet; if(delay>90)delay=90; else if(delay<-90)delay=-90; }` | 默认 [328]=30 ⇒ 排风机 t=0、送风机 t=30，与规格书完全吻合；正/负号 = 排风机先/后启动，语义也吻合。**唯一不符：可调范围**。user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90; Parameter_def[328]=30;`（User.h:646 `#define ExhaustFanDelaySet Parameter[INDOOR_SPEC_BASE+13]` 即 [328]），规格书写『±60s 可调』，代码允许 ±90s。另：调试记录_20260916.md §6.8 把这把 ±60 记到了 [135] ExhaustValveDelaySet 上，那是错的（[135] 零引用，见末尾条）。 ／复核 2/3 推翻 |
| 通风模式开机：[30s→回风机启动]（若有） | 符合 | UserAction.c:158-171 `returnDelay = (ReturnFanDelaySet < 0) ? 0 : (unsigned int)ReturnFanDelaySet; ... *returnStart = *supplyStart; if(ReturnFanModeSet != OUTPUT_NONE)*returnStart += returnDelay;`；UserAction.c:1381-1382 `returnFan = (IndoorFanSequenceReturnPresent && (*startSeqCount >= IndoorFanSequenceReturnStart)) ? 1 : 0;`；UserAction.c:240-241 `IndoorFanSequenceReturnPresent = (ReturnFanModeSet != OUTPUT_NONE) ? 1 : 0;`；user.c:243 `Parameter_max[132]=90; Parameter_min[132]=0; Parameter_def[132]=30;` | 『30s』由 [132] ReturnFanDelaySet 提供，默认 30、符合。**规格书没写这个 30s 可调**，代码自行做成了 0~90s 可调（不是缺陷，但属代码自决）。另：UserAction.c:1387 `if(*startSeqCount < IndoorFanSequenceEnd)return 0;` 使 returnStart==sequenceEnd 那一拍直接跳 step5，回风机的实际首次上电落在 step5（UserAction.c:1392-1396），比理论时刻晚约 1 个扫描，无实质影响。 |
| 通风模式开机：送风机压差开关持续闭合10s（若送风机有压差开关） | **[被推翻]** 部分符合 | UserAction.c:1284-1297 `supplyActive = IndoorSupplyFanOutputActive(); ... if(!supplyInstalled){supplyDelay=0; supplyOk=10; supplyBad=0;} else if(supplyActive){ if(supplyDelay<60)supplyDelay += uc1s; if(X_15){ supplyOk += uc1s; supplyBad = 0; } else { supplyOk = 0; ... } }`；UserAction.c:1342-1345 `if(supplyOk > 10)supplyOk = 10; ... SupplyPressureOkDisp = supplyOk;`；UserAction.c:1348-1351 `return (SupplyPressureOkDisp >= 10) && (ReturnPressureOkDisp >= 10);`；UserAction.c:1399 `if(!IndoorPressureReady())return 0;`；user.c:534 `X_15 = (InPutStatusSet21)?(X15):(!X15);`（注释：X14=回风压差，X15=送风压差，闭合=1=正常）；user.c:357 `Parameter_max[453]=1; Parameter_min[453]=0; Parameter_def[453]=1;`（SupplyPressureSwitchSet=User.h:742） | 『持续闭合』语义正确：X_15 一断开 supplyOk 立刻清 0（1300 行），不是累计不清零；『累计满 10』由 1342-1343 封顶、1348-1351 判 >=10。**与规格书的差异在串/并行**：规格书字面是『送风闭合10s → 回风闭合10s』串行（总时长 ≥20s），代码是两个计数器并行累积、最后一次性要求两者都 ≥10（总时长可能只需 10s）。另外计数只在风机输出有效时进行（1284、1290），风机没转不计——合理。判具用的是 DI X15（送风压差）。 ／复核 3/3 推翻 |
| 通风模式开机：回风机压差开关持续闭合10s（若回风机有压差开关） | **[被推翻]** 部分符合 | UserAction.c:1274-1275 `returnInstalled = ((ReturnPressureSwitchSet != 0) && (ReturnFanModeSet != OUTPUT_NONE)) ? 1 : 0;`；UserAction.c:1314-1332 `if(!returnInstalled){returnDelay=0; returnOk=10; returnBad=0;} else if(returnActive){ if(returnDelay<60)returnDelay += uc1s; if(X_14){ returnOk += uc1s; returnBad = 0; } else { returnOk = 0; ... } }`；user.c:534 `X_14 = (InPutStatusSet20)?(X14):(!X14);`；user.c:358 `Parameter_def[454]=1;`（ReturnPressureSwitchSet=User.h:743） | 同上：并行的『两个都满10』，而非规格书的串行两步。代码在『若回风机有压差开关』判据上多加了一个前提：回风机必须配了输出类型（ReturnFanModeSet != OUTPUT_NONE），否则整条当未装处理——这是合理的自决。 ／复核 2/3 推翻 |
| 通风模式开机：压差开关未装时是否跳过该步 | 符合 | UserAction.c:1273 `supplyInstalled = (SupplyPressureSwitchSet != 0) ? 1 : 0;`；UserAction.c:1286-1289 `if(!supplyInstalled){supplyDelay=0; supplyOk=10; supplyBad=0;}`；UserAction.c:1314-1317 同构的 returnInstalled 分支；user.c:1917-1920 `if(PressureProtectEnableSet < 2){ unsigned int installed = PressureProtectEnableSet ? 1 : 0; ... SupplyPressureSwitchSet = installed; ReturnPressureSwitchSet = installed; }` | 未装 ⇒ 计数值预置 10 ⇒ IndoorPressureReady 恒满足 ⇒ 跳过，符合『若有』。**风险提示（不是缺陷）**：[453]/[454] 出厂默认是 1=已装（user.c:357-358），现场没接压差开关又没改参数时，通风开机最后一步会卡在 135 等不到，并在 60s 后由 UserAction.c:1301-1309 置压差故障。规格书未规定默认值，方案是代码自决。 |
| 通风模式开机完成 | 符合 | UserAction.c:1399-1402 `if(!IndoorPressureReady())return 0; *startSeqStep = 0; *startSeqCount = 0; return 1;`；UserAction.c:2313-2315 `SysStep=2; DisinfectStepDisp = 0; DisinfectRunCountDisp = 0; WaterFlowCount=0;`；步骤打点 UserAction.c:1360 `else if(mode == MODE_VENT)stepBase = 130;` + 1370/1384/1397 的 131/132/135 | 开机完成的判据是『状态机退出启动序列并进入 SysStep=2』，与规格书句尾一致。 |
| 通风模式关机指令→（关机链启动） | 符合 | UserAction.c:2320 `if((SysStatus!=TIMERUN)&&(SysStatus!=NORRUN)&&(SysStatus!=11))SysStep=3;`；UserAction.c:2342-2347 `case 3: if(!IndoorStopSnapshotValid)IndoorCaptureStopSnapshot(); IndoorStopHumidifier(); ... if(stopSeqStep == 0){stopSeqStep = 1; stopSeqCount = 0;}` | 通风模式在 case 3 不做加湿器 30s（UserAction.c:2352 IndoorModeUsesHumidifier(MODE_VENT) 恒返回 0，见 551-557），不等外机停机（2361/2364 均以 `UserWorkModeSetTEMP != MODE_VENT` 为条件），也不等制热 60s/制冷 30s（2366-2373 的 HEAT/COOL 分支都不命中）⇒ 约 1 个扫描即 SysStep=4。规格书给通风关机没有这些前置等待，吻合。 |
| 通风模式关机：送风机关闭 | 符合 | UserAction.c:2395-2399 `if(!IndoorApplyReverseFanStop(stopSeqCount)){ stopSeqCount += uc1s; return; }`；UserAction.c:271-337 内：默认分支（排风先开）`supplyStop = 0;`（未赋值）→ UserAction.c:328-329 `supplyOn = (IndoorStopSupplyFanOn && (elapsed < supplyStop)) ? 1 : 0;`（elapsed=0 时即 0）→ UserAction.c:334-335 输出 | 送风机在逆序停的第一拍就断电，是关机链里第一个停的风机。 |
| 通风模式关机：[30s→回风机关闭](若有) | 符合 | UserAction.c:285-288 `if(IndoorFanSequenceReturnPresent){ returnDelay = IndoorFanSequenceReturnStart - IndoorFanSequenceSupplyStart; }`；UserAction.c:319-322 `else if(IndoorStopReturnFanOn){ returnStop = IndoorStopSupplyFanOn ? returnDelay : 0; }`；命令分支 UserAction.c:296 `if(IndoorStopReturnFanOn)returnStop = IndoorStopSupplyFanOn ? returnDelay : 0;`；UserAction.c:330-331 `returnOn = (IndoorStopReturnFanOn && (elapsed < returnStop)) ? 1 : 0;` | 真正的判据是『回风机停机时刻 = 送风机停机时刻 + returnDelay』，而 returnDelay 正是开机侧『送风机→30s→回风机』用的同一个 [132]，默认 30 ⇒ 符合。规格书没写这个 30s 可调，代码自决为 0~90s，不是缺陷。 |
| 通风模式关机：[30s（±60s可调）→排风机关闭](若有) | **[被推翻]** 部分符合 | UserAction.c:290-303 `if(IndoorFanSequenceExhaustPresent){ if(IndoorFanSequenceSupplyStart >= IndoorFanSequenceExhaustStart){ /* Exhaust started first: supply, return, then exhaust stop. */ ... if(IndoorStopExhaustFanOn){ fanDelay = IndoorFanSequenceSupplyStart - IndoorFanSequenceExhaustStart; if(IndoorStopReturnFanOn)exhaustStop = returnStop + fanDelay; else if(IndoorStopSupplyFanOn)exhaustStop = fanDelay; } } else { ...supplyStop = fanDelay; ... returnStop = supplyStop + returnDelay; } }`；UserAction.c:332-333 输出 | 默认配置（[328]=+30，排风机先开）走 292 分支：送风 0s → 回风 30s → 排风 60s，与规格书字面完全一致 ✓。**不符仅出现在把 [328] 设成负值（送风机先开、排风机后开）时**：代码走 305-317 分支，排风机在 elapsed=0 就关、送风机在 fanDelay 才关，停机次序变成『排风→送风→回风』，与规格书字面的『送风→回风→排风』相反。（此时开机次序本就相反，所以『先开的后关』这条规则仍成立。）±60s 的范围问题见第 9 条。 ／复核 2/3 推翻 |
| 通风模式关机：→30s→（风机全停后到关风阀之间的延时） | 符合 | UserAction.c:2403-2407 `IndoorApplyStopSnapshotFanValveState(0, 0, 0, 1); DisinfectStepDisp = 205; DisinfectRunCountDisp = stopSeqCount; stopSeqCount += uc1s; if(stopSeqCount < 30)return;`；UserAction.c:2400-2401 `stopSeqStep = 2; stopSeqCount = 0;`（逆序停完成时清零） | 30s 为硬编码常量（规格书未规定可调）。keepValves=1 保证了这 30s 内风阀保持关机前快照状态不关。 |
| 通风模式关机：新风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2413-2416 `case 5: IndoorStopIndoorLoads(); IndoorClearOutdoorRunCommands(); IndoorApplyFanValveState(0, 0, 0, 0);` → UserAction.c:700 `if(!valveOpen)freshDemand = 0;` → UserAction.c:703 `IndoorOutputByMode(FreshValveModeSet, &FreshValveY14, &FreshValveAO3, 0);` | 阀确实关了、『若有』也处理了；但 5 个风阀在同一扫描一起关，规格书的关闭级差未实现（见下一条）。 ／复核 2/3 推翻 |
| 通风模式关机：排风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2416 `IndoorApplyFanValveState(0, 0, 0, 0);` → UserAction.c:969 `ExhaustValveY11 = (exhaustValveOpen && (ExhaustValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`（=0）；AO 型 UserAction.c:1016-1024 `IndoorSetAO(&ExhaustValveAO6, exhaustValveOpen ? freshDemand : 0);`（=0） | 关断正确，无级差。 ／复核 3/3 推翻 |
| 通风模式关机：回风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2416 → UserAction.c:968 `ReturnValveY07 = (returnValveOpen && (ReturnValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`（=0）；AO 型 UserAction.c:1007-1011 `IndoorSetAO(&ReturnFanAO4, returnValveOpen ? ReturnValveManualVoltSet*10 : 0);`（=0） | 关断正确，无级差。 ／复核 2/3 推翻 |
| 通风模式关机：送风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2416 → UserAction.c:967 `SupplyValveY04 = (supplyValveOpen && (SupplyValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`（=0）；AO 型 UserAction.c:986-990 `IndoorSetAO(&SupplyFanAO7, supplyValveOpen ? SupplyValveManualVoltSet*10 : 0);`（=0） | 关断正确，无级差。 ／复核 3/3 推翻 |
| 通风模式关机：混风阀关闭（若有） | 符合 | UserAction.c:705-709 `if(MixAirValveTypeSet){ IndoorSetAO(&MixValveAO5, valveOpen ? (1000 - freshDemand) : 0); MixValveDemandDisp = MixValveAO5; }`（valveOpen=0 ⇒ 写 0）；UserAction.c:1054-1055 在 keepValves=1 时也会按快照恢复混风阀 | 关断正确（不论 MixAirValveTypeSet 与否，非 0 分支 712 行也写 0）。 |
| 通风模式关机：风阀关闭次序（新风→排风→回风→送风→混风，5 个『→』） | **[被推翻]** 不符合 | UserAction.c:2416 `IndoorApplyFanValveState(0, 0, 0, 0);` 一次调用即把 4 个 DO 阀（967/968/969）与新风阀（700/703）、混风阀（707）在**同一扫描**全部清零；工程内无任何风阀关断级差代码 | 与『开启次序』同性质的缺口。判定不符合。实际风险低（同一拍、开关型无差异），但若现场需要按序关闭（如保压/防倒灌）则不满足。 ／复核 2/3 推翻 |
| 通风模式关机：（关机时排风机和送风机先开的后关） | 符合 | 排风先开的情形 UserAction.c:292-303：exhaustStop 取到最大值（`exhaustStop = returnStop + fanDelay`）⇒ 排风机最后停；送风先开的情形 UserAction.c:308-313：`supplyStop = fanDelay;`（fanDelay = 排风启动时刻 − 送风启动时刻 > 0）而 exhaustStop 保持 0 ⇒ 送风机比排风机后停 | 对『排风机 vs 送风机』这一对，两种配置下『先开的后关』都成立。不合严格逆序的是回风机：在送风先开的配置下停机次序为 排风→送风→回风，而开机次序是 送风→回风→排风，回风机并非『后开先关』。 |
| 通风模式关机完成 | 符合 | UserAction.c:2418 `DisinfectStepDisp = 206;`；UserAction.c:2420-2424 `stopSeqStep=0; stopSeqCount=0; IndoorFanSequenceValid=0; IndoorStopSnapshotValid=0; SysStep=0;` | SysStep 回 0、启动/停机快照都失效，下一次开机（case 0→1，UserAction.c:2286）会重新取快照（IndoorCaptureFanStartTimeline / IndoorCaptureStopSnapshot），无残留。 |
| 参数设置〖排风阀〗『开关（需有相对排风机开关对应延时，无源接点）』 | 未实现 | User.h:617 `#define ExhaustValveDelaySet Parameter[135] /* 0~90s */`；全工程 grep `ExhaustValveDelaySet` 只命中 User.h 与其 .bak，UserSrc/ 下零引用；user.c:246 `Parameter_max[135]=90; Parameter_min[135]=0; Parameter_def[135]=30;`；排风阀与排风机在 UserAction.c:969/972 同一扫描各自置位，无相对延时 | 定义了一把排风阀相对排风机的延时参数 [135] 但从未使用 = 死参数。注意：调试记录_20260916.md §6.8 把规格书『30s（±60s可调）』对应到 [135]，这个映射是错的（真正的 ±可调在 [328] ExhaustFanDelaySet），建议一并更正。本条不完全属于 §3.3.1 的开关机链，但直接影响通风开机第 5、关机第 3 个风阀的动作时刻。 ／复核 1/3 推翻 |
| （规格书没规定、代码自行决定的项）汇总 | **[被推翻]** 无法核对 | user.c:243 `Parameter_max[132]=90; Parameter_min[132]=0; Parameter_def[132]=30;`（开机/关机共用的回风机 30s 被做成可调）；user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90; Parameter_def[328]=30;`（范围与规格书 ±60 不符，见第 9 条）；user.c:357-358 `Parameter_def[453]=1; Parameter_def[454]=1;`（压差开关默认『已装』）；UserAction.c:1369/2416（五个风阀同拍开、同拍关） | 这些规格书都没有明文规定（提取文本里也确认没有对应句子），因此不是『写了没做』的缺陷，只是需要产品确认是否接受：①[132] 回风机间隔可调 0~90s；②[328] 排风机延时范围 ±90s 而非 ±60s；③压差开关 [453]/[454] 默认置 1（已装）；④风阀无开启/关闭级差。 ／复核 3/3 推翻 |
| 无法核对项 | **[被推翻]** 无法核对 | scratch_spec.txt 中本节为纯文本（39536 字节、UTF-8），规格书原 .doc 为 `采用电子膨胀阀定频直膨机控制规格书260124.doc`；本节未附图，文字本身可读，故本节的『若有』判据、时限数值均已逐字核对 | 唯一卡住的地方是『±60s可调』的语义：既可读成『30s 上下各 60s 可调（即 −30~90s）』，也可读成『可调量不超过 ±60s』。这两种读法下代码的 −90~90 都不完全吻合，但『到底是哪一种』无法从文本判定，因此该子项只能给『部分符合』而无法给死结论。 ／复核 3/3 推翻 |

## §3.3.2 制冷开关机

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 制冷开机：制冷模式开机指令→…（进入开机序列，且必须是制冷分支） | 符合 | PSC330RK-V10-内机/User/UserSrc/UserAction.c:2283-2287（case 0：SysStatus==TIMERUN\|\|NORRUN → 2 秒后 SysStep=1）；同文件:2301-2304 `if(UserWorkModeSetTEMP == MODE_COOL){ if(!IndoorCoolingStartSequence(...))return; }`。开机指令来自 Switch.c:146-151 本地开机（HMIOnOffCommandSet==1 且 SysStep==0 → SysStatusLoader=1） | 规格书只写"制冷模式开机指令"，未规定指令来源优先级；代码分本地/远程(远控开关 FarSwitchX02)/PLC(TMOnOffFlag)/定时四种，属规格书没规定、代码自行决定。 |
| →送风阀开启(若有) | 符合 | UserAction.c:1367-1373 开机 step1 `IndoorApplyFanValveState(1, 0, 0, 0);` → :1060-1065 → :931-969 `SupplyValveY04 = (supplyValveOpen && (SupplyValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;` / AO 在 :986-990 | 与新风/回风/排风/混风阀**同一拍**一起开（见下条 150s 前的次序条目），规格书用 → 串起来但未给任何时间间隔。 |
| →新风阀开启（若有） | 符合 | UserAction.c:965 `freshDemand = IndoorApplyFreshAndMixValve(freshValveOpen, forcedFreshDemand);` → :697-703 `IndoorOutputByMode(FreshValveModeSet, &FreshValveY14, &FreshValveAO3, freshDemand);`（DO 或 AO 择一由 FreshValveModeSet 决定） | 同一拍。 |
| →回风阀开启（若有） | 符合 | UserAction.c:968 `ReturnValveY07 = (returnValveOpen && (ReturnValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`；AO 分支 :1007-1011 | 同一拍。 |
| →排风阀开启（若有） | 符合 | UserAction.c:969 `ExhaustValveY11 = (exhaustValveOpen && (ExhaustValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`；AO 分支 :1016-1024 | AO 分支有个规格书没写的额外约束：`else if` 要求 **ExhaustValveModeSet==AO 且 FreshValveModeSet==AO** 才输出 `freshDemand`（规格书说"开度与新风阀开度相等"）。若排风阀选 0-10V 而新风阀选开关量，排风阀 AO 恒为 0。同一拍。 |
| →混风阀开启（若有） | 符合 | UserAction.c:705-714 `if(MixAirValveTypeSet){ IndoorSetAO(&MixValveAO5, valveOpen ? (1000 - freshDemand) : 0); }` | 开度=1000-新风开度，与规格书"开度与新风阀开度之和为1"一致。同一拍。 |
| →150s（无风阀时取10S） | 符合 | UserAction.c:596-600 `static int IndoorValveOpenDelay(void){ if(!IndoorAnyValveConfigured())return 10; return 150; }`；用于 :1373 `if(*startSeqCount >= valveDelay){*startSeqStep = 2; *startSeqCount = 0;}` | "有风阀"判据 = 送/回/排/新风阀任一非 NONE 或 MixAirValveTypeSet（:586-594）。精确等于规格书的 150/10。 |
| {[排风风机启动→30s（±60s可调）] （若有） | 符合 | UserAction.c:147-179 `IndoorFanStartTimeline()`：:167-168 `*supplyStart = (fanInterval > 0) ? fanDelay : 0; *exhaustStart = (fanInterval < 0) ? fanDelay : 0;`（>0 时排风先起、送风延后）；:139-145 `IndoorExhaustFanSignedDelay()` 取 ExhaustFanDelaySet=Parameter[315+13]=Parameter[328]，限幅 ±90；user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90; Parameter_def[328]=30;` | 默认 30s 正确。规格书±60s 对应范围 [-30,90]，代码放到 [-90,90]，比规格书宽；这是参数范围，不是逻辑次序问题。 |
| →送风机启动 | 符合 | UserAction.c:1378 `supplyFan = (*startSeqCount >= IndoorFanSequenceSupplyStart) ? 1 : 0;` → :1383 `IndoorApplyFanValveState(1, supplyFan, returnFan, exhaustFan);` → :970 `UserPumpY00 = (supplyOn && supplyFanConfigured) ? 1 : 0;` | 无排风机时 supplyStart=0，送风机立即启动（:160-164 分支）。 |
| →[30s→回风机启动] （若有） | 符合 | UserAction.c:158-171 `returnDelay = (ReturnFanDelaySet < 0) ? 0 : ReturnFanDelaySet;` `*returnStart = *supplyStart; if(ReturnFanModeSet != OUTPUT_NONE)*returnStart += returnDelay;`；user.c:243 `Parameter_max[132]=90; Parameter_min[132]=0; Parameter_def[132]=30;` | 默认 30s 与规格书一致。规格书未写该段可调，代码把它做成了参数（0~90），属代码自行扩展（多给的可调项，不是缺陷）。 |
| →送风机压差开关持续闭合10s（若送风机有压差开关） | 符合 | UserAction.c:1290-1312 `else if(supplyActive){ … if(X_15){supplyOk += uc1s; supplyBad = 0;} else {supplyOk = 0; …} }`；:1342-1344 `if(supplyOk > 10)supplyOk = 10; SupplyPressureOkDisp = supplyOk;` | 断开即刻清零 → 确实是"持续"10s。**但发现一处不对称风险**：:1273 `supplyInstalled = (SupplyPressureSwitchSet != 0) ? 1 : 0;` 缺了送风机是否配置的判据，而 :1274-1275 回风侧写成 `(ReturnPressureSwitchSet != 0) && (ReturnFanModeSet != OUTPUT_NONE)`。若配了送风压差开关却未配送风机，supplyActive 恒 0 → supplyOk 恒 0 → IndoorPressureReady() 永假 → 开机序列永久卡在 SysStep==1（无超时），即开机永不完成。 |
| →回风机压差开关持续闭合10s（若回风机有压差开关） | 符合 | UserAction.c:1314-1345 `if(X_14){returnOk += uc1s; returnBad = 0;} else {returnOk = 0; …}`；:1345 `ReturnPressureOkDisp = returnOk;`；:1348-1351 `IndoorPressureReady(){ return (SupplyPressureOkDisp >= 10) && (ReturnPressureOkDisp >= 10); }` | 规格书写成"送风10s→回风10s"两段，代码是**与**条件（两者同时≥10）。因为送风风机比回风早 30s 起，实际结果等价（回风够 10s 时送风早已够），但若送风压差开关抖开，会连带把回风已累计的判据拖回 0。 |
| →室外机允许制冷启动 | 符合 | UserAction.c:2313 `SysStep=2;`（开机序列返回 1 后）；:3446-3452 `SysNergryAction_PID(); CompAction();`；CompAction.c:1360 `if(SysStep!=2)return;`；CompAction.c:208-214 `static unsigned char CompStartModeAllowed(void){ if(SysStep != 2)return 0; … }`；实际下发 modulecontrol.c:1136-1151 `ModComp1OnOFFFalg` | 外机侧还有一道独立门 CompAction.c:388-399 `if(CompMinStopCount[2*module+comp] < minStopSeconds)return 0;`（min=max(MinStopTimeSet,120s)，:10 `#define SPEC_COMP_MIN_STOP_SECONDS 120U`）。属 §3.4，不属本节，但会影响"外机真正起"的时刻。 |
| →再热允许启动（若有） | 符合 | UserAction.c:3462-3463 `else if(UserWorkModeSetTEMP != MODE_VENT) HeatControl_UP();`；:2701-2707 守卫 `if((SysStep != 2) \|\| (outputMode == HEAT_OUTPUT_NONE) \|\| …)`；:2663-2669 `if(UserWorkModeSetTEMP == MODE_COOL){ if(ReheatEnableSet){ outputMode = ReheatModeSet; heatAllow = separateStageConflict ? 0 : 1; } }` | "若有"=ReheatEnableSet(再热是否启用) 实现。制冷下 heatAllow 立即可为 1（无"等外机先起"条件，见三级次序条目）。另注意 :2654-2661 separateStageConflict 在**制冷**下也拿 〖制热方式〗HeatModeSet∈{3,4,5} 去判冲突并据此禁掉再热——规格书备注4 的该规则是讲制热与再热/辅热争同一组分段电加热器（代码里 Y00/Y01/Y02 确实共用），属代码自行决定，不改判。 |
| →加湿器允许启动（〖恒温恒湿制冷〗模式时） | 符合 | UserAction.c:3467-3468 `else HumidtCtrl();`（制冷不走 :3464-3466 的 IndoorHeatingStartStage 分支）；HumidtCtrl 守卫 :2516-2518 `humidityModeAllowed = (((UserWorkModeSetTEMP == MODE_COOL) && (CoolControlTypeSet == 1)) \|\| …)`，:2519 `if((SysStep != 2) \|\| (!humidityModeAllowed) \|\| …)` | 〖恒温恒湿制冷〗= CoolControlTypeSet(Parameter[125])==1，User.h:607 注释 "0 constant temperature, 1 constant temperature/humidity"；user.c:236 默认 0。"允许启动"的第二个隐含条件 `HumUseSet==0` 会在 :2524 直接清零 → 使能未开则不允许，符合手册〖加湿方式〗语义。 |
| →制冷开机完成 | 符合 | UserAction.c:2313 `SysStep=2;` 紧接 :2314-2315 `DisinfectStepDisp = 0; DisinfectRunCountDisp = 0;` | 外机命令、再热、加湿在同一 UserAction 拍内被放开（见下条）。 |
| 【核心】"室外机允许制冷启动→再热允许启动→加湿器允许启动"三级次序 | **[被推翻]** 部分符合 | 同一拍放开：UserAction.c:3406 `SysBoardAction();` 里 :2313 置 `SysStep=2` → 紧接着 :3407 的早退守卫 `if((SysStep != 2) \|\| …)` 不成立、:3411 的 SysStep∈{1,3,4,5} 守卫也不成立 → 同一拍内顺序执行 :3446-3452 `SysNergryAction_PID(); CompAction();`（外机）→ :3463 `HeatControl_UP();`（再热）→ :3468 `HumidtCtrl();`（加湿器）。三者的 gating 条件**完全相同**：UserAction.c:2701 `(SysStep != 2)`（再热）、:2519 `(SysStep != 2)`（加湿器）、CompAction.c:1360 `if(SysStep!=2)return;`（外机）。制冷侧**没有**分级变量：`IndoorHeatingStartStage` 只定义在 :24，全部使用点 :3448/:3460/:3465/:3478 都在 MODE_HEAT 分支内（对照 :3464-3466 `(UserWorkModeSetTEMP == MODE_HEAT) && (IndoorHeatingStartStage < 2)` 才挡加湿器）。 | 结论：调用次序与规格书文字顺序一致（外机→再热→加湿器），但 gating 没有创造任何先后，三者是**同一拍一起放开**，间隔为 0。制热侧有 IndoorHeatingStartStage 两级（含热泵辅热要等 IndoorHeatPumpFullLoad()，:2692），制冷侧没有任何对应物。风险：制冷下再热可在外机尚未运行时先满输出——:2768-2772 `else if((long)RealTempDisp * 2L < (long)ControlTempDisp){ demand = 1000; … REHEAT_PID_STATE_FORCE_FULL; }` 会在第一拍直接给 100%；加湿器同理 :2534-2540 `if(HumidtyHandle == 1){ demand = HumidtyValve ? 1000 : 0; }`。规格书没写这三级之间要隔多久，所以"算不算缺陷"取决于是否认为 → 表示必须等外机先跑起来；我按字面次序判部分符合。 ／复核 2/3 推翻 |
| 制冷关机：制冷模式关机指令→…（进入关机序列） | 符合 | UserAction.c:2320 `if((SysStatus!=TIMERUN)&&(SysStatus!=NORRUN)&&(SysStatus!=11))SysStep=3;`；关机指令来源 Switch.c:139-145（HMIOnOffCommandSet==0 → Old_SysStatus=0 → Switch.c:230 `if((Old_SysStatus==0)&&(SysStep>0))SysStatus =STOPING;`） | case 3/4/5 是制冷/制热/通风共用骨架，靠 UserWorkModeSetTEMP 分叉时长；制冷特有部分见下面各条。 |
| →[加湿器关闭→30s] （〖恒温恒湿制冷〗模式且有加湿时） | 符合 | UserAction.c:2344 `IndoorStopHumidifier();`（无条件先关，:529-534 清 HumidValveY13/HumidAO2）；:2347-2359 `if(stopSeqStep == 1){ DisinfectStepDisp = 201; … if(IndoorModeUsesHumidifier((unsigned char)UserWorkModeSetTEMP)){ stopSeqCount += uc1s; if(stopSeqCount < 30)return; } stopSeqStep = 2; stopSeqCount = 0; }` | 关加湿器在 case 3 进入第一拍就执行；30s 在 201 号步骤里等。顺序与规格书一致。 |
| 【核心②】"有加湿"的触发条件：恒温恒湿制冷 **且** 有加湿 | 符合 | UserAction.c:551-557 `static unsigned char IndoorModeUsesHumidifier(unsigned char mode){ if((HumUseSet == 0) \|\| (HumidifyModeSet == HUMID_OUTPUT_NONE))return 0; if((mode == MODE_COOL) && (CoolControlTypeSet == 1))return 1; … }` | 两个条件都齐：〖恒温恒湿制冷〗=CoolControlTypeSet==1，"有加湿"=HumUseSet!=0 且 HumidifyModeSet!=HUMID_OUTPUT_NONE。**规格书没说清的**："有加湿时"未区分"配置了加湿"还是"加湿器当时在运行"；代码取**配置口径**（不看 HumidDemandDisp/HumidValveY13 当时是否为 0），所以恒温恒湿制冷但此刻加湿器没输出，仍会白等 30s。属规格书没规定、代码自行决定。 |
| →再热关闭（若有） | 符合 | UserAction.c:2360 `IndoorStopHeatPreheatLoads();` → :559-579 `Heat1Y00 = 0; Heat2Y01 = 0; Heat3Y02 = 0; … ReheatDemandDisp = 0; IndoorHeatAnalogDemand = 0; IndoorHeatAnalogMode = HEAT_OUTPUT_NONE; IndoorApplyHeatAnalogOutputs(); DA0ValueDisp = 0; ReheatAO0 = 0;`；同时置 DisinfectStepDisp=202（:2362） | 含 Y00/Y01/Y02 开关量、ReheatAO0/DA0 模拟量、以及顺带清预热 Y15/Y16/Y17。在 201 的 30s 之后才执行，与规格书的括号次序一致。 |
| →室外机制冷关机完成 | 符合 | UserAction.c:2361 `if(UserWorkModeSetTEMP != MODE_VENT)IndoorClearOutdoorRunCommands();`（:111-123 清 CompControl/CompVVFfre + IndoorStopCompressors）；:2364 `if((UserWorkModeSetTEMP != MODE_VENT) && (!IndoorOutdoorStopped()))return;`；:125-137 `IndoorOutdoorStopped(){ … if((UnitReadStatus[i].CompStatus[j]==2)\|\|(==3)\|\|(==4))return 0; … return 1; }` | 判据是外机回报的 CompStatus ∈{2 制冷,3 制热,4 除霜}，即真的等外机停稳。注意它**不**检查 CompStatus==1（故障）——故障压机会被直接当作已停，规格书未规定，代码自行决定。 |
| 【核心③】再热关闭 → 室外机制冷关机完成（等外机停稳）→ 30s | 符合 | UserAction.c:2360 `IndoorStopHeatPreheatLoads();` 与 :2361 `IndoorClearOutdoorRunCommands();` 同一拍执行（再热先于外机指令，满足规格书顺序）→ :2364 阻塞等 `IndoorOutdoorStopped()` → :2365-2373 `stopSeqCount += uc1s; if(UserWorkModeSetTEMP == MODE_HEAT){if(stopSeqCount < 60)return;} else if(UserWorkModeSetTEMP == MODE_COOL){ if(stopSeqCount < 30)return; }` → :2376 `SysStep=4;` | 关键点：计数在 `stopSeqCount += uc1s` 之前有 :2364 的 return，所以 30s **只在**外机停稳之后才开始走表，符合"关机完成→30s"。制冷 30s / 制热 60s 与 §3.3.3 制热关机"预热关闭（若有）→60s"一致。**未规定项**：外机若因故障/通信丢失永远报 CompStatus∈{2,3,4} 也永远不返回 1，本段无超时保护（只有 ErrorCheck.c:608-617 在 SysErrStatus 时把 SysStep 跳到 4 兜底）。 |
| →30s→{送风机关闭→…} | 符合 | UserAction.c:2379-2402 case 4：:2395-2399 `if(!IndoorApplyReverseFanStop(stopSeqCount)){ stopSeqCount += uc1s; return; }`，DisinfectStepDisp=203（:2393） | 逆序停风机的整段由 IndoorApplyReverseFanStop（:271-337）一轮轮推进。 |
| 送风机关闭→[30s→回风机关闭](若有) | 符合 | UserAction.c:271-337 `IndoorApplyReverseFanStop()`：默认参数（ExhaustFanDelaySet=30>0 → supplyStart=30/exhaustStart=0，:167-168）走 :292-304 分支 → `supplyStop` 保持 0，`returnStop = IndoorStopSupplyFanOn ? returnDelay : 0;`，:287-289 `returnDelay = IndoorFanSequenceReturnStart - IndoorFanSequenceSupplyStart;` = Parameter[132] = 30。:328-331 `supplyOn = (IndoorStopSupplyFanOn && (elapsed < supplyStop)) ? 1 : 0;` `returnOn = …(elapsed < returnStop)…` | 实测/推演时序：elapsed=0 送风停、elapsed=30 回风停。30s 来自参数 Parameter[132]（默认 30，范围 0~90）。无回风机时 `IndoorStopReturnFanOn=0` → 该段直接跳过。 |
| →[30s（±60s可调）→排风风机关闭](若有) | 符合 | UserAction.c:296-303 `if(IndoorStopExhaustFanOn){ fanDelay = IndoorFanSequenceSupplyStart - IndoorFanSequenceExhaustStart; if(IndoorStopReturnFanOn)exhaustStop = returnStop + fanDelay; else if(IndoorStopSupplyFanOn)exhaustStop = fanDelay; }` | 默认 exhaustStop = 30 + 30 = 60（从送风停算起），即送风→30s→回风→30s→排风，与规格书嵌套括号读法一致；无回风机时 exhaustStop=30。间隔来自 Parameter[328]（默认 30，可调 -90~90，规格书为 30±60）。 |
| →30s（停风机后再 30s） | 符合 | UserAction.c:2403-2410 `IndoorApplyStopSnapshotFanValveState(0, 0, 0, 1); DisinfectStepDisp = 205; … stopSeqCount += uc1s; if(stopSeqCount < 30)return; stopSeqStep=0; stopSeqCount=0; SysStep=5;` | 固定 30s，无参数。风机已全停（keepValves=1 只保阀、不保风机），阀门按快照继续保持原开度——符合规格书"阀最后才关"。 |
| →新风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2413-2416 case 5 `IndoorStopIndoorLoads(); IndoorClearOutdoorRunCommands(); IndoorApplyFanValveState(0, 0, 0, 0);` → :1060-1065 → :965 → :703 `IndoorOutputByMode(FreshValveModeSet, &FreshValveY14, &FreshValveAO3, 0)` | 会关，但不是按规格书列的先后逐个关：新风/排风/回风/送风/混风 **全部在 case 5 进入的那一拍一起关**，五个阀门之间没有任何 gating 或延时。拆条列出只为对齐规格书句子，实质是同一条实现。 ／复核 2/3 推翻 |
| →排风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2416 `IndoorApplyFanValveState(0, 0, 0, 0);` → :969 `ExhaustValveY11 = (exhaustValveOpen && (ExhaustValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`（此时 exhaustValveOpen=0）；AO 在 :1016-1024 被清 0 | 与其余四个阀同拍，无先后。 ／复核 2/3 推翻 |
| →回风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2416 → :968 `ReturnValveY07 = (returnValveOpen && …) ? 1 : 0;`；AO 在 :997-1015 被清 0 | 与其余四个阀同拍，无先后。 ／复核 2/3 推翻 |
| →送风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2416 → :967 `SupplyValveY04 = (supplyValveOpen && (SupplyValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`；AO 在 :975-994 被清 0 | 与其余四个阀同拍，无先后。 ／复核 2/3 推翻 |
| →混风阀关闭（若有） | **[被推翻]** 部分符合 | UserAction.c:2416 → :705-714 `IndoorSetAO(&MixValveAO5, valveOpen ? (1000 - freshDemand) : 0);`，valveOpen=0 → MixValveAO5=0、MixValveDemandDisp=0 | 与其余四个阀同拍，无先后。 ／复核 2/3 推翻 |
| （关机时排风机和送风机先开的后关） | 符合 | UserAction.c:290-304：`if(IndoorFanSequenceSupplyStart >= IndoorFanSequenceExhaustStart){ /* Exhaust started first: supply, return, then exhaust stop. */ supplyStop 保持 0；exhaustStop = returnStop + fanDelay; }`；:323-333 `stopEnd = …max…` 后 `supplyOn/returnOn/exhaustOn = elapsed < 对应 stop 时刻` | 默认（排风先起 30s）得到 送风停@0 → 回风停@30 → 排风停@60，即"先起的排风最后停"，与规格书一致。**边界**：若操作者把 ExhaustFanDelaySet 设成负值（开机先起送风），代码走 :306-317 分支，停止次序反成 排风@0 → 送风@30 → 回风@60，仍满足"先开的后关"，但违反规格书写的固定次序（送风→回风→排风）；此时回风与排风同刻启动却最后停，连"先开的后关"也不严格成立。规格书没写负延时这种配置，代码自行支持了它。 |
| →制冷模式关机完成 | 符合 | UserAction.c:2417-2424 `if(!RemoteInterlockResumeFlag)SysStatusLoader=0; DisinfectStepDisp = 206; DisinfectRunCountDisp = 0; stopSeqStep=0; stopSeqCount=0; IndoorFanSequenceValid=0; IndoorStopSnapshotValid=0; SysStep=0;` | 回到 case 0 可再次开机（Switch.c:148 要求 SysStep==0 才允许置 SysStatusLoader）。 |
| （无对应规格书句子）消排毒路径里的同一套关机码 | **[被推翻]** 无法核对 | PSC330RK-V10-内机/User/UserSrc/UserAction.c:1661-1723 用 DisinfectStepDisp=210/211/212/214 复刻了 case 3/4/5 的全套 制冷/制热关机（210 加湿30s / 211 外机停稳+30或60s / 212 逆序停风机 / 214 再30s+关阀），但判据用 `preStopMode`（LastNormalModeSet）而不是当前 `UserWorkModeSetTEMP`（对照 :2352 用 UserWorkModeSetTEMP）。 | 这是"进入消排毒前先按原模式关机"的 §3.3.4 需求，不属 §3.3.2 正文；但两份实现是**复制**关系（时长、次序、步号规则都各写一遍，步号 201/202/203/205 与 210/211/212/214 不同），后续任一边改动都可能漂移。判"无法核对"是因为规格书本节没有覆盖它，不是代码有问题。 ／复核 3/3 推翻 |

## §3.3.3 制热开关机

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 制热模式开机指令→ | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/Switch.c:146-149 `if(HMIOnOffCommandSet==1){if(SysStep==0){SysStatusLoader = 1;Old_SysStatus=1;...}}`；Switch.c:111 `else SysStatus=NORRUN;`（来电自启）；d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/UserAction.c:2283-2286 `case 0: if((SysStatus==TIMERUN)\|\|(SysStatus==NORRUN)){WaterFlowCount +=uc1s; if(WaterFlowCount>1){...SysStep=1;...}}` | 开机指令统一表现为 SysStatus 变 NORRUN/TIMERUN，再由 SysStep 0→1 进开机序列；这是制热/制冷/通风共用入口，制热本身没有单独的开机触发标志。 |
| 送风阀开启（若有）→新风阀开启（若有）→回风阀开启（若有）→排风阀开启（若有）→混风阀开启（若有） | **[被推翻]** 部分符合 | UserAction.c:1367-1374 step1 `IndoorApplyFanValveState(1, 0, 0, 0); DisinfectStepDisp = stepBase + 1;` → 1060-1065 `IndoorApplyFanValveStateDetailed(valveOpen,valveOpen,valveOpen,valveOpen, ..., -1)` → 931-972：965 `freshDemand = IndoorApplyFreshAndMixValve(freshValveOpen, forcedFreshDemand);`（新风阀+混风阀）、967 `SupplyValveY04 = ...`、968 `ReturnValveY07 = ...`、969 `ExhaustValveY11 = ...` | 五阀在同一次函数调用内全部置位，写端口次序是 新风/混风→送风阀→回风阀→排风阀，与规格书写明的 送风→新风→回风→排风→混风 不同；但同处一个扫描周期，无任何时间差，实际等效同时开启（规格书这几步之间也没写延时）。另注：新风阀/混风阀走的是调节开度而非全开 —— 697-715 IndoorApplyFreshAndMixValve 在 forcedDemand<0 时取 662-695 IndoorFreshValveDemand() 的控制律值（压差/CO2/焓差/手动），混风阀=1000-该值；而送风阀/回风阀 AO 走的是手动电压值(988-989、1004-1005)。规格书此处只写『开启』，未写明是全开还是开到调节开度 → 这部分属规格书未规定。 ／复核 3/3 推翻 |
| →150s（无风阀时取10S）→ | 符合 | UserAction.c:596-600 `static int IndoorValveOpenDelay(void){ if(!IndoorAnyValveConfigured())return 10; return 150; }`；586-594 IndoorAnyValveConfigured 检查 SupplyValveModeSet/ReturnValveModeSet/ExhaustValveModeSet/FreshValveModeSet/MixAirValveTypeSet；2299 `valveDelay = (unsigned int)IndoorValveOpenDelay();`；1373 `if(*startSeqCount >= valveDelay){*startSeqStep = 2; *startSeqCount = 0;}` | 『无风阀』判定为五种阀一个都没配；若只配了部分风阀仍取150s。规格书对『部分配置』没细分 → 属规格书未规定。 |
| {[排风风机启动→30s（±60s可调）] （若有）→送风机启动→[30s→回风机启动] （若有）} | **[被推翻]** 部分符合 | UserAction.c:1376-1391 step2 `supplyFan = (*startSeqCount >= IndoorFanSequenceSupplyStart) ? 1 : 0; exhaustFan = (IndoorFanSequenceExhaustPresent && (*startSeqCount >= IndoorFanSequenceExhaustStart)) ? 1 : 0; returnFan = ... IndoorApplyFanValveState(1, supplyFan, returnFan, exhaustFan);`；时刻表 147-179 `*supplyStart = (fanInterval > 0) ? fanDelay : 0; *exhaustStart = (fanInterval < 0) ? fanDelay : 0; *returnStart = *supplyStart; if(ReturnFanModeSet != OUTPUT_NONE)*returnStart += returnDelay;`；默认值 user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90; Parameter_def[328]=30;`、user.c:243 `Parameter_max[132]=90; Parameter_min[132]=0; Parameter_def[132]=30;` | 默认值(排风间隔30、回风间隔30)时顺序与规格书完全一致：排风先开→30s→送风→30s→回风。『若有』由 ExhaustFanModeSet!=OUTPUT_NONE(173-175) 与 ReturnFanModeSet!=OUTPUT_NONE(176-178) 实现。ExhaustFanDelaySet 默认+30 表示排风先开，负值表示送风先开 —— 规格书 §3.3 只写正值语义，负值是代码自行扩展（点位表有记录）。 ／复核 2/3 推翻 |
| 30s（±60s可调）   ← 排风风机与送风机之间的间隔 | **[被推翻]** 不符合 | UserAction.c:139-145 `signed int delay = ExhaustFanDelaySet; if(delay > 90)delay = 90; else if(delay < -90)delay = -90;`；user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90;` | 规格书给的可调范围是 ±60s，代码允许并钳位到 ±90s，值域超出规格书。同项目里消排毒路径的同类间隔在 UserAction.c:247-257 `if(fanDelay > 60)fanDelay = 60;` 是钳到 60 的 —— 两条路径不一致，正常开关机路径宽了 50%。 ／复核 3/3 推翻 |
| 送风机压差开关持续闭合10s（若送风机有压差开关） | 符合 | UserAction.c:1273 `supplyInstalled = (SupplyPressureSwitchSet != 0) ? 1 : 0;`；1286-1299 `if(!supplyInstalled){supplyDelay=0; supplyOk=10; supplyBad=0;} else if(supplyActive){if(supplyDelay < 60)supplyDelay += uc1s; if(X_15){supplyOk += uc1s; supplyBad = 0;}}`；1342 `if(supplyOk > 10)supplyOk = 10;`；1348-1350 `IndoorPressureReady(){return (SupplyPressureOkDisp >= 10) && (ReturnPressureOkDisp >= 10);}`；1399 `if(!IndoorPressureReady())return 0;` | 未配压差开关时直接置 supplyOk=10（等效立即通过），对应『若有』。10 是字面常量，规格书也没写可调。 |
| 回风机压差开关持续闭合10s（若回风机有压差开关） | **[被推翻]** 部分符合 | UserAction.c:1274-1275 `returnInstalled = ((ReturnPressureSwitchSet != 0) && (ReturnFanModeSet != OUTPUT_NONE)) ? 1 : 0;`；1314-1325 `if(!returnInstalled){returnDelay=0; returnOk=10; returnBad=0;} else if(returnActive){if(returnDelay < 60)returnDelay += uc1s; if(X_14){returnOk += uc1s; returnBad = 0;}}`；1350 `return (SupplyPressureOkDisp >= 10) && (ReturnPressureOkDisp >= 10);` | 代码是两个计数器并行累计、取『两者都≥10』的与条件；规格书写的是『送风机压差10s → 回风机压差10s』串行两段。因为回风机本身晚30s启动，实际等待≈回风压差闭合后10s，效果接近；但若严格按规格书串行读，应是最长 20s。实现形式与规格书不同。 ／复核 2/3 推翻 |
| →预热允许启动（若有）→ | 符合 | UserAction.c:3445 `if(UserWorkModeSetTEMP == MODE_HEAT)PreHeatControl();`（在 stage 判定之前，无 IndoorHeatingStartStage 门控）；3102-3132 `void PreHeatControl(void)` 自身条件 3116-3121 `if((PreheatModeSet == HEAT_OUTPUT_NONE) \|\| PreheatFaultLatchDisp \|\| (SysStep != 2) \|\| (UserWorkModeSetTEMP != MODE_HEAT) \|\| ...) {PreHeat1Y15 = 0;... IndoorPreheatAnalogDemand = 0; IndoorPreheatAnalogMode = HEAT_OUTPUT_NONE; ... return;}` | 预热只被 SysStep==2 门控，而 SysStep==2 是压差满足后(1399→2313)才置位的 ⇒ 预热确实在『压差10s通过』之后、且在『制热允许启动』之前放开，次序与规格书一致。『若有』=PreheatModeSet != HEAT_OUTPUT_NONE。 |
| →制热允许启动（〖制热方式〗为热泵制热时，室外机允许启动…） | 符合 | UserAction.c:3446-3452 `if((UserWorkModeSetTEMP == MODE_COOL) \|\| ((UserWorkModeSetTEMP == MODE_HEAT) && (HeatModeSet == 1) && (IndoorHeatingStartStage >= 1))){SysNergryAction_PID(); CompAction();} else{IndoorClearOutdoorRunCommands();}`（3453-3456）；111-123 `IndoorClearOutdoorRunCommands(){... UnitWriteStatus[i].CompControl[j]=0; UnitWriteStatus[i].CompVVFfre[j]=0; ... IndoorStopCompressors();}` | HeatModeSet==1 即热泵制热(User.h:610)。仅在 IndoorHeatingStartStage>=1 才向 330 下发 CompControl/CompVVFfre；stage==0 与整个开机序列(SysStep!=2，3411 提前 return)期间外机命令恒为 0。 |
| →制热允许启动（…辅热允许启动） | **[被推翻]** 部分符合 | UserAction.c:3459-3463 `if((UserWorkModeSetTEMP == MODE_HEAT) && (IndoorHeatingStartStage == 0))IndoorStopMainHeatLoads(); else if(UserWorkModeSetTEMP != MODE_VENT)HeatControl_UP();`；2688-2693 `if(ReheatEnableSet && (ReheatModeSet != HEAT_OUTPUT_NONE)){outputMode = ReheatModeSet; heatAllow = separateStageConflict ? 0 : IndoorHeatPumpFullLoad();}`；2701 守卫 `if((SysStep != 2) \|\| (outputMode == HEAT_OUTPUT_NONE) \|\| MainHeatFaultLatchDisp \|\| ...)`；733-762 IndoorHeatPumpFullLoad 要求每个可用压缩机 `if(UnitReadStatus[i].CompStatus[j] != 3)return 0;` | 辅热确实延到 stage>=1 才放开（stage==0 时走 IndoorStopMainHeatLoads()，536-549 清 Heat1Y00/Heat2Y01/Heat3Y02 与制热 AO），但热泵制热下还额外要求 IndoorHeatPumpFullLoad()（所有可用压缩机都已进入制热=3）才真正输出 —— 规格书 §3.3.3 没有这条门控，它来自 §3.6/§3.1 备注3B 的实现。属『规格书没规定、代码自行决定』的加严。 ／复核 3/3 推翻 |
| →制热允许启动（〖制热方式〗非热泵制热时，制热=电加热） | 符合 | UserAction.c:2695-2699 `else if((HeatModeSet >= 2) && (HeatModeSet <= 5)){outputMode = HeatModeSet - 1; heatAllow = 1;}`；3446-3456 非热泵制热走 else 分支 `IndoorClearOutdoorRunCommands();`（外机永不下发）；2701 守卫仍要求 `(SysStep == 2)` | User.h:610 `HeatModeSet` 定义 0 none/1 heat pump/2 AO/3 1:2/4 1:2:4/5 SCR；>=2 为电加热，外机保持关闭、电加热由 HeatControl_UP 在 stage>=1 放开，与规格书括号『热泵制热时才有室外机+辅热』一致。 |
| →加湿器允许启动（〖恒温恒湿制热〗模式时）→ | 符合 | UserAction.c:3464-3468 `if((UserWorkModeSetTEMP == MODE_HEAT) && (IndoorHeatingStartStage < 2))IndoorStopHumidifier(); else HumidtCtrl();`；2516-2519 `humidityModeAllowed = (((UserWorkModeSetTEMP == MODE_COOL) && (CoolControlTypeSet == 1)) \|\| ((UserWorkModeSetTEMP == MODE_HEAT) && (HeatControlTypeSet == 1))) ? 1 : 0; if((SysStep != 2) \|\| (!humidityModeAllowed) \|\| ...)` | User.h:608 `HeatControlTypeSet Parameter[126]  /* 0 constant temperature, 1 constant temperature/humidity */` ⇒ HeatControlTypeSet==1 就是〖恒温恒湿制热〗。恒温制热下 HumidtCtrl 只清加湿输出，与规格书『此步骤仅在恒温恒湿时』一致。 |
| →制热开机完成 | 符合 | UserAction.c:2313-2316 `SysStep=2; DisinfectStepDisp = 0; DisinfectRunCountDisp = 0; WaterFlowCount=0;` | SysStep==2 是全系统『运行中』状态；预热/制热/加湿/PID/风机全都以 SysStep==2 为前置。没有额外的『开机完成』标志位。DisinfectStepDisp 在开机阶段用 stepBase+1/+2/+5 = 121/122/125 表示制热（1358-1360 `else if(mode == MODE_HEAT)stepBase = 120;`）。 |
| 【核对提示③的『分级变量』】预热允许启动→制热允许启动→加湿器允许启动 这三级 | **[被推翻]** 部分符合 | UserAction.c:24 `static unsigned char IndoorHeatingStartStage = 0;`；3448 `(IndoorHeatingStartStage >= 1)` 放开 PID+CompAction；3460 `(IndoorHeatingStartStage == 0)` 压住主制热；3465 `(IndoorHeatingStartStage < 2)` 压住加湿器；3475-3478 `if((UserWorkModeSetTEMP == MODE_HEAT) && (SysStep == 2) && !SysErrStatus && !ModUrgencyStop && !RemoteInterlockStopFlag && !IndoorSensorPartialStopFlag && (IndoorHeatingStartStage < 2)) IndoorHeatingStartStage++;`（无任何计时）；主循环 d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/src/main.c:21-26 `while (1){ SystemAction(); User(); }`；stage 复位 3394 / 3407-3409 / 3425 | 三级的次序是真的（stage0 只有预热在输出，stage1 加外机+辅热，stage2 加加湿），但每级只持续1个扫描周期：自增处没有任何计时器，主循环是自由跑的 while(1) ⇒ 三级在几毫秒内全部放开。规格书没给这三步之间的间隔时长 ⇒ 属『规格书没规定、代码自行决定』；但若设计意图是『预热先跑一段再上制热』，当前实现没有这个间隔。另：主循环里 3388-3390 `InitCount +=uc1s; if(InitCount>=10)InitCount=20; if(InitCount<5)return;` 只影响上电前5秒。 ／复核 3/3 推翻 |
| 【分级有效性的关键旁证】stage==0 那一拍预热输出不会被『压住主制热』的动作清掉 | 符合 | UserAction.c:3445 PreHeatControl() 先跑、3459-3461 IndoorStopMainHeatLoads() 后跑；536-549 IndoorStopMainHeatLoads 只清 Heat1Y00/Heat2Y01/Heat3Y02、HeaterFlag、IndoorHeatAnalog*、ReheatPid*、`IndoorApplyHeatAnalogOutputs()`，不碰 PreHeat1Y15/2Y16/3Y17 与 IndoorPreheatAnalog*；435-496 IndoorApplyHeatAnalogOutputs 在 462-464 清完 AO 后，486-495 `if(preheatActive){... IndoorSetAO(&PreheatAO1, preheatDemand);}` 又把预热原样重放（preheatActive 取自 459-460 `IndoorPreheatAnalogMode/Demand`，正是 PreHeatControl 刚设的） | 这一步是三级次序能成立的前提：即便 stage==0 只跑一拍，预热在该拍的输出是真实有效的（DO 与 AO 都保留）。 |
| 制热模式关机指令→ | 符合 | Switch.c:139-149 `if(HMIOnOffCommandSet==0){Old_SysStatus = 0; TouchScreenONOFF=0; RemoteInterlockResumeFlag=0; SysStatusLoader=0;}`；UserAction.c:2320 `case 2: if((SysStatus!=TIMERUN)&&(SysStatus!=NORRUN)&&(SysStatus!=11))SysStep=3;`；2292-2297 case 1 同向 `SysStep=3;` | 关机不区分模式，统一先进 SysStep=3（2429 行内 SysBoardAction 的公共段 2342-2377）。 |
| →加湿器关闭（〖恒温恒湿制热〗模式时）→ | 符合 | UserAction.c:2344 `IndoorStopHumidifier();`（SysStep==3 每一拍都执行）；529-534 `static void IndoorStopHumidifier(void){ HumidDemandDisp = 0; HumidValveY13 = 0; IndoorSetAO(&HumidAO2, 0); }` | 代码无条件关加湿器、不判模式，比规格书更宽（制冷规格书本来就要求关加湿器；通风本就不会开）。不算缺失。 |
| →30s→（加湿器关闭之后） | 符合 | UserAction.c:2347-2358 `if(stopSeqStep == 0){stopSeqStep = 1; stopSeqCount = 0;} if(stopSeqStep == 1){DisinfectStepDisp = 201; DisinfectRunCountDisp = stopSeqCount; if(IndoorModeUsesHumidifier((unsigned char)UserWorkModeSetTEMP)){stopSeqCount += uc1s; if(stopSeqCount < 30)return;} stopSeqStep = 2; stopSeqCount = 0;}`；551-557 `IndoorModeUsesHumidifier`: `if((HumUseSet == 0) \|\| (HumidifyModeSet == HUMID_OUTPUT_NONE))return 0; if((mode == MODE_HEAT) && (HeatControlTypeSet == 1))return 1;` | 30s 只在『该模式真的配了加湿器』时等待，正好对应规格书『恒温恒湿制热模式时』（外加『加湿方式≠无 且 启用加湿』两个前提）。30 是字面常量，未参数化 —— 规格书此处也没标可调。 |
| →制热关闭（〖制热方式〗为热泵制热时，室外机及辅热关闭）→ | 符合 | UserAction.c:2360 `IndoorStopHeatPreheatLoads();`（辅热侧 559-563 `Heat1Y00 = 0; Heat2Y01 = 0; Heat3Y02 = 0; HeaterFlag = 0; ... IndoorHeatAnalogDemand = 0; IndoorHeatAnalogMode = HEAT_OUTPUT_NONE; IndoorHeatScrRegion = 0; IndoorApplyHeatAnalogOutputs(); DA0ValueDisp = 0;`）；2361 `if(UserWorkModeSetTEMP != MODE_VENT)IndoorClearOutdoorRunCommands();` | 外机是『清命令』而非等回读确认：111-123 把 UnitWriteStatus[i].CompControl[j]=0、CompVVFfre[j]=0；真正停稳由 2364 等（见下条）。DisinfectStepDisp=202。 |
| →预热关闭（若有）→  ← 规格书这里是独立两级（制热关闭 与 预热关闭 分开） | **[被推翻]** 部分符合 | UserAction.c:2360 是 IndoorStopHeatPreheatLoads() 的唯一调用点；函数体 559-579 里 561-562 清 Heat1Y00/Heat2Y01/Heat3Y02（制热/辅热）与 568-572 清 `PreHeat1Y15 = 0; PreHeat2Y16 = 0; PreHeat3Y17 = 0; PreheatDemandDisp = 0; IndoorPreheatAnalogDemand = 0; IndoorPreheatAnalogMode = HEAT_OUTPUT_NONE; IndoorPreheatScrRegion = 0;` 在同一个函数、同一拍完成 | 你的提示②命中了：预热关闭没有独立步骤，被合并进『制热关闭』同一次调用。规格书这两步之间也没写延时，所以功能上不丢东西；但若工艺要求预热晚于制热断开（例如先让辅热顶住再撤预热），当前代码做不到，两次断电/上电时序上也没有可插入的位置。 ／复核 3/3 推翻 |
| 制热关闭（外机及辅热）→预热关闭 的先后次序 | 部分符合 | UserAction.c:2360（辅热+预热一起清）位于 2361（`IndoorClearOutdoorRunCommands()` 清外机 CompControl）之前 | 执行先后与规格书相反：规格书是『外机及辅热关闭 → 预热关闭』，代码是『辅热+预热 → 外机命令清零』。同处一个扫描周期，命令时间戳实际相同，不产生可观测差异。 ／复核 1/3 推翻 |
| →60s→（制热，紧接预热关闭之后） | 符合 | UserAction.c:2365-2373 `stopSeqCount += uc1s; if(UserWorkModeSetTEMP == MODE_HEAT){ if(stopSeqCount < 60)return; } else if(UserWorkModeSetTEMP == MODE_COOL){ if(stopSeqCount < 30)return; }` | 你的提示③命中了：制热 60s / 制冷 30s 明确分开写，且都是字面常量（未引用任何 Parameter[]）。通风模式不走这两个分支、无等待，也符合规格书（通风关机没有这一段）。 |
| 60s 的起算点（规格书：预热关闭之后起算） | 部分符合 | UserAction.c:2364 `if((UserWorkModeSetTEMP != MODE_VENT) && (!IndoorOutdoorStopped()))return;` 位于 2365 `stopSeqCount += uc1s;` 之前；125-137 `IndoorOutdoorStopped(){... if((UnitReadStatus[i].CompStatus[j]==2)\|\|(UnitReadStatus[i].CompStatus[j]==3)\|\|(UnitReadStatus[i].CompStatus[j]==4))return 0; ...return 1;}` | 代码先无限期等外机真的停稳（读 330 回读的 CompStatus 离开 2/3/4）再开始计 60s；规格书的 60s 从『预热关闭』起算。结果只会比规格书更晚进停风机、不会更早 —— 属规格书未规定的额外保护，但会让制热关机总时长超过规格书字面值（外机化霜/卸载时可能拖很久）。 ／复核 0/3 推翻 |
| →{送风机关闭→[30s→回风机关闭](若有)→[30s（±60s可调）→排风风机关闭](若有) } | 部分符合 | UserAction.c:2390-2402 `if(stopSeqStep == 0){stopSeqStep = 1; stopSeqCount = 0;} if(stopSeqStep == 1){DisinfectStepDisp = 203; ... if(!IndoorApplyReverseFanStop(stopSeqCount)){stopSeqCount += uc1s; return;} ...}`；实现 271-337，按启动时间轴反推停止时刻（287-288 `returnDelay = IndoorFanSequenceReturnStart - IndoorFanSequenceSupplyStart;`） | 停机顺序=启动顺序的反向，间隔沿用启动时用的 ExhaustFanDelaySet/ReturnFanDelaySet ⇒ 排风那段的间隔同样可到 ±90s，继承 Entry『30s（±60s可调）』的 不符合。只停『关机瞬间确实在转』的风机（204-230 IndoorCaptureStopSnapshot 的 IndoorStop*FanOn），与规格书『(若有)』一致。 ／复核 1/3 推翻 |
| （关机时排风机和送风机先开的后关） | 符合 | UserAction.c:292-303 `if(IndoorFanSequenceSupplyStart >= IndoorFanSequenceExhaustStart){ /* Exhaust started first: supply, return, then exhaust stop. */ if(IndoorStopReturnFanOn)returnStop = IndoorStopSupplyFanOn ? returnDelay : 0; if(IndoorStopExhaustFanOn){fanDelay = IndoorFanSequenceSupplyStart - IndoorFanSequenceExhaustStart; if(IndoorStopReturnFanOn)exhaustStop = returnStop + fanDelay; else if(IndoorStopSupplyFanOn)exhaustStop = fanDelay;}}`；对照 305-317 的『supply started first』反向分支 | 默认 排风间隔=+30 时：启动 排风→送风→回风，停止 送风→回风→排风，正好是先开的后关。若把 ExhaustFanDelaySet 设成负数（送风先开），停止次序随之反转 —— 规格书 §3.3 未提负值语义，属代码自行扩展。 |
| →30s→（风机全部停完之后） | 符合 | UserAction.c:2403-2407 `IndoorApplyStopSnapshotFanValveState(0, 0, 0, 1); DisinfectStepDisp = 205; DisinfectRunCountDisp = stopSeqCount; stopSeqCount += uc1s; if(stopSeqCount < 30)return;` | 这 30s 里只停风机、风阀仍按关机快照保持开着（第4个实参 keepValves=1）⇒ 风阀留到下一步才关，与规格书『…}→30s→新风阀关闭…』一致。DisinfectStepDisp 在 203→205 之后才到 206。 |
| →新风阀关闭（若有）→排风阀关闭（若有）→回风阀关闭（若有）→送风阀关闭（若有）→混风阀关闭（若有）→ | **[被推翻]** 部分符合 | UserAction.c:2413-2416 `case 5: IndoorStopIndoorLoads(); IndoorClearOutdoorRunCommands(); IndoorApplyFanValveState(0, 0, 0, 0);`（2418 `DisinfectStepDisp = 206;`）；1060-1065→931-972 关门写端口次序 965(新风+混风 InterApplyFreshAndMixValve 置 0)→967(SupplyValveY04=0)→968(ReturnValveY07=0)→969(ExhaustValveY11=0) | 五阀在同一拍全部置 0，无先后延时；规格书给的关闭次序（新风→排风→回风→送风→混风）在代码里体现不出来。规格书这几步之间同样没写延时，所以只是『次序不可观测』而非功能缺失。 ／复核 2/3 推翻 |
| →制热关机完成（关机时排风机和送风机先开的后关） | 符合 | UserAction.c:2413-2425 `case 5: IndoorStopIndoorLoads(); IndoorClearOutdoorRunCommands(); IndoorApplyFanValveState(0, 0, 0, 0); ... IndoorFanSequenceValid=0; IndoorStopSnapshotValid=0; SysStep=0;` | SysStep 回 0 = 完全停机。另：SysStep==3 里 2380-2386 有一条 240 分支 `if(SysErrStatus == 1){IndoorEmergencyStopLoads(); DisinfectStepDisp = 240; break;}` —— 故障急停（规格书另节的『紧急停机』），不在 §3.3.3 正常关机路径内。 |
| 制热关机的 60s / 制冷关机的 30s 是否为可调参数 | 符合 | UserAction.c:2366-2373 中的 60 与 30 均为字面常量，函数内未引用任何 Parameter[]；对比同项目里确实可调的间隔（user.c:273 Parameter[328] -90..90、user.c:243 Parameter[132] 0..90 带 Parameter_max/min/def） | 规格书对这两处都只写固定值、未标『可调』，硬编码是正确的。而风扇间隔规格书标了『±60s可调』、代码做成参数 —— 参数化的取舍方向与规格书一致，只是值域给宽了（见前面 不符合 那条）。 |
| （旁证）消排毒模式『执行当前运行模式下的关机逻辑』是否复用同一套制热关机 | 符合 | UserAction.c:1661-1693 step10/step11 与 SysStep==3 的 2344-2373 同构：1663-1671 `IndoorStopHumidifier(); IndoorApplyStopSnapshotFanValveState(1, 1, 1, 1); ... if(IndoorModeUsesHumidifier(preStopMode)){count += uc1s; if(count < 30)return;}` → 1677-1682 `IndoorStopHeatPreheatLoads(); if(preStopMode != MODE_VENT)IndoorClearOutdoorRunCommands(); ... if((preStopMode != MODE_VENT) && (!IndoorOutdoorStopped()))return;` → 1683-1690 `if(preStopMode == MODE_HEAT){if(count < 60)return;} else if(preStopMode == MODE_COOL){if(count < 30)return;}` | 属 §3.3.4 范围，此处只作旁证：预热关闭同样被合并进 IndoorStopHeatPreheatLoads，60s/30s 同样按制热/制冷区分，且这里用的是 preStopMode（关机前的模式）而不是当前模式。DisinfectStepDisp 用 201/202 对应 210/211。 |

## §3.3.4 消排毒开关机

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 选择【消毒模式】时：执行当前运行模式下的关机逻辑 | 符合 | UserAction.c:1634-1659，step==0 分支：`if(SysStep == 0){...}else if(!IndoorStopSnapshotValid){IndoorCaptureStopSnapshot();}` `step = (SysStep == 0) ? 1 : 10;`；1632 `if(SysStep > 0)IndoorStopCompressors();` | 进入 step 10 后复用关机分段 201/202/203/205 走完再进 step 1，逻辑链完整。 |
| （若机组原处于关机状态，此逻辑忽略） | 符合 | UserAction.c:1647-1659：`if(SysStep == 0){ IndoorStopSnapshotValid = 0; IndoorStopHumidifier(); IndoorStopHeatPreheatLoads(); IndoorClearOutdoorRunCommands(); IndoorApplyFanValveState(0, 0, 0, 0);} ... step = (SysStep == 0) ? 1 : 10;` | 规格书未规定、代码自行决定：即便原为关机态，仍顺手清一遍加湿器/加热/外机指令/风阀（此时本就是关闭态，属无害幂等）。 |
| 关机逻辑用的是“当前运行模式”（消毒前的模式） | 符合 | UserAction.c:1630 `if(lastMode <= MODE_VENT)LastNormalModeSet = lastMode;`、1636-1637 `if(LastNormalModeSet > MODE_VENT)LastNormalModeSet = MODE_COOL; preStopMode = (unsigned char)LastNormalModeSet;`，1667 `if(IndoorModeUsesHumidifier(preStopMode))` | 对比常规关机 UserAction.c:2352 用的是 `UserWorkModeSetTEMP`；消排毒路径改用 preStopMode 反而更贴合“当前运行模式”，不是缺陷。 |
| 关机逻辑 · 加湿器关闭后 30s（201 段） | 符合 | UserAction.c:1661-1674：`DisinfectStepDisp = 210; if(IndoorModeUsesHumidifier(preStopMode)){count += uc1s; if(count < 30)return;}`；对照常规 201 段 UserAction.c:2348-2359 | IndoorModeUsesHumidifier 只在 恒温恒湿制冷/制热(ControlType==1) 且 HumUseSet==1 且加湿方式非无 时返回 1（UserAction.c:551-557），与规格书“加湿器关闭”的前置条件一致。 |
| 关机逻辑 · 制热 60s / 制冷 30s 且外机停稳（202 段） | 符合 | UserAction.c:1675-1693：`DisinfectStepDisp = 211; if((preStopMode != MODE_VENT) && (!IndoorOutdoorStopped()))return;` `if(preStopMode == MODE_HEAT){if(count < 60)return;} else if(preStopMode == MODE_COOL){if(count < 30)return;}`；对照常规 202 段 UserAction.c:2360-2373 |  |
| 关机逻辑 · 逆序停风机（203 段） | 符合 | UserAction.c:1694-1707：`DisinfectStepDisp = 212; if(!IndoorApplyReverseFanStop(count)){count += uc1s; return;}`；IndoorApplyReverseFanStop 定义在 UserAction.c:271-336，按 IndoorFanStartTimeline 算出的启动顺序反着停 | 这里用的是常规开机时缓存的 IndoorFanSequence（IndoorCaptureFanStartTimeline，UserAction.c:232-245），带符号的间隔是被用上的（见“间隔符号”一条的对比）。 |
| 关机逻辑 · 停风机后再 30s（205 段） | 符合 | UserAction.c:1708-1723：`DisinfectStepDisp = 214; count += uc1s; if(count < 30)return; IndoorApplyFanValveState(0, 0, 0, 0); SysStep = 0; ... step = 1;`；对照常规 205 段 UserAction.c:2403-2410 |  |
| 【消毒】→开回风阀（全开） | 符合 | UserAction.c:1552-1566，path==1 时 `returnValveOpen=(path==1)?1:0`（1553），并对 0-10V 回风阀强制给满量程：`if((path == 1) && (ReturnValveModeSet == VALVE_OUTPUT_AO) && (ReturnFanModeSet != RETURN_FAN_MANUAL_AO)){IndoorSetAO(&ReturnFanAO4, 1000);}`（1562-1566） | 1000 经 IndoorSetAO(UserAction.c:373-376，限幅 0..1000) 即 0-10V 满量程=全开。边界：若 回风机==0-10V手动 且 回风阀==0-10V（DA4 复用冲突），1562 的条件为假、AO 保持 0（冲突由 IndoorUpdateAOConflict 处理，UserAction.c:952-964）。 |
| 【消毒】→开送风阀 | 符合 | UserAction.c:1552 `(path != 0) ? 1 : 0` 作为 supplyValveOpen；1557-1561 `if((path != 0) && (SupplyValveModeSet == VALVE_OUTPUT_AO) && (SupplyFanModeSet == SUPPLY_FAN_DO)){IndoorSetAO(&SupplyFanAO7, 1000);}`；DO 型则由 967 行 `SupplyValveY04 = (supplyValveOpen && (SupplyValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;` | 送风阀为 AO 且送风机为“无/关”配置时，走 986-990 行的分支，给的是用户设定的 手动电压(SupplyValveManualVoltSet*10) 而不是强制 1000，即并非严格“全开”——规格书只写“开送风阀”，未要求全开，故不计不符。 |
| 【消毒】→150S后 | 符合 | UserAction.c:1724-1743 step 1：`SysStep = 2; IndoorDisinfectOutputs(phase, 0, 0, 0, 0); count += uc1s; if(count >= 150){count = 0; ... step = 2;}` |  |
| 【消毒】→开送风机 | 符合 | UserAction.c:1744-1746 step 2 phase==1：`if(phase == 1)IndoorDisinfectOutputs(1, 1, 0, 0, 0);`（第 2 个入参 supplyFan=1）；最终落到 970 行 `UserPumpY00 = (supplyOn && supplyFanConfigured) ? 1 : 0;` | 规格书未规定、代码自行决定：消毒阶段只开送风机，回风机参数固定传 0（1555 行），与规格书只提“送风机”一致。 |
| 【消毒】→20S后 | 符合 | UserAction.c:1755 `if(((phase == 1) && (count >= 20)) \|\| ((phase == 2) && (count >= phaseFanSequenceEnd))){count = 0; step = (phase == 1) ? 3 : 4;}` |  |
| 【消毒】→开消毒装置并计时 | 符合 | UserAction.c:1762-1766 step 3：`IndoorDisinfectOutputs(1, 1, 0, 0, (DisinfectRunTimeSet == 0) ? 0 : 1); runCount += uc1s; DisinfectRunCountDisp = runCount;`；1567 `DisinfectY12 = disinfect ? 1 : 0;`（Y12 只由消排毒逻辑驱动，无别处写，见 grep 结果） |  |
| 【消毒】→当时间达到【消毒模式运行时间】后→关消毒装置 | 符合 | UserAction.c:1767-1769 `if((DisinfectRunTimeSet == 0) \|\| (runCount >= (unsigned int)DisinfectRunTimeSet * 60)){ DisinfectY12 = 0; ...}`（分钟×60 秒） |  |
| 【消毒】结束后·若选择停机，则按通风关机顺序停机 | **[被推翻]** 部分符合 | UserAction.c:1818-1828 step 7：`IndoorDisinfectOutputs(phase, 0, 0, 0, 0); count += uc1s; if(count >= 30){IndoorDisinfectOutputs(0, 0, 0, 0, 0); ... step = 8;}`；1829-1847 step 8 else 分支 `HMIOnOffCommandSet = 0; TouchScreenONOFF = 0; SysStatusLoader = 0; SysStatus = NORSTOP;` | 实现了停风机→30s→关阀→停机；但规格书 §3.3 通风关机的“回风阀关闭→30s→排风机关闭→30s→新风阀→排风阀→回风阀→送风阀→混风阀”这几级被并成 step 8 的一次性全关（1824/1831 行 IndoorDisinfectOutputs(0,...)），没有逐级 30s。另：step 7 的这 30s 规格书在本节未规定，属代码自行决定（对消毒模式多插了一段延时）。 ／复核 2/3 推翻 |
| 【消毒】结束后·若选择正常运行，则关机后按【消毒】前运行模式开机运行 | 符合 | UserAction.c:1833-1840：`if(AfterModeSelectSet){ IndoorSetNormalWorkMode((unsigned char)LastNormalModeSet); HMIOnOffCommandSet = 1; TouchScreenONOFF = 1; SysStatusLoader = 1; SysStatus = NORRUN;}`；1832 行先 `SysStep = 0;` 关断，1831 行先把所有阀/风机清零，再重进开机序列（SwitchStatusProcess case 0→1，UserAction.c:2279-2290） |  |
| 【消毒】“消毒前运行模式”的取法 | 符合 | UserAction.c:1601-1607（非消排毒时）`lastMode = UserWorkModeSetTEMP;` + 1630-1631（消排毒时）`if(lastMode <= MODE_VENT)LastNormalModeSet = lastMode; lastMode = UserWorkModeSetTEMP;`，即进入消排毒的第一次扫描把“上一拍的模式”锁进 LastNormalModeSet（Parameter[326]，掉电保存） | MODE_COOL=0/MODE_HEAT=1/MODE_VENT=2（User.h:486-488），所以 `<= MODE_VENT` 恰好接受 制冷/制热/通风 三种正常模式，而把 3/4/5 挡在外。同一参数也被来电自启降级复用（Switch.c:101-105）。上电瞬间模式就是消排毒时 lastMode 取静态初值 MODE_COOL，即“消毒前模式”退化为制冷——规格书未规定该边界。 |
| 【排毒】→开新风阀（全开） | 符合 | UserAction.c:1550-1556 path==2：`IndoorApplyFanValveStateDetailed((path == 2) ? 1 : 0, ..., (path == 2) ? 1000 : -1)` → 965 `freshDemand = IndoorApplyFreshAndMixValve(freshValveOpen, forcedFreshDemand)` → 701 `else if(forcedDemand >= 0)freshDemand = IndoorLimitValue(forcedDemand, 0, 1000);` = 1000(=10V 满开) |  |
| 【排毒】→开送风阀 | 符合 | UserAction.c:1552 `(path != 0) ? 1 : 0` → path==2 时为 1；967 行 `SupplyValveY04 = (supplyValveOpen && (SupplyValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;` |  |
| 【排毒】→开排风阀 | 符合 | UserAction.c:1554 `(path == 2) ? 1 : 0` 作为 exhaustValveOpen；969 行 `ExhaustValveY11 = (exhaustValveOpen && (ExhaustValveModeSet == VALVE_OUTPUT_DO)) ? 1 : 0;`；AO 型在 1016-1019 `if((ExhaustValveModeSet == VALVE_OUTPUT_AO) && (FreshValveModeSet == VALVE_OUTPUT_AO)){IndoorSetAO(&ExhaustValveAO6, exhaustValveOpen ? freshDemand : 0);}` 即与新风阀同开度 | 边界：排风阀为 AO 而新风阀非 AO 时，1016 条件为假 → 排风阀 AO 恒为 0，排风阀开不了（规格书要求排风阀 0-10V 时新风阀本就得按焓差控制，正常不会这样配）。 |
| 【排毒】→150S后 | 符合 | UserAction.c:1724-1743（step 1 与消毒共用，phase==2 时 1727 行给 path2 即 新风/送风/排风阀），1729 `if(count >= 150)` |  |
| 【排毒】→开送风机、排风机（两者顺序根据送排风机间隔时间） | 不符合 | UserAction.c:1633 `fanInterval = IndoorExhaustFanSignedDelay();`（带符号，UserAction.c:139-145 限幅 ±90）→ 1734-1739 交给 IndoorSpecialFanStartTimeline；后者 UserAction.c:255-256 先取绝对值 `fanDelay = (fanInterval < 0) ? (unsigned int)(-fanInterval) : (unsigned int)fanInterval;`，再 266-268 `*exhaustStart = 0; *supplyStart = fanDelay; *sequenceEnd = fanDelay;`；消费侧 1749-1752 `supplyFan = (count >= phaseSupplyStart) ? 1 : 0; exhaustFan = (phaseExhaustPresent && (count >= phaseExhaustStart)) ? 1 : 0;` | 符号被丢掉，排风机永远在 t=0 先起、送风机延后 \|间隔\| —— 无论 EFD 设 +30 还是 -30，顺序都不可切换，规格书“两者顺序根据送排风机间隔时间”只实现了“间隔”，没实现“顺序”。对照常规开机 UserAction.c:167-168 `*supplyStart = (fanInterval > 0) ? fanDelay : 0; *exhaustStart = (fanInterval < 0) ? fanDelay : 0;` 是正经用了符号的，可见本处是漏用。且 ExhaustFanDelaySet 可设负值（user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90;`），负值在这条路径上完全没有效果。 ／复核 1/3 推翻 |
| 【排毒】→…并计时 | 符合 | UserAction.c:1755-1759（step 2 等 `count >= phaseFanSequenceEnd` 才转 step 4）、1781-1786 step 4：`IndoorDisinfectOutputs(2, 1, 0, phaseExhaustPresent, 0); runCount += uc1s; ExhaustRunCountDisp = runCount; if(... runCount >= (unsigned int)ExhaustRunTimeSet * 60)` | 规格书未规定、代码自行决定：计时起点取“两台风机都启动之后”（即第一台风机启动后再等 \|间隔\|），若按“开风机即计时”读则有效排毒时间会比设定值短 \|间隔\| 秒。规格书没写死计时起点，故不判不符。 |
| 【排毒】→时间到后机组根据【结束后运行模式选择】执行（停机分支） | 部分符合 | UserAction.c:1786-1790（step 4 计时到 → step 6）、1798-1817 step 6、1818-1828 step 7（30s 后一次性关阀）、1829-1847 step 8 else 分支（NORSTOP） | 同消毒：逆序停风机有（step 6 里 送风机立即停、排风机多跑 \|间隔\| 秒，1804-1808），但“通风关机顺序”的逐级 30s 关阀被合并成一次全关。step 6/7 的这段收尾延时规格书未规定。 ／复核 1/3 推翻 |
| 【排毒】→若选择正常运行，则按【排毒】前运行模式开机运行 | 符合 | UserAction.c:1833-1840（AfterModeSelectSet==1 时 IndoorSetNormalWorkMode(LastNormalModeSet)+SysStatusLoader=1+SysStatus=NORRUN） | 与消毒共用同一段收尾逻辑，LastNormalModeSet 的取法见上文。“排毒前运行模式”同样记录。 |
| 【消排毒】第一阶段：同消毒（关机→开回风阀/送风阀→150S→开送风机→20S→开消毒装置计时） | 符合 | UserAction.c:1638-1644 `if((UserWorkModeSetTEMP == MODE_EXHAUST) \|\| ((UserWorkModeSetTEMP == MODE_AUTO_DISINFECT) && (DisinfectRunTimeSet == 0))){phase = 2;} else phase = 1;` → AUTO 且消毒时间非 0 时 phase=1，走 1724-1780 的 step 1/2/3，与消毒模式完全同码 |  |
| 【消排毒】→关消毒装置 | 符合 | UserAction.c:1767-1769 `if(...runCount >= DisinfectRunTimeSet*60){ DisinfectY12 = 0; runCount = 0; count = 0; ...}` |  |
| 【消排毒】→关闭送风机 | 符合 | UserAction.c:1772-1777 `if((UserWorkModeSetTEMP == MODE_AUTO_DISINFECT) && (ExhaustRunTimeSet != 0)){phase = 2; step = 5;}` → 1792-1794 step 5 `IndoorDisinfectOutputs(1, 0, 0, 0, 0);`（supplyFan 传 0） | 送风机关闭与“关消毒装置”不严格同拍：1775 只置 step=5，实际关风机在下一拍 step 5 里。相邻一拍（秒级扫描）内完成，判符合。 |
| 【消排毒】关闭送风机→30S后 | 符合 | UserAction.c:1792-1797 step 5：`IndoorDisinfectOutputs(1, 0, 0, 0, 0); count += uc1s; if(count >= 30){count = 0; step = 1;}` |  |
| 【消排毒】→关回风阀→开新风阀（全开）、送风阀、排风阀（是否为先后两步） | **[被推翻]** 部分符合 | step 5 结束后（1796 行 `step = 1`）下一拍进入 1724-1727：`SysStep = 2; IndoorDisinfectOutputs(phase, 0, 0, 0, 0);`（phase==2）；该调用在 UserAction.c:1550-1556 一次性给 新风阀=1(强制 1000)、送风阀=1、排风阀=1、回风阀=(path==1)?1:0 → 0，即关回风阀与开新风/送风/排风阀发生在同一次输出刷新里 | 规格书用“→”写作两步，但没有给这两步之间的延时；代码把它们并成一次原子更新，观察不到“先关后开”的过程。若验收按“必须有先后两拍”判定则不符，按“无延时要求即可同步”判定则符合——判为部分符合，卡在规格书未给延时。 ／复核 3/3 推翻 |
| 【消排毒】→开新风阀（全开）、送风阀、排风阀→150S后 | 符合 | UserAction.c:1724-1743（step 1 复用，phase==2 走 path2），1729 `if(count >= 150)` |  |
| 【消排毒】→开送风机、排风机（两者顺序根据送排风机间隔时间）并计时 | **[被推翻]** 不符合 | 同【排毒】一条：UserAction.c:1734-1739 调 IndoorSpecialFanStartTimeline，247-269 行取绝对值后固定 `*exhaustStart = 0; *supplyStart = fanDelay;`，符号失效；step 2（1744-1761）与 step 4（1781-1791）消费同一组时间轴 | 同一缺陷在本阶段的第二阶段（排毒阶段）复现，即消排毒模式下也无法把“送风机先开”选出来。 ／复核 3/3 推翻 |
| 【消排毒】→当时间达到【排毒模式运行时间】后，机组根据【结束后运行模式选择】执行 | **[被推翻]** 部分符合 | UserAction.c:1786-1817（step 4 计时满 → step 6 → step 7 → step 8）；step 8 的 1833-1847 两条分支与消毒/排毒共用 | 收尾多出 step 6/7 两段（排风机续跑 \|间隔\| 秒、再 30s 保持阀开），规格书未规定；停机分支同样把“通风关机顺序”的逐级 30s 合并为一次全关。 ／复核 2/3 推翻 |
| 【消排毒】若选择停机，则按通风关机顺序停机 | **[被推翻]** 部分符合 | UserAction.c:1831 `IndoorDisinfectOutputs(0, 0, 0, 0, 0);` + 1841-1847 `HMIOnOffCommandSet = 0; TouchScreenONOFF = 0; SysStatusLoader = 0; SysStatus = NORSTOP;` | 负载此时已全停，等价于“停机”成立；但与规格书 §3.3 通风关机的分步（送风机关闭→30s→回风阀→30s→排风机→30s→新风阀→排风阀→回风阀→送风阀→混风阀）不完全同构。 ／复核 3/3 推翻 |
| 【消排毒】若选择正常运行，则关机后按【消排毒】前运行模式开机运行 | 符合 | UserAction.c:1832-1840 `SysStep = 0; if(AfterModeSelectSet){ IndoorSetNormalWorkMode((unsigned char)LastNormalModeSet); HMIOnOffCommandSet = 1; TouchScreenONOFF = 1; SysStatusLoader = 1; SysStatus = NORRUN;}` |  |
| 【消毒模式运行时间】60（0~999）min | 符合 | user.c:268 `Parameter_max[323]=999; Parameter_min[323]=0; Parameter_def[323]=60;`，映射 User.h:641 `#define DisinfectRunTimeSet Parameter[INDOOR_SPEC_BASE+8]`（INDOOR_SPEC_BASE=315，User.h:632） |  |
| 【排毒模式运行时间】60（0~999）min | 符合 | user.c:269 `Parameter_max[324]=999; Parameter_min[324]=0; Parameter_def[324]=60;`，映射 User.h:642 |  |
| 【结束后运行模式选择】0：停机（默认为0）；1：正常运行 | 符合 | user.c:270 `Parameter_max[325]=1; Parameter_min[325]=0; Parameter_def[325]=0;`，映射 User.h:643；消费点 UserAction.c:1833 `if(AfterModeSelectSet)` | 规格书的两档 0/1 与 1833 行的真/假分支一一对应（非 0 即“正常运行”）。 |
| 若【消毒模式运行时间】/【排毒模式运行时间】为 0，则没有消毒/排毒功能 | **[被推翻]** 部分符合 | UserAction.c:1608-1620：三种 0 值组合下 `if(lastMode > MODE_VENT)lastMode = MODE_COOL; IndoorSetNormalWorkMode(lastMode); IndoorDisinfectOutputs(0, 0, 0, 0, 0); ...` | “不执行消毒/排毒”成立（不动作、清输出）。但代码额外把 UserWorkModeSetTEMP 强行改回上一模式（1613-1614），即用户选中的【消毒】被静默弹回——规格书只说“没有消毒功能”，没说要改模式；属代码自行决定。AUTO_DISINFECT 要求两个时间都为 0 才退出（1610-1611），任一非 0 仍保留模式，与“自动消排毒=消毒+排毒”一致。 ／复核 2/3 推翻 |
| “150S” 的实现方式 | 符合 | UserAction.c:1729 `if(count >= 150)`（三处 150s 全用同一个 step 1） | 规格书 §3.3 常规开机写的是“150s（无风阀时取10S）”，代码里确实有此函数 UserAction.c:596-600 `if(!IndoorAnyValveConfigured())return 10; return 150;`，但消排毒路径没有调用它，无条件等 150s。消排毒本节原文就写“150S”，故不计不符；仅提示与 §3.3 的“无风阀 10s”惯例不联动。 |
| “两者顺序根据送排风机间隔时间”中该间隔参数的取值范围 | **[被推翻]** 无法核对 | 代码侧：user.c:273 `Parameter_max[328]=90; Parameter_min[328]=-90; Parameter_def[328]=30;`（User.h:646 `ExhaustFanDelaySet`）；规格书侧：§3.3 常规开机只写“30s（±60s可调）”，本节未给范围 | 规格书没写消排毒用哪个参数、也没写量程（±60 是 §3.3 常规开机的上下文），无法判定 ±90 是否越界；但代码在消排毒路径里又把 \|间隔\| 截到 60（UserAction.c:257 `if(fanDelay > 60)fanDelay = 60;`），而常规路径截到 90（UserAction.c:142/157）。截到 60 与 §3.3 的“±60s可调”吻合，属代码自行决定，规格书本节无据。 ／复核 2/3 推翻 |
| （规格书未规定·代码自行决定）消排毒期间回风机/加湿器/再热等的处置 | **[被推翻]** 无法核对 | UserAction.c:1746/1752/1764/1783/1794/1801/1809/1820 所有 IndoorDisinfectOutputs 调用中 returnFan 恒为 0；UserAction.c:3398-3405 消排毒模式下提前 return，跳过 SysBoardAction（外机/加湿/再热/预热指令的常规生成路径） | 规格书消排毒一节只字未提回风机、加湿器、再热、外机；代码选择“回风机全程不启动、常规负载逻辑整体旁路”。不是缺陷，但属规格书留白、实现自行决定的部分，验收时应与业主确认。 ／复核 2/3 推翻 |
| （规格书未规定·代码自行决定）上电 20 秒内的屏蔽 | 无法核对 | UserAction.c:1600 `if(Systick_1ms < 20000)return;`（注释 1585-1599 说明是为了不与 Switch.c:86-91 的来电自启判定窗口打架） | 规格书无此条，属实现自保措施；副作用是上电 20s 内选消毒不动作，此时负载本就未启动，无可观察影响。 ／复核 0/3 推翻 |

## §3.3.5 紧急停机

| 规格书条款 | 判定 | 依据 | 备注 |
|---|---|---|---|
| 除电子膨胀阀外所有用电设备均失电 —— 内机 16 路开关量(DO)全清 | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/ErrorCheck.c:620-632  `if(UserSidePumpOverLoadErr\|\|AntiFreezeErr\|\|EnviTempErr) // emergency stop { ModUrgencyStop=1; ... for(i=0;i<TOL_OUTPUT_CHANEL;i++)output[i]=0;` ；TOL_OUTPUT_CHANEL=16（User/wellthinker/inc/io.h:6），Y00..Y17=output[0..15]（同文件 io.h:11-31） | io.c 源码不在仓库（只有 Project/Obj/io.o），我用 fromelf 反汇编核对 WriteOutput：16 路各写一个独立引脚，无移位寄存器中转；板上另有 LEDRUN/LEDERR(PE0/PE1, io.h:44-48) 属指示灯不属用电负载。 |
| 除电子膨胀阀外所有用电设备均失电 —— 内机 10 路模拟量(AO)全清 | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/ErrorCheck.c:632 `for(i=0;i<DA_CHANEL;i++)DA[i]=0;`（DA_CHANEL=10，User/wellthinker/inc/dac.h:5）；显示量同步归零 659-661 行 DA0ValueDisp/DA1ValueDisp/SteamHotWaterValveAO8Disp=0 | DA[] 是全部 0-10V 的唯一出口（PWMOutProcess，dac.h:12），循环覆盖 0..9 无遗漏。 |
| 隐含：急停置位后各负载不得被同一/后续扫描重新驱动（内机） | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/user.c:2001-2008 扫描序 UserAction→ErrorCheck→IndoorEnforceConfiguredOutputDisable→DispAction→IoOutAction；IndoorEnforceConfiguredOutputDisable 只会写 0（UserAction.c:1163-1226）；防冻最小开度规则显式排除急停 UserAction.c:1095-1103 `shutdownFault = ModUrgencyStop \|\| ...; if(...\|\|shutdownFault) return;`；手动调试被挡 UserAction.c:3351 `if((HandDebug==1)&&(SysStep==0)&&(SysErrStatus==0)...)` | 规格书未规定扫描次序，属代码自定。列出来是因为'+10 步'与'全失电'两条都依赖它在同一拍内成立。 |
| 除电子膨胀阀外所有用电设备均失电 —— 内机综合报警输出 Y05 例外的存在 | 部分符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/ErrorCheck.c:631 清 output[5]；但同一扫描稍后 User/UserSrc/DispAction.c:471-476 `if(ErrTotal){ErrorOutFlag=1;} if(ErrorOutFlag==1) ErrorOutY00=1; else ErrorOutY00=0;`（User.h:178 `#define ErrorOutY00 Y05`）。急停三个触发源全在 ErrTotal 里：DispAction.c:425 UserSidePumpOverLoadErr、426 AntiFreezeErr、463-465 FireFaultLatchDisp/ValveLinkFaultLatchDisp/LeakFaultLatchDisp | Y05 按点位表是干接点综合报警输出（PSC330_PSC316_IO点位说明_20260907.md 第127-129行 Y05=综合报警输出）。急停继电器线圈算不算'用电设备'规格书没写；调试记录 2026年9月8日实体复测清单.md:607 已把'急停时 Y05 曾保持吸合'列为历史疑点。这一条是**代码事实上没有做到'全失电'**，但可能是有意留的报警例外——需要规格方一句话确认。 ／复核 1/3 推翻 |
| 隐含要求④：EEV 不能被'全失电'逻辑波及（引脚层面） | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/EEV/eev.h:6-83 EEV 四相全是独立宏（MT1=GPIOG3/4/5/6、MT2=GPIOD9/10/11+GPIOG2、MT3=GPIOB13/14/15+GPIOD8、MT4=GPIOE13/14/15+GPIOB12、MT5=GPIOE9/10/11/12、MT6=GPIOG0/1+GPIOE7/8、MT7=GPIOF12/13/14/15、MT8=GPIOC5+GPIOB0/1+GPIOF11)，不经过 output[]/DA[] | 我反汇编 Project/Obj/io.o 的 WriteOutput 得到 16 路 Y 的实际引脚：Y00=PG7 Y01=PG8 Y02=PA8 Y03=PA12 Y04=PD1 Y05=PD3 Y06=PD4 Y07=PG11 Y10=PB7 Y11=PB6 Y12=PB5 Y13=PB4 Y14=PB3 Y15=PG15 Y16=PG13 Y17=PG12；与上面 EEV 引脚集合无交集（GPIOG 只在 PG0~PG8/PG11~PG15 分开占用）⇒ 清 output[] 不会碰 EEV 绕组。 |
| 隐含要求④：急停期间 EEV 驱动任务继续运行（内机） | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/wellthinker/src/sys.c:52-79 SystemAction() 每扫描无条件调 EEV_Priority(); MT1Output(); ... MT8Output();，与 SysStep/User() 无关；EEV_ActionAll 的急停分支 eevCTRL.c:628-643 之后照常由 EEV_Priority 驱动 | 规格书没写这条，但它是'+10 步关闭'能成立的必要条件。已核对成立。 |
| 隐含要求：急停关闭不可被手动或 PID 覆盖 | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/eevCTRL.c:628-643 `if(ModUrgencyStop){ for(j=0;j<8;j++){ Parameter[Manual+1+2*j]=0; EEVManualActive[j]=0; EEVSpecPidReset(j); EEVControlReinitialize[j]=1; Parameter[NewAddValue+j]=EEVCloseStepTarget(j,Parameter[OldAddValue+j],EEVSpecMaxStep(),EEV_CLOSE_EMERGENCY);} EEVSpecPulseTimeAction(); return; }` | 外机同构：PSC316RK-V12-外机/WELLTHINKER/User/eevCTRL.c:708-724 同样清 Manual/ManuCtrlEEVSet 并 return。 |
| 『电子膨胀阀以20步/秒速度关闭』—— 内机急停分支存在且覆盖全部 8 路 | **[被推翻]** 部分符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/eevCTRL.c:628-643（见上条）；调用点 UserAction.c:3457/3440/3439/3429/3417/3402 各分支都调 EEV_ActionAll() | 对 8 路无条件下发（含未配置压缩机的 4..#8 通道：EEVSpecChannelMap 返回 0 时也照样写 New/Old，见 eevCTRL.c:51-59 与 628-643 未做 configured 判断）——没有实际阀体时无害，但属于'代码自定'的过度覆盖。 ／复核 3/3 推翻 |
| 『20步/秒』的实现机制：脉冲间隔 + 轮转分时（内机） | **[被推翻]** 部分符合 | 间隔：d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/EEV/eev.c:316-323 `if(Flag==1){ if((Systick_1ms-MT1TimeCount)<(EEVPulseTimeSet+100))return;} else { if((Systick_1ms-MT1TimeCount)<EEVPulseTimeSet)return;} Count=0;`（EEVPulseTimeSet=Parameter[860]，User.h:1088）；分时：eev.c:132-232 EEV_Priority 每 130 个 uc10ms 换组 + eev.c:267-278 `if(EEVCtrlNum!=0){ TempCount+=uc10ms; if(TempCount>=50){...;Flag=1;} return;}` | uc10ms 的周期我从 Project/Obj/sysrun.o 反汇编确认：Systick_1ms - Systick_10ms >= 10 时置 1（即 10ms 一个脉冲）→ 换组周期确为 1.3s。所以占空比=1/活动组数，单组独占 1000/T 步/秒。 ／复核 3/3 推翻 |
| 『20步/秒』的实际数值（内机推算） | **[被推翻]** 部分符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/eevCTRL.c:24-38 `/* The driver time-slices one group every 1.3 s. Compensate the pulse period so every valve can still close at 20 steps/s. */ if(activeGroups>=4)EEVPulseTimeSet=10; else if(activeGroups==3)EEVPulseTimeSet=13; else if(activeGroups==2)EEVPulseTimeSet=20; else EEVPulseTimeSet=32;` | 按 1.3s/组、4 组满轮转(周期 5.2s)推算：1300/10=130 步/组 → 130/5.2 ≈ 25 步/秒；2 组时 1300/20=65 步/2.6s ≈ 25 步/秒；3 组 ≈ 25.6；只有 1 组在关时 1300/32=40 步/1.3s ≈ 31 步/秒。即：补偿表确实让 2/3/4 组情况下速度一致，但那个一致值是 ~25 而不是 20（注释与实现差约 25%）。这是**推算值，未实机测**；要坐实需按 EEV记录 采 目标/步数 时间序列。 ／复核 3/3 推翻 |
| 『电子膨胀阀以20步/秒速度关闭』—— 外机急停分支存在 | 符合 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/User/eevCTRL.c:707-724 `/* Emergency stop: close both main EEVs at 20 steps per second. */ if(UrgencyStop){ ManuCtrlEEVSet=0; ManuCtrlEEV2Set=0; for(j=0;j<2;j++){ Parameter[Manual+1+2*j]=0; EEVManualActive[j]=0; Parameter[NewAddValue+j]=EEVCloseStepTarget(j,Parameter[OldAddValue+j],EEVSpecMaxStep(),EEV_CLOSE_EMERGENCY); EEVSpecPidReset(j); EEVControlReinitialize[j]=1;} return; }`；调用点 WELLTHINKER/User/UserAction.c:2406 `EEV_ActionAll();` | 外机 EEV 只有 2 路（j<2），UrgencyStop=Parameter[PLCctrlPara+6]=Parameter[1406]（WELLTHINKER/User/User.h:929,936）。 |
| 含外机：急停信号由内机下发到 316（链路存在） | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/modulecontrol.c:1111-1115 `if(OLD_UnitWriteStatusUrgencyStop!=ModUrgencyStop){ OLD_UnitWriteStatusUrgencyStop=ModUrgencyStop; SendParaAddr3[1..4]=0; }`；同一分支 1172-1189 `Addr=PLCctrlPara; Num=14; ucUart5Buff[1]=16; ... ucUart5Buff[j*2+7]=Parameter[Addr+j]>>8; ... StartUart5SendData();` → 功能码 16 写 1400~1413，含 1406 ModUrgencyStop（User.h:1467,1474） | 逐台单写、无广播（每个 ModuleNum 单独组帧），与用户备忘'330→316 无广播通路'一致。风险：某台外机通信断链时收不到急停，只能靠内机的通信故障判据兜底——规格书没写这条。 |
| 含外机：外机侧接收急停寄存器（不被写保护挡） | 符合 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/modbus/modbus.c:96-102 `ModbusWriteOverlapsReadOnly()` 只屏蔽 COMP_RUNTIME / PSC316_VERSION 两个区间，不含 1400-1413；写入处理 515-527 直接落到 Parameter[] | 外机本地没有任何位置写 UrgencyStop（全仓 grep 只有读：eevCTRL.c:473/708/797、ErrorCheck.c:605/644、moduleAction.c:248、UserAction.c:433/686/1042）⇒ 外机急停**只能**由 RS485 写入，没有本地急停 DI。规格书没规定外机急停输入，属代码自定。 |
| 含外机：除 EEV 外所有用电设备均失电（外机 DO） | 符合 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/User/ErrorCheck.c:70-83 `static void StopOutdoorAll(void){ StopOutdoorSystem(0); StopOutdoorSystem(1); OutdoorFanAllOff(); Y00=0;Y01=0;Y02=0;Y03=0;Y04=0;Y05=0;Y07=0;} static void EmergencyStopOutdoorAll(void){ StopOutdoorAll(); Y06=0; }`，调用点 ErrorCheck.c:604-605 `/* Emergency stop is the only path that removes power from every output. */ if(UrgencyStop) EmergencyStopOutdoorAll();` | Y00..Y07 即该板全部 8 路 DO（SYSTEM/inc/io.h:13-24）。Y06(Under1Y13) 只在急停路径清、普通停机不清——代码自定的差异化处理（对外机而言合理：急停要更彻底）。 |
| 含外机：除 EEV 外所有用电设备均失电（外机 AO / 变频风机 0-10V） | 符合 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/User/ErrorCheck.c:74 经 OutdoorFanAllOff()；d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/User/UserAction.c:939-956 `void OutdoorFanAllOff(void){ FanOpenSet=0; FanOpenSet2=0; OutdoorFanStage=0; FanSpeedCtrl=0; FanSpeedCtrl2=0; DA0value=0; DA1value=0; DA0ValueDisp=0; DA1ValueDisp=0; OutdoorVfdOn=0; OutdoorFixedOn=0; ... }` | 外机 2 路 AO（DA0/DA1，User.h:232-233）都清了；变频风机的'使能'是 Y 继电器，也随 Y00..Y07 清。 |
| 含外机：急停后外机输出不会被后续扫描重新吸合 | 符合 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/User/user.c:499-507 扫描序 UserAction→ErrorCheck→EEVFaultSafetyAction→DispAction→IoOutAction→HandDebugY02Ctrl→OutdoorProtectOutputLimit；IoOutAction 只从已清的 Comp[]/Four[]/FanOpenSet 反推（ErrorCheck.c:687-757 `if(Comp[0]==1){Comp1Y05=1;...} else {Comp1Y05=0;...}`）；启动许可被急停封死（UserAction.c:433 `if(OutdoorDisableSet\|\|AmbientLockout\|\|UrgencyStop\|\|...)return 1;`、UserAction.c:686、UserAction.c:1042 `if(UrgencyStop) return 0;`、moduleAction.c:248 `&& !UrgencyStop`） | 规格书没规定扫描次序/重新吸合问题，属实现细节核对。 |
| 『关闭步数＝电子膨胀阀当前步数+10』—— 内机 | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/eevCTRL.c:171-185 `static int EEVCloseStepTarget(...){ const int closeStepPerSecond=20; ... if(EEVSpecCloseMode[i]==EEV_CLOSE_NONE){ extraStep=(closeMode==EEV_CLOSE_EMERGENCY)?10:20; Parameter[OldAddValue+i]=current+extraStep; EEVSpecCloseTarget[i]=current+extraStep; EEVSpecCloseMode[i]=closeMode; }` | 实现方式是把'当前位置寄存器'OldAddValue 直接写成 current+10，再把目标推 0，于是行程 = current+10 步；步数语义与规格书一致。 |
| 『关闭步数＝电子膨胀阀当前步数+10』—— 外机 | 符合 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/User/eevCTRL.c:180-198 `static signed int EEVCloseStepTarget(...){ const signed int closeStepPerSecond=20; ... extraStep=(closeMode==EEV_CLOSE_EMERGENCY)?10:20; Parameter[OldAddValue+i]=current+extraStep; EEVSpecCloseTarget[i]=current+extraStep; ...}`，急停入口同文件 716-719 | 内外机这段逻辑同源（改一处要改两处）。 |
| 急停关闭与普通关闭有区分（普通=当前步数+20） | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/eevCTRL.c:181 `extraStep = (closeMode==EEV_CLOSE_EMERGENCY) ? 10 : 20;`；普通关闭的调用点例如 eevCTRL.c:370 `EEVCloseStepTarget(i,current,maxStep,EEV_CLOSE_NORMAL)` 与 eevCTRL.c:575-578（StattFlag==0 分支） | 与规格书 §3.9『电子膨胀阀关闭时，以20步/秒速度关闭。电子膨胀阀关闭步数＝电子膨胀阀当前步数+20』一致；两个常量分别对应 §3.3.5 与 §3.9，没有混用。 |
| 普通关闭进行中触发急停时不叠加（不能变成 +30） | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/eevCTRL.c:186-194 `else if((EEVSpecCloseMode[i]==EEV_CLOSE_NORMAL)&&(closeMode==EEV_CLOSE_EMERGENCY)){ if(Parameter[OldAddValue+i]>10)Parameter[OldAddValue+i]-=10; else Parameter[OldAddValue+i]=0; if(EEVSpecCloseTarget[i]>10)EEVSpecCloseTarget[i]-=10; else EEVSpecCloseTarget[i]=0; EEVSpecCloseMode[i]=EEV_CLOSE_EMERGENCY; }` | 从 current+20 减 10 得 current+10，与规格书的 +10 对齐。外机同构：PSC316 同文件 199-207。 |
| 『20步/秒』的节流实现：目标每秒递减 20 | **[被推翻]** 部分符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/eevCTRL.c:173 `const int closeStepPerSecond = 20;` + 195-200 `if(uc1s && !EEVSpecCloseTickConsumed[i]){ EEVSpecCloseTickConsumed[i]=1; if(EEVSpecCloseTarget[i]<=closeStepPerSecond)EEVSpecCloseTarget[i]=0; else EEVSpecCloseTarget[i]-=closeStepPerSecond; }` | 这段只是把'目标'按 20 步/秒往 0 推（并把 1 秒后的目标夹到 0），**不构成对电机位移的限速**；真正的速度由 MTxOutput 的脉冲节拍/时分占空决定（见前两条）。即：规格书的'20步/秒'在代码里有两套来源，一套是限速器（名义 20），一套是实际脉冲源（推算 25）——两者都不等于 20 的场景存在，这是我对本条给'部分符合'的原因。 ／复核 3/3 推翻 |
| 含外机：外机 EEV 的 20步/秒 速率保证（无速率补偿） | 不符合 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/WELLTHINKER/User/user.c:389 `EEVPulseTimeSet=32;`（上电默认，EEVPulseTimeSet=Parameter[802]，User.h:747）；全工程对 EEVPulseTimeSet 的写只有 eevCTRL.c:538 `if(i==0)EEVPulseTimeSet=16;//上电回零` 与 eevCTRL.c:558 `if(i==0)EEVPulseTimeSet=32;`；分时逻辑仍在（同文件 948-1059，1.3s/组），但**没有**330 那种按活动组数改间隔的补偿表（对比 PSC330…/eevCTRL.c:24-38） | 该板只有 2 路 EEV（EEVRunGroup[0..1]），2 组轮转 ⇒ 占空 1/2；32ms 间隔 ⇒ 单组每秒 31.25 步、平均 ≈15.6 步/秒，比规格书的 20 慢约 22%。若只关 1 路则 ≈31 步/秒（快 56%）。'32ms 默认值'规格书没规定，属代码自定，但结果偏离 20 步/秒。同样**未实机测**，是我按 330 同构驱动推算的。 ／复核 0/3 推翻 |
| 外机 EEV 驱动源码是否在仓库里 | **[被推翻]** 无法核对 | d:/Companty/WEll thinker/F/code/PSC316RK-V12-外机/USER/PSC316RK-V12.uvprojx:645-651 EEV 组只挂 `..\OBJ\eev.o`（FileType 3，无 .c）；WELLTHINKER/EEV/ 下只有 eev.h 与 THB6128.c，**没有 eev.c**；OBJ/eev.d 第1行指出原文件是 `..\WELLTHINKER\EEV\eev.c`；PSC316RK-V12.map:475 `..\WELLTHINKER\EEV\eev.c  0x00000000 Number 0 eev.o ABSOLUTE` | 所以'外机 EEV 以 20 步/秒关闭'的底层节拍**无法从源码核对**。我改用反汇编（D:\Keil_v5\ARM\ARMCC\bin\fromelf.exe --text -c）：eev.o 导出 MT1Output/MT2Output/EEV_GPIO_Config/STEP_MOTOR0_OFF/STEP_MOTOR1_OFF/STEP_MOTOR0_8A/STEP_MOTOR1_8A/STEP_MOTOR_NUM；MT1Output 与 330 同构（uc10ms→Count、TempCount>=50 全相释放并置 Flag、Parameter[804]=EEVCtrlNum 门控、Parameter[802]=EEVPulseTimeSet 延时比较、Parameter[809] 逐步加减），且 eev.o 对 0x644(=Parameter[802]) 只有 16 次 LDRSH 读、无写。注意反汇编里 MT1Output **没有** 330 的 `EEVPulseTimeSet+100`（Flag==1）分支。若日后要改外机 EEV，必须先拿到 eev.c。 ／复核 3/3 推翻 |
| ⑤ 急停的触发路径清单 | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/ErrorCheck.c:620-622 `if(UserSidePumpOverLoadErr\|\|AntiFreezeErr\|\|EnviTempErr) // emergency stop { ModUrgencyStop = 1; ... }`。三个来源：① UserSidePumpOverLoadErr ← X03 送风机/泵过载（ErrorCheck.c:490 `if(PumpOverX00==1) UserSidePumpOverLoadErr=1;`，User.h:159 `#define PumpOverX00 X03_En`）；② 消防 X01 / 风阀连锁 X04 / 漏水 X13 三锁存汇合 → EnviTempErr（ErrorCheck.c:372-382 `if(FireFaultLatchDisp\|\|ValveLinkFaultLatchDisp\|\|LeakFaultLatchDisp){ EnviTempErr=1; IndoorSpecFaultDisp=1; }`，User.h:157/160/167/924） | 规格书 §3.3.5 只写了'怎么做'没写'谁触发'，§3.11 七条保护里也没有'紧急停机'条目，也没有急停 DI 的定义 ⇒ 触发源选择属**代码自定，不是缺陷**。X11 排风机过载/X12 回风机过载只置 IndoorSpecFaultDisp（ErrorCheck.c:383-393），**不**进急停——规格书也未要求。 |
| ⑤ 触发条件里的死分支：AntiFreezeErr | **[被推翻]** 不符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserHead/User.h:921 `#define AntiFreezeErr Parameter[HREST+8]`（HREST=594 ⇒ Parameter[598]，User.h:911）；全仓 grep AntiFreeze：只有读，无写（ErrorCheck.c:602/620、UserAction.c:1096/3016、DispAction.c:426）；Parameter[598] 也不在 HMI 量程表内（User.h:34 ParameterSaveSize=500，边界校验只覆盖 0..499，ErrorCheck.c:589-596） | 结论：'防冻'这个急停触发条件在这份固件里**永远为 0**，该路径不可能触发。规格书没要求防冻作为急停源（§3.11.10 的防冻指的是'新风<0℃时阀门最小开度'），所以严格说不算规格书违反；但它是判据表里的死分支，属**代码缺陷**（要么补来源，要么删判据）。 ／复核 3/3 推翻 |
| 另一条同名'紧急停机'路径不做 EEV 处理（命名陷阱） | **[被推翻]** 部分符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/UserAction.c:339-372 `static void IndoorEmergencyStopLoads(void){ IndoorStopHumidifier(); IndoorStopHeatPreheatLoads(); IndoorClearOutdoorRunCommands(); DisinfectY12=0; IndoorApplyFanValveState(0,0,0,0); for(i=0;i<DA_CHANEL;i++)DA[i]=0; SysStatusLoader=0; ...}` —— 既不置 ModUrgencyStop，也不碰 EEV；调用点 UserAction.c:1623（消排毒模式下 SysStatus≠NORRUN/TIMERUN 硬停）与 UserAction.c:2382（关机序列 case 4 检出 SysErrStatus==1） | 这两条路径下 EEV 走普通关闭 +20（eevCTRL.c:575-578 StattFlag==0 分支），与规格书 §3.9 一致，但不是 §3.3.5 的 +10。危险点：函数名就叫 EmergencyStopLoads，验收/复查时极易把它当成 §3.3.5 的实现。真正的 §3.3.5 实现是 ModUrgencyStop 那条（ErrorCheck.c:620-662）。 ／复核 3/3 推翻 |
| 区分『规格书写了但代码没有』与『规格书没规定、代码自行决定』—— SysErrStatus 故障停机不按急停处理 | 符合 | d:/Companty/WEll thinker/F/code/PSC330RK-V10-内机/User/UserSrc/ErrorCheck.c:606-619 `//内机急停控制  ModUrgencyStop=0; if((SysErrStatus==1)\|\|(WriteEEpromErr)\|\|SystemComunicateErr){ 压机命令清 0；if((SysStep>0)&&(SysStep<4))SysStep=4; }` 与 620 起的 ModUrgencyStop 分支是两个独立 if；前者**不清** output[]/DA[] | 规格书 §3.11.1~3.11.9 对一般故障只要求'（相关设备）应停止'，§3.3.5 的'全线失电+EEV+10步'只挂在'紧急停机'名下 ⇒ 分层正确。规格书没定义'哪些故障算紧急停机'，这层映射是代码自定。 |

## 统计

- 符合：309 条
- 部分符合：61 条
- 无法核对：19 条
- 不符合：11 条
- 未实现：7 条

被对抗验证**推翻**的条目在表里标了 `**[被推翻]**`，保留但降级参考。
