# PSC330 DI 点建议接线极性与报警判断表

> 生成日期: 2026-06-22  
> 依据代码: `PSC330RK-V10-内机/User/UserHead/User.h`、`PSC330RK-V10-内机/User/UserSrc/user.c`、`PSC330RK-V10-内机/User/UserSrc/UserAction.c`、`PSC330RK-V10-内机/User/UserSrc/ErrorCheck.c`

## 1. 先说结论

330 的 DI 不是全部固定常闭。

- `X00~X04` 支持软件反相，靠 `155~159` 这 5 个参数决定逻辑极性。
- `X05~X15` 当前代码基本按固定逻辑使用，没有单独做 HMI 极性切换。
- 从机组用途上看，保护类和联锁类点现场通常更适合按常闭思路接。
- `X14/X15` 代码注释明确是“闭合为通”，这两路最明确，正常状态应为闭合。

## 2. 极性参数说明

这 5 个参数决定 `X00~X04` 的逻辑是否反相：

| 地址 | 4x | 参数 | 对应点位 | 作用 |
|---:|---:|---|---|---|
| 155 | 40156 | `InPutStatusSet8` | X00 | 1=按原始输入，0=反相 |
| 156 | 40157 | `InPutStatusSet9` | X01 | 1=按原始输入，0=反相 |
| 157 | 40158 | `InPutStatusSet10` | X02 | 1=按原始输入，0=反相 |
| 158 | 40159 | `InPutStatusSet11` | X03 | 1=按原始输入，0=反相 |
| 159 | 40160 | `InPutStatusSet12` | X04 | 1=按原始输入，0=反相 |

默认值:

- `155~159` 默认都是 `0`
- 也就是 `X00~X04` 默认按“反相逻辑”处理

## 3. 每个 DI 点建议接线和报警判断

### 3.1 总体判断规则

先看总报警:

| 地址 | 4x | 含义 |
|---:|---:|---|
| 861 | 40862 | `ErrorOutFlag`，总报警标志，1=有报警 |
| 703 | 40704 | `ErrTotal`，报警计数 |
| 710 | 40711 | `SysErrStatus`，系统故障停机，1=已故障停机 |

再看本机报警单点:

| 地址区 | 4x 地址区 | 作用 |
|---:|---:|---|
| 594~623 | 40595~40624 | 330 本机具体报警单点，哪个为 1 就是哪类报警 |
| 630~631 | 40631~40632 | 594~625 的打包报警字 |
| 895 | 40896 | `IndoorSpecFaultDisp`，规格书联锁故障码 |
| 888 | 40889 | `SupplyPressureOkDisp`，送风压差状态 |
| 889 | 40890 | `ReturnPressureOkDisp`，回风压差状态 |

### 3.2 DI 逐点表

| 点位 | 功能 | 建议接线极性 | 软件极性参数 | 默认值 | 正常状态建议 | 如何看是否报警 |
|---|---|---|---|---|---|---|
| X00 | 电源缺相/相序保护 `PowerPhaseErrX0` | 建议常闭保护点 | 155 | 0 | 正常回路闭合 | 报警时看 `605=1`，`630` 的 bit11=1，`861=1`，`710` 可能=1 |
| X01 | 消防报警 `FireAlarmX1` | 建议常闭联锁 | 156 | 0 | 正常闭合，无消防动作 | 报警时看 `605=1`，`895=1`，`861=1` |
| X02 | 远程启停 `FarSwitchX02` | 建议按现场控制要求，通常无源开关量 | 157 | 0 | 由远程启停策略决定 | 这路一般不是故障点。看运行/停机，不看报警。辅助显示 `829` |
| X03 | 送风机过载 `PumpOverX00` | 建议常闭保护点 | 158 | 0 | 正常闭合，过载断开 | 报警时看 `601=1`，`630` 的 bit7=1，`813=1`，辅助显示 `824` |
| X04 | 电动阀联锁 `ValveLinkX4` | 建议常闭联锁 | 159 | 0 | 正常闭合 | 报警时看 `605=1`，`895=1`，`861=1` |
| X05 | 过滤器压差开关 `FilterBlocking` | 建议按压差开关实际触点选，常见常开报警/动作闭合 | 无独立参数 | 固定逻辑 | 正常应为未堵塞状态 | 报警时看 `599=1`，`630` 的 bit5=1，辅助显示 `826` |
| X06 | 加热/再热/辅热故障 `HeatFaultX6` | 建议常闭保护点 | 无独立参数 | 固定逻辑 | 正常闭合 | 报警时看 `605=1`，`895=1`，辅助显示 `825` |
| X07 | 新风预热故障 `PreHeatFaultX7` | 建议常闭保护点 | 无独立参数 | 固定逻辑 | 正常闭合 | 报警时看 `605=1`，`895=1`，`861=1` |
| X10 | 加湿器故障 `HumidifierFaultX10` | 建议常闭保护点 | 无独立参数 | 固定逻辑 | 正常闭合 | 报警时看 `600=1`，`630` 的 bit6=1，`895=2`，辅助显示 `827` |
| X11 | 排风机过载 `ExhaustFanOverX11` | 建议常闭保护点 | 无独立参数 | 固定逻辑 | 正常闭合 | 报警时看 `605=1`，`895=1`，`861=1` |
| X12 | 回风机过载 `ReturnFanOverX12` | 建议常闭保护点 | 无独立参数 | 固定逻辑 | 正常闭合 | 报警时看 `605=1`，`895=1`，`861=1` |
| X13 | 漏水保护 `LeakProtectX13` | 建议常闭保护点 | 无独立参数 | 固定逻辑 | 正常闭合，无漏水 | 报警时看 `605=1`，`895=1`，`861=1` |
| X14 | 回风压差开关 `ReturnPressureX14` | 建议常闭，且正常闭合为通 | 无独立参数 | 固定逻辑 | 正常闭合 | 先看 `889` 是否能累计到 10；异常 10s 后看 `602=1`、`630` 的 bit8=1、`895=4` |
| X15 | 送风压差开关/水流开关 `SupplyPressureX15` | 建议常闭，且正常闭合为通 | 无独立参数 | 固定逻辑 | 正常闭合 | 先看 `888` 是否能累计到 10；异常后看 `602=1`、`630` 的 bit8=1、`895=3`。辅助原始显示看 `828` |

## 4. 每路点怎么现场确认“接对了”

### 4.1 X00~X04

因为 `X00~X04` 可反相，所以现场最稳的做法不是猜常开常闭，而是直接用参数确认。

做法:

1. 先按建议把保护点按常闭接。
2. 上电后不触发故障时，确认总报警 `861=0`。
3. 人为断开该回路。
4. 如果立刻出现对应报警，说明极性对。
5. 如果断开不报警、闭合反而报警，就把对应参数 `155~159` 从 `0` 改 `1`，或从 `1` 改 `0` 再试。

### 4.2 X05~X15

这几路当前代码基本没有做 HMI 极性切换，所以更适合按业务逻辑去试。

做法:

1. 看该路正常状态下，机组是否无报警。
2. 模拟故障动作一次。
3. 看对应报警地址是否变成 1。
4. 如果动作后没有报警，说明现场触点类型或接线逻辑可能和代码假设不一致。

## 5. 最好用的报警对应表

| 报警地址 | 4x | 含义 | 常见对应 DI |
|---:|---:|---|---|
| 599 | 40600 | 过滤器/风压差相关故障 | X05 |
| 600 | 40601 | 加湿/湿度相关故障 | X10 |
| 601 | 40602 | 送风机过载 | X03 |
| 602 | 40603 | 送风/回风压差或水流/防冻故障 | X14、X15 |
| 605 | 40606 | 严重联锁故障综合 | X00、X01、X04、X06、X07、X11、X12、X13 |
| 895 | 40896 | 规格书联锁故障码 | 多路联锁辅助判断 |

`895` 当前代码含义:

| 值 | 含义 |
|---:|---|
| 0 | 无规格书联锁故障 |
| 1 | 消防/阀联锁/漏水/排风机过载/回风机过载/加热故障/预热故障等 |
| 2 | 加湿器故障或湿度相关停用 |
| 3 | 送风压差未建立 |
| 4 | 回风压差未建立 |

## 6. 现场调试建议

最推荐 HMI 做这几组点:

| 页面内容 | 地址 |
|---|---|
| 总报警 | 861、703、710 |
| 本机报警列表 | 594~623 |
| 规格书联锁码 | 895 |
| 送风压差状态 | 888 |
| 回风压差状态 | 889 |
| X03 辅助显示 | 824 |
| X05 辅助显示 | 826 |
| X06 辅助显示 | 825 |
| X10 辅助显示 | 827 |
| X15 辅助显示 | 828 |
| 远程启停状态 | 829 |

## 7. 现在这台机组最该先盯哪几路

结合你前面查到的报警:

- `600=1`：先查 `X10` 加湿器故障和湿度传感器链路
- `601=1`：先查 `X03` 送风机过载
- `605=1`：重点查 `X00/X01/X04/X06/X07/X11/X12/X13`
- `616~619=1`：这是传感器链路，不是 DI 触点本身，分别查 AI2、AI3、房间温度、房间湿度
