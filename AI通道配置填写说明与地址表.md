# 模拟量（AI）通道配置 —— 填写说明与地址表

> 适用：PSC330 内机 / PSC316 外机 / PC 监控软件（FFP-2026.09.14）
> 依据：源码逐行核对（`PSC330RK-V10-内机`、`PSC316RK-V12-外机`、`psc330_monitor_tool`）
> 说明：本文档为**只读核对结论**，未改动任何代码。

---

## 一、数据格式总则（先看这条，其余都好理解）

| 项 | 结论 |
|---|---|
| **数据类型** | **全部是 16 位整数**。Modbus 寄存器只有整数，**没有小数、没有浮点** |
| **负值** | 用二进制补码。界面上直接填负号即可（如 `-50`），固件按 `int16_t` 解释 |
| **下发是否换算** | **不换算**。你在监控里填什么，就原样送到 316 的 `Parameter[]` |
| **换算在哪发生** | **固件读取侧**：固件拿原始 ADC 采样值，按你配置的量程做一次线性映射，得到工程量 |

**线性映射公式**（两块板逻辑一致）：

```
工程量 = (原始值 − 信号下限) × (工程量上限 − 工程量下限) ÷ (信号上限 − 信号下限)
         + 工程量下限 + 校准偏移
```

> 例（316，默认值）：原始 1200，信号 400~2000，工程量 0~5000
> → (1200−400)×5000÷1600 + 0 = **2500**

---

## 二、每个字段是什么意思、范围多少、怎么填

### 概括表

| # | 字段 | 控件 | 316 范围 | 330 范围 | 单位 | 说明 |
|---|---|---|---|---|---|---|
| 0 | **使能** | 下拉 | 0 / 1 | 0 / 1 | — | 0=禁用 1=启用 |
| 1 | **信号类型** | 下拉 | 0 / 1 | 0 / 1 | — | 0=0-10V，1=4-20mA |
| 2 | **信号下限** | 输入 | 0 ~ 2000 | int16 全域 | 见下 | 传感器输出下限对应的**原始采样值** |
| 3 | **信号上限** | 输入 | 0 ~ 2000 | int16 全域 | 见下 | 传感器输出上限对应的**原始采样值** |
| 4 | **工程量下限** | 输入 | −20000 ~ 20000 | int16 全域 | 工程量单位 | 信号下限对应的物理量 |
| 5 | **工程量上限** | 输入 | −20000 ~ 20000 | int16 全域 | 工程量单位 | 信号上限对应的物理量 |
| 6 | **副下限** | 输入 | −20000 ~ 20000 | int16 全域 | 工程量单位 | **只有 330 的 AI3 用** |
| 7 | **副上限** | 输入 | −20000 ~ 20000 | int16 全域 | 工程量单位 | **只有 330 的 AI3 用** |
| 8 | **校准偏移** | 输入 | −20000 ~ 20000 | int16 全域 | 工程量单位 | 叠加在换算结果上 |
| 9 | **断线判据** | 输入 | 0 ~ 2000 | int16 全域 | 同信号单位 | 低于此值判失效；0=不判 |
| 10 | **单位代码** | 下拉 | 0 ~ 4 | 0 ~ 4 | — | 仅显示用 |
| 11 | **小数位** | 下拉 | 0 ~ 3 | 0 ~ 3 | — | 仅显示用 |

> **"330 范围 = int16 全域"的含义**：330 的配置块在 `Parameter[1229..1288]`，超出了参数表的
> `ParameterSaveSize = 500`，因此**不受 `Parameter_min/max[]` 约束**；固件只校验 4 个枚举字段
> （使能/类型/单位/小数位），数值字段没有上下限。**填超范围的值不会被拦，但换算结果会失真**。

---

### 逐字段详解

#### 0. 使能（下拉：`0: 禁用` / `1: 启用`）

- **只有 0 和 1**，没有其他值。
- 用途：某路没接传感器时置 0，固件直接输出失效标记，**不参与控制也不报故障**。
- 出厂默认：**1（启用）**

#### 1. 信号类型（下拉：`0: 0-10V` / `1: 4-20mA`）

- **只有这两个选项**。
- 作用：只决定你按哪种传感器去填「信号下限/上限」，**固件本身不做类型判断**
  （换算只用你填的数值，不看类型字段）。
- 出厂默认：**0（0-10V）**

#### 2. 信号下限 / 3. 信号上限 —— ⚠️ 两块板单位不同

这是**最容易填错**的地方。

| 板 | 单位 | 换算 | 举例 |
|---|---|---|---|
| **316 外机** | **原始 ADC 端点** | 固件**直接使用**，不再×10 | `400` ≈ 0.5V，`2000` ≈ 4.5V |
| **330 内机** | **0.1V / 0.1mA** | 固件**内部 ×10** 后与原始值比较 | `50` = 5.0V（×10 → 500 = 0.01V 单位的 5.00V） |

**为什么不一样**：330 的原始 AI 值是"0.01V"刻度（界面上"原始值"栏 400 = 4.00V），
而它的量程参数历来按 0.1V 存（历史约定），所以固件里统一 ×10 对齐；
316 的采样刻度本身就是 400~2000（对应 0.5~4.5V），所以直接填原始端点。

**怎么填**：
- 316：查你的压力变送器量程。0.5~4.5V 型 → 下限 `400`、上限 `2000`；1~5V 型 → 先换算成端点再填。
- 330：0~10V 型 → 下限 `0`、上限 `100`；0.5~4.5V 型 → 下限 `5`、上限 `45`；4~20mA 型 → 下限 `40`、上限 `200`。

**填错的后果**：工程量整体偏移（零点不对）或斜率不对（满量程不对），严重时出现负值。

#### 4. 工程量下限 / 5. 工程量上限

信号端点对应的**物理量**。单位就是该通道的工程量单位：

| 通道 | 工程量单位 | 说明 |
|---|---|---|
| 316 全部 4 路压力 | **0.01 Bar** | 例如 `5000` = 50.00 Bar（与 `Parameter[52]` 高压量程一致） |
| 330 AI0 房间压差 | Pa | 整数 |
| 330 AI1 房间湿度 | **0.1 %RH** | `1000` = 100.0 %RH |
| 330 AI2 CO₂ | ppm | 整数 |
| 330 AI3 送风压力 | Pa | 主工程量 |
| 330 AI3 送风风量 | **0.1 m/s** | 副工程量 |
| 330 AI4 新风湿度 | **0.1 %RH** | — |

> 316 的 `0.01 Bar` 可从固件故障判据反推：`ErrorCheck.c` 中高压保护比较 `> 3800`，
> 对应铭牌上的 38.00 Bar。

#### 6. 副下限 / 7. 副上限 —— **只有 330 的 AI3 需要填**

**什么意思**：330 的 `AI3` 这一路物理输入，**同时算出两个工程量**，分别喂给两个控制逻辑：

| 输出 | 落点 | 被谁消费 |
|---|---|---|
| 主工程量（Pa） | `Parameter[1125] MainFanPaDisp` | 热回收控制（`UserAction.c:3153`） |
| 副工程量（0.1 m/s） | `Parameter[1126] MainFanm3hDisp` | 送风机 PID（`FanControl.c:506-518`） |

所以 AI3 需要**两套工程量量程**：一套给压力、一套给风量。
- 副下限/副上限 = 风量那一套（0.1 m/s）
- **其余 4 路（AI0/AI1/AI2/AI4）以及 316 的全部 4 路都不用，保持 0 即可**

#### 8. 校准偏移 —— 偏的是什么？

**偏的是"换算之后的工程量结果"**，不是原始值、不是量程。

```
最终显示值 = 线性映射结果 + 校准偏移
```

- **单位**：该通道的工程量单位（316 是 0.01 Bar，330 各通道见上表）
- **用途**：修掉传感器本身或采集板的系统误差
- **怎么定**：拿标准表比对。若设备显示 24.80 Bar、标准表 25.00 Bar → 偏移填 **+20**（0.20 Bar）

> 注意：330 的换算函数**会把结果钳位到 [工程量下限, 工程量上限]**，所以偏移加完后若超出量程会被截断；
> 316 的换算**不钳位**，超范围就如实显示（保留了升级前 `PressureToDisplay` 的行为）。

#### 9. 断线判据 —— 怎么填？

**含义**：原始采样值**低于**这个数 → 判定该通道传感器失效。

| 场景 | 怎么填 |
|---|---|
| **0-10V 型传感器** | 填 **0（不判）**。因为 0V 是合法读数，无法区分"断线"和"读到 0" |
| **4-20mA 型传感器** | 填 **3.6mA 对应的原始值**（行业惯例：<3.6mA 即断线）<br>· 316：若 4mA=400、20mA=2000，则 3.6mA ≈ **360**<br>· 330：若下限 40(4.0mA)、上限 200(20.0mA)，则 3.6mA ≈ **36** |
| 不想判 | 填 **0** |

**失效后的输出**（两块板不同，对接显示要留意）：

| 板 | 失效标记 |
|---|---|
| 316 外机 | **9999** |
| 330 内机 | **0** |

#### 10. 单位代码（下拉）

`0: Pa` / `1: ℃` / `2: %RH` / `3: ppm` / `4: CMH` / `5: Bar`
**仅供显示**，不参与任何计算。

#### 11. 小数位（下拉）

`0 位小数` / `1 位小数` / `2 位小数` / `3 位小数`
**仅供显示**，不参与任何计算。

---

## 三、地址表

### 3.1 330 内机 AI 配置块 —— `Parameter[1229..1288]`
> 监控上读写的 **HMI 地址 = Modbus 地址**（330 的 HMI 直接映射 `Parameter[]`）

| 通道 | 基址 | +0 使能 | +1 类型 | +2 信号低 | +3 信号高 | +4 工低 | +5 工高 | +6 副低 | +7 副高 | +8 偏移 | +9 断线 | +10 单位 | +11 小数 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AI0 房间压差 | 1229 | 1229 | 1230 | 1231 | 1232 | 1233 | 1234 | 1235 | 1236 | 1237 | 1238 | 1239 | 1240 |
| AI1 房间湿度 | 1241 | 1241 | 1242 | 1243 | 1244 | 1245 | 1246 | 1247 | 1248 | 1249 | 1250 | 1251 | 1252 |
| AI2 CO₂ | 1253 | 1253 | 1254 | 1255 | 1256 | 1257 | 1258 | 1259 | 1260 | 1261 | 1262 | 1263 | 1264 |
| AI3 送风压/风量 | 1265 | 1265 | 1266 | 1267 | 1268 | 1269 | 1270 | 1271 | 1272 | 1273 | 1274 | 1275 | 1276 |
| AI4 新风湿度 | 1277 | 1277 | 1278 | 1279 | 1280 | 1281 | 1282 | 1283 | 1284 | 1285 | 1286 | 1287 | 1288 |

**持久化**：330 固件的 `IndoorAiJournal` 原子双槽日志
（EEPROM 槽 A `1152`、槽 B `1280`，各 128 字节）。
断电瞬间写入不会写坏：要么旧值、要么新值。

### 3.2 316 外机 AI 配置块 —— `Parameter[302..349]`（316 **本地**地址）
> 注意：这是 **316 自己的参数地址**，不是 330 的地址。正常使用不用直接对这些地址读写。

| 通道 | 基址 | +0 使能 | +1 类型 | +2 信号低 | +3 信号高 | +4 工低 | +5 工高 | +6 副低 | +7 副高 | +8 偏移 | +9 断线 | +10 单位 | +11 小数 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| AI0 1#高压 | 302 | 302 | 303 | 304 | 305 | 306 | 307 | 308 | 309 | 310 | 311 | 312 | 313 |
| AI1 2#高压 | 314 | 314 | 315 | 316 | 317 | 318 | 319 | 320 | 321 | 322 | 323 | 324 | 325 |
| AI2 1#低压 | 326 | 326 | 327 | 328 | 329 | 330 | 331 | 332 | 333 | 334 | 335 | 336 | 337 |
| AI3 2#低压 | 338 | 338 | 339 | 340 | 341 | 342 | 343 | 344 | 345 | 346 | 347 | 348 | 349 |
| **版本标记** | 350 | — | — | — | — | — | — | — | — | — | — | — | — |

**持久化**：随 316 的参数区一起保存（`WriteParamaterEEprom`，EEPROM 字节 `16 + 2×索引`）。
**版本标记**：固件写入 `12641`（`0x3161`）表示该外机配置已完成迁移；**只读**。

### 3.3 330 网关镜像窗口 —— `Parameter[2680..2875]`
> **监控软件实际读写的地址**。每台外机 49 字（48 配置 + 1 版本标记）。

| 外机 | 范围 | +0 使能 | 通道基址（4 路） |
|---|---|---|---|
| 1# | `2680..2728` | 2680 | AI0=2680, AI1=2692, AI2=2704, AI3=2716，版本=2728 |
| 2# | `2729..2777` | 2729 | AI0=2729, AI1=2741, AI2=2753, AI3=2765，版本=2777 |
| 3# | `2778..2826` | 2778 | AI0=2778, AI1=2790, AI2=2802, AI3=2814，版本=2826 |
| 4# | `2827..2875` | 2827 | AI0=2827, AI1=2839, AI2=2851, AI3=2863，版本=2875 |

每通道内偏移与 3.2 一致（+0 使能 … +11 小数位）。

### 3.4 316 发布区 —— `Parameter[1528..1576]`（316 本地，只读镜像）
> 316 每扫描周期把 `302..350` 复制到这里，供 330 轮询。
> **不要直接写这里**——写了也会在下一周期被覆盖。

---

## 四、填进去的值能否被 330 读取并转发给 316？—— **能，已逐跳核对**

| 跳 | 位置 | 对值的处理 | 结论 |
|---|---|---|---|
| 1 | 监控 → 330 HMI `2680+` | Modbus FC6 单寄存器写，16 位整数 | **原样** |
| 2 | 330 `ParamCanWriteFromHMI()` | 网关地址 → 检查 `IsHMIReadOnly`（返回可写）；枚举字段范围校验 | **不改值** |
| 3 | 330 `PSC316Gateway_HMIWrite()` | `targetAddr = 302 + offset`；值不动 | **原样** |
| 4 | 330 `PSC316Gateway_QueueSingleWrite()` | 存入写任务 | **原样** |
| 5 | 330 `BuildWriteSingleFrame()` | `buff[4]=value>>8; buff[5]=value` | **字节级原样** |
| 6 | 316 `ModbusUart1Fun6()`（对应串口） | `Parameter[Addr] = (hi<<8)+lo` | **原样** |
| 7 | 316 `OutdoorAiCfgWriteValid()` | 只校验 0/1/10/11 四个枚举字段 | **不改值** |
| 8 | 316 换算 `OutdoorScaleAiChannel()` | 按你配置的量程做线性映射 | **此处才换算** |

**回读路径同样原样**：
316 `System_Real()` 镜像 → 330 轮询 `1528` → `StoreBlock` 存入
`PSC316AiCfgMirror`（`int16_t`，负值正确） → `PublishToHMI()` 发布到 `2680+` → 监控读取。

**所以：你在监控里填的整数，会一分不差地到达 316 并被应用。**

---

## 五、⚠️ 重要限制：**整组写入会被 330 拒绝**

### 现象
用 Modbus **FC16（写多个寄存器）** 向网关地址 `2680+` 一次写多个寄存器，会被 330 直接丢弃。

### 代码依据
`PSC330RK-V10-内机/User/modbus/modbus.c`，`ModbusUart1Fun16()`：

```c
for(i=0;i<Num;i++)
{
    if(PSC316Gateway_IsHMIAddr(Addr+i) && (Num != 1))
    {
        /* The gateway has one asynchronous write slot; reject before any partial write. */
        ucUart1ReceLen = 0;
        return;
    }
}
```

### 原因
330 的网关转发**只有一个异步写任务槽**（`PSC316WriteJob`），无法排队多个寄存器，
所以设计上**在写入前就整体拒绝**，避免出现"写了一半"的中间态。

### 影响
- ✅ **单寄存器写可用**：FC6，或 `Num == 1` 的 FC16
- ❌ **多寄存器整组写不可用**：`Num > 1` 的 FC16 一律被拒

> 因此监控软件里的 **「整通道」** 和 **「整组写入全部通道」** 两个按钮，
> 当前下发会被 330 拒绝（写入无效果）。**请使用每个字段后面的「写」按钮逐字段写入。**
>
> 待办：二选一 —— ①把这两个按钮改成逐字段循环写入（界面不变，后端循环发 FC6）；
> ②移除这两个按钮。

### 附带说明
FC16 路径下，330 **不做**枚举字段校验（`HMIParamMultiWriteValid` 对网关地址 `continue`），
但**316 侧仍会校验**（`OutdoorAiCfgWriteValid`），非法值不会写进参数区。最终把关在 316。

---

## 六、330 与 316 的差异对照（容易踩坑处）

| 项 | 330 内机 | 316 外机 |
|---|---|---|
| 配置块地址 | `Parameter[1229..1288]` | `Parameter[302..349]`（+350 版本标记） |
| 监控访问地址 | 直接读写 `1229+` | **经网关**读写 `2680 + (台-1)×49` |
| **信号上下限单位** | **0.1V / 0.1mA**（50 = 5.0V） | **原始采样端点**（400 ≈ 0.5V） |
| **压力工程量单位** | — | **0.01 Bar**（5000 = 50.00 Bar） |
| 数值字段范围校验 | 无（超出参数表范围） | 有（表范围 −20000~20000 等，**上电时生效**） |
| 换算结果钳位 | **钳位**到工程量上下限 | **不钳位** |
| 失效输出标记 | **0** | **9999** |
| 持久化 | 原子双槽日志（EEPROM 1152/1280） | 随参数区保存（EEPROM `16+2×索引`） |
| 副工程量（+6/+7） | **AI3 使用** | 不使用，保持 0 |

### 关于 316 的"上电时生效"
316 的 `Readeeprom()` 在上电时会把 `Parameter[302..349]` 与
`Parameter_min/max[]` 比对，**超出范围的值会被静默回退成默认值**。
所以如果填了超出第二节表格范围的值，**当次运行有效，但断电重启后会恢复默认**。
（这也是本次改造中必须同时重设三张参数表的原因。）

---

## 七、快速上手（推荐操作顺序）

1. 顶部确认**串口号 / 从站地址**，连接 330
2. 切到 **「AI 通道配置」** 页
3. 点该区块右上角的 **「读回」** → 把外机当前配置取回并填入各控件
4. 按第二节的说明，修改需要改的字段
5. 点**该字段后面的「写」** → 只下发这一个寄存器，并**立即回读校验**；
   若弹出"回读不一致"，说明固件拒绝了该值（多半是枚举字段填了非法值）
6. 改完断电重启一次，确认值仍在（验证持久化）

> 316 的改动建议在**机组停机**时进行。
