# 直膨机组调试记录 —— 2026-09-15 / 09-16

> 以后现场出问题先看这份。**第 1 节是未修的问题，优先看。**

---

## 1. ⚠️ 未修 —— `UserAxSet` 被固件强制覆盖（**我引入的，优先级最高**）

### 现象
某路模拟量接 **0-10V**，输入 6V，工程量读到 **5985**（配置的量程上限是 5000）。**正好是应有值 3000 的 2 倍。**

### 原因
`PSC316RK-V12-外机/WELLTHINKER/User/user.c:395-404`：

```c
UserA0Set = 1;  //A0: 4-20mA      ← 无条件执行，在每次开机的 ParameterInit() 里
UserA1Set = 1;  //A1: 4-20mA
UserA2Set = 1;  //A2: 4-20mA
UserA3Set = 1;  //A3: 4-20mA
```

**这四行把四路 ADC 支路强行设成 4-20mA，覆盖 EEPROM 里存的值 —— 每次开机都覆盖。**

- `UserA0Set..A3Set` = `Parameter[468..471]`，**是存在 EEPROM 里的、按机器接线而定的设置**，设计上就该允许 4-20mA / 0-10V 的机器各存各的。固件不该在开机时改写它。
- 这四行是 **2026-09-15 的 V12.3 改动**加的（原代码 A0/A1 是 0，A2/A3 是 1；改动把 A0/A1 也设成 1）。**意图是对的**（让信号量程与 AI 配置块的 400/2000 对上），**手段错了** —— 应该只改出厂默认 `Parameter_def`，不该写运行时赋值。

### 造成的两个后果
1. **0-10V 的机器读数量程差 2 倍**
   | 支路 | 系数 | 6V 的信号量 | 工程量（信号 0~1000 → 0~5000） |
   |---|---|---|---|
   | 0-10V（应该用的） | ×0.268 | 600 | **3000** |
   | 4-20mA（实际被强制的） | ×0.537 | 1202 | **6010** ← 实测 5985 |

2. **可能误报探头故障**：4-20mA 支路的有效性下限是 `AD > 500`，0-10V 是 `AD > 100`。0-10V 信号套 4-20mA 的窗 → **低于约 1.3V 的信号被判成断线/故障** → `AIxTempErr` → 系统级+整机级故障 → **停机**。

### 修法（V12.8，未做）
1. **删掉那四行无条件赋值** → 让 EEPROM 说了算
2. 出厂默认仍由 `Parameter_def[ParameterSaveSize-32..-29] = 1`（4-20mA）给，与产线一致
3. 0-10V 的机器：把 `Parameter[468+ch]` 写 **0** 并存进 EEPROM → 永久生效

### 动手前必须先问清楚
**现场各台外机分别接 4-20mA 还是 0-10V？** 混用 / 全 0-10V / 全 4-20mA，三种情况下默认值该取不同。

---

## 2. 已修 + 已实机验证

| 问题 | 修法 | 验证 | 提交 |
|---|---|---|---|
| **偏移 `[498]` 清零后反复变回 28** | **V12.7**：`Parameter[1500..1521]` 温度校准镜像块与上传块 `1420..1501` 的最后两个字**地址撞车** —— `UserAction.c` 每扫描把 `Parameter[1501]`（故障字）镜像到 `Parameter[498]`（AI1 高压偏移）。镜像块下移到 `1580..1601`，`ParameterSize` 1600→1640 | 故障字=28 活跃时 `[498]` 保持 0、240 秒 0 次写入；硬复位后仍是 0 | 316 `8df36eb` |
| **AI 通道配置「写了不保存」** | **V12.6** `Parameter_max[72] = 600` —— 修「`Parameter[190]` 每扫描覆写 `Parameter[72]`、72 超范围卡死 → 存盘扫描永久停在 72、**73 以上全部存不进 EEPROM**」 | `Parameter[]` 与 `Old_Parameter[]` **500 个下标全部一致**；AI 配置块端到端：写 → `Old` 同步 → **硬复位后仍在** | 316 `8df36eb` 之前 |
| **越界写入焊死写槽**（写不存在的外机后，所有写入报错误码 3、只能断电） | **V10.5**：`QueueSingleWrite` 加外机号越界拒绝（错误码 5）+ 写任务停滞超时放弃 | 越界立即被拒、写槽不占；紧接着正常写入 4.2 秒完成 | 330 `11acb08` |
| **工具误报"写成功"**（330 会把自己发出去的值填进镜像） | `wait_outdoor_gateway_done` 在镜像匹配之外**再看错误码** | 语法/逻辑核对 | 工具 `c026841` |

---

## 3. 未验证 / 已知未做

- **330 写任务超时自愈**：代码已改已编译（`PSC316_WRITE_JOB_TIMEOUT 250`），但**那条路径实测触发不到**（要精确到毫秒的人工配合：任务建好后单元才掉线）。风险已评估：它**只可能释放写槽、不会阻断正常写入**（建任务/重试/phase1→2/phase2→3 四处清零计数），最坏是"比预期慢"。
- **工具 AI 配置页的下拉框从不被设备值播种**：`_ai_cfg_sync_current()` 只更新蓝色"当前值"标签，**不碰写入控件**（设计如此）。但下拉框永远停在第 1 项 —— 界面上写"当前值 `4: Bar`"、下拉框却是 `0: Pa`，**点旁边的「写」就会把单位改成 Pa**。「整通道写入」更危险。建议改法：首次刷新时播种一次。

---

## 4. 现场速查

### 4.1 故障字 `OutdoorFaultStatusDisp` = 316 `Parameter[1501]`
生成处：`ErrorCheck.c:137-148`

| 位 | 含义 | 置位条件 |
|---|---|---|
| bit0 / bit1 | 压缩机级 1# / 2# | `CompLowPress` / `CompHightPress` / `Over` / `UnitHTemp` / `OilProtect` |
| **bit2** | **系统级 1#** | `AI0TempErr`(1#高压) 或 `AI2TempErr`(1#低压) 或 `Exhaust1` / `XiQi1` / `Fin1` |
| **bit3** | **系统级 2#** | `AI1TempErr` / `AI3TempErr` / `Exhaust2` / `XiQi2` / `Fin2` |
| **bit4** | **整机级** | `EnviTempErr` / 风机 / `SystemComunicateErr` / `ModulePowerErr`；**另外 `ErrorCheck.c:602`——所有启用的压缩机都故障时自动置位** → `StopOutdoorAll()` 停机 |
| bit5 / bit6 | 环境锁定 / 配置非法 | |

**2026-09-16 实测 28 = bit2+bit3+bit4** = AI0/AI1/AI3 三路探头故障 → 两个系统级 → 所有压缩机故障 → 整机级 → 停机。**根因是台面上那三路模拟量悬空**（AI2 接了信号就正常）。**⚠ 但请注意第 1 节：如果是 0-10V 接线而支路被强制成 4-20mA，低电压信号会被误判成故障 —— 这条要看是不是同一个原因。**

故障标志 = `Parameter[COMPERR + n]`，`COMPERR = 605`；`{3:AI0, 4:AI1, 5:AI2, 6:AI3, 7:环境, 8:翅片1, 10:排气1, 12:吸气1, 18:模块电源, 19:系统通讯}`

### 4.2 ADC 支路（`Parameter[468..471]`，0=0-10V，1=4-20mA）

| | 0-10V | 4-20mA |
|---|---|---|
| 换算 | `AI = AD × 0.268` | `AI = AD × 0.537` |
| 满量程 | 10V → **1000** | 20mA → **2000** |
| 零点 | 0V → 0 | 4mA → **400**（活零点） |
| 有效窗 | `100 < AD < 3800` | `500 < AD < 3800` |

硬件同一路（499Ω 采样 + 43/143 分压，12 位 ADC、3.3V 参考）：4mA→AD≈745，20mA→AD≈3725。

**上层工程量换算与支路无关**（`OutdoorScaleAiChannel`，**不截断**）：
```
工程量 = (raw − 信号下限) × (工程量上限 − 工程量下限) / (信号上限 − 信号下限) + 工程量下限 + 偏移
```
**改支路必须同时改信号上下限**：4-20mA 是 `400~2000`，0-10V 是 `0~1000`。只改一个 → 工程量差 2 倍。

### 4.3 ST-Link 调试地址

**316（V12.7，`target_override="stm32f103rc"`）**：`Parameter`=0x2000164A、`Old_Parameter`=0x20001206、`EEpflag`=0x20000028、`CPURomWriteError`=0x2000003E、`OldParame1300`=0x20002328、`Parameter[498]`=0x20001A2E、`Parameter[1501]`=0x20002204、`Parameter[1580]`=0x200022A2

**330（`target_override="stm32f407ze"`）**：`Parameter`=0x200030C8、`PSC316WriteJob`=0x200098B8、`PSC316WriteState`=0x20000EA0、`PSC316WriteError`=0x20000EA4、`UnitWriteStatus`=0x20007858

**有用的手法**：
- 写监视点抓"谁在写某个变量"：`t.set_watchpoint(addr, 2, Target.WatchpointType.WRITE)`，命中后读 PC/LR/xPSR(IPSR)/R0-R3。**IPSR=0 说明是主循环不是中断。**
- **复位探针**：把 `CPURomWriteError` 写成 4242 → `target.reset()` → 读回 0 = 确实重启过。验证断电保存用它。
- **校验"是否真写进 EEPROM"**：`WriteParamaterEEprom()` 的 case 3 **只在回读校验通过后**才更新 `Old_Parameter[t]` → **`Old == Parameter` 就等于已入库**，不必断电。

### 4.4 各仓库

| 目录 | 远程 | 说明 |
|---|---|---|
| `code/PSC316RK-V12-外机/` | `.../PSC316RK-V12--.git` | 316 外机固件，分支 `fixed-frequency-production` |
| `code/PSC330RK-V10-内机/` | `.../PSC330RK-V10--.git` | 330 内机固件，同分支 |
| `code/psc330_monitor_tool/` | `.../psc330_monitor_tool.git` | 监控软件（子模块），同分支 |
| `code/`（总目录） | `.../tool.git` | 它自己的 `.gitignore` **排除了上面两个固件目录**（那两个是独立仓库） |
| `F/直膨机组tool/` | `.../tool.git` | 2026-09-16 建的归档：docs + tools + 过程文档 |

---

## 5. 待做：再热 / 辅热 规格书符合性审计（**下一轮开工就做这件**）

### 5.1 规格书条款（要逐条核对固件是否强制）

> **1、再热**：制冷时恒温用，或恒湿时再热用。
> **2、辅热**：热泵制热时补充加热量。
> **3、〖再热/辅热方式〗中"再热"与"辅热"的确定：**
> - **A**　〖制热方式〗**非"热泵制热"**时：〖再热/辅热方式〗里的选项**均不指辅热**；此时〖再热是否启用〗选"是"才指再热，否则〖再热/辅热方式〗**无效**。
> - **B**　〖制热方式〗**为"热泵制热"**时：〖再热/辅热方式〗里的选项**均指辅热**；此时〖再热是否启用〗选"是"则**再热/辅热共用**，且〖再热/辅热方式〗**不能为"无"**。
> **4、**〖制热与再热/辅热是否共用〗选"**否**"时，制热与再热/辅热**不能同时选分段电加热器**。

### 5.2 四个字段（已定位，可直接开工）

| 字段 | 地址 | 取值 | 常量名 |
|---|---|---|---|
| **再热是否启用** | `127` | 0=禁用, 1=启用 | `ReheatEnableSet` |
| **制热方式** | `128` | 0=无, **1=热泵制热**, 2=AO, 3=1:2, 4=1:2:4, **5=SCR** | `HeatModeSet` |
| **制热与再热/辅热是否共用** | `129` | 0=不共用, 1=共用 | `HeatReheatShareSet` |
| **再热/辅热方式** | `332`（=`INDOOR_SPEC_BASE(315)+17`） | 待查 | `ReheatModeSet` |

**这四个正是工具里的"联动写入组"** `COUPLED_HEAT_CONFIG_ADDRESSES = {127,128,129,332}`
—— 与规格书要求的"互相牵制"吻合 ✓

### 5.3 ⚠️ 核对时必踩的坑：两处 SCR 编号不同

| 字段 | 无 | AO(0-10V) | 1:2 | 1:2:4 | **SCR** |
|---|---|---|---|---|---|
| **制热方式** `HeatModeSet`(128) | 0 | 2 | 3 | 4 | **5** |
| **预热输出方式** `PreheatModeSet`(333) | 0 | 1 | 2 | 3 | **4** |

**同样是"可控硅"，一个是 5、一个是 4。** 核对条款 B（"选项均指辅热"）必须对到
`HeatModeSet` 的表，**不能拿预热那张表套**。

### 5.4 要查什么

1. 找 `127 / 128 / 129 / 332` 被**读取**的地方（`grep -rn "ReheatEnableSet\|HeatModeSet\|HeatReheatShareSet\|ReheatModeSet" User/`）
2. 逐条比对 **A / B / 4** 是否被固件真正强制：
   - **A**：`HeatModeSet != 1` 时，`ReheatModeSet` 的选项是否**都不指辅热**；`ReheatEnableSet != 1` 时 `ReheatModeSet` 是否**被忽略**
   - **B**：`HeatModeSet == 1` 时，选项是否**都指辅热**；`ReheatEnableSet == 1` 时是否共用、且 `ReheatModeSet` 是否**禁止为"无"**
   - **4**：`HeatReheatShareSet == 0` 时，是否**真的禁止**制热与再热/辅热**同时选分段电加热器**
3. 参考：制热/辅热的分段执行在 `UserAction.c` 里，关键词 `HEAT_OUTPUT_*`（`User.h:770-774`）、
   `PreheatModeSet` 的用法见 `UserAction.c:852-859 / 1188-1200`、`ErrorCheck.c:334-337`

---

## 6. 待做：3.3 机组开关机逻辑 逐模式时序验证（**实机任务，未开始**）

### 6.0 ST-Link 位置（用户问过）
- **跑时序阶段 → 插 330**（开关机时序是 330 的逻辑，要读时序状态/计时器）
- **测紧急停机 + 关阀步数 → 换到 316**（**电子膨胀阀由 316 驱动**，`eevCTRL.c` 在 316 工程里）

### 6.1 通风开关机
**开机**：开机指令 → 送风阀(若有) → 新风阀 → 回风阀 → 排风阀 → 混风阀 → **150s**（无风阀时 **10s**）
→｛[排风机启动 → **30s(±60s 可调)**](若有) → 送风机启动 → [**30s** → 回风机启动](若有)｝
→ 送风机压差开关**持续闭合 10s**(若有) → 回风机压差开关**持续闭合 10s**(若有) → **完成**

**关机**：关机指令 →｛送风机关闭 → [**30s** → 回风机关闭](若有) → [**30s(±60s 可调)** → 排风机关闭](若有)｝
→ **30s** → 新风阀关 → 排风阀关 → 回风阀关 → 送风阀关 → 混风阀关 → **完成**
（注：**关机时排风机和送风机"先开的后关"**）

### 6.2 制冷开关机
**开机**：同通风开机（阀 → 150s/10s → 排风机→30s→送风机→30s→回风机 → 压差开关各 10s）
→ **室外机允许制冷启动** → **再热允许启动**(若有) → **加湿器允许启动**（〖恒温恒湿制冷〗时）→ **完成**

**关机**：[**加湿器关闭 → 30s**]（〖恒温恒湿制冷〗且有加湿时）→ **再热关闭**(若有) → **室外机制冷关机**
→ **30s** →｛送风机关闭 → [30s → 回风机关闭] → [30s(±60s) → 排风机关闭]｝→ **30s**
→ 新风阀关 → 排风阀关 → 回风阀关 → 送风阀关 → 混风阀关 → **完成**

### 6.3 制热开关机
**开机**：同通风开机（阀 → 150s/10s → 风机链 → 压差开关各 10s）
→ **预热允许启动**(若有) → **制热允许启动**（〖制热方式〗为热泵制热时：**室外机允许启动 + 辅热允许启动**）
→ **加湿器允许启动**（〖恒温恒湿制热〗时）→ **完成**

**关机**：**加湿器关闭**（〖恒温恒湿制热〗时）→ **30s** → **制热关闭**（热泵制热时：**室外机及辅热关闭**）
→ **预热关闭**(若有) → **60s** →｛送风机关闭 → [30s → 回风机关闭] → [30s(±60s) → 排风机关闭]｝→ **30s**
→ 新风阀关 → 排风阀关 → 回风阀关 → 送风阀关 → 混风阀关 → **完成**

> ⚠ **用户特别要求：制热和制冷都要单独验证。**
> 注意制冷关机是 `加湿器→30s→再热→室外机→30s`，制热关机是 `加湿器→30s→制热→预热→60s` —— **两个 60s/30s 不同**。

### 6.4 消排毒开关机
**消毒模式**：先按当前模式执行**关机逻辑**（原本就关机则忽略）→ 开**回风阀(全开)+送风阀** → **150s**
→ 开送风机 → **20s** → 开消毒装置并计时 → 到〖消毒模式运行时间〗→ 关消毒装置
→ 按〖结束后运行模式选择〗：**停机** = 按通风关机顺序停；**正常运行** = 按消毒前的模式开机

**排毒模式**：先按当前模式执行**关机逻辑**（原本就关机则忽略）→ 开**新风阀(全开)+送风阀+排风阀** → **150s**
→ 开**送风机+排风机**（顺序按**送排风机间隔时间**）并计时 → 到〖排毒模式运行时间〗
→ 按〖结束后运行模式选择〗执行（同上）

**消排毒模式**：先按当前模式执行**关机逻辑**（原本就关机则忽略）→ 开**回风阀(全开)+送风阀** → **150s**
→ 开送风机 → **20s** → 开消毒装置并计时 → 到〖消毒模式运行时间〗→ 关消毒装置 → 关送风机 → **30s**
→ 关回风阀 → 开**新风阀(全开)+送风阀+排风阀** → **150s** → 开**送风机+排风机**（顺序按间隔时间）并计时
→ 到〖排毒模式运行时间〗→ 按〖结束后运行模式选择〗执行（同上）

### 6.5 紧急停机
**除电子膨胀阀外所有用电设备均失电；电子膨胀阀以 20 步/秒速度关闭。**

> **关阀步数 = 电子膨胀阀当前步数 + 10 步**（原文如此，需实测确认）
> 这一条要用 ST-Link 读 316 的 EEV 步数（`eevCTRL.c` 在 316 工程），**需要把 ST-Link 换到 316**。

### 6.6 验证方法（下一轮）
1. **静态核对**：把每段时序在 330 源码里找到对应的状态机/计时器常量，逐条对时间值（150s / 10s / 30s / 20s / 60s）
2. **实机跑时序**：每个模式各跑一次开机+关机，用串口/ST-Link 记录各步实际时刻，与规格逐条比对
3. **重点**：制冷关机 vs 制热关机的**不同延时**（30s vs 60s）；以及"排风机和送风机先开的后关"
4. **紧急停机**：ST-Link 在 316，读 EEV 当前步数 → 触发紧急停机 → 看关阀速率是否 20 步/秒、步数是否 = 当前+10

### 6.7 计时测试的读取点（已定位，ST-Link 插 **330**）

用户的要求是「**主要是计时，看 IO 电输出**」。做法：ST-Link 插 330 → **高频轮询 IO 输出变量 + 打时间戳**
→ 触发一个模式 → 自动算出每一位/每一路跳变的间隔 → 与规格的 150s/10s/30s/20s/60s 逐条相减比对。

| 读什么 | 位置 | 说明 |
|---|---|---|
| **`DA[0..9]`** 模拟输出 | **`0x20001872`**，`0x14`=**20 字节**（10 个字） | **一次读全 10 路 AO** ✓ |
| `FreshValveAO3` / `MixValveAO5` / `ExhaustValveAO6` | `DA[3]` / `DA[5]` / `DA[6]` | 新风阀 / 混风阀 / 排风阀（模拟量型） |
| **`Y00..Y07` 等数字输出** | **`output[]` @ `0x20001364`，16 字节** —— `io.h:24 #define Y00 output[0]` | **`Y00..Y15` = `output[0..15]`，一次读全 16 路 DO** ✓ |
| `Heat1Y00`(Y00) `SupplyValveY04`(Y04) `ExhaustValveY11`(Y11) `FreshValveY14`(Y14) | 即 `output[0]` / `[4]` / `[11]` / `[14]` | 制热1 / 送风阀 / 排风阀 / 新风阀 |
| **打包 IO 字** | `DispAction.c:157-158`：`Y00→bit8 … Y07→bit15` | 8 个 DO 打包进一个 16 位字（就是 HMI 看到的 IO 状态） |

**★ 计时测试一次读全 36 字节**：`output`(0x20001364, 16B) + `DA`(0x20001872, 20B) —— 全部 DO + AO。

### 6.8 已找到的时序参数（与规格对照）

| 参数 | 地址 | 范围 | 规格对应 |
|---|---|---|---|
| `ReturnFanDelaySet` | **132** | **0~90s** | 「送风机→**30s(±60s可调)**→回风机」✓ **30±60=0~90 吻合** |
| `ExhaustValveDelaySet` | **135** | **0~90s** | 「**30s(±60s可调)**→排风机关闭」✓ |
| `SupplyFanSensorTypeSet` | 133 | 0=风量/转速, 1=压差 | 「压差开关」相关 |
| `CoolControlTypeSet` / `HeatControlTypeSet` | 125 / 126 | 0=恒温, 1=恒温恒湿 | 决定是否走加湿器支路 |

**尚未找到**（下一轮继续）：`150s / 无风阀 10s` 的阀开启延时、`送风机→30s→回风机` 的间隔常量、
`加湿器关闭→30s`、**制热关机的 60s**（与制冷关机的 30s 不同，是重点）。

**下一轮第一步**：`fromelf -s` 把 `Y00..Y1x` 的地址取全 → 写"高频采样 + 自动比对时序"脚本。

---

## 7. 已做：X 输入极性「反转」按钮（工具 `FFP-2026.09.16`，**未实机验证**）

### 背景
330 的 **14 路 X 输入**全部支持极性反转，参数已在工具寄存器表里（**不用加定义**）：

| 地址 | 点 | 地址 | 点 |
|---|---|---|---|
| `155` | X00 极性 电源/相序保护 | `392` | X10 极性 加湿器故障 |
| `156` | X01 极性 消防报警 | `393` | X11 极性 排风机过载 |
| `157` | X02 极性 远程启停 | `394` | X12 极性 回风机过载 |
| `158` | X03 极性 送风机/泵过载 | `395` | X13 极性 漏水保护 |
| `159` | X04 极性 风阀联锁 | `396` | X14 极性 回风压差开关 |
| `389` | X05 极性 过滤器堵塞 | `397` | X15 极性 送风压差开关 |
| `390` | X06 极性 加热/再热故障 | | |
| `391` | X07 极性 预热故障 | | |

`POLARITY_MAP = {0: "反相/常闭断开报警", 1: "正相/常开闭合报警"}`（`app.py:302`）
**掉电保存 ✓**（都在 EEPROM 范围内）

### 设计
每行：**名称 + 当前状态（轮询刷新）+ `[ 反转 ]` 按钮**

```python
cur = 读最新轮询值(address)      # ★ 每按一次都重新读，不用本地状态，否则连按两下失效
write_single(address, 1 - cur)
禁用按钮 → 等轮询确认变成 1-cur → 再启用      # ★ 同上
```

**用户明确要求：不加确认弹窗**（"保护逻辑反了就是故障了" —— 反转会立刻报故障，一眼可见）。

### 可抄的现成模式
`self.mode_config_write_buttons: dict[int, ttk.Button]`（`app.py:2654`）、
`action_write_mode_config_field(address)`（`app.py:4625`）、按钮绑定（`app.py:3711`）。

### 316
**没有 X 输入，也没有极性设置** —— 硬件上 X 型数字输入只在室内板（330）上。

---

### 7.1 实现（2026-09-16 完成，代码在工具仓）

新增 `PolarityInvertPanel`（`app.py`，紧挨 `RegisterTable` 之后）+
`Psc330MonitorApp.action_invert_di_polarity()`。

**每行把写地址标出来了**（用户 2026-09-16 追加要求），表头「点 / 地址 / 当前状态」：

```
点                    地址   当前状态                [反转]   点                    地址   当前状态                [反转]
X00 电源/相序保护     155    0 · 反相/常闭断开报警   [反转]   X07 预热故障          391    1 · 正相/常开闭合报警   [反转]
X01 消防报警          156    1 · 正相/常开闭合报警   [反转]   X10 加湿器故障        392    0 · 反相/常闭断开报警   [反转]
X02 远程启停          157    0 · 反相/常闭断开报警   [反转]   X11 排风机过载        393    1 · 正相/常开闭合报警   [反转]
X03 送风机/泵过载     158    1 · 正相/常开闭合报警   [反转]   X12 回风机过载        394    0 · 反相/常闭断开报警   [反转]
X04 风阀联锁          159    0 · 反相/常闭断开报警   [反转]   X13 漏水保护          395    1 · 正相/常开闭合报警   [反转]
X05 过滤器堵塞        389    1 · 正相/常开闭合报警   [反转]   X14 回风压差开关      396    0 · 反相/常闭断开报警   [反转]
X06 加热/再热故障     390    0 · 反相/常闭断开报警   [反转]   X15 送风压差开关      397    1 · 正相/常开闭合报警   [反转]
```
（`_polarity_panel_check.py` 的 D 段会把这张表按实际控件文字 dump 出来，改排版后可重跑核对。）

**与上面定稿设计的三处出入，都是刻意的：**

| 设计稿 | 实际做法 | 为什么 |
|---|---|---|
| 目标值读**轮询值** | 后台线程**现读设备**当前值 | 缓存最多旧一个轮询周期，按旧值取反会往**错的方向**写。现读值回填给面板，超时判据以它为准 |
| 只等轮询确认 | 先做真实回读校验，**再**等轮询确认 | 回读不一致立刻报错放开按钮，不用干等 20 秒 |
| 未提超时 | `CONFIRM_TIMEOUT_S = 20`，到点放开并提示 | 否则这一行永久卡在禁用状态，现场看不出为什么 |

**面板位置：右栏，顶替原来的「DI 极性参数」表。** 两者是同一批寄存器的两种
呈现，并排放既重复又要多占一份竖向空间；面板比原表**更窄**（417 vs 955）、
**更矮**（261 vs 389），换掉之后这一页在任何窗口尺寸下只会更宽松。
原表的「地址/权限/单位/说明」四列对极性来说全是空的或恒定的，没有信息损失。

**三个量出来的坑**（都是排版，不是逻辑）：

1. 提示语那一行最初 `wraplength=900`，文本自然宽度 ~751px，把**面板的请求
   宽度**顶到 751 —— 而右栏实际只有 499px（= 页宽 1576 − 左栏 DI 状态表请求
   的 996）。网格被挤得只剩半截。改成 `wraplength=520` 后面板只要 417px。
   **教训：`wraplength` 不是"最多折到多宽"，它参与请求宽度的计算。**
2. 列表排 **2 列 × 7 行**。三列要 600px 以上，右栏放不下。
3. 加了地址列之后，**表头文字比内容宽时，是表头在定这一列的宽度** ——
   「写地址」三个字比 3 位数字（~21px）宽，白占右栏；改成「地址」+ 组间距
   18→10px 之后，面板请求宽度从 567 降到 **527**，对右栏 554px 才留出余量。
   （一开始只剩 7px 余量，换台机器换个字体就截了。）

### 7.2 ⚠ 最小窗口（1180×720）下面板会被挤扁
`di_left`（DI 状态表，请求 996px）先 pack，拥挤时由后 pack 的右栏吃掉亏空，
右栏只剩 160px。**这不是本次引入的** —— 原来那张 955px 宽的表在同一位置只能
显示 160px，一样没法用；整页在这个尺寸下本来就是坏的（DI 页总高只有 135px，
而两张表各要 389px）。

正常窗口（1600×980，本机默认）下量测：面板 **需要 417×261，实得 499×387**，
14 行全可见可点。

**如果要支持小窗口，得先解决页面上方约 520px 的固定占用**，不是面板的问题。

### 7.3 自检脚本（不连板、不发串口）
```
psc330_monitor\_polarity_panel_check.py
```
26 项：按钮可用性、连按两下的守卫、目标值回填前后、写失败 abort、确认超时、
断线清理、状态文字、以及**整个主界面能否构造起来**（捕 pack/grid 写错）+
两种窗口尺寸的排版量测。结果写 `_polarity_panel_check_result.txt`。

**实机还没跑过**：真实反转、固件是否接受、掉电保存 —— 都要插 330 验一遍。

---

## 8. 已定位：制热压缩机不启动 = 316 环境锁定（**不是故障**）

### 现象
制热模式下压缩机一台都不起，压机状态恒为 0，`Y05` 不动作、无报警。

### 根因链（2026-09-16 实机逐环验证）
```
① 环温 330[1552]=26.0°C  330[1622]=28.0°C       ← 就是室温
② RWS = PSC316[171] = 18.0°C                    ← 出厂默认，两台外机都是
③ 制热判据（316 UserAction.c:2128）：
   环温 ≥3.0°C 且 环温 > RWS+2.0 = 20.0°C   →  AmbientLockout = 1
④ AmbientLockout → 故障字 bit5（316 ErrorCheck.c:145  Status|=0x0020）
⑤ 330 读到故障字 = 32 (0x20)
⑥ 330 CompAction.c:176  CompFaultBlocked(): if(faultWord & 0x0070) return 1;  ← 命中
⑦ 该外机两台压机全部屏蔽，启动命令根本不下发
```

**这是设计的环境保护**：室外比"制热高环温启动点 + 2°C"还暖，制热没意义，锁死压缩机。
**台面测试必踩**：室温 26~28°C 做制热，压缩机永远不起。

### 解法
改 `RWS`（制热高环温启动点，`PSC316[171]`）到**高于当前环温**。
工具里已有现成面板「机组级外机参数 LWS / RWS」，读写都带逐台回读校验。

**改完不会立刻解**（316 UserAction.c:2116-2124 → 2153）：
解除条件是「环温 **< RWS**（注意不是 RWS+20）持续 **300 秒**」。
- 要么等 5 分钟
- 要么**给 316 断电重启** —— 重启走 `JustSwitched` 分支按当前电平直接播种，立即解除

### 诊断脚本
`psc330_monitor_tool/psc330_monitor/_comp_start_diag.py`（**全程只读**）
按固件闸门顺序逐个读，第一个不通的就是元凶。跑法：
```
python.exe _comp_start_diag.py COM13
```
它报的「3#/4# 外机离线」在只接 2 台的台面上属正常。

### 330 侧启动闸门全表（供以后对照）
`CompStartModeAllowed()`(CompAction.c:208) → `CompCanLoad()`(388) → `CompStartRequest()`(352) → 下发(361)
1. `SysStep == 2`
2. `PhaseErrX16 == 0` **且** `PowerPhaseFaultDisp == 0`
3. 模式 = 制冷(0) 或 制热(1)；**制热还要求 `Copheatfunction`(=`Parameter[99]`) == 1**，
   否则每周期直接调 `Unloading()` 强制卸载（CompAction.c:1369-1373）
4. `EnviOFFfalg`(330[737]) == 0
5. `CompIsConfigured`：`CompUse`(330[250..257]) 非 0、外机未禁用、压机未禁用
6. `UnitCommErr[module]` == 0
7. `CompEvapLowBlocked` / `CompFaultBlocked` / `OutdoorIsDefrosting`
8. 最小停机 ≥`MinStopTimeSet`（规范 120s）、全局动作间隔 ≥30s（这两项在 RAM，串口读不到）

---

## 9. 已修未验证：外机温度校准「写得进去、温度不变」（330 V10.6 / 工具 .16b）

### 现象
316 外机温度校准点「校准」/「写偏移」：工具报**写入并回读确认**，但温度读数不变。

### 根因（就是 §2 那条 V12.7 改动的连带）
316 V12.7 把**温度校准镜像块**从 `Parameter[1500..1521]` 整体下移到 `1580..1601`（+80），
但 330 侧三处仍在用旧基址 → **写进死区**：

| 位置 | 原 | 改 |
|---|---|---|
| 330 `modulecontrol.c:315` `PSC316Gateway_HMIWrite()` | `targetAddr = 1506 + offset` | `1586` |
| 330 `modulecontrol.c:750/752` 镜像更新范围与偏移 | `1506..1513` / `-1506` | `1586..1593` / `-1586` |
| 工具 `app.py` `_write_outdoor_temperature_offset()` | `p316_addr = 1506 + channel` | `1586` |

**为什么"报了成功却不生效"**：330 写完会用自己的值更新镜像
（`modulecontrol.c:754` `PSC316StatusMirror[unitIndex][65+offset] = value[0]`），
工具回读这个镜像 → 一致 → 报成功。但 316 已经不看那块了。

**压力校准没坏** —— 它走 `499 - channel`（直接命中存储，不经镜像块），不受 V12.7 影响。

### 地址换算（交叉验证过）
316：`Parameter[1580+i] ↔ Parameter[SaveSize-1-i] = Parameter[499-i]`。
旧地址 `1506+c` = `1500+(6+c)` → 存储 `493-c`，即通道 0~7 ↔ **493..486**，
与室内侧 NTC 校准列表的通道号（484~493）落在同一区间 ✓。新基址 = **1586+c**。

### ⚠️ 改这个仓库的源文件必须先探测编码
**同一仓库里编码是混的**：

| 文件 | 编码 |
|---|---|
| `User.h` / `modulecontrol.c` / `user.c` | **UTF-8**（CRLF） |
| `ErrorCheck.c` | **GBK** |

按 GBK 往 UTF-8 的 `User.h` 里插中文注释 → 两边都解不开；这次编译因此炸了一屏
`#7: unrecognized token`。**纯 ASCII 的代码替换用字节级操作（编码无关）**，
中文注释必须按文件实际编码写入。补丁脚本：`tools/byte_patch_330_calib_base_1586.py`。

### 状态
- 330 **V10.6** 已编译：`0 Error(s), 0 Warning(s)` —— **未烧录、未实机验证**
- 工具 **FFP-2026.09.16b** 自检 29 项通过（不连板）
- **验证方法**：烧 330 V10.6 → 工具里选一台外机 → 温度校准点「校准」→
  温度读数应立即跟着变；再断电重启确认掉电保存

## 10. 已修未验证：制热只开一台低速风机（316 V12.8，2026-09-17）

### 现象
机型=1、**风机类型=3**、Z=7.0C / ZD=4.0C，环温 **5.0C**（< Z，`OutdoorHeatFanLowCount`
累到 30 → `OutdoorHeatTwoFan = 1`，**本该开两台**）。
实际 1#外机长期只有**一台双速风机运行在低速**：

| 输出 | 应有 | 实测 |
|---|---|---|
| Y02 定速（`FanOpenSet`） | 1 | **0** |
| Y03 双速低速 | 0 | 1 |
| Y07 双速高速（`FanOpenSet2`+`OutdoorTypeIIFan2High`） | 1 | 0 |
| `OutdoorFanStage` | 4 | 1 |

对照组：2#外机（机型0 / 风机类型1）Y 位 `00111111`，Y02=1 正常 —— 所以是 type 3 专有。

### 采集方法（可复用）
```bash
python.exe tools/read_316_fan_state.py 300 COM6   # ST-Link(attach) 读 316 内部量 + COM6 读 330
python.exe tools/probe_316_fan_tick.py 20         # 10Hz 盯换挡状态机
```
实测 300 秒**零变化**；10Hz 下 `OutdoorFanChangeSeconds` 精确地 `0→14→15→0` 循环，
其余量（`FanOpenSet`/`FanOpenSet2`/`OutdoorFanStage`/`OutdoorTypeIIFan2High`/
`OutdoorTypeIIFan2SwapDead`/`OutdoorFanLastPhysical`）**一个都不动**。

### 根因：`OutdoorFanHeatPump()` 对 type 3 同时调了两层换挡引擎
`UserAction.c` `OutdoorFanHeatPump()`：
```c
if((type==1)||(type==3)||(type==4)) {          /* 外层 */
    if(OutdoorHeatTwoFan) OutdoorFanSelectTwoSequential(&Fan1On,&Fan2On);
    else                  OutdoorFanSelectOneByRuntime(&Fan1On,&Fan2On);
}
...
else if(type==3) OutdoorTypeIIFanApply(Fan1On,Fan2On,Fan2On,&Fan1On,&Fan2On);  /* 内层 */
```
而 `OutdoorFanApplySequential()` 开头**无条件**把调用者的输出写回"当前物理态"：
```c
OutdoorFanPhysicalState(&CurrentFan1,&CurrentFan2);
*Fan1On = CurrentFan1;      /* ← 不看 Desired */
*Fan2On = CurrentFan2;
if((CurrentFan1==DesiredFan1)&&(CurrentFan2==DesiredFan2)) return;
if(OutdoorFanChangeSeconds < 15) return;
```
于是每 15 秒一个周期必然发生：

1. **外层**批准启动定速风机（`*Fan1On=1`），并把 `OutdoorFanChangeSeconds` 清 0；
2. **内层** `OutdoorTypeIIFanApply` 重新 `OutdoorFanPhysicalState()` 拿到当前物理态，
   再调一次 `OutdoorFanApplySequential` → 开头两行把刚批准的 `*Fan1On` **抹回 0**；
3. 内层随后走 `CurrentFan2 && (OutdoorTypeIIFan2High != DesiredFan2High)` 分支，
   要求 `OutdoorFanChangeSeconds >= 15` —— 计数器**刚在第 1 步被清 0** → 升高速被否；
   `DesiredFan2High` 永远设不进去，`OutdoorTypeIIFan2High` 永远是 0。

**所以定速风机永远起不来、双速风机永远升不到高速**，整机停在预热阶段
`OutdoorFanPrestart()` 留下的状态 —— 那里对 type 3 调的是 `OutdoorTypeIIFanApply(0,1,0,...)`，
**只有一层引擎，所以能落地**，开出来的正是"双速低速一台"。

> 注：`OutdoorFanTypeIIFanApply` 里那条 `ChgSec>=15` 会触发 3 秒换绕组死区
> （`OUTDOOR_TYPEII_SWAP_DEAD_SECONDS=3`）的分支**不会**触发 —— 因为第 1 步已经把它清了。
> 这正是实测 `OutdoorTypeIIFan2SwapDead` 始终为 0 的原因。

### 已排除的可能
- **不是 330 写下来的**：330 下发 316 的范围是 `PSC316_SET_BASE=165` + `PSC316_SET_NUM=42`
  → `[165,207)`，另有 `486…` / `155…` / `[1586,1593]`；**872/874 不在其中**。
  （330 自己的 `Parameter[874]=100` 是 330 那块板的参数，与 316 的 `FanOpenSet2` 无关。）
- **不是读错内存**：316 `Parameter[1000..1005]` 读出 `[316,12,7,2026,9,15]` = V12.7，
  与仓库 `User.h` 完全一致；符号表地址与实测自洽
  （`OutdoorFanLastPhysical`=(0,1) 恰好 == `FanOpenSet,FanOpenSet2`）。
- **不是 `OutdoorFanUpdateTimers` 清零**：那条路要 `Current != LastPhysical`，实测该量恒定。

### 改法（V12.8）
`OutdoorFanHeatPump()` 里 type 3 **只算"想要哪几台"，换挡全部交给一层引擎**：
```c
if(OutdoorFanTypeSet == 3)
{
    unsigned char PreferredFan;
    PreferredFan = (OutdoorFanCommandRunSeconds[0]
        <= OutdoorFanCommandRunSeconds[1]) ? 0 : 1;
    if(OutdoorHeatTwoFan)                 { Fan1On = 1; Fan2On = 1; }
    else if(CurrentFan1 && CurrentFan2)   { /* 只该开一台却两台在转：留运行时间短的 */
                                            Fan1On = (PreferredFan==0); Fan2On = (PreferredFan==1); }
    else                                  { Fan1On = CurrentFan1; Fan2On = CurrentFan2;
                                            if(!CurrentFan1 && !CurrentFan2)   /* 两台都停 */
                                            { Fan1On = (PreferredFan==0); Fan2On = (PreferredFan==1); } }
}
else if((OutdoorFanTypeSet == 1)||(OutdoorFanTypeSet == 4)) { ...原来的... }
```
**不能**反过来改 `OutdoorFanApplySequential` 的 `*Fan1On=CurrentFan1` 两行 ——
`OutdoorFanSelectOneByRuntime` 在"两台都停/都在转"时靠它拿当前态。

### 机型 × 风机类型 全组合审计（确认不影响别处）
合法搭配由 `OutdoorFanConfigurationValid()` 限定：

| 机型 `OutdoorUnitTypeSet` | 允许的风机类型 | 风机控制路径 | 换挡引擎层数 |
|---|---|---|---|
| 0 | 0, 1 | 制热 `OutdoorFanHeatPump`；制冷 `OutdoorFanTypeI` | 1 ✅ |
| 1 | 2, 3 | 制热同左；制冷 type2→`OutdoorFanTypeIII` / type3→`OutdoorFanTypeII` | 1 ✅ |
| 2 | 2, 4 | 制热同左；制冷 `OutdoorFanTypeIII` | 1 ✅ |

逐路径核对"有没有第二层引擎"：

| 场景 | 函数 | 结论 |
|---|---|---|
| 预热 type 0 | `OutdoorSingleTwoSpeedApply(1,0)` | 单层 ✅ |
| 预热 type 1 | `OutdoorFanSelectOneByRuntime` + 直接赋 | 单层 ✅ |
| 预热 type 2/4 | `OutdoorFanPrestartVfd` 直接赋 | 单层 ✅ |
| 预热 type 3 | `OutdoorTypeIIFanApply(0,1,0,…)` | 单层 ✅ |
| 制热 type 0 | `OutdoorSingleTwoSpeedApply` | 单层 ✅ |
| 制热 type 1 / 4 | `OutdoorFanSelect*` 后直接赋 `FanOpenSet/FanOpenSet2` | 单层 ✅ |
| 制热 type 2 | `OutdoorSingleTwoSpeedApply` + DA0 | 单层 ✅ |
| **制热 type 3** | **原 2 层 → 已改 1 层** | **本次修复** |
| 制冷 type 0/1 | `OutdoorFanTypeI` | 单层 ✅ |
| 制冷 type 2/4 | `OutdoorFanTypeIII`（VFD 状态机直接赋） | 单层 ✅ |
| 制冷 type 3 | `OutdoorFanTypeII` 算完 `TargetFan1/2/2High` 后**单次**调 `OutdoorTypeIIFanApply` | 单层 ✅ |

**结论：制热 type 3 是全工程唯一一处"双层换挡引擎"，其余全是单层。**
`OutdoorFanHeatPump` 里 type 1/4 那一支和 type 0/2 那一支的代码**一字未动**
（只是 if 链第一项从 `(1||3||4)` 变成 `3`），行为完全不变。

一个必须小心的岔路（V12.8 初稿漏了、已补）：`OutdoorHeatTwoFan` 由 1 掉到 0
（环温升过 `Z+ZD`）时，如果**两台都在转**，必须主动减掉一台。
初稿写成"一台在转就保持"，两台都转时就不会减载 —— 现在按
`OutdoorFanCommandRunSeconds` 留短的那台，与原 `OutdoorFanSelectOneByRuntime` 一致。
新代码算出的**期望值**与原引擎在全部 8 种 `(CurrentFan1,CurrentFan2)` 状态下逐一等价：
(0,0) 和 (1,1) 都按运行时间选一台；(1,0)/(0,1) 保持；TwoFan=1 时恒为 (1,1)。

### 预期行为（推演，常数用实测值：最短停机 29s、换挡间隔 15s、绕组死区 3s）
| 时刻 | 动作 | 输出 |
|---|---|---|
| 0~15s | `ChgSec` 爬到 15 | Y03（双速低速）|
| t=15s | `IIFan2High 0≠1` → `FanOpenSet2=0; SwapDead=3; ChgSec=0` | 全断 3s |
| t=18s | `SwapDead→0`，`IIFan2High=1`，`FanOpenSet2=1` | 仍 Y03（Stage 要两台都 on 才到 4）|
| t=33s | `ChgSec` 再爬 15，批准 `FanOpenSet=1` → `Stage=4` | **Y02=1 + Y07=1** |
| 之后 | 期望态==物理态，两个引擎都直接 return | 稳定 |

### 状态
- 316 **V12.8** 已编译：`0 Error(s), 1 Warning(s)`（warning 是 `eevCTRL.c` 原有的，无关）
- 新 `.axf`/`.hex` 时间戳 **2026-09-17 09:11**，**符号地址与 V12.7 完全一致**（探针可复用）
- **未烧录、未实机验证**
- 验证方法：烧 316 V12.8 → 保持机型1/风机类型3/制热、环温 < Z →
  `python.exe tools/probe_316_fan_tick.py 30` 看终态是否为
  `Fan1=1 Fan2=1 Stg=4 IIHi=1`，同时 330 侧 Y 位应为 **Y02=1 且 Y07=1**

## 11. 已修未验证：类型0（单风机双速）低速输出接错端子（316 V12.9，2026-09-17）

### 现象
机型=0 + **风机类型=0（单风机双速）**，用户观察「**低速的时候把定速风机给开了**」。

`output[]` 实测：低速态 `Y02=1 / Y03=0 / Y07=0`，高速态 `Y02=0 / Y03=0 / Y07=1`。

### 权威定义
`PSC330_PSC316_IO点位表_20260907`（`PSC330_PSC316_IO点位导出_20260907.zip` 内的原始 CSV）：

| 端子 | 宏 | 功能 |
|---|---|---|
| Y02 | `Fan1Y03` | 风机1 / **定速风机** / 变频风机使能 |
| Y03 | `Fan3Y07` | 风机2 / **低速** / 辅助定速风机 |
| Y07 | `FanHighY07` | **双速风机高速接触器** |

### 逐类型核对结果（对着这张表）
| 风机类型 | Y02 | Y03 | Y07 | 判定 |
|---|---|---|---|---|
| 1 双风机两定 | 风机1 | 风机2 | 不用 | ✅ |
| 2 单风机变频 | 变频使能 | 不用 | 不用 | ✅ |
| 3 定速+双速 | 定速风机 | 双速低速 | 双速高速 | ✅ |
| 4 变频+定速 | 变频使能 | 辅助定速 | 不用 | ✅ |
| **0 单风机双速** | **低速** ❌ | **不驱动** ❌ | 高速 ✅ | ❌ |

**只有类型0 不符，而且是唯一一处。** 其余 Y 宏（`Comp1Y05`→Y00、`Comp2Y06`→Y01、`Four1Y02`→Y04、
`Four2Y03`→Y05、`Under1Y13`→Y06、`FanHighY07`→Y07）逐条对得上。

### 根因：两处代码都按「类型0 低速 = Y02」写

1. `ErrorCheck.c` `IoOutAction()` 的 case 0：
   ```c
   case 0: /* one two-speed fan: Y03 low, Y04 high */
       if(FanOpenSet2) { Fan1Y03 = 0; Fan2HighY04 = 1; }
       else { Fan1Y03 = FanOpenSet; Fan2HighY04 = 0; }   /* ← 低速写到 Y02 */
   ```
   **注释自己就写着「Y03 low」**，与点位表一致 —— 是宏名（`Fan1Y03` 名字里带 Y03）与实际引脚
   （`#define Fan1Y03 Y02`）脱节之后，代码没跟着改。注释里的 "Y04 high" 同理是旧引脚
   （Y04 现在改成四通阀了；宏 `Fan2HighY04` 已重定向到 Y07，所以高速那半是对的）。

2. `UserAction.c` `OutdoorProtectOutputLimit()` 的类型0 互锁块：
   ```c
   Fan3Y07 = 0;                          /* ← 无条件把 Y03 清零 */
   HandDebugY03 = 0;
   if(Fan2HighY04) Fan1Y03 = 0;
   ```
   **只改第 1 处没用** —— 这个块跑在 `ErrorCheck()` 之后，会把 Y03 立刻拉回去。

两处互相印证，说明当初是**系统性**按 Y02 写的，不是笔误。`_utf8tmp/ErrorCheck.c` 旧副本
一字不差，不是最近改坏的。

### 改法（V12.9）
| # | 文件 | 改动 |
|---|---|---|
| 1 | `ErrorCheck.c` case 0 | 低速 `Fan1Y03`(Y02) → **`Fan3Y07`(Y03)**；注释 `Y04 high` → `Y07 high` |
| 2 | `UserAction.c` 类型0 互锁块 | 无条件清 Y03 → **Y03/Y07 互斥**；改清 `HandDebugY02`（Y02 改成类型0 用不到的端子）|
| 3 | `modbus.c` `ApplyManualFanInterlockAfterWrite` | **不需要改** —— 它类型0 清的就是 `HandDebugY02`，在新语义下恰好正确 |

**影响面**：只影响风机类型0。类型1/2/3/4 的映射本来就与点位表一致，未动。

⚠ **烧录前务必确认外机1 端子排上 Y02 / Y03 的实际接线** —— 若现场把低速接触器接在 Y02 上，
改完类型0 低速档会完全没电。用户已确认「低速统一到 Y03」。

### 顺带修的两份文档
- `PSC330_PSC316内外机完整IO与通讯点位表.md` 第 233/234/238 行原来把 Y02 写成「风机 1/低速输出」，
  与上面那份权威 CSV 矛盾 —— 已统一到 CSV 的措辞。

### 状态
- 316 **V12.9** 已编译：`0 Error(s), 1 Warning(s)`（warning 是 `eevCTRL.c` 的 `Time3` 未使用，原有）
- 新 `.axf`/`.hex` 时间戳 **2026-09-17**，12 个关键符号地址 **0 漂移**，探针脚本可继续复用
- 补丁脚本：`tools/byte_patch_316_type0_fan_pin.py`（字节级、编码无关，可重跑核对）
- **未烧录、未实机验证**
- 验证方法：烧 V12.9 → 机型0/风机类型0 制冷低速 →
  `python.exe tools/probe_316_fan_tick.py 30`，`output[2]`(Y02) 应为 0、**`output[3]`(Y03) 应为 1**

## 12. 已修未验证：变频风机「减到低频为止不停机」+ 加减载动态化（316 V12.10，2026-09-17）

用户 2026-09-17 就规格书两处给出拍板，连同工具一个刻度 bug 一起改。

### 规格书原文（单风机室外机，变频）
> 变频风机最低运转频率执行【变频轴流风机最低频】，最高频率按【变频轴流风机最高频】，
> 采用冷凝温度进行控制：冷凝温度在 34-36 度之间保持，低于 34 度减载，高于 36 度加载，
> 若风机没开，冷凝温度大于 45 度满频。

### 偏差 A：减载到最后把变频风机整个停掉（规格书没写）
`OutdoorFanTypeIII()` 类型2 分支原来：
```c
else if(NeedUnload && OutdoorVfdOn)
{
    if((OutdoorVfdSpeed > MinSpeed) && uc1s) OutdoorVfdSpeed -= Step;
    else if((OutdoorFanRunSeconds[0] >= FanMinRunTimeSet) && (OutdoorFanChangeSeconds >= 15))
    { OutdoorVfdOn = 0; OutdoorVfdSpeed = 0; OutdoorFanChangeSeconds = 0; }   /* ← 停机 */
}
```
类型4 的 `else`（定速已关）分支同样有一处 `OutdoorVfdOn = 0`。

**用户裁定：「减到低频为止、不停机。所在外机有压缩机运行就不能停机！」**
→ 两处停机分支已删；减到 `MinSpeed` 后由函数末尾
`if(OutdoorVfdOn && (OutdoorVfdSpeed < MinSpeed)) OutdoorVfdSpeed = MinSpeed;` 兜住。
`OutdoorFanTypeIII()` 只在有压机运行时被调用（`OutdoorBoardFanControl()` 末尾），
所以这条等于「有压机 → 变频风机恒开」。

### 偏差 B：加减载前要先干等 30 秒
原来：
```c
if(LoadCondTemp > 360) { if(OutdoorVfdLoadCount < 30) OutdoorVfdLoadCount += uc1s; ... }
else if(UnloadCondTemp < 340) { if(OutdoorVfdUnloadCount < 30) OutdoorVfdUnloadCount += uc1s; ... }
NeedLoad = (OutdoorVfdLoadCount >= 30) ? 1 : 0;
NeedUnload = (OutdoorVfdUnloadCount >= 30) ? 1 : 0;
```
**用户裁定：「变频风机加减载不是动态的吗？应该没有保持 30s 这一说吧？」**
→ 改成直接按温度判：冷凝 >36.0 每秒加、<34.0 每秒减、34~36 之间保持
（这个带就是规格书给的迟滞）。加减载速率仍是 `FanStepSpeedSet`（×10，0.1Hz/s）。
两个计数变量保留成 0/1，只为调试口能看出当前处于加载/减载/保持哪一态。

### 未动（用户明确说保持原样）
- **C** `LoadCondTemp` 与 `UnloadCondTemp` 都取 `OutdoorCondTempMax()`（冗余，无害）
- **D** 强制段4 里「环温>20 使能 / <18 退出」各 30 秒确认（`OutdoorFanTypeII()`，规格书未标）
- **E** 段1备注的「开」条件用同一个 30 秒计数器（`环温>10 || 冷凝max>42`），比规格书宽松
- 「若风机没开、冷凝 >45 度满频」里的 30 秒确认（`ForceFull`）**保留** —— 用户只说了加减载；
  且改完 A 之后风机基本不会停，这条只影响首次启动

### 顺带修：工具刻度 bug（工具 FFP-2026.09.17a）
`app.py` `OUTDOOR_GATEWAY_UI_FIELDS` 里偏移 11/12 的刻度写成 10：
```python
(11, "变频轴流风机最高频", "Hz", 10),   # ← 错
(12, "变频轴流风机最低频", "Hz", 10),   # ← 错
```
但 316 的 `FanMaxFreqSet/FanMinFreqSet` **存的是整数 Hz**（固件里
`MaxSpeed = FanMaxFreqSet * 10` 才换算成 0.1Hz）。后果：
- 台面上的 50Hz 被显示成 "5.0"（用户截图里看到的「最高频=4.0」就是 raw 40 ÷ 10）
- 填 50 会被范围校验拒绝（`OUTDOOR_GATEWAY_MAX[11] = 50`）
- 填 5 会真的往 316 写 5Hz

已改成刻度 **1**。反证：`OUTDOOR_GATEWAY_MIN/MAX` 的 11/12 项本来就是按整数 Hz 写的
（30~50 / 0~30，且注释标了 "spec 0-30"）。
偏移 13 加减载速度的刻度 10 是**对的**（`FanStepSpeedSet` 确实存 ×10），未动。

### 新增探针
`tools/probe_316_cooling_fan.py [秒数] [每几秒一行]` —— 一次读全制冷风机状态机：

| 关注 | 读的量 |
|---|---|
| 类型2/4 变频 | `OutdoorVfdOn`(0x20000D01)、`OutdoorVfdSpeed`(0x20000D30, 0.1Hz)、<br>`OutdoorVfdLoadCount`(0x20000D48)、`OutdoorVfdUnloadCount`(0x20000D4C)、<br>`OutdoorVfdForceFullCount`(0x20000D50)、`OutdoorFixedOn`(0x20000D02) |
| 类型3 四段 | `OutdoorTypeIIStage`(0x20000D08)、`OutdoorTypeIIPendingStage`(0x20000D09)、<br>`OutdoorTypeIIPendingCount`(0x20000D54)、`OutdoorTypeIIFan2High`(0x20000D06)、<br>`OutdoorTypeIIFan2SwapDead`(0x20000D07)、`OutdoorTypeIILowFanOff`(0x20000D0B) |
| 通用 | `FanOpenSet`=316[872]、`FanOpenSet2`=316[874]、翅片=316[1431/1432]、<br>TL/TLD=316[172/173]、输出口 `output[0..7]` |

### 状态
- 316 **V12.10** 已编译：`0 Error(s), 1 Warning(s)`（`eevCTRL.c` 的 `Time3`，原有）
- 新 `.axf`/`.hex` 时间戳 **2026-09-17 11:26**，符号地址 **0 漂移**
- 工具 **FFP-2026.09.17a**，已导入自检通过（字段表刻度正确）
- 补丁脚本：`tools/byte_patch_316_vfd_fan_dynamic.py`（字节级、编码无关、可重跑核对）
- **未烧录、未实机验证**
- 台面当前：外机1 = **机型1 / 风机类型2（单风机变频）**，压机停机中，变频风机关（正常）
- 验证方法：烧 V12.10 → 制冷、压机起来 →
  `python.exe tools/probe_316_cooling_fan.py 180 2`，看
  ① `VfdOn` 在有压机期间**不出现 0**；② 冷凝 >36 时 `VfdHz` 每秒 +2.0、<34 时每秒 -2.0、34~36 保持

## 13. 已修未验证：变频风机「没开且冷凝>45 → 满频」原来要等 30 秒（316 V12.11，2026-09-17）

### 规格书原文
> 若风机没开，冷凝温度大于45度满频。

**没有保持时间。** 代码却要求冷凝 >45 **持续 30 秒**才给满频。

### 原代码
```c
if(MaxCondTemp > 450)
{
    if(OutdoorVfdForceFullCount < 30) OutdoorVfdForceFullCount += uc1s;
}
else OutdoorVfdForceFullCount = 0;
ForceFull = (OutdoorVfdForceFullCount >= 30) ? 1 : 0;

if(!OutdoorVfdOn
    && (!NeedUnload || ForceFull)
    && (OutdoorFanStopSeconds[0] >= FanMinStopTimeSet)
    && (OutdoorFanChangeSeconds >= 15))
{
    OutdoorVfdOn = 1;
    OutdoorVfdSpeed = ForceFull ? MaxSpeed : MinSpeed;
}
```

### 真问题不是"多等 30 秒"，而是"起步给的是最低频"
`NeedUnload = (冷凝 < 34)`，冷凝 >45 时 `!NeedUnload` **已经为真** —— 所以只要最短停机
和换挡间隔允许，风机在**第一条扫描**就启动了，此时 `ForceFullCount` 才 1、`ForceFull = 0`
→ 给的是 `MinSpeed`。风机一开，`!OutdoorVfdOn` 就为假，**那个启动块从此再也不执行**，
`ForceFull` 再也轮不到用。

实际行为：**最低频 20.0Hz 启动，再靠 >36 的常规加载逻辑（2.0Hz/s）爬到 50.0Hz，约 15 秒。**

`ForceFullCount` 唯一有用的场合：启动被 `FanMinStopTimeSet`(30s) 或 `FanChangeSeconds`(15s)
卡住超过 30 秒时 —— 等卡住解除才以 `MaxSpeed` 启动。属于边角情形，不是规格书描述的那条。

> 注释 `/* An off fan starts at full speed only after ... 30-second hold time */` 描述的是
> **意图**，和实际行为不符 —— 我第一版测试用例就是照注释写的，写错了（已改）。

### 改法（V12.11）
```c
OutdoorVfdSpeed = (MaxCondTemp > 450) ? MaxSpeed : MinSpeed;   /* 启动瞬间直接看温度 */
```
- 启动条件里的 `|| ForceFull` 去掉（冷凝>45 时它恒等于 `!NeedUnload`，是冗余）
- `ForceFull` 局部变量删除
- `OutdoorVfdForceFullCount` **保留**，只当「冷凝>45 已持续多少秒」的指示器

### 影响面
只影响风机类型 2 / 类型 4 的变频风机**启动瞬间**。而且 V12.10 改了「有压机就不许停机」之后，
风机基本不会处于"没开"状态 —— 这条实际只在**压机首次启动、且此时高压已经 >45℃** 时生效。

### 状态
- 316 **V12.11** 已编译：`0 Error(s), 1 Warning(s)`（`eevCTRL.c` 的 `Time3`，原有）
- 新 `.axf`/`.hex` 时间戳 **2026-09-17 13:28**，符号地址 **0 漂移**
- 补丁脚本：`tools/byte_patch_316_vfd_force_full.py`
- 测试用例：`冷凝风机_测试用例_20260917.md` 的 **A5**（预期已按新行为改正）
- **未烧录、未实机验证**

## 14. 实机测试：变频风机（机型1/类型2）—— 第1组通过，A5 暴露预启动绕行（316 V12.12，2026-09-17）

台面：外机1 = 机型1 / 风机类型2（单风机变频），固件 V12.11，制冷，TL=30.0 TLD=15.0，R410A。

### 加压手段：改 AI 通道偏移（不是往 RAM 写压力）
`OutdoorCondTemps()` 每扫描从 `HighPressDisp1/2` 现算，往 RAM 写 `Parameter[1443]` 会被下一扫描覆盖。
要在**输入端**偏 —— 316 的 AI 换算 `value += engLow + AiCfgOffset(ch)` 是**纯 kPa 加法**：

    330[2680 + (u-1)*49 + 8]        -> 316[310]   AI0 1#高压 偏移
    330[2680 + (u-1)*49 + 12 + 8]   -> 316[2700->322]  AI1 2#高压 偏移

走 330 网关转发，写协议照抄工具 `write_outdoor_gateway_register()`：
等写槽空闲 → FC06 单寄存器（多寄存器会被 330 `modbus.c` 丢帧）→ **判据只看镜像回读 == 目标值**
（状态位比镜像早约 5.5 秒，且会读到上一轮遗留值）。一次写 8~17 秒。
两路设成同一个值 → `CondTempMax == CondTempMin`，判据干净。

工具：`tools/bench_cond_temp.py <目标温度C> [--watch 秒] [--restore] [--dry-run]`

> ⚠ **工具「AI / 温度校准」页的「外机压力校准」按钮是失效的** —— 它写 `316[499]`(`UserA0ValueSet`)，
> 而 `AiCfgOffset(0) = UserA0ValueSet` 只在 `OutdoorAiConfigMigrate()` 里执行**一次**，
> `Parameter[350]` 已是 MAGIC 就不再同步。能改到读数的是「**AI 通道配置**」页（走 316[310]）。

### 第1组：34-36 保持 / <34 减载 / >36 加载 —— **全部通过**

| 用例 | 目标冷凝 | 实测 |
|---|---|---|
| 加载 >36 | 36.7℃ | `Load=1`，`VfdHz` 加到 **50.0** 后停住 ✓ |
| 带内保持 | 35.0℃ | `Load=0 Unld=0`，**70 秒纹丝不动**（停在进入带内时的 50.0）✓ |
| 减载 <34 | 32.0℃ | `Unld=1`，**34.0→20.0 每秒 −2.0**，7 秒走完 14Hz，停在 20.0 ✓ |
| **不停机** | 同上 | 79 秒采样里 **`VfdOn=0` 出现 0 次**，最低频停留 73 秒 ✓ |
| 再加载 | 36.5℃ | `Load=1`，回到 50.0 ✓ |

**「带内保持」的正确含义是停在原地不动**（不是往中间调）—— 实测 70 秒零变化。
**A4 不停机是 V12.10 的核心验证**，旧固件在这里会 `OutdoorVfdOn = 0`。

### A5：风机没开 + 冷凝>45 → 满频 —— **不通过，V12.12 才修好**

做法：冷凝设到 47.0℃（2690 kPa）→ 等存盘 → **ST-Link 复位 316** → 看启动瞬间频率。

实测序列：
```
复位 → 压机启动挂起 → OutdoorFanPrestartVfd() 预启动到 20.0Hz（最低频）
     → 压机起来 → OutdoorFanTypeIII() 靠 NeedLoad(>36) 以 2.0Hz/s 爬
     → 约 15 秒后才到 50.0Hz
```

**根因不在 V12.11 改的那处，在预启动路径。** `OutdoorFanPrestart()` 在「压机全停 + 有启动请求」时
先把风机开起来（固定最低频、不看冷凝温度），`OutdoorVfdOn` 一置 1，`OutdoorFanTypeIII()` 里那个
能判 `>45` 的启动分支就**永远轮不到**了。

**所以规格书这条在当前结构下不可能成立**：压机没跑 → 预启动给最低频；压机在跑 → 风机本来就转着
（V12.10 保证的）。

### 副产物 1：`AiCfgOffset` 能扛住复位
复位前后 `AI0=875 / AI1=800` 原封不动、冷凝仍 47.0℃、`Parameter[350]` 仍是 MAGIC。
说明偏移进了 EEPROM，不依赖那个一次性迁移。**ST-Link 复位全程 `state=RUNNING`（连续确认 10 次）**，
没把板子留在停机态。

### 副产物 2：工具的网关诊断字段命名错位
`OUTDOOR_GATEWAY_DIAG_NAMES = ["在线状态","写状态","错误码","状态块刷新计数"]` 对应 `base+42..45`，
但实测 `base+42` 恒为 0、`base+43` 是个**递增计数器**、`base+45` 恒为 0。
我一度按"在线=0"判断成"两台外机离线"，**是误读**。独立证据（板运行时间镜像 1# 107310→107322 在涨、
版本镜像 `[316,12,11,...]` 正确、`330[2688]=875` 与 316 一致）证明 **330↔316 通讯一直是好的**。
→ 工具这条待修。

### V12.12 改法
`OutdoorFanPrestartVfd()` 里也判冷凝温度：
```c
OutdoorVfdSpeed = (OutdoorCondTempMax() > 450) ? MaxSpeed : MinSpeed;
```
并补 `MaxSpeed` 下限保护 —— 原来只有 `if(MinSpeed <= 0) MinSpeed = 10;`，没有 MaxSpeed 的兜底；
而刚置 `VfdOn=1` 的这一次**不经过**下面那个 `else if(OutdoorVfdSpeed < MinSpeed)`，
最高频参数若为 0 或低于最低频就会把速度设到最低频以下。
```c
MaxSpeed = FanMaxFreqSet * 10;
if(MaxSpeed < MinSpeed) MaxSpeed = MinSpeed;
```

**影响面**：类型2/类型4 共用此函数，两者都生效（用户确认要一起）；类型0/1/3 不走它；
制热 `OutdoorFanHeatPump()` 本来就对类型2/4 直接给满频，不受影响。

### 状态
- 316 **V12.12** 已编译：`0 Error(s), 1 Warning(s)`（`eevCTRL.c` 的 `Time3`，原有）
- 新 `.axf`/`.hex` 时间戳 **2026-09-17 14:01**，符号地址 **0 漂移**
- 补丁脚本：`tools/byte_patch_316_prestart_cond_temp.py`
- 测试脚本：`tools/bench_cond_temp.py`（改冷凝温度 + 盯风机）、`tools/reset_316.py`（复位前后对比）
- **未烧录**；外机1 的 AI 偏移（+875/+800）**暂留**，等 V12.12 烧录后一起清
- 验证路径：烧 V12.12 → ST-Link 复位 → 偏移还在（冷凝仍 47.0℃）→ 看启动瞬间是否 500

### V12.12 复测：**通过 ✓**（2026-09-17 14:15）
```
复位前: 版本 [316,12,12,...]  AI偏移 875/800  冷凝 47.0C
复位
 t= 0.0s   VfdOn=0    0.0 Hz   Comp=0,0  NoCompSec=15  Stop0=300
 t=14.5s   VfdOn=1   50.0 Hz   Comp=0,0  NoCompSec=0   Stop0=304
判定: [通过] 符合规格书
```
整个日志里 `VfdHz` 只出现过 0.0 与 50.0 —— **中间没有 20.0Hz**。
`Comp=0,0` 全程（压机未起、风机已预启动满频）正是规格书「若风机没开，冷凝>45 满频」的情形。
对比 V12.11：启动给 20.0Hz，约 15 秒才爬到 50.0。

验证脚本：`tools/verify_a5_prestart_full_speed.py`（复位与 10Hz 采样同进程，抓 `VfdOn` 0→1 那个变化点）。
测完偏移已清零，压力回到真实值 1815/1890 kPa（30.7/32.4℃）。

> 复测后减载实测变成每秒 −1.0Hz：查下来是 `Parameter[178]` 被用户从 20 改成 10，
> **不是异常** —— 两次实测都与该参数一致（20→±2.0Hz/s、10→±1.0Hz/s）。

## 15. 实机测试：类型3（定速+双速）四段控制 8 条全通过 + 工具「外机压力校准」按钮修复（2026-09-17）

台面：外机2 = 机型1 / 风机类型3（定速+双速），V12.12，制冷，TL=30.0 TLD=15.0，R410A。
ST-Link 移到外机2；串口仍直连 330。

### 环温的注入口（本次新查明）
`EnviTempAI0Disp = AI[6] = NTC[0]`，而
`NTC[i] = round(Tempbuf*10) + Parameter[493-i]`（NTC16bit.c:171）。
316 自己把镜像块同步回存储位：`Parameter[1580+i] <-> Parameter[499-i]`（UserAction.c:263-270）。
→ **NTC[0]（环境温度）对应 `Parameter[1586]`**，映射：

    330[2534 + (u-1)*16 + 0]  -> 316[1586]   环境温度校准偏移 (0.1C)

⚠ 台面 2# 的原偏移是 **127（+12.7C）**，真实室温 7.3C 被报成 20.0C —— 改之前必须记下原值，
测完放回去（`bench_cond_temp.py --unit 2 --ambient-offset 127`），**不能清 0**。

### 四段实测（8 条全部通过）
| 用例 | 条件 | 实测输出 |
|---|---|---|
| B1 段1 | min 29.5C | `IIStg=1` → **Y02=0 Y03=1 Y07=0** |
| B2 段2 | min 31.9C | `IIStg=2` → **Y02=1 Y03=0 Y07=0** |
| B3 段3 | min 42.0C | `IIStg=3` → **Y02=1 Y03=1 Y07=0** |
| B4 段4 | min 47.0C | `IIStg=4` IIHi=1 → **Y02=1 Y03=0 Y07=1** |
| B5 每段延时 | — | `PndCnt` 爬到 **15** 才落段；4→3→2→1 每级 15 秒 |
| B6 段1备注-关 | 环温 7.0 + max 29.5 | `LowOff` 满 30 秒 → **段0：三路全 0** |
| B7 段1备注-开 | 环温 11.0 | `LowOff` 清0 → 15 秒回段1 |
| B8 强制段4 | 环温 21.0 + hp1 50.5 / hp2 25.0 | **`PendS` 直接=4**（不是 2）→ 跳段4 |

- **B4 抓到换绕组死区**：`Dead=3` 秒两路接触器全开 → `IIHi=1`（Y07=1）→ 隔 15 秒才开单速。
- **B6 就是用户强调的那条**：LowFanOff=1 → TargetStage=0 → `OutdoorTypeIIFanApply(0,0,0,…)`
  → Y02=Y03=Y07 全 0 = 「双速低速关闭就是所有风机都关」✅
- **B8 必须把两路拉开才测得出**：强制判据用 `CondTempMax`、段界用 `CondTempMin`；两路同值时
  `min=50 > TL+TLD=45` 常规判据本来就给段4。拉开后 min=25 常规只给段1，max=50.5 走强制 → 段4，
  **`PendS=4` 直接跳，不逐级** ✅
- 过渡现象（不是 bug）：从段4 降到段1 时段2 稳态不出现 —— 段4→段3 的换绕组死区把
  `OutdoorFanRunSeconds[1]` 清零，而关双速要它连续运行 ≥30 秒，段2 只持续 15 秒。

### 工具 Bug 1 已修： 「外机压力校准」按钮一直失效
原实现写 `316[499-channel]`（`UserA0ValueSet`/`UserA1ValueSet`）：
```python
p316_addr = 499 - channel
```
但 316 里 `AiCfgOffset(ch) = UserAxValueSet` **只在 `OutdoorAiConfigMigrate()` 里执行一次**，
而版本标记 `Parameter[350]` 已是 MAGIC(12641) → 迁移不再跑 → 两者脱钩，**写它不影响任何读数**。
（实机确认：316[499]=252 却与生效的偏移 875 无关。）

改法：三处（写偏移 / 校准 / 刷新显示）都改走 **AI 配置块**：
`330[2680 + (u-1)*49 + ch*12 + 8] -> 316[302 + ch*12 + 8]` = 310/322/334/346。
新增 `outdoor_ai_cfg_offset_addr(unit, channel)` 统一算地址。

**端到端验证**：经新地址写 1# AI0 偏移 +100 → 高压1 从 1818 变 1915 kPa ✅（还原后回 1815）。

### 工具 Bug 2 —— **误报，撤回**
我上一轮说 `OUTDOOR_GATEWAY_DIAG_NAMES` 与 `base+42..45` 排列错位，**是错的**。
查 330 的 `PSC316Gateway_PublishToHMI()`（modulecontrol.c:505-508）确认顺序就是
`在线状态 / 写状态 / 错误码 / 状态块刷新计数`，与工具命名一致。

我用错了基址：网关诊断区在 **`1830 + (u-1)*48 + 42`**（1#=1872、2#=1920），
而我临时脚本里写成了 `1550 + (u-1)*70 + 42`（=1592，那是状态镜像区的别的值，所以看着像"离线"）。按正确地址读：`330[1872..1875] = [1, 2, 0, 575]`、`330[1920..1923] = [1, 2, 0, 576]` —— 两台都正常在线。

### 台面已恢复
- 外机2：环温偏移 127（环温 20.0C）、高压偏移 AI0/AI1 = 0（高压 2388/1865 = 42.0/31.9C）
- 与测试开始前完全一致

### 工具版本
`FFP-2026.09.17c`（a=变频最高/最低频刻度、b=冷凝风机判据面板、c=修外机压力校准按钮）

---

## 16. V12.13 —— 类型4 恢复「变频风机关」

### 为什么改
规格书流程图《双风机室外机时（一定一变）的逻辑框图》最上面那一支：

```
变频风机减频 → 变频风机在最低频? ──是──→ 【变频风机关】
                                 └─否──→ 变频风机减频
```

V12.10 时我按你当时的口头指示「所在的外机有压缩机运行就不能停机」，
把**类型2 和类型4 两处的停机分支一起删了**（当时专门问过"类型4 要不要跟着变"，
你答"要"）。现在按流程图分开处理。

### 改法
只改 `OutdoorFanTypeIII()` 里类型4 的 **else（定频已关）** 分支：

```c
else if(OutdoorVfdOn
    && (OutdoorFanRunSeconds[0] >= FanMinRunTimeSet)
    && (OutdoorFanChangeSeconds >= 15))
{
    OutdoorVfdOn = 0;
    OutdoorVfdSpeed = 0;
    OutdoorFanChangeSeconds = 0;
}
```

类型2 不动 —— 类型2 只有一台变频风机，关了就没风机了。

### ⚠ 一处必须说明白的事（我上一轮把理由讲错了）
我给你的 (a) 选项里写的是「变频关掉后定频风机还在转，不会没风机」——
**这句是错的**。这支 `else` 的前提恰恰是 `OutdoorFixedOn == 0`（定频已经关着），
所以 VfdOn 一清零，**两台风机全关，而压缩机还在转**。

不是死锁：`OutdoorFanTypeIII()` 只在压缩机运行时被调用（[UserAction.c:1652](PSC316RK-V12-外机/WELLTHINKER/User/UserAction.c#L1652)
两台压机都为 0 时提前返回），此时没有风机 → 冷凝温度回升 → 冷凝 >34.0
使 `NeedUnload` 变 0 → [UserAction.c:1489](PSC316RK-V12-外机/WELLTHINKER/User/UserAction.c#L1489) 的启动支路
（`!VfdOn && !NeedUnload && StopSeconds[0] >= FanMinStopTimeSet && ChgSec >= 15`）
把变频风机重新拉起来，**最短停 30 秒**。

计时器这一环也核过了：类型4 走 `OutdoorFanPhysicalState()` 的默认支路
（[UserAction.c:669](PSC316RK-V12-外机/WELLTHINKER/User/UserAction.c#L669)），
`Fan1On = FanOpenSet = OutdoorVfdOn`、`Fan2On = FanOpenSet2 = OutdoorFixedOn`，
所以 VfdOn 清零后 `StopSeconds[0]` 正常累加，重启门能开，不会卡死。

**净效果**：类型4 到阶梯底部会「两台风机全停 → 冷凝回升 → 30 秒后重启」地循环，
和流程图一致。如果你认为"压机运行时一台风机都不许停"，
那就得回退这条改动（= 恢复 V12.10 的写法），告诉我一声。

### 阈值复核（写死在代码里，不随 TL/TLD 走）
| 判据 | 值 | 位置 |
|---|---|---|
| 加载 `NeedLoad` | 冷凝 > 36.0 | UserAction.c:1459 |
| 减载 `NeedUnload` | 冷凝 < 34.0 | UserAction.c:1464 |
| 满频启动 / 预启动满频 | 冷凝 > 45.0 | UserAction.c:1482 / 1495 |

34.0~36.0 就是那条迟滞带。台面 TL=30.0 / TLD=15.0 **不参与**这三个阈值。

### 编译与符号
`UV4.exe -b PSC316RK-V12.uvprojx -j0` → **0 Error, 1 Warning**（`eevCTRL.c` 的
`Time3` 未使用，是既有告警，与本次无关）。
`Code=67098 RO=5318 RW=4428 ZI=23092`。hex：`OBJ/PSC316RK-V12-20250327.hex`。
按新 map 复核，本次新增只落 `.text`，**风机相关 RAM 地址与 V12.12 完全一致**
（`0x20000d00`~`0x20000da8`），台面探针脚本无需改地址。

### 台面测试脚本
`tools/test_316_type4_ladder.py`：
```
python.exe tools/test_316_type4_ladder.py --status   # 读版本/机型/风机类型/阈值
python.exe tools/test_316_type4_ladder.py --run      # 走完整条阶梯，10Hz 采样
```
版本读 `Parameter[1000..1005]`（`PSC316_VERSION_MODBUS_BASE=1000`），
用来确认新固件真的烧进去了（应显示 minor=13）。

### 待测（烧录 V12.13 后）
| 用例 | 冷凝 | 期望 |
|---|---|---|
| C1 起机满频 | 47.0 | 风机没开且 >45 → 变频直接满频 |
| C2 段1 加载 | 40.0 | 变频低→高，定频不开 |
| C3 段1→段2 | 40.0 | 变频到最高 → 定频开 + 变频回最低 |
| C4 段2 加载 | 40.0 | 定频开着，变频再升到最高 |
| C5 段2→段1 | 30.0 | 变频降到底 → 定频关 + 变频回最高 |
| C6 段1 减载 | 30.0 | 变频从最高降到最低 |
| **C7 变频风机关** | 30.0 | **V12.13 新行为**：到最低频且满门 → 两台全关 |
| C8 自动重启 | 45.0 | 停机 ≥30s 后变频重启 |
| C9 保持带 | 35.0 | 34~36 之间，加减载都不动 |

---

## 17. 实机测试：类型4（变频+定速）阶梯 9 条全通过 + C1 起机（316 V12.13，2026-09-17）

台面：外机2，机型=2 风机类型=4，TL=30.0 TLD=15.0，加减载 1.0Hz/s，最短运行/停止 30s，
环温 20.0C。手段同 §14（改 AI 通道偏移推冷凝温度）。脚本 `tools/test_316_type4_ladder.py`。

**端子确认**（类型4）：`Y02` = 变频风机（`Fan1Y03 <- FanOpenSet`），
`Y03` = 定速风机（`Fan3Y07 <- FanOpenSet2`），`Y07` 不用。宏名里的数字是旧引脚号，与实际端子相反。

### 主阶梯（`--run`，8 段约 17 分钟）

| 用例 | 冷凝 | 观察到的 |
|---|---|---|
| C5 段2→段1 | 30.0 | `t=552`：`FixOn 1→0`、`VfdHz` 跳回 50.0、`Stage 2→1`、`ChgSec=0`、`Y03 1→0` |
| C6 段1 减载 | 30.0 | `VfdHz 49→11` 线性，1.0Hz/s |
| **C7 变频风机关** | 30.0 | **`t=592`：`VfdOn=0 FixOn=0 Stage=0`，`Y02=Y03=Y07=0`** |
| C8 自动重启 | 47.0 | `t≈825` 重启。定速风机在重启后**恰好 15 秒**动作 → 反推变频**已是 MaxSpeed** |
| C9 保持带 | 35.0 | `Load=0 Unld=0`，60s 内频率与状态零变化 |

### 升档补测（`--ascent`，补主阶梯漏掉的升档方向）

主阶梯进 C1~C4 时风机已在 S2 顶（前一支探针把速度留在 31Hz，等脚本写完 AI 偏移它已爬完），
所以升档方向**没有逐帧记录**。补测先降到 S0 再从 40.0C 走一遍：

```
t=178  重启（冷凝 40.0 < 45）→ 给最低频 10Hz
t=197  VfdHz=29.0  RunVFD=19  FixOn=0  Stage=1     ← 10 + 19×1.0，段1 按 1.0Hz/s 爬
t=217  VfdHz=50.0                                   ← 到最高频
t=217  FixOn=1  VfdHz=10.0  Stage=2  ChgSec=0       ← 同一秒：段1→段2
t=220~237  VfdHz 10→50                              ← 段2 按 2.0Hz/s（满载速率）
t=237~309  VfdHz 停在 50.0                          ← S2 顶不再加载
```

### C1 起机补测（复位 + 300s 采样）—— **通过**

`tools/verify_a5_prestart_full_speed.py 300`（复位与采样必须同进程，两进程抢 ST-Link 会冲突）：

```
复位前   VfdOn=1 VfdSpeed=50.0Hz Comp=1,1
>>> reset
 t=  0.0  VfdOn=0 VfdHz= 0.0 Comp=0,0
 t= 10.1  VfdOn=1 VfdHz=50.0 Comp=0,0 Y02=1        <- 预启动：压机停着，风机直接满频
   ...    保持 179 秒
 t=189.2  VfdHz 50.0->10.0  Comp=1,0  Y03=1  Load=1
 t=190~209  VfdHz 10->50  @2.0Hz/s
 t=219.2  Comp=1,1
```

- **预启动满频确认**：风机没开 + 冷凝 47 > 45 → **50.0Hz**（V12.11 实测是 20.0Hz）✓
- **起机瞬间的连锁**：预启动已把变频放在 MaxSpeed，压机一进来 `OutdoorFanTypeIII()` 第一次扫描
  就满足 `VfdSpeed == MaxSpeed`，**立刻走「段1顶→段2」**（定速风机开 + 变频回最低频）。
  所以**类型4 在冷凝 >45 起机时不在段1停留**。这解释了为什么前几轮 C1 总是"一进去就在 S2 顶"。
- **压机启动要等 180 秒**：门槛是 `max(MinStopTimeSet=180, SPEC_COMP_MIN_STOP_SECONDS=120)`，
  复位后 `CompStopTime[]` 从 0 重算。第一次采 180s 正好卡边界没抓到，放到 300s 才拿到。

### 顺带确认

- **满载 2Hz/s 直接测到**（`t=220~237`，16→50 用 17 秒 ≈ 2.0Hz/s）。代码
  `UserAction.c:1452-1453`：`LoadStep = Step; if((类型==4) && OutdoorFixedOn) LoadStep = 20;`
  加载用 `LoadStep`，**减载一律用 `Step`** —— 与规格书「满载 2Hz/s、其余按【变频轴流风机加减载速度】」一致。
- **日志里 `Y02` 单帧 0 是探针读竞争**，不是固件问题：`IoOutAction()` 每扫描先 `Fan1Y03 = 0;`
  再按类型赋值，探针正好读在那个窗口。下一帧即恢复，且 `VfdOn/Stage` 全程未变。

### 我的工具 bug（已修）

`--ascent` 漏加到 case 识别元组 `("--setup","--run","--restore","--status")`，
第一次补测实际**重跑了主阶梯**，升档方向没测到。已补上。

---

## 18. ⚠️ 未修（已知，**用户裁定暂不改**）—— 预启动选定速度后没有降速路径

**裁定**：2026-09-17 用户确认"不改也行，先把问题记录下来"。理由站得住：
不是安全问题，且在规格书怎么写还不确定的情况下改行为，风险比留着大。

### 硬判据：`OutdoorVfdSpeed` 的全部写入点（22 处）

| 位置 | 函数 | 压机停着时会执行吗 |
|---|---|---|
| `371` | 声明初始化 | 只在上电 |
| `952` `=0` | `OutdoorFanAllOff` | ⚠ **启动挂起时够不到**（见下） |
| **`1080`** | `OutdoorFanPrestartVfd` | ✅ **唯一能改速度的地方，一次性** |
| `1086` `=MinSpeed` | 同上 | ✅ 但是**下限**（`if(<MinSpeed)`），**不会降速** |
| `1246/1253` | `OutdoorFanHeatPump` | 制热，制冷不走 |
| `1495`~`1579`（10 处） | `OutdoorFanTypeIII` | ❌ 压机停着根本不调用 |

⇒ **压机停着时，唯一能改速度的是预启动那一次性赋值，加上一个只升不降的下限。**

### 问题一：预启动是"只看一眼"的

```c
if(!OutdoorVfdOn)
{
    if((StopSeconds[0] >= MinStop) && (ChgSec >= 15))
    {
        OutdoorVfdOn = 1;
        OutdoorVfdSpeed = (OutdoorCondTempMax() > 450) ? MaxSpeed : MinSpeed;  // 只看这一眼
        ChgSec = 0;
    }
}
else if(OutdoorVfdSpeed < MinSpeed) { OutdoorVfdSpeed = MinSpeed; }  // 只是下限
```

判一次冷凝就锁定风量，**整段等待期不再重判**。台面实测：`t=10.1` 判到 >45 给 50Hz，
之后 179 秒一直 50Hz，压机全程没转。

**会引起的**：夏天压机刚停、高侧还热 → 冷凝 >45 → 但压机要等**最短停机 180 秒**
（`MinStopTimeSet` 默认 180）→ 这 3 分钟风机**满频空吹**。高侧被吹一两分钟就凉了，
但代码不会再降速。代价是电、噪声、风机运行小时数。**不是安全问题**（压机没转、无冷媒循环）。
台面上被放大了（冷凝用偏移钉死），真机上高侧会掉，但**掉下去代码也不会减速**。

**V12.12 引入**：改动前预启动固定给 MinSpeed，没有"锁在满频"这回事。

### 问题二：`OutdoorFanPrestartVfd()` 完全没有降速/停机路径（**与 V12.12 无关**）

压机**正常停机**（非复位）时，若停机那刻 `VfdOn=1` 且速度还在 50Hz（比如刚在段2 顶）：

1. 压机停 → 需求还在 → `OutdoorCompressorStartPending()` 为真
2. `OutdoorFanPrestart()` 走 `if(!OutdoorVfdOn)` **不成立**（VfdOn 还是 1）→ 一次性分支跳过
3. 落到那个只升不降的下限 → **保持 50Hz 吹满整个最短停机期**，既不降速也不停

**并且 `OutdoorFanAllOff()` 够不到**：`UserAction.c:1652-1659` 在 `OutdoorCompressorStartPending()`
为真时 `return 1`，后面的 `OutdoorFanAllOff()`（`1673/1681`）不可达。
而 `OutdoorCompressorStartPending()` **只看"压机使能、无故障、未被禁"，不看时间** ——
所以只要还有需求，压机一停它立刻为真，风机的归零路径就永远走不到。

### 边界（别把话说满）

不是"压机停着就一直锁"，而是"**启动挂起期间锁**"。需求消失 → 不再挂起 → 才会走
`OutdoorFanAllOff()` 归零。

### 可能的修法（待规格书确认后再动）

把 `OutdoorVfdSpeed = (OutdoorCondTempMax() > 450) ? MaxSpeed : MinSpeed;` 从那个一次性分支
里**挪出来**，改成预启动期间每扫描执行 —— 预启动期间持续跟冷凝走，凉了就降到最低频。
**动手前必须先确认**：规格书/流程图对预启动这一段是怎么写的？是"那一刻判一次"还是"持续判"？

---

## 19. 规格书 4.4 化霜 逐条核对 + 316 V12.14 修正（2026-09-17）

用户提供了 4.4 的**完整条款**（item1 化霜开始 7 条件 / item2 结束 3 原因 / item3 开始动作 /
item4 结束动作），这才有了可逐条对照的依据。**注意：在此之前我只拿到正文那句概括，
据此报的 4 条"偏差"全部是误报**（见下）。

### 19.1 实现文件

化霜真正的实现在 **`DefAction.c`**（不是 `UserAction.c`）：

```
DefEnable()                     判「该不该开始化霜」-> DefComp0[i]
  ├ PSC316UnitTimedDefrostMode()   非环温传感器故障？（环温故障不算）
  ├ PSC316DefrostGapNeed()         环温>0 -> [23]；否则 -> [24]
  ├ 定时模式时 GapNeed 改取 [18]
  └ 起判：制热 && 本路压机在转 && CompRunTime >= [20]*60
          && DefGapCount >= GapNeed && (环温条件 / 翅片条件)
PSC316PublishDefrostRequest()  -> CanDefrost[i] 上报 330
PSC316DefrostGrant()           -> 要 DefrostPermit（330 发来）才发 DefrostSignal
PSC316DefrostAction()          状态机 0=空闲 1=化霜中 2=收尾60秒
```

**关键**：`DefGapCount[i] += uc1s`（`DefAction.c:125`）累计的是**压机实际运行的秒数**，
不是墙上时间。而 `CompRunTime[i]` 压机一停就**归零**。所以「化霜间隔」实际是
「压机累计运行 N 分钟」，且 `CompRunTime` 要求**连续**。

### 19.2 参数范围：11 个全部与规格书一致 ✓

| 网关# | 参数 | 规格书 | 网关 MIN~MAX |
|---|---|---|---|
| 17 | 环境温度定时化霜温度设定值 | 5-15 | 5.0~15.0 ✓ |
| 18 | 环境温度定时化霜时间间隔 | 20-50 | 20~50 ✓ |
| 19 | 环境温度定时化霜时间 | 2-8 | 2~8 ✓ |
| 20 | 化霜开始前压缩运转最小时间 | 5-30 | 5~30 ✓ |
| 21 | 化霜开始前翅片温度 | -4~-1 | -4.0~-1.0 ✓ |
| 22 | 环境温度与翅片温度差 | 5-15 | 5.0~15.0 ✓ |
| 23 | 环温>0 两次化霜间隔 | 15-60 | 15~60 ✓ |
| 24 | 环温<0 两次化霜间隔 | 15-70 | 15~70 ✓ |
| 25 | 化霜结束室外机翅片温度 | 30-60 | 30.0~60.0 ✓ |
| 26 | 环温>0 化霜时间 | 2-8 | 2~8 ✓ |
| 27 | 环温<0 化霜时间 | 2-8 | 2~8 ✓ |

### 19.3 ⚠ 我误报的 4 条（拿到完整条款后全部撤回）

| 我之前说 | 规格书实际 | 判定 |
|---|---|---|
| `NormalEnvLowCount≥60` 是"规格书没有的确认延时" | item1-2「环境温度低于10度**保持60秒以上**」 | ❌ **误报** |
| `FinLowCount≥30` 同上 | item1-4「翅片温度低于【…】**保持30秒以上**」 | ❌ **误报** |
| 化霜里 30 秒错开启动是"多出来的" | item3「未开启的压缩机**间隔30秒开**」 | ❌ **误报** |
| 结束判据用高压开关是偏差（该用压力传感器） | item2「**高压开关跳开**（此时高压不报警）」 | ❌ **误报** |

**教训**：只凭正文那句概括（「采用…高压压力…判断化霜结束」）去比代码，会得出错误结论。
必须拿到逐条条款。

### 19.4 高压：开关 vs 传感器 —— 两个都有，按场景分开用

| 用途 | 规格书用哪个 | 代码 | |
|---|---|---|---|
| 化霜结束 | **高压开关**跳开 | `CompXHightOverX06/X10`（= `X_00`/`X_02`，数字输入）| ✓ |
| 化霜期间 | **不检测高压** | `if(InDefrost){HighPressCount[]=0;}` —— 开关和传感器两条路都跳过 | ✓ |
| 高压故障 | 开关断开 **或** 高压压力（**若有**）>38.0Bar **维持2秒** | 开关→立即；传感器→`HighPressCount>=20`×100ms=2秒 | ✓ |
| 冷凝温度 | 高压**传感器** | `HighPressDisp1/2` → PT 表反解 | ✓ |

「（若有）」= `PressSensor = Parameter[107]`，注释 `1=high+low, 2=low only` —— 只有 `1` 才有高压传感器。

代码注释自证：`A mechanical high-pressure switch trips immediately; analog pressure needs 2 seconds.`

### 19.5 已确认合规、未改动

| 规格书 | 代码 |
|---|---|
| item1 ①热泵制热+至少一台压机在转 | `UserWorkModeSet==1 && AnyCompRun && ActualRun[i]` |
| item1 ③压机运转 ≥【…】 | `CompRunTime[i] >= DefrostCompRunMinSet*60` |
| item1 ④翅片低保持30秒（定时除外）| `FinLowCount[i]>=30` + `FinTemp[i]<DefrostFinTempStartSet` |
| item1 ⑤温差 >【…】（定时除外）| `(EnviTempAI0Disp-FinTemp[i]) > DefrostEnvFinGapSet` |
| item1 ⑥环温>0/<0 两档间隔 | `DefGapCount[i] >= GapNeed` |
| item1 ⑦没有其它外机在化霜 | **330** 的 `PSC316DefrostOwner` + `DefrostPermit` |
| item1 任一台满足→所有压机进入 | 316 的 `DefrostSignal` → `PSC316DefrostAction` 拉全部 `Eligible` |
| item2 翅片达【…】→ 对应压机关闭 | `FinTemp[i] >= DefrostFinTempEndSet` |
| item2 高压开关跳开且**不报警** | `InDefrost` 时 `HighPressCount=0` |
| item2 化霜达设定时间 | 定时→`TimedDefrostSeconds()`；否则 环温>0?[26]:[27] |
| item2 「压缩机**全关闭**才退出」 | `AllEnded` 后才进状态2 |
| item3 屏蔽低压开关 | `if(InDefrost){LowPressCount[]=0;}` |
| item3 风机关 / 四通阀失电 | `Fan[0]=Fan[1]=0; Four[0]=Four[1]=0;` |
| item3 未开压机间隔30秒开 | `Comp[i]=1` 错开 30 秒 |
| item4 全关 + 延时60秒 + 发结束指令 | 状态2，`StateTime>=60` → `CanDefrost=7` |

**330 侧排他**（用户 2026-09-17 要求确认）：`PSC316UpdateDefrostOwner()`（`modulecontrol.c:108-143`）
用**一个静态变量** `PSC316DefrostOwner` 保证同时只有一台外机化霜 —— 因为是单变量不是数组，
天然唯一。只给 owner 发 `DefrostSignal`（140-141）和 `DefrostPermit`（1166）。
`CanDefrost` 状态：0空闲 / 1请求 / 5手动请求 / 6不可用 / 7收尾；`KeepsOwner()` 认 1/5/7。
两个特点（规格书未规定，非偏差）：**先到先得、外机号优先**（`for(unit=1;...)` 扫到就 break，
1 号反复请求会让 2 号饥饿）；**通讯故障会强制释放所有权**（这条是好的，否则掉线会占死化霜权）。

### 19.6 V12.14 改的三处

用户 2026-09-17 逐条确认后要改的三处（`tools/byte_patch_316_defrost_v1214.py`）：

| | 改动 | 依据 |
|---|---|---|
| **A** | 正常化霜环温阈值 → **字面 10.0°C**（新常量 `DEFROST_NORMAL_ENV_MAX = 100`）；定时模式仍用【环境温度定时化霜温度设定值】 | item1-2 写的是「低于**10度**」（字面），正文定时那条才写【…设定值】。原实现两模式共用一个门，正常模式的阈值会跟着参数跑偏 |
| **B** | 定时模式「环温低保持」**30 秒 → 60 秒**（计数器上限 + 起判条件两处）| item1 的 7 个条件里只有 ④⑤ 标了「环境温度定时化霜**除外**」，item1-2 的「保持60秒以上」没除外 |
| **C** | 定时化霜时间分界：单点 4.0 → **4.0/6.0 双点回差** | 正文给两个点（<4 满时间、>6 减1分钟），4~6 未定义。用户 2026-09-17 确认按回差带：**≤4.0 判低、≥6.0 判高、中间保持上次判定**（`static EnvHighBand`）|

**未改、保持现状**（已向用户说明，用户未反对）：`TimerDefrostMode[i] = UnitTimedDefrostMode`
两路共用 —— 一台系统有非环温传感器故障时，健康的那台也走定时化霜。
理由：一个化霜周期只有一个 `MaxDefrostTime`，而 item1 要求所有压机一起化霜，
所以同一周期内不可能两种模式并存，整机统一是唯一自洽做法。

### 19.7 环温探头故障不走化霜

`EnviTempErr` 不在 `PSC316NonAmbientSensorErr()` 里（✓ 符合「**除**环境温度以外」），
它走另一条路：`ErrorCheck.c:595 UnitFault=(EnviTempErr||...)` → **整机级故障 → 停机**。
所以环温坏了连化霜都不会跑。

### 19.8 编译与符号（V12.14）

**0 Error, 1 Warning**（`eevCTRL.c` 的 `Time3`，既有）。`Code 67098 -> 67138`。
新 `EnvHighBand` 落 `0x20000df2`，**风机相关 RAM 地址与 V12.12/13 完全一致**
（`OutdoorFanStage`=0x20000d00、`OutdoorVfdOn`=0x20000d01、`OutdoorVfdSpeed`=0x20000d30、
`OutdoorFanRunSeconds`=0x20000da0），探针脚本无需改地址。

⚠ `DefAction.c` 是 **UTF-8 + LF**（仓内其他文件多为 CRLF）—— 写回后编码/换行均已校验。

### 19.9 台面可测性（2026-09-17 新查明）

| 路径 | 能不能测 | 怎么造 |
|---|---|---|
| **定时化霜（故障回退）** | ✅ | 环温 <【设定值】+ 一路非环温传感器故障 |
| **正常化霜** | ✅ **也能测** | 注入**翅片温度** |

**翅片温度的注入路径**（本次新查明，与推环温同一套机制）：

```
NTC[i] = round(Tempbuf*10) + Parameter[493-i]
镜像对  Parameter[1580+i] <-> Parameter[499-i]      （UserAction.c:254/268）
两者错开 6  ->  NTC[i] 读的 Parameter[493-i] == 镜像块的 Parameter[1586+i]

  NTC[0]=环温  <- 316[1586] <- 330[2534+(u-1)*16+0]
  NTC[1]=翅片1 <- 316[1587] <- 330[2534+(u-1)*16+1]   <-- 外机2 = 330[2551]
  NTC[2]=翅片2 <- 316[1588] <- 330[2534+(u-1)*16+2]   <-- 外机2 = 330[2552]
```

330 侧 `modulecontrol.c:522` 注释佐证：`compact mirror: +0~7 calibration offsets`。

### 19.10 测试参数（都用范围下限，能缩短的缩短）

| 参数 | 测试值 |
|---|---|
| 17 定时化霜温度设定值 | **15.0**（上限，最易满足）|
| 18 定时化霜时间间隔 | **20**（下限）|
| 19 定时化霜时间 | **2** |
| 20 化霜前压机最小运行 | **5** |
| 25 化霜结束翅片温度 | **30.0** |
| 26/27 化霜时间 | **2 / 2** |

**硬底线**：`DefGapCount` 要累计 **20 分钟压机运行** + `CompRunTime` **连续** 5 分钟。
→ 第一次定时化霜最早在第 **20 分钟**触发，化霜 2 分钟 + 60 秒收尾 ≈ **一个周期 23 分钟**。
不能再压 —— 再往下超出规格书范围，固件 `ErrorCheck.c:677-684` 的 `Parameter_min/max` 会拒。

### 19.11 待办（2026-09-18 结算）

- [x] 烧 V12.14 -> 核对 `Parameter[1000..1005]` = `[316,12,14,2026,9,17]` —— ✅ 已核
- [x] 写 17/18/19/20/25/26/27 到上表值 —— ✅
- [x] 切制热（用户操作）—— ✅
- [x] 正常化霜：注入翅片温度（`330[2551]`）-> 跑满一个周期 —— ✅ **见 §20**
- [x] 盯 `DefComp0 / PSC316DefrostState / Comp / Fan / Four / DefGapCount / StateTime` —— ✅
- [x] ~~定时化霜：注入环温 + 一路非环温传感器故障 -> 跑满一个周期~~
      ⇒ **不是「没跑」，是「跑不了」**：实测坐实该路径**不可达**，见 **§20.11**。
      触发条件与 `SystemFault[0]` 是同一个表达式，故障一置位就把系统停了，
      而化霜启动的三条判据全部要求压机在转 —— **零时间窗**。

**⇒ 本节 6 条待办已全部了结。定时化霜那条的结论从「待验」变成「已验：不可达」。**

---

## 20. 化霜实机测试（2026-09-18）：规格书 4.4 逐条验证

### 20.0 台面与方法

| | |
|---|---|
| 外机1 | 机型2 / 风机类型2（单风机变频）|
| 外机2 | 机型2 / 风机类型4（变频+定速）|
| **传感器注入** | 环温 `330[2534+(u-1)*16+0]`→`316[1586]`；翅片1/2 `+1/+2`→`316[1587/1588]`（详见 §19.9）|
| 排他性观测 | 330 的 `PSC316DefrostOwner`（**单个静态字节**）+ 各外机 `UnitWriteStatus[].DefrostSignal[]` |
| 探针 | `probe_316_defrost.py`（316 侧）、`probe_330_defrost.py`（330 侧）、`probe_330_eev.py`、`probe_316_xin.py` |

**关键前提**：`DefGapCount` 累计的是**压机实际运行秒数**，`CompRunTime` 一停就归零。
所以"化霜间隔"= 压机累计运行 N 分钟。台面上把 `[23]` 调到下限 15 分缩短等待。

### 20.1 化霜开始 7 条件（规格书 item1）—— **6 条实测通过，1 条部分**

| # | 规格书 | 实测 | |
|---|---|---|---|
| ① | 热泵制热 + 至少一台压机在转 | `WMd=1`、`Comp=(1,1)` | ✅ |
| ② | 环温<10 度**保持 60 秒以上** | `NormalEnvLowCount=60`（封顶）| ✅ |
| ③ | 压机运转 ≥【化霜开始前压缩运转最小时间】| `CompRunTime=15 分` ≥ `[20]`=5 分 | ✅ |
| ④ | 翅片<【化霜开始前翅片温度】**保持 30 秒以上** | 翅片 −8.0 < `[21]`=−2.0，`FinLowCount=30` | ✅ |
| ⑤ | 温差 >【环境温度与室外机翅片温度差】| `5.0 − (−8.0) = 13.0` > `[22]`=10.0 | ✅ |
| ⑥ | 间隔 >【环温>0/<0 两次化霜间隔】| `DefGapCount` **恰在 15 分**（`[23]`）触发 | ✅ |
| ⑦ | 没有其它室外机处于化霜状态 | 见 §20.5 | ⚠ **部分** |

**⚠ ⑦ 只验到一半**：验到了"只有 owner 拿 `DefrostSignal`、其余全 0"（外机1 当 owner 时外机2 的
`DefrostSignal=[0,0]`）。但**"两家同时请求、只放行一家"那个更强的场景没撞上时间窗**
（两台 `DefGapCount` 相位差 + 化霜窗口只有 2~3 分钟）。**排他性机制本身来自代码**
（`PSC316DefrostOwner` 是单字节，天然唯一），实机只验了"单家请求"那半。

**时间类条件是自然累计的**：②④③⑥ 里的 60 秒/30 秒/5 分/15 分都不是我能"设"出来的，
是压机真跑了那么久攒出来的 —— 所以这几条是**真验过**，不是"因为把门限调小了所以过"。

### 20.2 化霜结束三原因（规格书 item2）—— **三个全部实测**

| 原因 | 实测证据 |
|---|---|
| **① 翅片温度达【化霜结束室外机翅片温度】** | 外机2：化霜中**只把翅片1 抬到 32.0°C**（`[25]`=30.0）→ **系统0 在 `DefRT=101` 秒关闭**（提前 19 秒）；**翅片2 没动 → 系统1 走满 120 秒** |
| **② 高压开关跳开（此时高压不报警）** | 外机1：跳开 `X_00` → **系统0 在 `DefRT=18` 秒关闭**（提前 162 秒）；`Comp1HightPressErr[606]` **全程保持 0** |
| **③ 到时** | 外机1 `[26]`=3 分 → 180 秒；外机2 `[26]`=2 分 → 120 秒，都走满 |

**「满足其一即可」+「对应压缩机关闭」的直接证据**（外机1 一次化霜里同时出现两种原因）：

```
t=575.9s  St 0->1                     化霜开始
t=593.8s  跳开 X_00  -> Ended[0]=1, Comp[0]=0, RT[0]=18 秒   <- 原因②（系统0）
t=619.6s  复位 X_00  -> Ended[0] 保持 1（**锁存，不回退**）
t=755.6s  RT[1]=180  -> Ended[1]=1, Comp[1]=0 -> St=2        <- 原因③（系统1）
t=815.6s  收尾满 755.6->815.6 = **60.0 秒**  -> St=0
```

### 20.3 化霜动作（规格书 item3 / item4）

| 条款 | 实测 | |
|---|---|---|
| item3 室外机**风机关** | `Fan=(0,0)`，**`Y02=Y03=Y07` 全 0** | ✅ |
| item3 **四通阀失电** | `Four` 从 `(1,1)`（制热）→ **`(0,0)`** | ✅ |
| item3 已开启的压机**不关闭** | `Comp=(1,1)` 化霜全程不变 | ✅ |
| item3 未开启的压机**间隔 30 秒开** | 实测两台压机错开约 30 秒启动 | ✅ |
| item3 屏蔽低压开关 | `if(InDefrost){LowPressCount[]=0;}`（代码）| ⚠ 代码审阅 |
| item2 「压缩机**全关闭**…退出化霜」| `Ended=(1,1)` 与 `Comp=(0,0)` **同刻**才进 `St=2` | ✅ |
| item4 所有压缩机关闭 | `t=755.6` `Comp=(0,0)` | ✅ |
| item4 **延时 60 秒** | `755.6→815.6` = **60.0 秒** | ✅ |
| item4 进热泵待机 | `St=0`，`Ended` 全部复位 | ✅ |
| item4 向室内机发化霜结束指令 | `CanDefrost` 出现 **7**（收尾态）| ✅ |

### 20.4 辅热（330 侧，需 ST-Link 插 330）

| 规格书 | 实测 | |
|---|---|---|
| 收到化霜开始指令后**辅热全开** | `IndoorHeatAnalogDemand = 1000`（满量程），`ReheatPidStateDisp[942] = 7 = REHEAT_PID_STATE_DEFROST` | ✅ |
| 收到化霜结束指令后**辅热全关** | 化霜结束 `IndoorHeatAnalogDemand = 0`，之后按正常 PID 重新建立（110 秒爬到 1000）| ✅ |

**台面配置**：【制热方式】`[128]`=1（热泵制热）、【再热是否启用】`[127]`=1、【共用】`[129]`=1、
【再热/辅热方式】`[332]`=1（**AO**）→ 所以**辅热全开只体现在模拟量上，三个接触器
`Heat1Y00/Heat2Y01/Heat3Y02` 不动**（配置决定的，代码按 `outputMode` 分派）。

**两个前提条件**（代码审阅，未实测其反例）：
- `heatPumpAux` 只在 `制热 && HeatModeSet==1` 时为 1 → **`HeatModeSet>=2`（非热泵电加热）时化霜不触发辅热全开**
- 还要再热共享配置齐全（`ReheatEnableSet && HeatReheatShareSet!=0 && ReheatModeSet!=无`），
  否则 `HeatControl_UP()` 提前 return，化霜分支根本到不了

### 20.5 同时只能有一个室外机化霜（330 侧排他）

```
PSC316DefrostOwner = 2
外机1: CompStatus=[制热,制热]  CanDefrost=[0,0]  DefrostSignal=[0,0]
外机2: CompStatus=[除霜,除霜]  CanDefrost=[1,0]  DefrostSignal=[1,1]  <== owner
外机3~6: 停                     全 0              全 0
```

`PSC316DefrostOwner` 是**单个静态字节**（`modulecontrol.c:101`），`DefrostSignal[u] = (u == owner)`
—— **结构上就只能有一台**。⚠ 但"两家同时请求"的竞争窗口没撞上，见 §20.1 ⑦。

两个特点（规格书未规定，非偏差）：**先到先得、外机号优先**（`for(unit=1;...)` 扫到就 break，
1 号反复请求会让后面的饥饿）；**通讯故障会强制释放所有权**（`UnitCommErr != 0` → owner=0）。

### 20.6 电子膨胀阀

| 规格书 | 状态 |
|---|---|
| 热泵制热：**室内机 EEV 关闭** | ✅ 实测：制热观测窗 0~691.6s / 0~947.6s 内 `EEV1..4` **全程 0/0** |
| 化霜：**室内机 EEV = 【化霜时电子膨胀阀开度】350** | ✅ 实测（**原本被钳到 200，已修**，见下）|
| 化霜：**室外机 EEV 关闭** | ✅ 代码审阅（两条算法路径都实现，判据不同：`DefrostSignal\|\|SysHaveDef` / `CompWorkMode[i]==2`）|
| 常规关闭：**20 步/秒** + **关闭步数 = 当前步数 + 20** | ✅ **实测 ×4 + 代码双证**（见下）|
| 紧急停机：**20 步/秒** + **关闭步数 = 当前步数 + 10** | ⚠ 代码审阅（未实测，代价太大）|

#### 20.6.1 关闭速率实测（2026-09-18，`probe_330_eev.py` 抓到两次化霜）

两次化霜结束（外机1 `t=877.5`、外机2 `t=1074.8`）触发的室内 EEV 关闭，**四路序列完全一致**：

| | 关闭第一拍 | 逐拍 | 步间 | 最后一拍 | 全程 |
|---|---|---|---|---|---|
| EEV1/2（外机1） | `350 -> 370` **+20** | 每拍 −20 | 0.90~1.10s（均 1.00） | **−10** | 370步 / 18.90s = **19.6 步/秒** |
| EEV3/4（外机2） | `350 -> 370` **+20** | 每拍 −20 | 均 1.00s | **−10** | 370步 / 18.70s = **19.8 步/秒** |

**「关闭第一拍先 +20」就是规格书那句「关闭步数 = 当前步数 + 20」** —— 它不是"多走 20 步"，
是**过冲**：先把目标抬到当前位置之上 20 步，再匀速降到 0（相当于重新确认零位）。

代码 `eevCTRL.c:171-202 EEVCloseStepTarget()` 逐字对上：

```c
const int closeStepPerSecond = 20;                              // 20 步/秒
extraStep = (closeMode == EEV_CLOSE_EMERGENCY) ? 10 : 20;       // 正常 20 / 紧急 10
Parameter[OldAddValue+i] = current + extraStep;                 // 「关闭步数 = 当前 + N」
EEVSpecCloseTarget[i] = current + extraStep;
...
if(uc1s && !EEVSpecCloseTickConsumed[i]) {                      // 每 1 秒一拍
    if(EEVSpecCloseTarget[i] <= closeStepPerSecond) EEVSpecCloseTarget[i] = 0;
    else EEVSpecCloseTarget[i] -= closeStepPerSecond;           // −20
}
return EEVSpecLimitValue(EEVSpecCloseTarget[i], 0, maxStep + 20);  // 上限要容得下过冲
```

**连"最后一拍只掉 10"这个细节都对上**（`target <= 20` 直接置 0，所以 `10 -> 0` 而不是 `10 -> -10`）。

#### 20.6.2 ⚠ 紧急停机是 **+10**，不是 +20（两条并列规则，不是谁取代谁）

规格书 5）紧急停机：*「除电子膨胀阀外所有用电设备均失电，电子膨胀阀以 20 步/秒速度关闭。
说明：电子膨胀阀关闭步数＝电子膨胀阀当前步数**+10**」*

| 场景 | `closeMode` | `extraStep` | 速率 | 调用点 |
|---|---|---|---|---|
| 常规关闭（含化霜结束） | `EEV_CLOSE_NORMAL` | **+20** | 20 步/秒 | `eevCTRL.c:370/487/562/578` |
| **紧急停机** | `EEV_CLOSE_EMERGENCY` | **+10** | 20 步/秒（同一常量，不改） | `eevCTRL.c:636-639`，`if(ModUrgencyStop)` |

**规格书没写、代码里有的一条**：正常关闭进行到一半突然来紧急停机 → **立刻再砍 10 步并切模式**
（那 +20 的过冲不要了）：

```c
else if((EEVSpecCloseMode[i] == EEV_CLOSE_NORMAL) && (closeMode == EEV_CLOSE_EMERGENCY))
{
    if(EEVSpecCloseTarget[i] > 10)EEVSpecCloseTarget[i] -= 10;
    else EEVSpecCloseTarget[i] = 0;
    EEVSpecCloseMode[i] = EEV_CLOSE_EMERGENCY;
}
```

#### 20.6.3 驱动器分时复用（规格书未提，实现里有）

```c
/* The driver time-slices one group every 1.3 s. Compensate the pulse
   period so every valve can still close at 20 steps/s. */
if(activeGroups >= 4)EEVPulseTimeSet = 10;
else if(activeGroups == 3)EEVPulseTimeSet = 13;
else if(activeGroups == 2)EEVPulseTimeSet = 20;
else EEVPulseTimeSet = 32;                     // 空载默认（user.c:451）
```

阀组 = `EEV1/5`、`EEV2/6`、`EEV3/7`、`EEV4/8` 共 4 组。原始日志里"EEV3 动一下、EEV4 动一下"
就是这个分时在起作用 —— **代码按同时动作的阀数补偿脉冲周期**，保证每路**都**能到 20 步/秒，
而不是被平分（实测同时关两路，各自都 ~20 步/秒 ✅）。

**开启速率（规格书未规定）**：`0 -> 350` 用 14.6~15.3s ≈ **24~25 步/秒**。非偏差。

#### 20.6.4 规格书 10 个 EEV 参数 ↔ `Parameter[]` 下标（330）

范围全部与规格书逐条吻合（`user.c` 的 `Parameter_max/min` **大数组字面量** + `Parameter_def`）。

| 规格书参数 | 下标 | min~max | def |
|---|---|---|---|
| 过热度设定值 Y | `366` | 20~100 (2.0~10.0°C) | 50 |
| 过热度设定幅差 YD | `367` | 20~50 (2.0~5.0°C) | 20 |
| PID 比例系数 KP | `368` | 1~100 | 5 |
| PID 扫描周期 TP | `16` | 1~10 | 5 |
| PID 积分时间 TI | `369` | 1~1000 | 100 |
| PID 微分时间 TD | `370` | 0~1000 | 0 ⚠ 规格书写 (1-1000) |
| 电子膨胀阀起始开度 | `411` | 0~500 | 200 |
| 电子膨胀阀每周期加减最大步数 | `371` | 3~50 | 20 |
| **电子膨胀阀调整最大开度** | **`12`** | **200~500** | — |
| 化霜时电子膨胀阀开度 | `372` | 100~500 | 350 |

⇒ 宏名：`EEVSuperHeatTargetSet=366`、`EEVSuperHeatDeadSet=367`、`EEVPidKpSet=368`、`EEVPidTiSet=369`、
`EEVPidTdSet=370`、`EEVStepLimitSet=371`、`DefrostEEVStepSet=372`（=`INDOOR_SPEC_BASE(315)+51..57`）、
`EEVPidScanTimeSet=16`、`EEVStartStepSet=411`、`EEVOpenMaxSizeSet=12`（=`EEVPARAMETER(10)+2`）。

**⚠ 监控软件原先只注册了 9 个 —— `[12]`/`[10]`/`[11]` 三个没做界面字段**（2026-09-18 已补进
`INDOOR_EEV`）。`[12]` 正是把化霜开度钳到 200 的那个，**固件层面范围守卫本来就在（200~500），
只是 HMI 上看不见、改不了**。

**⚠ 化霜开度被钳到 200 —— 根因是参数配错，不是固件 bug**：

```c
// eevCTRL.c:589
static int EEVSpecMaxStep(void)
{
    int motorMax = EEVSpecLimitValue(MotorTotalPulseSet, 200, 500);   // 下限被抬到 200
    return EEVSpecLimitValue(EEVOpenMaxSizeSet, 200, motorMax);       // 下限也是 200
}
```

台面 `MotorTotalPulseSet[10] = 50`（规格默认 500）→ 被 `clamp(…,200,500)` 抬到 200
→ `[21] = clamp([372]=350, 100, 200) = 200`。

**改 `330[10] = 500` 后**：`EEVSpecMaxStep() = 500` → **`[21]` 下一个扫描周期自动变成 350** ✅
（顺带印证了 `[21]` 是**派生量**，被 `EEVSpecParamBridge()` 每周期用 `[372]` 覆写 ——
**HMI 上真正配的是 `[372] DefrostEEVStepSet = INDOOR_SPEC_BASE(315)+57`**）

⚠ 330 自身参数可通过 Modbus 直接写（**0 基址 = Parameter 下标**），但不一定立即存 EEPROM。

### 20.7 发现的缺陷与隐患

**① 330 会在化霜中撤掉外机的压机命令**（代码审阅 + 实测证实）

```c
// CompAction.c:1019
if(UnitWriteStatus[module].CompControl[comp]
    && (!CompIsRunningStatus(module,comp) || !CompEnvLowEligible(module,comp)))
    UnitWriteStatus[module].CompControl[comp] = 0;
```

`CompIsRunningStatus()` 只认状态 2/3，**状态 4（除霜）不算"运行"** → 外机一进化霜就被撤令。

**实测**（330 下发的压机使能 `Parameter[PLCctrlPara+0/+3]`，即 `[1400]/[1403]`）：

```
化霜中:  [1,1] -> [0,1] -> [0,0]      撤令
化霜后:  [0,0] -> [1,0] -> [1,1]      约 90 秒内自动恢复
```

**实际后果：没有。** 316 的 `PSC316DefrostAction()` 直接强制 `Comp[i]=1`，**压过了 330 的撤令**，
化霜结束后压机也正常重启。**定性：真实存在的代码隐患，被 316 兜住了。**

**② 其它口径不一致**（未实测，代码审阅）：
- `CompMinRunStopCount()` 把状态 4 计为运行，`CompIsRunningStatus()` 不认 —— 两个"是否在跑"口径不一
- `case 4/5`（`CompEnvLowLoad`/`CompEnvHoldLoad`）是**死代码**（`SystemNengry` 只取 0~3），
  所以 `CompEnvHoldLoad()` 里那句 `if(OutdoorIsDefrosting(module))continue;` 当前不可达

**③ ⛔ 定时化霜路径不可达（316 侧，2026-09-18 实测坐实）** —— **详见 §20.11**

一句话：`PSC316UnitTimedDefrostMode()` 的触发条件与 `SystemFault[0]` 是**同一个表达式**，
故障一置位就 `StopOutdoorSystem()`，而化霜启动的 3 条判据全部要求压机在转 → 零时间窗。
**V12.14 的改动 B、C 改的正是这条走不到的路**（改动 A 有效，已验）。
修法 A/B/C 已列，**用户 2026-09-18 裁定「暂不改动」。**

### 20.8 ⚠ 台面参数原始值（还原用）

| | 外机1 | 外机2 |
|---|---|---|
| NTC 偏移 [环温, 翅片1, 翅片2] | `[-44, 29, 31]` | `[-23, -172, -177]` |
| `[23]` 环温>0 两次化霜间隔 | **29** | **30** |
| `[25]` 化霜结束翅片温度 | 39.0 | 30.0 |
| `[26]` 环温>0 化霜时间 | 3 分 | 2 分 |

⚠ **外机2 的 `[23]` 原值是 30，我一度在还原脚本里记成 15（那是我改完之后的读数）。** 已修正。

### 20.9 我这一轮犯的错（都已在过程中纠正）

| 错 | 后果 | 纠正 |
|---|---|---|
| 说 316 探针的 `CanDefrost` 地址错了一个字段（该在 320）| 误报"三列不可信" | 重算 `UnitReadInfo`：`CanDefrost` **确实在 304**，我算错了 |
| 说 `Perm/Sig/CanDef` 三列数据异常 | 误报仪器 bug | 逐字重排日志行后确认**三列全对**（Perm=1、CanDef=7、Sig=0 都符合代码）|
| 抬翅片写死 `+400` | 只抬到 18.0°C，没过外机1 的 `[25]`=39.0，白跑一轮 | 改成**按 `[25]` 自动算抬升量** |
| 脚本 `UNIT=1` 但 ST-Link 已挪到外机2 | **改错了对象**（改了外机1 的翅片）| 立刻还原，并把 `UNIT` 改成命令行参数 |
| 把"报警"盯在 `Parameter[608]` | 看错了变量 | `Comp1HightPressErr = Parameter[COMPERR+1] = 606`；608 是 `AI0TempErr`（传感器故障）|
| 说"外机1 的 X 输入整排没接" | 下了错误结论 | 后来跳开关时 `input[0]` 明确响应 —— **那条实测证据推翻了它** |
| `defrost_fin_end_test.py` 的 `ORIG` 里外机2 `[23]` 记成 15 | 还原会还原错 | 改成 30 |

### 20.10 新增工具

```
tools/probe_316_defrost.py     316 化霜状态机（v2：补齐 item1-③⑦ + Fan/Four/化霜指令/330En）
tools/probe_330_defrost.py     330 排他性 + 辅热（PSC316DefrostOwner 等）
tools/probe_330_eev.py         330 电子膨胀阀（参数 + EEV1..8 步数）
tools/probe_316_xin.py         316 X 输入口 + 高压开关判据
tools/defrost_fin1_end.py      化霜中抬翅片验「翅片温度结束 / 对应压机关闭」
tools/defrost_fin_end_test.py  330 侧排他性 + 抬翅片（合成）
tools/inject_316_ntc.py        任意一路 NTC 偏移注入
tools/set_316_gateway_param.py 写网关参数（带范围预检）
tools/probe_316_timeddef.py    定时化霜可达性（TMode/Y00/CRT/Gap/故障字变化，2026-09-18 §20.11）
tools/_316_faults.py           316 故障字 [605..620] + 压机使能 + 化霜参数一次读全
tools/_eev_rate.py             离线：从 probe_330_eev.txt 算 EEV 开关速率（§20.6.1）
```

⚠ **`DefrostSignal`/`CanDefrost` 的结构体偏移在 316 和 330 上不一样**：
316 是 220/508 字节，330 是 **120/196**。两个仓的各算各的，别混用。
330 的 `.map` **只有 Memory Map、没有符号表**，必须用 `fromelf -s` 从 `.axf` 取地址。

### 20.11 ⛔ 定时化霜路径不可达（死锁）—— 2026-09-18 实测坐实

**规格书 4.4 的「环境温度定时化霜」，在当前固件（V12.14）下永远触发不了。**

#### 20.11.1 机制：钥匙和扳机是同一个条件

```c
// DefAction.c:165   —— "钥匙"：打开定时模式
static unsigned char PSC316UnitTimedDefrostMode(void)
{
	if((Comp1Enable==0) && PSC316NonAmbientSensorErr(0)) return 1;
	if((Comp2Enable==0) && PSC316NonAmbientSensorErr(1)) return 1;
	return 0;
}

// DefAction.c:146-153   上面用到的判定
static unsigned char PSC316NonAmbientSensorErr(unsigned int SysNo)
{
	if(SysNo == 0)
	{
		return (((PressSensor==1)&&(AI0TempErr!=0))
			||(((PressSensor==1)||(PressSensor==2))&&(AI2TempErr!=0))
			||(Fin1TempErr!=0)
			||(Exhaust1TempErr!=0)||(XiQi1TempErr!=0));
	}
	...
}

// ErrorCheck.c:555-558   —— "扳机"：停系统      ← **与上面逐项相同**
SystemFault[0]=(((PressSensor==1)&&AI0TempErr)
	||(((PressSensor==1)||(PressSensor==2))&&AI2TempErr)
	||Exhaust1TempErr||XiQi1TempErr
	||Fin1TempErr) ? 1 : 0;
...
if(SystemFault[i]) ApplyOutdoorSystemFault(i);   // ErrorCheck.c:592
  -> Comp1Err = 1;  StopOutdoorSystem(0);        // ErrorCheck.c:124-127
     -> Comp[0]=0; Four[0]=0; Fan[0]=0;          // ErrorCheck.c:55-57
```

**⇒ `UnitTimedDefrostMode==1` ⟺ `SystemFault[0]==1` ⟺ 系统被停。物理上不可兼得。**

⚠ 注意 `DefAction.c:139-144` 还有个 `PSC316FinSensorErr()`（只查翅片），
**`UnitTimedDefrostMode` 用的不是它**，别读串。

#### 20.11.2 启动侧：三条判据全都要求「压机在转」

```c
// DefAction.c:297-303
if((UserWorkModeSet==1) && (AnyCompRun) && (ActualRun[i])
	&& ((TimerDefrostMode[i] && (TimedEnvLowCount>=60)) || …)
	&& (CompRunTime[i] >= (DefrostCompRunMinSet*60))   // ← 压机不转 = 0
	&& (DefGapCount[i] >= GapNeed))                    // ← 压机不转不累计
```

定时模式**只豁免了 ④翅片 和 ⑤温差**（即规格书写明的「环境温度定时化霜除外」），
**没有豁免 ①压机在转、③压机运行时间、⑥间隔累计** —— 而这三条全部依赖 `Comp1Y05` 接通：

```c
// moduleAction.c:174-175
if(ActualRun[i]){CompRunTime[i] +=uc1s; …} else {CompRunTime[i]=0; …}
// DefAction.c:118-126
if((UserWorkModeSet==1)&&(Comp1Y05)&&(Eligible)&&…) DefGapCount[i]+=uc1s;
```

**⇒ 停机那一拍三个计数器同时冻结/归零，没有任何时间窗能凑齐。**

#### 20.11.3 实测证据（拔探头，ST-Link attach 盯 316）

**试验①：拔外机1「翅片1」探头**（`[613] Fin1TempErr`）

```
t=  5.0   翅片1 -21.9 -> 999.9      FinLow0 30 -> 0
t=  9.6   [613] 0 -> 1               （GErrCheckSet 延时 ≈ 5 秒）
t= 10     TMode   0 -> 1    ← 钥匙：定时模式打开
          Comp[0] 1 -> 0    ┐
          Four[0] 1 -> 0    ├ StopOutdoorSystem(0) 三件套（ErrorCheck.c:55-57）
          Fan[0]  1 -> 0    ┘
          CompRunTime 517 -> 0       ← moduleAction.c:175 的归零分支
          DefGapCount 522            ← 冻结，不再累计
          NormalEnvLowCount 60 -> 0  TimedEnvLowCount 0 -> 1 -> 2 -> …
```

**试验②：拔「吸气1」探头**（`[617] XiQi1TempErr`）—— 独立复现

```
t= 73.2   [617] 0 -> 1
t= 73     TMode 0 -> 1               ← 同样打开
          TimedEnvLowCount 归零后重新累加
```

**决定性一行（t=133）**：

```
t=133   [617]=1  TMode=1  Comp=[0,1]  CRT=0  Gap0=522  St=0  TEnv=60  FinLow0=30
                                                               ↑↑↑↑↑↑↑
                     定时模式"环温低保持"计数器**满 60**（V12.14 改动 B 的门槛已满足）
                     化霜仍然没有启动 —— St 全程 = 0
```

**两次试验全程 `St=0`、`Elig=[0,0]`、`Ended=[0,0]`、`Strt=[0,0]` —— 化霜一次都没启动。**

| 定时模式下的启动条件 | 要求 | t=133 实测 | |
|---|---|---|---|
| `TimedEnvLowCount >= 60` | 60 | **60** | ✅ **满足**（改动 B 可验部分，通过）|
| `EnviTempAI0Disp < DefrostEnvTempSet` | < `[182]`=9.0 | 5.0 | ✅ |
| `AnyCompRun` | 1 | 1（2号在转）| ✅ |
| **`ActualRun[0]`** | 1 | **0** | ❌ |
| **`CompRunTime[0] >= [185]×60`** | 540s | **0** | ❌ |
| **`DefGapCount[0] >= [183]×60`** | 1740s | **522** | ❌ |

#### 20.11.4 连带结论

| | |
|---|---|
| **V12.14 改动 B**（定时模式保持 30→60 秒）| 改的是**走不到的路**（但计数器本身能跑满，已验证）|
| **V12.14 改动 C**（定时化霜时间 4.0/6.0 双点回差）| 改的是**走不到的路** |
| **V12.14 改动 A**（正常模式字面 10.0°C）| ✅ **有效，已验**（见 §20.1-②）|
| **§20 起初没测定时化霜** | 不是漏了，是**测不了** |

#### 20.11.5 修法（三选一，**用户 2026-09-18 裁定「暂不改动」**）

| | 方案 | 评估 |
|---|---|---|
| A | 定时模式下豁免 `ActualRun`/`CompRunTime`/`DefGapCount` | ❌ 等于**在故障停机状态下强行拉压机**；化霜靠压机排气加热翅片，不安全 |
| **B** | **把温度类探头从 `SystemFault[]` 摘掉**（保留在 `PSC316NonAmbientSensorErr` 里当触发条件）| ✅ **唯一自洽**：压力探头坏了没法保护压机 → 必须停；温度探头坏了规格书本来就给退路。但属**安全相关的故障停机行为变更**，须用户拍板 |
| C | 承认未实现，撤回 V12.14 的 B/C，规格书 4.4 标「暂不支持」 | 保守，留一个已知功能空洞 |

**⇒ 当前固件一字未改，V12.14 原样。**

#### 20.11.6 取证物料

```
tools/probe_316_timeddef.py     探针（TMode / Y00 / CRT / Gap / 故障字变化）
tools/probe_316_timeddef.txt    原始日志 268 行（t=0~133，末尾因 ST-Link USB Errno 5 掉线中断）
tools/_316_faults.py            故障字 [605..620] + 压机使能 + 化霜参数一次读全
```

⚠ **传感器故障不能用「校准偏移顶出范围」来注入** —— `NTC16bit.c:161-178` 的范围判定用的是
**原始 ADC 电阻 `res`**，校准偏移是判定通过**之后**才加的。只能物理拔探头。

#### 20.11.7 ⚠ 顺带更正一个我自己犯的标注错误

`Comp1Y05` **宏名骗人**：

```c
// User.h:249-250
#define Comp1Y05   Y00      // 1#压缩机控制标志位   ← 实际是 output[0]
#define Comp2Y06   Y01      // 2#压缩机控制标志位   ← 实际是 output[1]
```

**不是 `output[5]`/`output[6]`。** 同 `Fan1Y03`→Y02（§17）的**旧引脚号遗留家族**，
写探针时别照宏名取址。
（本次结论不受影响：`CompRunTime` 归零本身就反证了 `ActualRun[0]==0` → `Y00==0`。）

---

## 21. 规格书 3.9 电子膨胀阀逐条验证（2026-09-18）

### 21.0 结论速览

**16 条里完成 14 条 —— 制热半边 + 化霜 + PID 公式 + A/B/C + 死区 + 限幅全部实测通过。
只剩「制冷」那一档（需切模式 + ST-Link 分两趟）。**

### 21.1 两套参数：330 室内 vs 316 室外

⚠ **两块板各有一套 EEV 参数，下标完全不同，别混用。**

| 规格书参数 | **316 室外 EEV** | 330 室内 EEV |
|---|---|---|
| 过热度设定值 Y | `[198]` | `[366]` |
| PID 比例系数 KP | `[199]` | `[368]` |
| PID 使能（0=传统分级 1=规格书PID）| `[200]` | `[365]` |
| 电子膨胀阀起始开度 | `[201]` | `[411]` |
| 过热度设定幅差 YD | `[202]` | `[367]` |
| PID 扫描周期 TP | `[203]` | `[16]` |
| PID 积分时间 TI | `[204]` | `[369]` |
| PID 微分时间 TD | `[205]` | `[370]` |
| 每周期加减最大步数 | `[206]` | `[371]` |
| 化霜时电子膨胀阀开度 | `[21]`（派生）| `[372]` |
| 调整最大开度 | `[12]` | `[12]` |

台面实测：316 与 330 的这 10 项**全部已是规格值**（Y=5.0 / YD=2.0 / KP=5 / TP=5 / TI=100 /
TD=0 / 起始开度 200 / 每周期最大 20 / 最大开度 500 / 化霜开度 350），无需改动。

### 21.2 ⚠ 温度语义：制冷/制热下「蒸发/冷凝/翅片」的含义**会翻转**

**这是本次被用户点名要搞清的一条，结论分两层：**

**第一层 —— 代码取「蒸发温度/冷凝温度」是按压力侧，与模式无关：**

| 量 | 取自 | 物理 |
|---|---|---|
| **蒸发温度** `CompEva1Disp` | **低压** `Filter_A[0]`（`SensorStatus[2]`→`LowPressDisp1`）→ 饱和温度 | 低压侧**永远**是蒸发器 |
| **冷凝温度** | **高压** `HighPressDisp1/2` → 饱和温度 | 高压侧**永远**是冷凝器 |
| **吸气温度** `SuctionTempAI16/17` | `AI[11]`/`AI[12]`（NTC6/NTC7）| 压缩机吸气 |
| **过热度** | `UserAction.c:217` `MainOverHeat1Disp = SuctionTempAI16 − CompEva1Disp` | 与规格书一致 |

**⇒ 「蒸发温度 = 吸气压力对应的饱和温度」在代码里落实到位**（热泵里吸气压力就是低压压力）。

**第二层 —— 「翅片温度」的含义随模式翻转，代码用机型区分：**

```c
// UserAction.c:608-618  OutdoorCondTemps()
static void OutdoorCondTemps(signed int *T1, signed int *T2)
{
	*T1 = OutdoorPTTempFromPressure(HighPressDisp1);   // 高压 -> 饱和温度
	*T2 = OutdoorPTTempFromPressure(HighPressDisp2);
	if(EveNumSet == 0) *T2 = *T1;
	if(OutdoorUnitTypeSet == 0)          // 0 = 15~43C = **单冷机**
	{
		*T1 = FinTemp1AI5Disp;             // 翅片恒为冷凝器 -> 直接用翅片温度
		*T2 = FinTemp2AI7Disp;
	}
}
```

| | 制冷时 | 制热时 |
|---|---|---|
| 室外机翅片 | **冷凝器** → 翅片 ≈ 冷凝温度 | **蒸发器** → 翅片 ≈ **蒸发温度** |

**⇒ 单冷机（`OutdoorUnitTypeSet==0`）永不反向，室外翅片恒为冷凝器，可直接用翅片温度当冷凝温度；
热泵（1=0~43C / 2=-15~43C）必须用高压饱和温度。代码处理正确。**

⚠ **顺带纠正一个我一路标错的宏**：`OutdoorUnitTypeSet = Parameter[OUTDOOR_SPEC+0] = [165]`，
**不是"机型"，是「室外机运行环境温度范围 / 单冷-热泵选择」**：
`0=15~43C(单冷) / 1=0~43C / 2=-15~43C`，且 **`Parameter_def[165]=0`（出厂默认单冷）**，台面被设成 2。

### 21.3 PID 公式核对 —— **代码正确，两处"疑似差异"都不是缺陷**

规则：`步数 = KP×[E(K)−E(K−1) + TP/TI×E(K) + TD/TP×(E(K)−2E(K−1)+E(K−2))]`

**代码（330 与 316 的 `eevCTRL.c` 是同一份内核）：**
```c
numerator = (E0-E1)*ti*t + t*t*E0 + td*ti*(E0 - 2*E1 + E2);
denominator = 10 * ti * t;
scaledStepMilli = kp * numerator * 1000 / denominator;
step = accumulatedStepMilli / 1000;        // 毫步余量累加（EEVSpecResidualMilli）
```

| # | 项 | 判定 |
|---|---|---|
| 比例 / 积分 / 死区 / 限幅 / `E(K)=过热度−Y` | ✅ 与规格书一致 |
| **微分项括号** | ✅ **代码正确**（`E0−2E1+E2` 标准二阶差分）。规格书写成 `E0−2(E0−E1)+E2`，展开 = `−E0+2E1+E2`，**是规格书笔误**。仓库里 `内外机规格书完整实现分析与改动方案.md:1570` 记的也是标准形式，交叉印证 |
| **`KP/10`** | ✅ **正确 —— 0.1°C 单位补偿**。`E` 存的是 0.1°C（Y=20~100 表示 2.0~10.0°C），比规格书的 °C **大 10 倍**，分子大 10 倍、分母补 10 才还原 |

**数值验证（KP=5, TP=5s, TI=100s, TD=0）：**
```
规格书(°C)  : err=5.0 -> 5×[(5/100)×5.0] = 1.25 步
代码(0.1°C) : 5 × [(50-50)*100*5 + 25*50] / (10*100*5) = 5×1250/5000 = 1.25 步   ✅
误差跳变    : 5.0->6.0°C，规格书 5×[1.0+0.30]=6.5；代码 5×6500/5000=6.5        ✅
```

### 21.4 A/B/C 三分支（`eevCTRL.c:124-135`）—— 代码与规格书一致

```c
if((evapTemp > 120) && (step >= 0))                            return -5;   // A: >12.0C 固定减 5 步
else if((evapTemp >= 100) && (evapTemp <= 120) && (step >= 0)) return  0;   // B: 10.0~12.0C 不动作
if((err <= dead/2) && (err >= -(dead/2)))                      return  0;   // 死区 Y±YD/2
step 钳到 ±limit;                                              return step; // C
```

- **A/B 判定在死区之前** ✓（规格书 A/B 是凌驾于过热度控制之上的）
- 边界：`>120` 严格大于 12.0；**恰好 12.0 落 B** ✓
- `step` 用的是**未限幅、未过死区的原始 PID 输出** ✓ 与规格书「以过热度控制…加减步数大于等于零」一致

### 21.5 实测：PID 逐拍吻合（316 外机1，制热）

探针 `probe_316_eev_pid.py` 直接读**固件内部的 `EEVSpecErrHist[0][0..2]`**（E(K)/E(K-1)/E(K-2)），
PC 侧用规格书公式重算再与实际对照。

```
   t | 吸气T 蒸发T 过热  Y | E0   E1   E2 | 算式 分支后 | EEV1 目标/当前 | 备注
335  |  3.0   2.2   0.8  5.0 | -42   38   38 | -41  -20  | 480/496  Δ=-20 | C(限幅)
345  |  3.0   2.2   0.8  5.0 | -42  -42  -42 |  -1   -1  | 478/478  Δ=-1  | C
471  |  7.2   2.2   5.0  5.0 |   0    0    0 |   0    0  | 453/453   —    | 死区
869  | 18.0  12.2   5.8  5.0 |   8 -100 -100 |  54   -5  | 367/371  Δ=-5  | **A(蒸发>12→-5)**
1122 | 18.0  11.1   6.9  5.0 |  19    8    8 |   5    0  | 122/122   —    | **B(10~12→0)**
```

| 场景 | 手算 | 实际 | |
|---|---|---|---|
| E 突变 | `(−42−38)·100·5 + 25·(−42) = −41050`，`5×(−41050)/5000 = −41.05` | 限幅后 **−20** | 限幅 ✓ |
| 稳态 | `25·(−42)/5000×5 = −1.05`（含毫步余量）| **−1 步/周期** | 公式 ✓ |
| **A 覆盖** | PID 给 **+54（想开）** | 实际 **−5（关）** | **A 凌驾 ✓** |
| **B 覆盖** | PID 给 **+5（想开）** | 实际 **0（不动）** | **B 凌驾 ✓** |

**⇒ 规格书公式、限幅、毫步余量累加、A/B 覆盖，四样逐拍吻合。**

**注入方法**（台面造工况）：
- 过热度 → 改**吸气温度**：`330[2534+(u-1)*16+5]`（= NTC[5] = `AI[11]` = 吸气1）
- 蒸发温度 → 改**低压压力**：`330[2680+(u-1)*49+2*12+8]` = `330[2712]` → `316[334]`（AI 配置块 ch2 的校准偏移）
- ⚠ **不能用 `UserAxValueSet`**（`316[499]`），见 §21.6

### 21.6 ⚠ 三条观察（不是偏差，但值得记）

**① PID 积分作用很慢。** 稳态增益 = `KP × TP/TI = 5 × 5/100 = 0.25 步/周期 每 °C`。

| | |
|---|---|
| 4.2°C 持续偏差 | 只有 **1 步/5秒 = 0.2 步/秒** |
| 走完 500 步 | ≈ 42 分钟 |
| 要多大的偏差才吃到限幅 20 步/周期 | `20/0.25 = **80°C**` ← 物理上不可能 |

**⇒ 规格书这组默认参数（KP=5, TP=5s, TI=100s）下，`[206] 每周期最大加减步数` 只在"误差突变"那一拍起作用，稳态下形同虚设。**

**② A 分支的 `−5` 不受 `[206]` 限幅约束**（规格书如此，代码也如此）。但 `[206]` 最小可设 3，
那种配置下 A 的 5 步会比限幅还大。**规格书没规定这种情况。**

**③ 台面稳态下阀顶格 500**：过热度 8.8 > Y=5.0，阀全开仍不够 —— 台面工况使然，非缺陷。

### 21.7 完成度

| # | 条款 | 状态 |
|---|---|---|
| 1 | 制冷：**室外** EEV 关闭 | ❌ 待做（切制冷，ST-Link 在 316）|
| 2 | 制冷：**室内** EEV 按过热度 + 初始步数 | ❌ 待做（切制冷，ST-Link 挪 330）|
| 3 | 制热：室内 EEV 关闭 | ✅ §20.6 + §21 复验 |
| 4 | 制热：室外 EEV 按吸气过热度控制 | ✅ **§21 实测** |
| 5 | 初始步数 = 【起始开度】200 | ✅ **§21 实测**（化霜后重启恰好 200）|
| 6 | 化霜：室内 EEV = 350 | ✅ §20.6 |
| 7 | 化霜：室外 EEV 关闭 | ✅ **§20 实测**（制热 302/245 → 化霜 0/0）|
| 8 | 关闭速度 20 步/秒 | ✅ 实测（两块板）|
| 9 | 关闭步数 = 当前 + 20 | ✅ 实测（`350→370`）|
| 10 | 过热度 = 吸气 − 蒸发 | ✅ **§21 实测** |
| 11 | PID 公式 | ✅ **§21 实测逐拍吻合** |
| 12 | 限幅 | ✅ **§21 实测** |
| 13 | A 分支 | ✅ **§21 实测** |
| 14 | B 分支 | ✅ **§21 实测** |
| 15 | C 分支 | ✅ **§21 实测** |
| 16 | 死区 Y±YD/2 | ✅ **§21 实测** |

### 21.8 本轮新增工具

```
tools/probe_316_eev_pid.py   316 室外 EEV 动态记录：读固件内部 E(K)/E(K-1)/E(K-2)，PC 侧当场验算规格书公式
tools/set_ntc_offset.py      纯 Modbus 写 NTC 偏移（**不占 ST-Link**，可和探针并行）
tools/rw330.py               通用 330 寄存器读写（读 a..b / 写 a=v）
tools/_which_board.py        读 DBGMCU_IDCODE 判 ST-Link 挂在 316 还是 330
tools/_316_eev_once.py       316 EEV 参数一次读全
tools/_330_defrost_ask.py    经 Modbus 读 330 各外机化霜请求
```

⚠ **两个坑：**

- **`set_ntc_offset.py` / `rw330.py` 的"写完立刻回读"不可靠** —— 网关写槽忙时会读到旧值，
  看着像"写入失败"其实成功了。**要复核请隔 5 秒再读一次。**
- **PC 侧重算必须用 C 的向零截断除法**，不能用 Python 的 `//`（向下取整）——
  负误差会差 1（`−41.05` 会被算成 `−42`）。探针里已用 `tdiv()` 处理。

### 21.9 关键地址（fromelf -s 取自 axf，`.map` 缺符号表）

| 符号 | 316（`PSC316RK-V12-20250327.axf`）| 330（`PSC330RK-V10.axf`）|
|---|---|---|
| `AI[]`（int16）| `0x20001172`（14 元素）| `0x200017ae`（18 元素）|
| `EEVSpecErrHist`（int32[n][3]）| `0x20002778`（n=2）| `0x200023f0`（n=8）|
| `EEVSpecResidualMilli`（int64[n]）| `0x20002790` | `0x20002470` |
| `Filter_A`（int16[4]）| `0x20000de6` | — |

`NTC[i] == AI[6+i]`（`NTC16bit.c:182-189`）；`MainOverHeat1Disp = Parameter[PLCREADPara+17]`
→ 316 = `[1437]`；**蒸发温度 = `AI[11] − [1437]`**（由 `MainOverHeat` 的定义反推，实测吻合）。

### 21.10 制冷工况实测（2026-09-18 下午，ST-Link 在 330）

台面切制冷后：`330[1] UserWorkModeSet = 0` ✓，外机1 两台压机状态 = 2/2（制冷）✓。

**① 制冷时室外 EEV 关闭 —— ✅（用户目视确认）**

**② 制冷时室内 EEV 按吸气过热度控制 —— ✅ 实测**

```
 t   | 外机1压机 | E0  | EEV1..4 目标/当前
   0 | 制冷/制冷 | 38  | 500/500  390/390  500/500  500/500
 161 | 制冷/制冷 | 38  | 500/500  405/405  500/500  500/500      EEV2 在缓慢加
```

**③ 制冷时室内 EEV 初始步数 = 【起始开度】200 —— ✅ 实测**

```
t= 57  EEV1 目标 0 -> **200**（Δ=+200）  当前 2 -> 200 用 ~6 秒（~33 步/秒）
t=104  EEV2 目标 0 -> **200**（Δ=+200）
```

⚠ **时序**：EEV 是**开机前置位** —— `t=57` 时压机还是 `停/停`，EEV 就先开到 200 了；
压机 `t=88` 才起。与 `eevCTRL.c:315-340` 的 `coolingCommand` 分支一致。

**④ 常规关闭：关闭步数 = 当前+20 + 20 步/秒 —— ✅ 实测（第三次复现）**

```
t=123  EEV3/EEV4 目标 500 -> **520**（+20）   关模式 = **正常/正常**
       EEV4: 520 -> 480 -> 460 -> 440 -> 420 -> 400 -> 380 -> 360 -> ... -> 0
       每拍 −20、约 1 拍/秒 = **20 步/秒**
```

### 21.11 ⚡ 紧急停机 +10 —— 实测通过（2026-09-18）

**这是 3.9 最后一条，之前只有代码审阅。** 触发方式与完整因果链：

```
跳开 X04（阀门联锁）
  -> X04_En = (X04_Cnt>=30)（去抖 30 拍，user.c:513-517）
  -> ValveLinkX4 -> ValveLinkFaultCurrentDisp -> **ValveLinkFaultLatchDisp（闩锁）**
  -> **EnviTempErr = 1**                （ErrorCheck.c:373-382）
  -> **ModUrgencyStop = 1**             （ErrorCheck.c:620-622）
  -> EEV_ActionAll(): EEVCloseStepTarget(..., **EEV_CLOSE_EMERGENCY**)
  -> extraStep = **10**                 （eevCTRL.c:194）
```

**实测（X04 跳开那一帧）：**

```
t=15（跳闸前） EEV1 455  EEV2 319  EEV3 500  EEV4 491    关模式 -/-/-/-
t=17（跳闸后） EEV1 465  EEV2 329  EEV3 510  EEV4 501    关模式 **紧急/紧急/紧急/紧急**
                Δ=+10     Δ=+10     Δ=+10     Δ=+10
                E0=E1=E2 全部归 0（PID 被 reset）

Modbus 复核： [605] EnviTempErr = 1    [1406] ModUrgencyStop = 1
             EEV1..4 目标/当前 = 0/0（已关到底）    压机 = 停/停
```

**⇒ 规格书 5）「关闭步数＝当前步数 **+10**」实测通过，四路全中；
`关模式` 从 `-` 变 `紧急`（`EEV_CLOSE_EMERGENCY=2`）。与常规关闭的 **+20** 形成对照。**

#### 21.11.1 紧急停机的三个触发条件（完整）

```c
// ErrorCheck.c:620-622
if(UserSidePumpOverLoadErr || AntiFreezeErr || EnviTempErr)   // emergency stop
{ ModUrgencyStop = 1; ... 压机命令=0、变频频率=0、output[]=0、DA[]=0、SysStep=0 }
```

| # | 变量 | 触发源 | 可测性 |
|---|---|---|---|
| ① | `UserSidePumpOverLoadErr` `[601]` | **X3 输入口 = 水泵过载开关**（`PumpOverX00 = X03_En`）| ✅ 跳 X3 |
| ② | `EnviTempErr` `[605]` | **消防 / 阀门联锁(X4) / 漏液保护(X13)** 任一，**闩锁** | ✅ **跳 X4（本次用的）** |
| ③ | `AntiFreezeErr` `[602]` | `Parameter[594+8]` —— **330 仓里没有任何赋值语句** | ❌ **不可测** |

**③ 为什么不可测**（两条路都堵死）：
```
ParameterSaveSize = 500，AntiFreezeErr = [602] >= 500
modbus.c ParamCanWriteFromHMI(): if((addr >= ParameterSaveSize) && (addr < 1300)) return 0;
⇒ 只读运行区，Modbus 写不进；固件里又没人赋值 ⇒ 很可能是**死变量（恒为 0）**
```
⚠ 文档里 `EnviTempErr` 常被误读成"环温传感器故障"——**它不是**，是消防/联锁/漏液那一组。

⚠ `ValveLinkFaultLatchDisp` 是**闩锁**的 —— **开关复位后不会自动恢复**，必须做一次故障复位。

### 21.12 3.9 最终结果：**17/17 全部实测完成**

（§21.7 的 16 条 + 本节新增的「紧急停机 +10」）

| # | 条款 | 状态 | 章节 |
|---|---|---|---|
| 1 | 制冷：室外 EEV 关闭 | ✅ | 21.10 |
| 2 | 制冷：室内 EEV 按过热度 + 初始步数 | ✅ | 21.10 |
| 3 | 制热：室内 EEV 关闭 | ✅ | §20.6 + 21 |
| 4 | 制热：室外 EEV 按过热度控制 | ✅ | 21.5 |
| 5 | 初始步数 = 【起始开度】200 | ✅ | 21.10 |
| 6 | 化霜：室内 EEV = 350 | ✅ | §20.6 |
| 7 | 化霜：室外 EEV 关闭 | ✅ | §20 |
| 8 | 关闭速度 20 步/秒 | ✅ | §20 + 21.10 |
| 9 | 关闭步数 = 当前 + 20 | ✅ | §20 + 21.10 |
| 10 | 过热度 = 吸气 − 蒸发 | ✅ | 21.5 |
| 11 | PID 公式 | ✅ | 21.5 |
| 12 | 限幅 | ✅ | 21.5 |
| 13 | A 分支 | ✅ | 21.5 |
| 14 | B 分支 | ✅ | 21.5 |
| 15 | C 分支 | ✅ | 21.5 |
| 16 | 死区 Y±YD/2 | ✅ | 21.5 |
| **17** | **紧急停机 +10** | ✅ | **21.11** |

### 21.13 本节新增工具

```
tools/probe_330_eev_pid.py   330 室内 EEV 动态记录（E(K) 验算 + 关模式/关目标 + PID周期）
tools/set_ntc_offset.py      纯 Modbus 写 NTC 偏移（不占 ST-Link，可和探针并行）
tools/rw330.py               通用 330 寄存器读写
tools/_which_board.py        读 DBGMCU_IDCODE 判 ST-Link 挂在 316 还是 330
```

⚠ **`probe_330_eev_pid.py` 的 `关模式` 列是本轮的关键工具** —— 它把「常规 +20 / 紧急 +10」
直接标出来，不用去猜。读 `EEVSpecCloseMode[] = 0x20000f2c`（0=NONE 1=NORMAL 2=EMERGENCY）。

---

## 22. 规格书 3.8 加湿器加减载 逐条验证（2026-09-18）

### 22.0 结论

**8 条里 7 条实测通过**，1 条（`E(K-1)/E(K-2) 无记录 → N=0,Z=0`）按用户决定不做。
**代码与规格书一致，无缺陷。** 两处「实现比规格书更严/更宽」的记录见 §22.4。

### 22.1 参数与代码位置

| 规格书参数 | 下标 | min/max/def | 规格书 | |
|---|---|---|---|---|
| 【加湿PID比例系数】KPS | `[353]` `HumidKpSet` | 1 / 100 / **5** | 5（1-100）| ✅ |
| 【加湿PID积分时间】TIS | `[354]` `HumidTiSet` | 1 / 1000 / **100** | 100秒（1-1000）| ✅ |
| 【加湿PID微分系数】KDS | `[355]` `HumidTdSet` | **0** / 1000 / **0** | 0（**1**-1000）| ⚠ §22.4① |
| 「PID扫描周期」T | — | 共用 `[412] PidAdjustTimeSet1` | 3.8 参数表**没给 T** | ✅ |
| SDF（死区全宽）| `[361]` `HumidityDiffSet` | 100 / 800 / 200 | x0.1%RH，默认 20.0%RH | — |

**代码**：
- 调用点 `UserAction.c:2480-2534`（`HumidifyAction()` 一族）
- PID 内核 `UserAction.c:748` `IndoorPidDemandStep()` —— **与压缩机/预热/再热共用同一内核**
- BX 查表 `UserAction.c:739` `IndoorDemandBx10()`

### 22.2 代码审计（与规格书逐条对照）

```
N(KS)   = MIN(1, N(KS-1) + Z(KS))        规格书
demand  = clamp(demand + z, 0, 1000)     代码（0~1000 对应 0~1）      ✅

Z(KS)   = KPS*BX/100*[E0-E1 + T/TIS*E0 + KDS/T*(E0-2E1+E2)]
z       = kp * bx10 * num / (10*ti*t)
          num/(ti*t) = (E0-E1) + (t/ti)*E0 + (td/t)*(E0-2E1+E2)        ✅

bx10 = IndoorDemandBx10(N)：
  N <= 100 -> 1    (0.1]      N <= 200 -> 2    (0.1,0.2]
  N <= 400 -> 4    (0.2,0.4]  N <= 500 -> 6    (0.4,0.5]
  否则     -> 10   (0.5,1]                                      ✅ 五档边界逐条一致

死区:  if(IndoorAbsInt(error)*4L < deadWidth) -> Z = 0          ✅ 等价 |E| < SDF/4
无记录: if(validSamples < 2) -> N=0, Z=0                        ✅
E:     error = HumiditySet - HumiditySensorAI1Disp              ✅
制热故障: 见 §22.4②
```

**⚠ 那个 `/10` 又是「0.1 单位补偿」**（同 EEV §21.3）：`HumiditySet` 注释写着 `50.0% RH`，
量程 200~1000，即 **x0.1%RH**；`E` 也是 x0.1%RH，比规格书的 %RH 大 10 倍。

**数值验证（台面实测参数 KPS=5, TIS=100, T=5, KDS=0）：**

| 场景 | `N(KS-1)` | bx10 | 手算 z | 实测 |
|---|---|---|---|---|
| 设定 90→50%RH 阶跃（E: +396→−5）| 1000 | 10 | `5×10×25×396/5000 = 99` | **94~104 围 99 抖** ✓ |
| 设定 20→45%RH 阶跃（E: −305→−54）| 0 | 1 | `5×1×124150/5000 = 124.15` | **124** ✓ |
| 稳态（E0=E1=E2=−55）| 121 | 2 | `5×2×25×(−55)/5000 = −2.75` | **−3** ✓ |
| 稳态（E0=E1=E2=−305）| 0 | 1 | `5×1×25×(−305)/5000 = −7.6` | **−7** ✓ |
| 大阶跃（E: +296→−305）| 1000 | 10 | `5×10×(−308125)/5000 = −3081` | **−3091** ✓ |

### 22.3 实测：BX 五档全部穿越

**方法**：把【湿度设定值】`[162]` 设到刚出死区一点（`SDF/4 = 5.0%RH`），让每拍 `z` 只有几十，
`N` 缓慢穿过各档。探针 `probe_330_humid.py`（**纯 Modbus**，不占 ST-Link）。

```
t=62   N = 100  BX **2 -> 1**     ← 穿过 100
t=94   设定 45.0 -> 90.0   E: −55 -> +396
t=97   N: 91 -> 325   BX = 4   Z = 234      ← 阶跃脉冲
t=107  N = 406  BX = 6            ← 穿过 400
t=117  N = 522  BX = 10           ← 穿过 500
t=142  N = 1000 BX = 10（封顶）
```

| `bx10` | 区间 | 实测覆盖到的 N |
|---|---|---|
| 1 | (0, 0.1] | 0 / 91 / 96 / 100 |
| 2 | (0.1, 0.2] | 102 / 107 / 111 / 121 |
| 4 | (0.2, 0.4] | 325 |
| 6 | (0.4, 0.5] | 406 / 463 |
| 10 | (0.5, 1] | 522 / 621 / 1000 |

**⇒ 五档全部覆盖，边界跳变位置正确。**

**另两条也同时验掉**：`N` 封顶 **1000**（`MIN(1,…)`）；`N` 一拍从 1000 **clamp 到 0**（下限）。

### 22.4 死区 / 制热故障 实测

**① 死区 `-SDF/4 < E < SDF/4 → Z=0`** —— ✅

```
t=219  设定 90.0  E=+396  N=1000  Z=104
t=220  设定 **50.0**  E=**−5**   N=1000  Z=104   ← PID 还没到拍
t=223  设定 50.0  E=−5    N=1000  Z=**0**   ← **死区生效**
t=224~337  E 在 −3~−6 抖   N 一直 **1000 不动**   Z 一直 **0**
```
**判据的关键**：`E` **不为 0**（−5），`Z` 却是 0 —— 正是「带内不动作」的语义；
且 `N` 因 `Z=0` 而**完全停住**。

**② 制热故障 → 加湿器全关** —— ✅

```
（断 X6 之前）  N = 1000   制热故障 = 0
（断 X6 之后）  N = **0**   制热故障 = **1**
```

⚠ **证据强度说明**：探针中途重启过，**没抓到「1000→0」那一帧的过渡**，只有前后两个端点。
因果明确，但不如抓到跳变硬。**要做干净往返**：故障复位（→ N 回 1000）再断一次（→ 又回 0）。

**触发方式**：`HeatFaultX6 = X06_En = (X06_Cnt>=30)`（去抖 30 拍），**闩锁**（`MainHeatFaultLatchDisp`），
**要故障复位才能恢复**。X7 是预热故障，同理。

### 22.5 ⚠ 两处「实现与规格书不完全一致」（都不是缺陷，但值得记）

**① `KDS` 量程：固件 `0~1000`，规格书写 `1~1000`。**
规格书**自相矛盾**（默认值写 0，范围写 1-1000）。**固件取 0 是对的**（否则默认值不合法）。
和 EEV 的 `TD` 是同一个毛病（§21.1）。

**② 「制热故障 → 加湿器全关」有两条独立实现，范围不同：**

```c
// UserAction.c:2486  —— 带模式判断
((UserWorkModeSetTEMP == MODE_HEAT) && HeatingFaultDisp) ||

// ErrorCheck.c:414-419  —— **无条件**
if(HeatingFaultDisp) { HumidValveY13=0; HumidAO2=0; HumidDemandDisp=0; }
```

**⇒ 代码是「无条件全关」，规格书写的是「制热模式下」。实机验证时台面在**制冷**模式，
断 X6 后加湿照样被强制全关 —— 走的是 `ErrorCheck.c` 那条。**

**⇒ 实现比规格书更严（保护更保守），定性为"非缺陷"。但规格书那句若日后修订，这里要对齐。**

### 22.6 未做项

| 条款 | 原因 |
|---|---|
| `E(K-1)/E(K-2) 无记录 → N=0, Z=0` | 只在**开机头两个 PID 周期**（约 10 秒）出现，要重启系统 + 探针提前跑着才能抓。**用户决定不做。** |

### 22.7 新工具

```
tools/probe_330_humid.py   330 加湿 PID 动态记录（**纯 Modbus，不占 ST-Link**）
                           列：设定/实际 E | N(892) BX Z(939) 期望Z | 制热故障(924)
                           PC 侧按规格书公式重算 z，与固件实际对照
```

⚠ **纯 Modbus 探针会独占 COM6** —— 它跑着的时候 `rw330.py` 开不了串口。
要写参数得**停探针 → 写 → 重启**，或者给探针加定时注入（把注入表填进 `sched` 即可）。

---

## 23. 规格书 3.7 预热加减载：代码审计 + 一处**真实偏差**（2026-09-18）

### 23.0 结论

**审计中发现一处规格书自身矛盾：方式2 的 `E(KR)` 该取「室内温度」还是「预热后温度」。**
先按正文改成「室内温度」（V10.7 未烧录）→ **用户同日裁定改回「预热后温度」**（与标题一致）。
**⇒ 代码实质未变，版本号还原为 V10.6 / DAY 16。** 其余与规格书一致。**实测尚未做**（要先切制热）。

### 23.1 参数对照（全中）

| 规格书 | 宏 | 下标 | min/max/**def** | 规格书 | |
|---|---|---|---|---|---|
| 【预热控制方式】| `PreheatCtrlModeSet` | `[374]` | 0/1/**0** | 环境温度 / 预热后温度 | ✅ |
| 【预热开启温度】| `PreheatStartTempSet` | `[356]` | −100/100/**0** | 0度（−10~10）| ✅ |
| 【预热满载温度】| `PreheatFullLoadTempSet` | `[357]` | −300/100/**−50** | −5度（−30~10）| ✅ |
| 【预热控制温度】YWD | `PreheatAfterTempSet` | `[375]` | 0/100/**50** | 5度（0~10）| ✅ |
| 【预热控制温度幅差】YWDF | `PreheatAfterDiffSet` | `[376]` | 0/80/**40** | 4度（0~8）| ✅ |
| 【预热PID比例系数】KPY | `PreheatKpSet` | `[350]` | 1/100/**5** | 5（1-100）| ✅ |
| 【预热PID积分时间】TIY | `PreheatTiSet` | `[351]` | 1/1000/**100** | 100秒（1-1000）| ✅ |
| 【预热PID微分系数】KDY | `PreheatTdSet` | `[352]` | **0**/1000/**0** | 0（**1**-1000）| ⚠ 老毛病 |
| 预热使能 | `PreheatModeSet` | `[333]` | 0/4/0 | — | — |

（`INDOOR_SPEC_EXT_BASE = 374`；x0.1°C）

### 23.2 两种控制方式（`UserAction.c:3065-3112 IndoorPreheatControl()`）

```c
usePostTempPid = ((PreheatCtrlModeSet == 1) && (PreheatModeSet == HEAT_OUTPUT_AO)) ? 1 : 0;
```

**方式1 —— 按环境温度，插入法** ✅ 与规格书一致

```c
if(FreshAirTempDisp <= PreheatFullLoadTempSet) demand = 1000;      // 环温<=满载温度 -> 满载
else if(FreshAirTempDisp >= PreheatStartTempSet) demand = 0;       // 环温>=开启温度 -> 关
else if(PreheatStartTempSet > PreheatFullLoadTempSet)
    demand = (PreheatStartTempSet - FreshAirTempDisp) * 1000
           / (PreheatStartTempSet - PreheatFullLoadTempSet);        // 中间 -> 线性插入
else demand = 0;
```

⚠ **方式1 的输入是 `FreshAirTempDisp`（`Innercoil1Disp` = NTC2 **新风温度**），
规格书写「根据**环境温度**控制」。新风温度 ≈ 环境温度但不是同一探头 —— 记一笔，未改。**

**方式2 —— 按预热后温度，PID** —— 用同一个 `IndoorPidDemandStep()` 内核
（与加湿/压缩机共用，所以 **PID 公式 / BX 五档 / 死区 `±YWDF/4` 这些已在 §22 验过**）。

**`&& (PreheatModeSet == HEAT_OUTPUT_AO)`** ⇒ 与规格书「只适用于 0-10V 输出」一致 ✅

### 23.3 ⚠ 发现的真实偏差：`E(KR)` 用错了量

**规格书 3.7 方式2 自己前后矛盾：**

```
标题：「根据**预热后**温度控制」
正文：「E(KR)：预热时 YWD － 当前【PID扫描周期】T结束时的**室内温度**」
```

**原实现取的是「预热后温度」：**
```c
error = PreheatAfterTempSet - PreheatAfterTempDisp;    // [375] − [513]，NTC3 预热后温度
```

**系统里两个量都有、是不同的探头：**

| | 变量 | 下标 | 物理 |
|---|---|---|---|
| **室内温度** | `RoomTempAI11Disp`（= `AI[5]`）| **`[511]`** | NTC0 室内/回风温度 |
| 预热后温度 | `PreheatAfterTempDisp` = `Innercoil2Disp` | `[513]` | NTC3 预热后温度 |

**处置过程（两次裁定）：**

1. 先按**正文**改成「室内温度」：`error = PreheatAfterTempSet - RoomTempAI11Disp;`（版本拨到 V10.7，**未烧录**）
2. **用户 2026-09-18 随后裁定改回「预热后温度」**（与标题一致）

**⇒ 最终代码回到原样（`PreheatAfterTempDisp`），版本号还原为 V10.6 / DAY 16。**
**代码里留了一条 2026-09-18 的注释**说明这个矛盾与两次裁定，**并写明"此处若再被提起，先问用户，别自行改"**。

⚠ **教训（我这轮犯的）**：我第一次看这段时直接写了「判断为正文笔误」，把矛盾绕过去了。
**正确做法是把矛盾原样摆出来、由人裁定，不能自己判"笔误"就跳过。**
（§21.3 那个微分项括号确实是笔误，但那是**有两份独立文档交叉印证**才敢定的；
这里没有任何旁证，我不该套用同样的处理。）

### 23.4 顺带发现的**既有问题**（非本次引入）

```c
// UserAction.c:3070  方式2 的故障保护
(usePostTempPid && TSysInTempErr) ||
```

**`TSysInTempErr`（`Parameter[HREST+5]`）全仓没有任何赋值点** —— 死变量，恒为 0，
**所以这层"进风温度故障 → 预热全关"的保护本来就是空转的。** `SysInTempErr`（`HREST+9`）同样无人赋值。

**⇒ 既然已改用室内温度，这层保护应挂室内温度的故障字。本次未动**（用户只说了改 E(KR)），
**但这是个该修的洞。**

### 23.5 待做实测（需先切制热）

| # | 验什么 | 怎么造 |
|---|---|---|
| 1 | 方式1 插入法 | `[374]=0`；注入 **NTC2（新风）**：`≤−5.0°C`→1000、`≥0°C`→0、中间逐点核对线性 |
| 2 | 方式2 分派条件 | `[374]=1` 且 `PreheatModeSet == AO` |
| 3 | 方式2 的 `E = YWD − 室内温度` | 注入 **NTC0（室内）**，看 `[929] PreheatPidEDisp` 是否 = `[375] − [511]` |

⚠ **切制热前必须先做故障复位** —— `[924] HeatingFaultDisp` 那个闩锁还挂着（§22.4②）。

---

## 24. 规格书 3.7 预热加减载：**注入链打通** + 实测准备（2026-09-20）

### 24.0 本轮结论速览

- ✅ **找到了 330 内机 NTC 温度的注入点**（此前一直不知道怎么把温度写进去）
- ✅ **实测验证**：注入 1:1 生效、立即反映
- ⚠️ **发现 `[492]/[493]` 本来就带台面标定值，不是 0** —— 还原时**不能写 0**
- ⛔ 3.7 实测本身**未跑**：`SysStep = 0`（停机），预热函数直接清零 return

### 24.1 ★ 330 内机 NTC 注入链（本轮主要成果）

**此前的问题**：写显示寄存器 `[510..523]` 不生效 —— **每扫描被 NTC 层覆写**。

```
Parameter[484..495]   ← 唯一可注入的活口（校准偏移，0.1°C）
   │  NTC16bit.c:247-262
   │  NTC[i] = round(Tempbuf[i]*10) + calibration        i = 0..11
   │  NTC16bit.c:274-285
   │  AI[5..16] = NTC[0..11]
   ▼
AI[7] / AI[8]
   │  UserAction.c:1971-1972   ← 每扫描覆写！
   ▼
[512] 新风温度  /  [513] 预热后温度
```

`User.h:110-126` 里叫 `UserA5ValueSet`..`UserA16ValueSet` —— **宏名里的数字就是 AI 通道号**，
对应 `NTC16bit.c:247-261` 的 switch。

| NTC | **校准偏移（注入点）** | 显示值 | |
|---|---|---|---|
| NTC0 | `[495]` | `[511]` | 房间/回风 |
| NTC1 | `[494]` | `[510]` | 送风 |
| **NTC2** | **`[493]`** | **`[512]`** | **新风** ← 方式1 用 |
| **NTC3** | **`[492]`** | **`[513]`** | **预热后** ← 方式2 用 |
| NTC4 | `[491]` | `[515]` | A-1 蒸发 |
| NTC5 | `[490]` | `[516]` | A-2 蒸发 |
| NTC6 | `[489]` | `[514]` | B-1 蒸发 |
| NTC7 | `[488]` | `[517]` | B-2 蒸发 |
| NTC8 | `[487]` | `[520]` | C-1 蒸发 |
| NTC9 | `[486]` | `[521]` | C-2 蒸发 |
| NTC10 | `[485]` | `[522]` | D-1 蒸发 |
| NTC11 | `[484]` | `[523]` | D-2 蒸发 |

**为什么能写进去**：`modbus.c:339` 有一条把 450~499 全封掉的规则 ——

```c
/* AI calibration and hidden factory values require engineering tools... */
if((addr >= 450) && (addr < ParameterSaveSize))return 0;
```

**但 `modbus.c:278-279` 的白名单在它前面就 return 1 了：**

```c
if((addr >= 484) && (addr <= 495))
    return (value >= -NTC_CALIBRATION_LIMIT_TENTHS_C)
        && (value <=  NTC_CALIBRATION_LIMIT_TENTHS_C);
```

范围 `±NTC_CALIBRATION_LIMIT_TENTHS_C = ±500`（±50.0°C，`User.h:33`）；
min/max/def 由 `user.c:361-366` 给（def = 0）。

**三点注意：**

1. **是叠加不是替换**：`显示 = 真实 + 偏移`。**先读基线再加偏移量。**
2. **不会引发探头故障**：有效性判在 `IsNtcAdInvalid(NTC_AD[i])`（原始 ADC，`NTC16bit.c:226`），
   **在加偏移之前**。真实探头插着就 `SensorStatus[5+i]=0`。
3. **监控软件已有面板**：`app.py:1458 INDOOR_TEMP_CALIBRATION` →「内机温度校准」
   （写入函数 `app.py:6641`，**带写后回读校验**）。
   命令行用 `tools/rw330.py 493=-68`（`=` 形式写，地址就是 `Parameter` 下标）。

**存储与磨损**：这 17 个字（`457` + `484..499`）走 `IndoorCalibrationJournal`
（`user.c:942-948` 索引表，双槽 A/B，`user.c:1246-1268`）：**改值后稳定 500ms 才落盘**
（`INDOOR_CALIBRATION_JOURNAL_STABLE_MS`），且**与已存值相同则跳过**。
⇒ 台面反复注入**不必担心擦写寿命**。

⚠ **330 没有 316 那个"开机强写"的毛病**：全仓 `*.c` 里 `[484..495]` **一个写入点都没有**
（grep 确认），只由 EEPROM 和 HMI 决定。
（316 的坑是 `user.c:395-404` 强写 `UserA0Set..A3Set` = `[468..471]`，**输入类型选择**，
是另一组寄存器，别混。）

### 24.2 ⚠️ 台面标定值（**还原清单必须含这两项**）

```
[492] = -164   →  预热后真实 = [513] - [492] = 113 - (-164) = 27.7°C
[493] =  -88   →  新  风真实 = [512] - [493] = -20 -  (-88) =  6.8°C
```

**⇒ 这组校准是现场标定过的，不是出厂默认 0。**
**还原时写回 `[492]=-164` / `[493]=-88`，绝不能写 0** —— 否则台面标定被冲掉。

（§23 那轮我给的还原清单里**没有这两项**，因为当时还不知道它们的存在。
这也是本轮最有价值的一个副产品：**先读、再写**。）

### 24.3 注入链实测（2026-09-20）

NTC 层与 `SysStep` 无关 —— **不开机也能验注入**：

```
[493] = −88   →   [512] = −20
[493] = −68   →   [512] =   0      ← 正好 +20（+2.0°C）
[493] = −88   →   [512] = −20      ← 已恢复原值
```

**⇒ 1:1 线性、无中间量化损失、立即生效。** 方式1 / 方式2 的注入路径打通。

### 24.4 实测计划（待开机）

**⛔ 当前阻塞**：`[709] SysStep = 0`、`[570] MCSET = 1`（关机）。

`PreHeatControl()` 在 `UserAction.c:3069` 有 `(SysStep != 2)` 就**清零 return** ——
**不开机，预热根本不计算**，两种方式都是死数。

其余条件已具备：`[333]=1`(AO) ✓、`[968]` 预热无故障 ✓、`[236]=1` 制热 ✓。

**方式1 插入法**（`[374]=0`；当前 `[356]=0.0°C` / `[357]=−5.0°C`，斜坡 5°C 宽）

`N = ([356] − [512]) × 1000 / ([356] − [357])`，代码 `UserAction.c:3112-3119`

| 目标 `[512]` | °C | 写 `[493]` | 预期 N |
|---|---|---|---|
| −50 | −5.0 | −118 | **1000**（≤满载，跳变）|
| −40 | −4.0 | −108 | 800 |
| −25 | −2.5 | −93 | 500 |
| −10 | −1.0 | −78 | 200 |
| 0 | 0.0 | −68 | **0**（≥开启，跳变）|
| +20 | 2.0 | −48 | 0（钳住）|

（`[493]` = 目标显示值 − 真实值 68；`[357]=−50` 是 −5.0°C，`[356]=0` 是 0.0°C）

**方式2 PID**（切 `[374]=1`，需 `[333]==HEAT_OUTPUT_AO`；`E = [375] − [513] = 50 − [513]`）

| 目标 `[513]` | °C | 写 `[492]` | E | 预期 |
|---|---|---|---|---|
| −200 | −20.0 | −477 | 250 | 大正 Z；N 跨 100/200 可看 **BX 翻倍** |
| 0 | 0.0 | −277 | 50 | 正 Z |
| 50 | 5.0 | −227 | 0 | **死区 Z=0** |
| 100 | 10.0 | −173 | −50 | 负 Z |

（`[492]` 下限 −500，所以 −477 是能做到的最低温；再低就超量程）

### 24.5 §23.5 的更正

§23.5 表第 3 行「方式2 的 `E = YWD −` **室内温度**，注入 **NTC0**」**已作废** ——
当日代码已按用户裁定撤回 `PreheatAfterTempDisp`，
**E 用的是预热后温度（NTC3）→ 注入点是 `[492]`，不是 `[511]`。**

第 1 行「注入 NTC2（新风）」的**方法**即 §24.1，**注入点是 `[493]`**。

### 24.6 本轮新增/重写工具

- `tools/probe_330_preheat.py`（**重写**）：读 `[492]/[493]`；把 `IndoorPidDemandStep`
  完整复刻成模拟器（含 **1/1000 余数累加器 `residualMilli`** 与"前 2 拍 N=0"的 `validSamples`），
  逐拍打「实测 vs 期望」；`t` 取 `[412] PidAdjustTimeSet1`（`User.h:796 PIDPA=412`）。
- `tools/rw330.py`：通用寄存器读写（`493=-68` 形式）。**注意**：调用 python 要用全路径，
  `python.exe` 别名会 exit 49。
- ⚠ 跑这些之前 **COM6 会被监控软件 `app.py` 独占**，按既定规则直接关掉它。

### 24.7 PID 公式复核（含一个此前没说清的细节）

```c
// UserAction.c:808-830  IndoorPidDemandStep
numerator    = (h0-h1)*ti*t + t*t*h0 + td*ti*(h0-2*h1+h2);
denominator  = 10*ti*t;
scaledZ      = kp * bx10 * numerator * 1000 / denominator;   // ← 注意 ×1000
accumulatedZ = residualMilli + scaledZ;
z            = accumulatedZ / 1000;                          // C 向零截断
residualMilli= accumulatedZ - z*1000;                        // ← 余数留存
N            = clamp(N + z, 0, 1000);
if((N==0 && accumulatedZ<0) || (N==1000 && accumulatedZ>0)) residualMilli = 0;
```

**⇒ 不是"每拍直接截断"** —— 小数部分通过 `residualMilli` 累积，
所以低速（|Z|<1）时也不会丢积分，只是**攒够了才跳一步**。

- `bx10 = IndoorDemandBx10(N)`（`UserAction.c:739`）用的是**加 z 之前**的 N：
  `N≤100→1, ≤200→2, ≤400→4, ≤500→6, else 10`
- `residualMilli` 是 `PreHeatControl` 的 static，只在**函数出口清零分支**（`UserAction.c:3081`）
  和**死区分支**（`UserAction.c:798`，注意是把它设 0 而不是保留）被重置
- `validSamples < 2` 时 `N=0`、`Z=0` —— **开机/切换方式后头 2 拍必然是 0，记录时别当异常**
- `t` 被 `clamp(1,10)`；`kp∈[1,100]`、`ti∈[1,1000]`、`td∈[0,1000]` 也被 clamp
- **规格书给的 `KP=5 / TI=100 / T=5` 组合响应很慢**：稳态下 `Z ≈ −1/拍`，
  要看到明显动作得把 E 造大（见 24.4 表的 E=250 那行）

---

## 25. 可控硅输出改为「1:1:1」—— 制热/预热分开，各 1 路模拟量 + 2 路开关量（2026-09-20）

### 25.0 一句话

**把可控硅从「独占 3 路模拟量、制热预热共用」改成「制热一套、预热一套，各 1 路模拟量（可控硅，占 40%）+ 2 路开关量（电加热组，各 30%）」。**
分段阈值**不动**（本来就和规格书一致），改的是**输出口**和**可控硅那一路的算法**。

### 25.1 依据：规格书「电加热能力需求（1:1:1（可控硅）」图

| 区 | demand | 开（↗） | 关（↘） |
|---|---|---|---|
| 0组电加热 + 可控硅 | 0 ~ **40%** | — | **30%** 关 1组 |
| 1组电加热 + 可控硅 | **40% ~ 70%** | **40%** 开 1组 | **60%** 关 2组 |
| 2组电加热 + 可控硅 | **70% ~ 100%** | **70%** 开 2组 | — |

**→ 图上 40%/70% 开、30%/60% 关，与固件 `IndoorScrRegion()`（`UserAction.c:378-397`）的
`400/700/300/600` 逐条吻合。⇒ 阈值和分段比例本来就是对的不动。**

**→ 由此反推容量比 = 可控硅 40% : 组1 30% : 组2 30%**（只有这样 400/700 才自洽）。
用户 2026-09-20 确认此比例（标题的「1:1:1」不是三等分）。

⚠ **本轮我犯过的错、已作废的结论**：看到标题「1:1:1」时我**外推**成"三个单元各 1/3、
阈值应为 333/666"，并据此说"固件 400/700 配 1:1:1 硬件会少给"。**图出来后证明是错的** ——
我拿标题的字面去做算术，没有先看规格图。**教训：规格书里的数字要先找图/表，别从标题外推。**

### 25.2 输出口分配（用户 2026-09-20 指定）

| 系统 | 可控硅（AO） | 电加热组1（DO） | 电加热组2（DO） | 空置 |
|---|---|---|---|---|
| **制热** | `DA0` | `Y0`（`Heat1Y00`） | `Y1`（`Heat2Y01`） | `Y2` |
| **预热** | `DA1` | `Y15`（`PreHeat1Y15`） | `Y16`（`PreHeat2Y16`） | `Y17` |

（`Y2`/`Y17` 是 `1:2:4 分段`（方式3）才用的第三路，两种模式互斥，不冲突。）

### 25.3 ★ 关键：**换口和改算法必须一起做**

第 2/3 段改成**只能全开全关的开关量**后，可控硅那一路**必须变成「余数通道」**：

| demand | 旧（3 路 AO） | 新（1 AO + 2 DO） |
|---|---|---|
| 0 ~ 400 | `DA0` = d/400 | `AO` = d/400｜DO 全关 |
| 400 ~ 700 | `DA0`=100%、`DA1`=(d−400)/300 | **`AO` = (d−300)/400**｜**组1 开** |
| 700 ~ 1000 | `DA0`=100%、`DA1`=100%、`DA9`=(d−700)/300 | **`AO` = (d−600)/400**｜**组1+组2 开** |

即 `AO = clamp(demand − 300·组1 − 300·组2, 0, 400) × 1000/400`。

**切换瞬间可控硅由 100% 跳到 25%**（扣掉一组 30%，而自身满量程是 40%），**总输出连续无跳变**。

⚠ **若只把 `DA1`/`DA9` 改接到 `Y0`/`Y1` 而不动 `scr1` 那两处 `=1000`** ——
demand=400 会输出 40%(顶格) + 30%(组1) = **70%，多给一倍**。

**数值验证（新旧等效）**：全量程 0~1000 扫描，**最大偏差 0.100/1000 = 0.01%**
（仅整数取整差：旧用 `(…+150)/300`、新用 `(…+200)/400`）。⇒ **输出等效，换的只是物理实现。**

### 25.4 改了哪些地方

**`User/UserSrc/UserAction.c`（4 处）**

| # | 位置 | 改动 |
|---|---|---|
| 1 | `IndoorSetScrOutputs()` 399-433 | **重写**。签名改为 `(demand, region, volatile uint16_t *scrAO, unsigned char *group1DO, unsigned char *group2DO)`；按 region 置两个 DO，AO 出余数 |
| 2 | `IndoorApplyHeatAnalogOutputs()` 435-482 | **删除 `sharedConflict` 分支**（"两边都要 AO 且一方是 SCR ⇒ 预热优先、制热被丢"）。制热走 `&ReheatAO0,&Heat1Y00,&Heat2Y01`，预热走 `&PreheatAO1,&PreHeat1Y15,&PreHeat2Y16`，互不抢口。`DA9` 保留置 0 |
| 3 | `IndoorUpdateAOConflict()` | **不再上报 `INDOOR_AO_CONFLICT_HEAT_SCR`**（新方案下不成立）；随之 `heatUsesAO`/`preheatUsesAO`/`heatPumpAuxEnabled` 三个中间量失去消费者，一并删除。**`heatUsesStage`/`reheatUsesStage` 加入 SCR 模式** —— 可控硅现在也占 `Y0/Y1`，与 `1:2`/`1:2:4` 同性质 |
| 4 | `separateStageConflict`（`HeatingControl()` 内） | `HeatModeSet==5`(SCR) 与 `ReheatModeSet==HEAT_OUTPUT_SCR` 一并计入 —— 都占 `Y0/Y1`，`HeatReheatShareSet==0` 时是真冲突 |

**`User/UserHead/User.h`（1 处）**

- 版本 **V10.6 / DAY 16 → V10.7 / DAY 20**，并在版本块补了本次改动说明。
  （按既定规矩：改固件必拨版本号，否则烧录前后报出的字符串一模一样，现场验不出烧没烧进去。）

**未改动**：`IndoorScrRegion()` 的分段阈值与迟滞（本就和规格书一致）。
`INDOOR_AO_CONFLICT_HEAT_SCR` 的宏定义保留（监控软件可能在用），只是不再置位。

### 25.5 验证到哪一步 / 没到哪一步

| | 状态 |
|---|---|
| **编译** | ✅ Keil `UV4 -b Project/PSC330RK-V10.uvprojx` → **0 Error(s), 0 Warning(s)**，Code=133720 |
| **数值等效** | ✅ 全量程扫描，新旧最大偏差 **0.01%** |
| **注入链** | ✅（本轮早先）`[493]`/`[492]` 写入 → `[512]`/`[513]` 1:1 生效 |
| **烧录** | ❌ **未烧**（用户自己烧） |
| **台面实测** | ❌ **未测** —— 需先烧录，再开机（`SysStep=2`），然后把 `[332]`/`[333]` 置 4（可控硅），
用 `[891]`/`[929]`/`[930]` 看 demand，用 `FinalDA0Disp`/`FinalDA1Disp`（`[915]`附近）与 `Y0/Y1/Y15/Y16` 看输出 |

### 25.6 待确认

- 台面实测时注意：**预热 PID 在 `SysStep!=2` 时直接清零 return**（`UserAction.c:3069`），
  要验必须真开机并切制热（`[236]=1`）。

相关：§24（NTC 注入链）、§23（3.7 预热代码审计）。

### 25.7 ⚠️ 本轮踩的坑：Edit 工具把文件里的**历史乱码字节烧成了 U+FFFD**

第一次改完，`git diff` 显示 **342 行改动**（189 增 / 168 删），远超实际改动量。查下来：

```
UserAction.c   索引版 U+FFFD = 0     工作区 U+FFFD = 132     ← 我引入的
User.h         索引版 U+FFFD = 0     工作区 U+FFFD = 0
```

**原因**：这个文件里混着**本来就无法按 UTF-8 解码的历史乱码字节**（GBK 残留之类）。
`Edit` 工具读文件时按 UTF-8 解码，解不出来的字节变成替换字符 `�`(U+FFFD)，
**写回时就把它们永久写成了 `�`** —— 原字节再也拿不回来。

**症状特征**：diff 里出现大量"**看起来一模一样**却算改动"的行：

```
-void	EEV_ActionAll(void);				//锟斤拷锟斤拷锟斤拷锟酵凤�?
+void	EEV_ActionAll(void);				//锟斤拷锟斤拷锟斤拷锟酵凤�?
```

**修法（已做）**：`git checkout` 还原 → 用**字节级**脚本重打补丁
（`tools/_fix_scr_bytes.py`：全程 `read_bytes()` / `replace()` / `write_bytes()`，
不 encode/decode，且每个锚点断言**恰好命中 1 次**，不全中就不写入）。

**修后**：diff 收敛成 **5 个 hunk、全是本次改动**；两文件 U+FFFD 均为 **0**；
重新编译 **0 Error / 0 Warning，Code=133720** —— **与损坏版完全相同**，
说明损失只发生在注释字节上，**代码未受影响**。

**教训（已并入操作规矩）**：

1. **改完立刻看 `git diff --stat` 的行数**是否符合预期 —— 明显偏大就是编码事故，
   别再往下走、更别提交。
2. 量化判据：`git show :<file>` 与工作区各自数一遍 U+FFFD，**差值应恒为 0**。
3. **一旦发现就用字节级脚本整体重做**，不要逐行去修 `�`（原字节已经没了，修不回来）。
4. 这类文件**从一开始就该用字节级方式改**，不要用会重新编码整文件的编辑器/工具。
   （§23 改 `UserAction.c` 时用的也是 Edit，**当时就埋了这个雷**，只是没查 diff 行数没发现。）

---

## 26. 可控硅模式 Y15/Y16 输出脉冲化 —— **未解**（2026-09-20）

### 26.0 一句话

**`[333]=4`（预热=可控硅）时 `output[13]/[14]`（Y15/Y16）是脉冲（占空比 6~36%），继电器吸不住。
所有输入变量实测都正确稳定，源码里找不到第二个写者。V10.8 的修法失败，已回退。**

### 26.1 实测数据（ST-Link attach 直读 RAM，可靠）

**输入变量 16 次采样完全一致，从不翻转：**

| 变量 | 地址 | 实测 |
|---|---|---|
| `IndoorPreheatAnalogMode` | `0x20000d21` | **4**（HEAT_OUTPUT_SCR）|
| `IndoorPreheatAnalogDemand` | `0x20000d1c` | **1000** |
| `IndoorPreheatScrRegion` | `0x20000d28` | **2** |
| `IndoorHeatAnalogMode` | `0x20000d20` | 4 |
| `IndoorHeatAnalogDemand` | `0x20000d18` | 0 |

**⇒ `preheatActive` 恒 TRUE、`group1=group2=1` ⇒ `IndoorSetScrOutputs()` 每次执行必然写 `output[13]=output[14]=1`。**

**但输出是脉冲：**

```
V10.7 原始:   采 14 次 →  9 次 "00 00" /  5 次 "01 01"   ⇒ 占空比 ~36%
V10.8 改动后:  采 16 次 → 15 次 "00 00" /  1 次 "01 01"   ⇒ 占空比 ~6%（更糟）
```

**⇒ 1:2:4 分段模式下同样两路**能**吸合 —— 因为 `IndoorSegLevel` 的迟滞把值锁成常量、占空比 100%。**

### 26.2 ⚠ 两个观测陷阱（我踩过，别重复）

1. **`read8 0x20001371 2` 会采到 `01 00` / `00 01`**（Y15/Y16 不同步，按代码不可能）——
   那是**两次字节读取之间的窗口**造成的假象。**必须整块采 16 字节**，
   真实图案只有 `全0` 和 `Y15+Y16 同时为1` 两种。
2. **`DA1=1000` 和 `[941] IndoorHeatAOOwnerDisp=2` 可能是陈旧值**，不能当作
   "`IndoorApplyHeatAnalogOutputs()` 跑过"的证据。

### 26.3 已排除（逐条查过）

| 排除项 | 依据 |
|---|---|
| 硬件/引脚 | GPIOG MODER 确认 pin15/13/12 都是输出模式；1:2:4 下同样两路能吸合 |
| 驱动缺失 | `WriteOutput()`（`User/wellthinker/src/sys.c:66`，每扫描调用）16 路全覆盖，Y15→GPIOG-pin15、Y16→pin13、Y17→pin12 |
| region 卡 0 | 实测 `IndoorPreheatScrRegion = 2` |
| 函数没被调用 | 它是唯一给 DA1 赋值处，而 `DA1=1000` 与 SCR 公式吻合 |
| `ErrorCheck.c:631` | 只在紧急停机跑，且会 `SysStep=0`（实测 709=2）|
| `IndoorStopHeatPreheatLoads()` 4 个调用点 | 全在停机流程，正常运行不走 |
| 指针/实参错 | 反汇编：`STRB r9,[r8,#0]`，实参 `r3=output+13`、`[sp]=output+14`、`r2=DA[1]` 全对 |

### 26.4 ✗ 失败的修法（V10.8，**已回退**）

**做法**：删掉 `PreHeatControl()` / `HeatControl_UP()` 开头的无条件清零，
并在 `IndoorApplyHeatAnalogOutputs()` 里加"非 STAGE 模式时清零"分支，
让 DO 只有一个稳态写者。（脚本 `tools/_fix_scr_pulse.py`）

**结果：占空比 36% → 6%，更糟。**
**⇒ 证明清零点不在那两处，方向错了。**

**回退方法**：把 `tools/_fix_scr_pulse.py` 里 `EDITS` 的 `old`/`new` 互换跑一次。
已回退：diff 恢复 132 行、版本回 V10.7、U+FFFD=0、编译 0 Error/0 Warning。

### 26.5 ⚠ 下一步唯一有效的办法

**ST-Link DWT 数据观察点挂在 `0x20001371`（output[13]）上，抓出到底是谁在写它。**
**需要 halt 核（会冻板）—— 按既定规矩必须先征得用户同意，抓完立刻 attach+resume。**

### 26.6 硬件地址速查

```
output[] 基址 0x20001364 (16B)   Y15=0x20001371  Y16=0x20001372  Y17=0x20001373
DA[]     基址 0x20001872 (20B)   DA1=0x20001874
GPIOG    MODER=0x40021800   ODR=0x40021814
pyocd cmd -t stm32f407ze -O connect_mode=attach -c "read8 0x20001364 16"
fromelf --text -s / -c  Project/Obj/PSC330RK-V10.axf
```

---

## 27. 四个"死参数"审计：`[358]`~`[361]`（2026-09-20）

**结论：`[358] ReheatFullOnDevSet`、`[359] ReheatFullOffDevSet`、
`[360] HumidFullOnDevSet`、`[361]`（连宏都没定义）——能读能写能存 EEPROM，但全仓无任何读取点，改它们不产生任何行为变化。**

### 27.1 三重确认"真的没人读"

1. **无宏引用**：`ReheatFullOnDevSet`/`ReheatFullOffDevSet`/`HumidFullOnDevSet`
   在 `*.c` 里零使用点；`HumidFullOffDevSet` 在 `User.h` 里**根本没定义**
2. **无裸下标访问**：`Parameter[358]`/`[359]` 一处都没有（排除"宏名陷阱"）
3. **无间接消费**：330 往外机镜像的基址全部 ≥ 1229（`INDOOR_AI_CFG_BASE`=1229、
   `PLCREADPara`=1420、`PLCMonitor`=1550、`PSC316_HMI_BASE`=1830…），**358 远在它们之下**，
   不在任何镜像块里；也没有任何批量循环覆盖到它们

### 27.2 代码实际用的是什么（硬编码比例，不是偏差参数）

`UserAction.c` 的再热强制全开/全关：

```c
if((long)RealTempDisp * 4L > (long)ControlTempDisp * 5L) demand = 0;      // 实际 > 设定×1.25 → 全关
else if((long)RealTempDisp * 2L < (long)ControlTempDisp) demand = 1000;   // 实际 < 设定×0.5  → 全开
```

**与参数本意（绝对偏差）两处对不上**：

| | 代码实际 | 按参数本意 |
|---|---|---|
| 判据种类 | **相对比例**（×1.25 / ×0.5）| **绝对偏差**（°C）|
| 松紧（设定 20°C）| 超 **5°C** 才全关 / 低 **10°C** 才全开 | 超 **0.5°C** 就关 / 低 **2.0°C** 就全开 |

**⇒ 差一个数量级，这层"快拉快收"的保护基本没在起作用，全靠 PID 慢慢调。**

### 27.3 ⚠ 文档本身打架，查不到权威定义

同一批地址 `[358]~[361]`，**两份同名文档给了完全不同的定义**：

| | `PSC330RK-V10-内机/PSC330接HMI读写地址逐点表.md`（9-07）| `PSC330接HMI读写地址逐点表.md`（外层，9-13，更新）|
|---|---|---|
| 358 | 再热全开温度偏差 | **送风高温保护阈值** |
| 359 | 再热全关温度偏差 | **送风低温保护阈值** |
| 360 | 加湿全开湿度偏差 | **风机通风模式电压/比例** |
| 361 | 加湿全关湿度偏差 | **风机制冷制热模式电压/比例** |

**规格书本体 `内机规格书.md` 里搜不到这组参数。**
且**两种定义都没实现**（代码里也没有任何"送风高温/低温保护"）。

**⇒ 用户 2026-09-20 裁定：「既然没有什么实际影响就不要改了」——
保持现状，不改代码。**

**⇒ 但真正的害处是误导**：它们能改、能存、名字看起来像正经配置项，
现场调试的人容易以为"把再热全开偏差从 2.0 改成 0.5 就能让再热响应更快"—— 白改。
