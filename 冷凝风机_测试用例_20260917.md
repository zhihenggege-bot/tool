# PSC316 冷凝风机 测试用例（机型1 / 制冷）

适用：**机型 = 1（0-43℃ 宽环温）**，制冷工况。冷凝温度由**高压传感器**换算，
风机类型 2（单风机变频）与 3（定速+双速）。台面 1#=类型2、2#=类型3。

## 0. 前置条件

- 工作模式 = 制冷（`330[236] = 0`）
- 制冷剂 = R410A（`Parameter[39] = 3`）；换别的制冷剂要用下表重算压力
- TL=30.0 / TLD=15.0（两台已统一）→ **TL+TLD/2 = 37.5**、**TL+TLD = 45.0**
- 最短运行/停止时间 = 30 / 30 秒（`Parameter[179]/[180]`，会拖慢风机起停，别误判成 bug）
- 316 固件 **V12.10**（V12.8~V12.10 三条改动都要验）

## 1. 怎么把「冷凝温度」做到指定值

**原理**：`OutdoorCondTemps()` 每扫描从 `HighPressDisp1/2` 现算，往 RAM 写 `Parameter[1443]`
没用（下一扫描被覆盖）。要在**输入端**偏 —— 316 的 AI 换算里 `value += engLow + AiCfgOffset(ch)`
是**纯 kPa 加法**，所以给高压通道加偏移就能把读数推上去。

**操作**：工具 →「AI / 温度校准」页 →「**外机压力校准**」→ 选外机号 →
在「**AI0 1#高压**」和「**AI1 2#高压**」两行**都填同一个目标 kPa** → 各点一次「**校准**」。

> 「校准」= 填**目标值**，工具自动算偏移；「写入」= 填**偏移量**。推荐用「校准」。
> 两路设成同一个值 → `CondTempMax == CondTempMin`，段1~4 和段1备注（一个用 min 一个用 max）都干净。

### R410A 冷凝温度 → 目标高压

| 冷凝温度 ℃ | 目标高压 kPa | bar |
|---:|---:|---:|
| 24.0 | **1505** | 15.05 |
| 29.5 | **1755** | 17.55 |
| 30.0 | **1780** | 17.80 |
| 31.0 | **1824** | 18.24 |
| 34.0 | **1970** | 19.70 |
| 35.0 | **2020** | 20.20 |
| 36.0 | **2070** | 20.70 |
| 37.5 | **2145** | 21.45 |
| 40.0 | **2275** | 22.75 |
| 42.0 | **2385** | 23.85 |
| 42.5 | **2415** | 24.15 |
| 45.0 | **2560** | 25.60 |
| 47.0 | **2690** | 26.90 |
| 50.0 | **2890** | 28.90 |

⚠ **上限 3800 kPa**（38 bar）—— `ErrorCheck.c` 里模拟量高压 >3800 持续 2 秒报故障。
本表最高 2890 kPa（50℃），安全。

⚠ 偏移是**累积**的，测完务必点「**清零**」。真实压力会随工况漂，每次改完用
「状态/故障」页新增的「**冷凝风机判据**」面板确认实际落到的温度，别只看填进去的数。

### 环温怎么做

工具 → 同页 →「**外机温度校准**」→ 第一行「**环境温度**」→ 填目标 ℃ →「校准」。
（316 侧是 `EnviTempAI0Disp = AI[6]`，走温度校准镜像块）

## 2. 怎么读

```
python.exe tools/probe_316_cooling_fan.py 300 2      # ST-Link 在外机1 上；每 2 秒一行
```
同时开工具的「外机状态」页，`Do_table` 的 Y00~Y07 和新的「冷凝风机判据」面板一起看。

关键内部量（ST-Link 绝对地址，符号表已核对）：

| 量 | 地址 | 含义 |
|---|---|---|
| `OutdoorVfdOn` | `0x20000D01` | 变频风机开/停 —— **V12.10 后必须恒为 1** |
| `OutdoorVfdSpeed` | `0x20000D30` | 0.1Hz |
| `OutdoorVfdLoadCount` / `UnloadCount` | `0x20000D48` / `0x20000D4C` | V12.10 后是 **0/1**，不再是 0~30 |
| `OutdoorVfdForceFullCount` | `0x20000D50` | 满频确认（仍是 30 秒） |
| `OutdoorTypeIIStage` | `0x20000D08` | 4 段的当前段 |
| `OutdoorTypeIIPendingStage` / `PendingCount` | `0x20000D09` / `0x20000D54` | 待切换段 / 15 秒确认计数 |
| `OutdoorTypeIIFan2High` | `0x20000D06` | 双速风机高/低 |
| `OutdoorTypeIILowFanOff` | `0x20000D0B` | 段1备注的「双速低速关闭」标志 |
| `output[0..7]` | `0x200000D4` | Y00~Y07 实际继电器 |

## 3. 类型2（1#外机 单风机变频）用例

判据：冷凝 **>36.0 每秒加**、**<34.0 每秒减**、**34~36 保持**；加减载速率 = `FanStepSpeedSet`
（`Parameter[178]`，×0.1Hz/s）。最低频 `Parameter[177]`=20 → 200；最高频 `Parameter[176]`=50 → 500。

> 加减载速率是可配的：第1组实测时是 **20（2.0 Hz/s）**，实测每秒 ±2.0Hz ✓；
> 之后台面被改成 **10（1.0 Hz/s）**，实测每秒 ±1.0Hz ✓。**两次都与参数一致**，这条逻辑没问题。

| # | 目标冷凝 | 目标 kPa | 预期 | 判定 |
|---|---:|---:|---|---|
| A1 | 36.0℃ 以上 | 2070 | `LoadCount=1`、`UnloadCount=0`，`VfdSpeed` **每秒 +20** 直到 500 | 加载**立即**开始，不是等 30 秒；速率 2.0Hz/s |
| A2 | 34.0℃ 以下 | 1970 | `UnloadCount=1`，`VfdSpeed` **每秒 −20** 直到 200 | 同上，减载方向 |
| A3 | 35.0℃（带内）| 2020 | 两个计数**都=0**，`VfdSpeed` **不动** | 速度保持不变 |
| **A4** | 30.0℃（远低于减载线）| 1780 | `VfdSpeed` 减到 **200** 后停住；**`OutdoorVfdOn` 恒为 1** | 连续看 ≥60 秒，`VfdOn` **一次都不能为 0**；Y02=1 |
| **A5** | 45.0℃ 以上 | 2890 | 风机没开时：**启动瞬间就是 500（50.0Hz）** | 启动瞬间 `VfdSpeed=500` 而不是 200<br>**实测 V12.11 不通过 → V12.12 修** |

> **A4 是 V12.10 的核心用例** —— 旧固件在这里会 `OutdoorVfdOn = 0` 把风机关掉。
> **A4 是 V12.10 的核心用例** —— 旧固件在这里会 `OutdoorVfdOn = 0` 把风机关掉。

### 第 1 组 实测结果（2026-09-17，全部通过 ✓）

| 用例 | 目标 | 实测 |
|---|---|---|
| A1 加载 | 36.7℃ | `Load=1`，`VfdHz` 加到 **50.0** 后停住 ✓ |
| A3 带内保持 | 35.0℃ | `Load=0 Unld=0`，**70 秒纹丝不动**（停在进入带内时的 50.0）✓ |
| A4 减载 | 32.0℃ | `Unld=1`，**34.0→20.0 每秒 −2.0**，7 秒走完 14Hz，停在 20.0 ✓ |
| A4 不停机 | 同上 | 79 秒采样里 **`VfdOn=0` 出现 0 次**，最低频停留 73 秒 ✓ |
| A1' 再加载 | 36.5℃ | `Load=1`，回到 50.0 ✓ |

> 「带内保持」的正确含义是**停在原地不动**（不是往中间调），实测 70 秒零变化。

### A5 实测结论（2026-09-17）：**V12.11 不通过 → V12.12 修复后通过 ✓**

实测序列：

```
ST-Link 复位 316 → 压机启动挂起 → OutdoorFanPrestartVfd() 预启动到 20.0Hz（最低频）
                → 压机起来 → OutdoorFanTypeIII() 靠 NeedLoad(>36) 以 2.0Hz/s 爬
                → 约 15 秒后才到 50.0Hz
```

**根因不在 V12.11 改的那处，在预启动路径**：`OutdoorFanPrestart()` 在「压机全停 + 有启动请求」时
先把风机开起来（固定最低频、不看冷凝温度），`OutdoorVfdOn` 一置 1，`OutdoorFanTypeIII` 里那个
能判 `>45` 的启动分支就**永远轮不到**了。

**V12.12 修法**：`OutdoorFanPrestartVfd()` 里也判冷凝温度
`OutdoorVfdSpeed = (OutdoorCondTempMax() > 450) ? MaxSpeed : MinSpeed;`，
并补 `MaxSpeed` 的下限保护（最高频参数若为 0 或低于最低频时兜到最低频）。

### V12.12 复测（2026-09-17 14:15）—— **通过 ✓**

```
复位前: 版本 [316,12,12,...]  AI偏移 875/800  冷凝 47.0C
复位
 t= 0.0s   VfdOn=0    0.0 Hz   Comp=0,0  NoCompSec=15  Stop0=300
 t=14.5s   VfdOn=1   50.0 Hz   Comp=0,0  NoCompSec=0   Stop0=304   <- 启动瞬间直接满频
判定: [通过] 符合规格书
```

**整个日志里 `VfdHz` 只出现过 0.0 和 50.0 两个值 —— 中间没有 20.0Hz。**
`Comp=0,0` 全程（压机还没起、风机已预启动满频），正是规格书「**若风机没开**，冷凝>45 满频」的情形。
对比 V12.11：启动给 20.0Hz，约 15 秒才爬到 50.0。

验证脚本：`tools/verify_a5_prestart_full_speed.py <秒数>` —— 复位与 10Hz 采样在同一进程内
（两个进程抢 ST-Link 会冲突），抓的就是 `VfdOn` 0→1 那一个变化点。

测完偏移已清零（`bench_cond_temp.py --restore`），压力回到真实值 1815/1890 kPa = 30.7/32.4℃。

### 顺带发现

**`AiCfgOffset` 能扛住复位** —— 复位前后 AI0=875 / AI1=800 原封不动、冷凝仍 47.0℃。
说明它进了 EEPROM，不依赖 `OutdoorAiConfigMigrate()`（`Parameter[350]` 仍是 MAGIC 也没关系）。
这也说明：工具「AI / 温度校准」页的那个「外机压力校准」按钮走 `Parameter[499]`（`UserA0ValueSet`）
是**失效**的，能改到读数的是「**AI 通道配置**」页（走 316[310]）。

## 4. 类型3（2#外机 定速+双速）用例

判据用**冷凝温度较小值**（`OutdoorCondTempMin`）。段→输出（机型1/类型3）：

| 段 | 条件（冷凝 min）| `FanOpenSet` | `FanOpenSet2` | `IIFan2High` | Y02 | Y03 | Y07 |
|---|---|---|---|---|---|---|---|
| 1 | ≤ TL = 30.0 | 0 | 1 | 0 | 0 | **1** | 0 |
| 2 | 30.0 < x ≤ 37.5 | 1 | 0 | 0 | **1** | 0 | 0 |
| 3 | 37.5 < x ≤ 45.0 | 1 | 1 | 0 | **1** | **1** | 0 |
| 4 | > 45.0 | 1 | 1 | 1 | **1** | 0 | **1** |

| # | 目标冷凝 | 目标 kPa | 预期 | 判定 |
|---|---:|---:|---|---|
| B1 | 29.5℃ | 1755 | `Stage=1` | `output[2]=0 output[3]=1 output[7]=0` |
| B2 | 35.0℃ | 2020 | `Stage=2` | `output[2]=1 output[3]=0 output[7]=0` |
| B3 | 42.0℃ | 2385 | `Stage=3` | `output[2]=1 output[3]=1 output[7]=0` |
| B4 | 47.0℃ | 2690 | `Stage=4` | `output[2]=1 output[3]=0 output[7]=1` |
| B5 | B2↔B3 来回改 | — | `PendingStage` 立刻变，`PendingCount` 爬到 **15** 才 `Stage` 才变 | 掐表，段状态落后命令约 15 秒 |

### 段1 备注

| # | 条件 | 怎么做 | 预期 |
|---|---|---|---|
| B6 | 环温 **<8** 且 冷凝 **max <30** 保持 **30 秒** → 双速低速**关** | 环温校准填 **7.0**；高压两路 **1755** | `LowOffCount` 满 30 秒 → `LowFanOff=1` → `Stage=0` → **Y02=Y03=Y07=0** |
| B7 | 环温 **>10** 保持 30 秒 **或** 任一冷凝 **>42** 保持 30 秒 → 恢复 | 环温校准填 **11.0**（或保持 7℃、高压推 **2415**≈42.5℃）| `LowRecoverCount` 满 30 秒 → `LowFanOff=0` → 回段1 |

> B6 是 `&&`（两个条件都要满足），B7 是 `||`（任一即可）—— 与规格书一致。
> ⚠ B7 代码用的是**同一个计数器**，比规格书「各自保持 30 秒」宽松（偏差 E，已记录，暂不改）。

### 强制段4

| # | 条件 | 怎么做 | 预期 |
|---|---|---|---|
| B8 | 环温 **>20**（使能）且 任一冷凝 **≥50.0** 保持 **10 秒** | 环温校准填 **21.0**；高压两路 **2890** | `AmbientHighCount` 满 30 秒 → `ForceHighEnable=1`；再 `ForceHighCount` 满 10 秒 → **直接跳段4** |

> 从低段**直接跳到 4**，不逐级走。另：环温使能/退出各要 30 秒确认（偏差 D，规格书未标注，暂不改）。
>
> ⚠ **B8 要能区分出「强制」就必须把两路拉开**：强制判据用 `CondTempMax`，段界用 `CondTempMin`。
> 两路设成同一个值时 `min=50 > TL+TLD=45`，常规判据本来就给段4，看不出强制逻辑。
> 所以用 `--hp1 50.5 --hp2 25.0`：min=25 ≤ TL 常规只给段1，而 max=50.5 ≥50 走强制 → 段4。

### 第 2 组 实测结果（2026-09-17，外机2 机型1/类型3，**8 条全部通过**）

| 用例 | 条件 | 实测输出 | 判定 |
|---|---|---|---|
| B1 段1 | min 29.5℃ | `IIStg=1` Fan1=0 Fan2=1 → **Y02=0 Y03=1 Y07=0** | 通过 |
| B2 段2 | min 31.9℃ | `IIStg=2` Fan1=1 Fan2=0 → **Y02=1 Y03=0 Y07=0** | 通过 |
| B3 段3 | min 42.0℃ | `IIStg=3` Fan1=1 Fan2=1 → **Y02=1 Y03=1 Y07=0** | 通过 |
| B4 段4 | min 47.0℃ | `IIStg=4` Fan1=1 Fan2=1 IIHi=1 → **Y02=1 Y03=0 Y07=1** | 通过 |
| B5 每段延时 | 段2→3 / 4→3→2→1 | `PndCnt` 爬到 **15** 才落段；4→3→2→1 每级 15 秒 | 通过 |
| B6 段1备注-关 | 环温 7.0 + 冷凝 max 29.5 | `LowOff` 满 30 秒置1 → **段0：Y02=Y03=Y07 全 0** | 通过 |
| B7 段1备注-开 | 环温 11.0 | `LowOff` 清0 → 15 秒后回段1 → Y02=0 Y03=1 Y07=0 | 通过 |
| B8 强制段4 | 环温 21.0 + hp1 50.5 / hp2 25.0 | **`PendS` 直接=4**（不是 2）→ 跳段4 | 通过 |

**B4 抓到了换绕组死区**：`Dead=3` 秒、两路接触器全开 → 再 `IIHi=1`（Y07=1）→ 隔 15 秒才开单速（Y02=1）。

**B6 就是用户强调的那条**：「双速低速关闭 = 所有风机都关」—— `LowFanOff=1` → `TargetStage=0`
→ `OutdoorTypeIIFanApply(0,0,0,…)` → **Y02=Y03=Y07 全 0**，与规格书一致。

**B8 的 `PendS=4` 是「不逐级」的铁证**：`if(ForceStage4) NextStage = 4;` 让目标段直接是 4，
绕过了 `Stage±1` 的逐级步进。

> B1 测试时观察到一个**过渡现象**（不是 bug）：从段4 一路降到段1 时，段2 的稳态没出现 ——
> 因为段4→段3 的换绕组死区把 `OutdoorFanRunSeconds[1]` 清零了，而关双速需要它已连续运行
> ≥`FanMinRunTimeSet`(30 秒)，段2 只持续 15 秒。B2 单独测（从初始稳定态）就正常。

## 5. 收尾（务必做）

1. 「外机压力校准」四行全部点「**清零**」
2. 「外机温度校准」的「环境温度」点「**清零**」
3. 回「状态/故障」页看「冷凝风机判据」，确认冷凝温度回到真实值、偏移列显示 0

## 6. 和规格书的已知出入（测的时候别当成 bug）

| 条目 | 现状 | 处理 |
|---|---|---|
| 偏差 C | `LoadCondTemp`/`UnloadCondTemp` 都取 max（冗余） | 你说保持，未改 |
| 偏差 D | 强制段4 的环温>20 使能 / <18 退出各 30 秒确认 | 你说保持，未改 |
| 偏差 E | 段1备注的「开」用同一个 30 秒计数器，比规格书宽松 | 你说保持，未改 |
| 满频 30 秒 | ~~「风机没开、冷凝>45 满频」仍要 30 秒确认~~ | **V12.11 已去掉**（启动瞬间直接满频）|

## 7. 顺带要验的旧改动

| 固件 | 验什么 | 怎么验 |
|---|---|---|
| V12.8 | 制热、机型1+类型3 的两台（定速+双速高速）| 环温压到 Z 以下（Z=7.0，校准环温填 5.0），看 `Fan1=1 Fan2=1 Stg=4 IIHi=1`、Y02=1 **且** Y07=1 |
| V12.9 | 机型0+类型0 的低速走 Y03 | 需切到机型0/类型0，低速时 `output[3]=1`、`output[2]=0` |
| V12.10 | 见 A4 / A1 / A2 | 本轮主验 |
